2026-09-20 — runMuxReadinessGate / runTicketAuditGate collapse `result.status ?? 1` — conf=100 severity below floor — same AP-EXT-ITER318-02 shape, but both gates are ADVISORY (AP-EXT-ITER91-01 demoted readiness; R-GATE-ADVISORY ticket audit): a signal-killed gate is logged as findings and the run PROCEEDS, so the collapse costs a misattributed log line, never a verdict, a halt or a Done-flip. Fix when next touching those spawn sites.
2026-09-20 — spawn EACCES is unreachable through runCommand — conf=100 not a defect — the probe that surfaced it used an ABSOLUTE path; every live caller spells a bare command NAME (`npm`, `npx`, gate-commands entries) and POSIX exec PATH search SKIPS a non-executable file rather than failing on it (measured: fixture `npm` at mode 0644 was skipped and the real npm ran). A pin for it would be a green light wired to nothing.
2026-09-20 — `pipeline-runner.ts` carries TWIN git-toplevel resolvers, `gitRepoRoot:840` and `resolveGitRepoRoot:4005` — conf=100 severity below floor — byte-equivalent bodies (`git -C <cwd> rev-parse --show-toplevel`, same `GIT_REPO_ROOT_TIMEOUT_MS`, same fall-back-to-argument), differing only in that one returns `out || cwd` and the other `if (out) return out` then `return cwd`. A DRY/P2 finding, not a CRITICAL/HIGH: both are correct and agree on every input. Collapsing them is a STRUCTURAL change and would mix with this iteration's behavioural fix (Tidy First). Do it when next touching either.
2026-09-20 — `scope-resolver.ts:filterByPaths` has ZERO production callers — conf=100 not a defect — it is exported, exercised by `tests/scope-filters.test.js`, and named by a live AC in `tests/codegraph-context-section.test.js`. Recorded because the AP-EXT-ITER309-01 PATTERN_SHAPE names `filterByPaths(` as a member: the census over `src/` is EMPTY for that half, so a future replay must not read the absence of findings there as "swept and clean" without re-deriving that there is nothing to sweep.
