## Public Exports

Exports enumerated from `grep -E '^export ' extension/src/hooks/*.ts` (top-level files only; `handlers/` sub-directory is covered under Handler Invariants below).

### `resolve-state.ts`
- `sameWorkingDir(a, b)` — compare canonical realpaths for hook cwd matching
- `selectScannedStateFile(stateFiles, cwd)` — choose the best matching state file from a scanned list
- `resolveStateFile(dataDir)` — resolve a single authoritative state file path
- `loadActiveState(stateFile)` — load and validate a `State` object from disk
- `approve()` — emit the hook approve response to stdout

### `dispatch.ts`
CLI entry point — no named exports. Spawns the appropriate handler subprocess based on hook event type.

---

## Handler Invariants

Summary of security-critical invariants for each handler. Full detail is in the `## Trap Doors` entries below; cross-references are noted per handler.

### `handlers/config-protection.ts`
- **Bash config-guard**: glob, brace (`{}`), bracket (`[]`), and `?` patterns in shell commands must be matched against protected config basenames to prevent bypass. See trap-door entry for `handlers/config-protection.ts` (glob/brace invariant).
- **`PROTECTED_WRITE_GLOBS` state-file write blocking**: `Write`/`Edit` tools and every bash write construct in `WRITE_COMMANDS` (redirects `>`/`>>`; destination-arg writers `tee`/`cp`/`mv`/`rsync`; in-place editors `sed -i`/`perl -i`/`vim`/`vi`/`nano`/`emacs`/`ed`/`ex`) targeting state files are blocked unless `state.flags.allow_state_writes_reason` is set. See trap-door entry for `handlers/config-protection.ts` (R-WSRC-3) and (single write-command class).
- **R-WSRC-GR git-verb blocker** (`detectProhibitedGitVerb`): `git reset`, `switch`, `stash`, `rebase`, `pull`, `push`, `checkout-with-ref`, `commit --amend`, and `fetch --prune` are blocked in worker Bash. See trap-door entry for `handlers/config-protection.ts` (R-WSRC-GR).
- **Chained-command segmentation**: the worker-forbidden-op detectors (`detectProhibitedGitVerb`, `isBashInvokingInstallSh`, `extractNodeTestPaths`) evaluate every `splitShellSegments` segment, not just the leading token — `cd sub && git reset` cannot bypass them. `splitShellSegments` treats `&&`, `||`, `|`, `&`, `;`, an unquoted newline, AND the grouping / command-substitution delimiters `(`, `)`, `{`, `}`, and a backtick as boundaries — glued or spaced, since bash requires no whitespace around an operator — so `git status\ngit reset --hard`, `cd sub&&git reset --hard`, and `(git reset --hard)` are segmented too, and it unwraps a `bash -c '<cmd>'` payload into its own segments so the detectors see `<cmd>` rather than the `-c` flag. The write/config scanners consume it too, as of AP-EXT-ITER19-02 — `findBashWriteTarget` walks the redirect-normalized command AND every segment, so a grouped write (`(echo x > state.json)`) is no longer invisible. See trap-door entries for `handlers/config-protection.ts` (chained-command detection), (newline segmentation), and (AP-EXT-ITER10-01 `bash -c` command-string unwrap).

### `handlers/stop-hook.ts`
- **tmux passthrough APPROVE**: when `state.tmux_mode === true`, the hook approves unconditionally so launcher conversations do not block tmux-owned loops. See trap-door entry for `src/hooks/handlers/stop-hook.ts` (tmux passthrough).
- **Idle-backoff progression**: after 3 consecutive degenerate wait-pattern manager turns, nudges are suppressed until state mtime / worker artifact mtime / liveness changes or the bounded fallback timer fires. See trap-door entry for `stop-hook.ts (idle backoff)`.
- **Update-cadence conversion**: `update_check_interval_hours` is multiplied by `3600` before rate-limit comparison — not divided. See trap-door entry for `stop-hook.ts (update cadence)`.
- **`Decision` type**: always `'approve' | 'block'` — the string `'allow'` is never a valid decision value. See trap-door entries and `Decision` type at `handlers/stop-hook.ts:66`.

### `handlers/tsc-gate.ts`
- Gate fires **only on git-commit-class Bash commands** (`isGitCommitCommand`), evaluated across every top-level shell segment so chained `git add … && git commit` still triggers it. Non-commit commands are passed through without a tsc check.
- `allow_tsc_failed_reason` override is **consumed on the next clean commit** — the flag auto-clears via `tsc_gate_override_consumed`.
- See trap-door entry for `handlers/tsc-gate.ts`.

---

## Trap Doors

- `../bin/log-commit.ts` — INVARIANT: every successful `PostToolUse` in an active session clears `last-tool-error.json` before commit parsing. BREAKS: old tool failures poison later worker spawns with stale retry-circuit guidance. ENFORCE: `extension/tests/log-commit.test.js`. PATTERN_SHAPE: PostToolUse hook resolves same-cwd active session and leaves `last-tool-error.json` untouched.
- `handlers/config-protection.ts` — INVARIANT: Bash config guards must match shell-expanded protected basenames across `*`, `?`, `[]`, and `{}` patterns. BREAKS: glob, bracket, or brace commands can bypass protected-file blocking. ENFORCE: `extension/tests/config-protection.test.js`. PATTERN_SHAPE: `tool_name==='Bash'` token with shell pattern chars matching a protected config basename.
- `handlers/config-protection.ts` (R-WSRC-3) — INVARIANT: PreToolUse hook MUST block `Write`/`Edit` tool calls AND bash output-redirects targeting `**/state.json*`, `**/circuit_breaker.json*`, `**/pipeline-status.json*`, `~/.claude/pickle-rick/**`, and `pickle_settings.json*` UNLESS `state.flags.allow_state_writes_reason` (or `allow_settings_writes_reason` for settings) is a non-empty trimmed string (which emits `state_write_override_used` per bypass). Hook fails open on scanner crash per dispatch.js contract. BREAKS: workers retain ad-hoc write access to runtime state files, enabling R-WSRC corruption class. ENFORCE: `extension/tests/config-protection-state-files.test.js`. PATTERN_SHAPE: `PROTECTED_WRITE_GLOBS` constant in source MUST include the named globs; the `findBashWriteTarget` scanner MUST match `>`/`>>` redirects AND every `WRITE_COMMANDS` member (`tee`/`cp`/`mv`/`rsync` + the in-place editors) against the same globs — do NOT re-pin this list to the redirect-only six, which is what let the in-place-editor bypass survive.
- `handlers/config-protection.ts` (single write-command class) — INVARIANT: ONE `WRITE_COMMANDS` set (redirect-dest `tee`/`cp`/`mv`/`rsync` + in-place `sed`/`perl`/`vim`/`vi`/`nano`/`emacs`/`ed`/`ex`) backs `findBashWriteTarget`, which takes NO per-caller command-class parameter; the state probe and the config probe differ ONLY by their `probe` fn. BREAKS: a per-caller class let the state gate run NARROWER than the lint gate — `sed -i state.json`, `sed -i ~/.claude/pickle-rick/**`, and `sed -i pickle_settings.json` all APPROVED while `sed -i tsconfig.json` blocked (proven 2026-07-12). ENFORCE: the arity itself — `findBashWriteTarget` takes no command-class param, so a narrower per-caller class is unrepresentable, not merely unguarded — PLUS the 8 `AP-EXT-STATE-GATE` in-place-editor cases in `extension/tests/config-protection-state-files.test.js`, which are now LANDED (the prior `PENDING-…patch` pointer was stale: the referenced file no longer exists). Arity alone was NOT sufficient: the config gate pins only `sed` (`config-protection.test.js:251`), so deleting `perl`/`vim`/`vi`/`nano`/`emacs`/`ed`/`ex` from the shared set broke ZERO tests — proven by mutation 2026-07-19. PATTERN_SHAPE: `findBashWriteTarget(` arity == 2 AND zero occurrences of `extraWriteCommands|NO_EXTRA_WRITE_COMMANDS|CONFIG_INPLACE_WRITE_COMMANDS` AND every `WRITE_COMMANDS` in-place member appears in an `AP-EXT-STATE-GATE` case.
- `handlers/config-protection.ts` (R-WSRC-3 home-expansion) — INVARIANT: `isInsideRuntimeRoot` calls `expandLeadingHome` before `path.resolve` so `~`/`$HOME` write targets under `~/.claude/pickle-rick/**` are contained. BREAKS: `echo x > ~/.claude/pickle-rick/extension/bin/mux-runner.js` slips the guard because `path.resolve('~/...')` yields `<cwd>/~/...`. ENFORCE: `extension/tests/config-protection-state-files.test.js`. PATTERN_SHAPE: `path.resolve(expandLeadingHome(filePath))` in `isInsideRuntimeRoot`.
- `handlers/config-protection.ts` (AP-EXT-CASEFOLD case-insensitive filesystem) — INVARIANT: every protected-path comparison case-folds BEFORE matching — `matchProtectedStateBasename` lowercases the basename (before `stripTmpSuffix`, so `State.json.TMP.<pid>` is covered), `isInsideRuntimeRoot` lowercases BOTH the resolved path and the runtime root, and `PROTECTED_PATTERNS` + `shellPatternToRegex` carry `/i`. BREAKS: macOS/APFS and Windows are case-insensitive — `State.json` is the SAME INODE as `state.json` (verified) — so exact-equality against the all-lowercase `PROTECTED_STATE_BASENAMES` APPROVED `Write{file_path:"<session>/State.json"}`, `echo {} > STATE.JSON`, `Write Pickle_Settings.json`, and `Edit ~/.CLAUDE/pickle-rick/**`, defeating the state, settings, circuit-breaker and runtime-root gates at once via the default worker tool path (7/10 variants bypassed, proven 2026-07-19). Over-matching a genuinely distinct `STATE.JSON` on a case-SENSITIVE filesystem is fail-closed and intended; `allow_state_writes_reason` remains the escape hatch. Parity: `tsc-gate.ts` config regexes already carried `/i`. ENFORCE: `extension/tests/config-protection-state-files.test.js` (`AP-EXT-CASEFOLD` cases). PATTERN_SHAPE: `path.basename(filePath).toLowerCase()` in `matchProtectedStateBasename` AND `.toLowerCase()` on both operands in `isInsideRuntimeRoot` AND zero un-`/i` members of `PROTECTED_PATTERNS`.
- `handlers/config-protection.ts` (AP-EXT-ITER5-01) — INVARIANT: `shellPatternToRegex`'s bracket arm emits the fixed `[^/]`, never a copied class body. BREAKS: a descending range (`[anatomy-park]`) throws; the SyntaxError reaches the entrypoint catch, which calls `approve()` — config gate fails OPEN (6/8925 live commands). ENFORCE: `extension/tests/config-protection-state-files.test.js` `AP-EXT-ITER5-01` cases. PATTERN_SHAPE: that arm's `regex +=` right-hand side must stay a literal; interpolating a `pattern` slice into an emitted class is the regression.
- `handlers/config-protection.ts` (AP-EXT-EXECNAME one exec-token normalizer + one prelude) — INVARIANT: every "is this token command X?" comparison folds through `execName` (strip trailing `;` → basename → lowercase), and no detector re-inlines that fold. NO leading-command detector locates the executable POSITIONALLY any more: `segmentInvokesInstallSh` was the last, and AP-EXT-ITER63-04 gave it a WRAPPER anchor while RETAINING the `execTokenIndex` prelude (env assignments → optional shell wrapper → env assignments) as a first arm; `findGitVerb` left the prelude for the list-free `execAnchorIndex` (AP-EXT-ITER63-02), `parseFirstShellWord` was DELETED with it, and tsc-gate's `segmentIsGitCommit` followed (AP-EXT-ITER63-03). `segmentInvokesInstallSh` tokenizes via `tokenizeShellCommand`, not a bare `split(/\s+/)`. BREAKS: (a) case — `GIT --version` really runs git on a case-insensitive filesystem, so `=== 'git'` approved `GIT reset --hard`/`GIT push`/`GIT stash` while `git RESET --hard` blocked, because `findGitVerb` folded the VERB but not the EXECUTABLE (half the pair); same for `bash INSTALL.SH`/`BASH install.sh`; (b) path — `tokens[0] === 'bash'` missed `/bin/bash install.sh`; (c) quotes — the bare split read `"install.sh"` with quotes attached, so `bash "install.sh"` slipped (this was the last detector still off the quoted-token-parity fix); (d) ORDER — a wrapper-skip-then-env-skip prelude never recognized `PICKLE_ROLE=x bash install.sh`, since the shell writes assignments BEFORE the interpreter. All proven 2026-07-19; (d) was caught by its own new test, not by inspection. ENFORCE: `extension/tests/config-protection-state-files.test.js` (`AP-EXT-EXECNAME` cases). PATTERN_SHAPE: zero `=== 'git'|=== 'bash'|=== 'sh'|=== 'install.sh'` raw-token compares outside `execName`/`isShellWrapper`; `execTokenIndex(` called by `segmentInvokesInstallSh` (and by NO other detector — `segmentIsGitCommit` moved to `execAnchorIndex` in AP-EXT-ITER63-03, so an anchor demanding it there is stale and matches nothing); zero `parseFirstShellWord` anywhere in `extension/src/hooks/`; no `split(/\s+/)` in `segmentInvokesInstallSh`.
- `shell-exec.ts` (AP-EXT-EXECFOLD one exec-fold for BOTH handlers) — INVARIANT: `execName` lives in `hooks/shell-exec.ts` and is the ONLY strip-`;`/basename/lowercase fold; `config-protection.ts` and `tsc-gate.ts` both import it, neither redefines it. BREAKS: the two handlers each carried their own fold and DRIFTED — config-protection folded case, tsc-gate did not, so `GIT commit` blocked as a git verb yet classified non-commit at `segmentIsGitCommit`, skipping the R-WACT tsc gate and landing broken TypeScript (5/7 variants bypassed, proven 2026-07-19: `GIT`/`Git`/`/usr/bin/git`/env-prefixed/`cd x &&`-chained). Same replay shape closed the raw `!== 'node'` in `extractNodeTestPathFromSegment`, which let `NODE --test`/`/usr/bin/node --test` slip the R-CSIS-B1 expensive-test guard. RESIDUAL CLOSED by AP-EXT-ITER46-01 (`extractNodeTestPathFromSegment` moved onto `tokenizeShellCommand`) — see that entry. ENFORCE: `extension/tests/tsc-gate.test.js` (`AP-EXT-EXECFOLD` cases), `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-EXECFOLD replay` cases); both mutation-verified RED. PATTERN_SHAPE: exactly one `function execName(` in `extension/src/hooks/`, in `shell-exec.ts` (AP-EXT-ITER12-01 moved the segmenter/tokenizer to the same home for the same reason); zero `(===|!==) *'(git|gh|bash|sh|node|install\.sh)'` raw-token compares in either handler outside an `execName(...)`/`isShellWrapper(...)` result.
- `handlers/config-protection.ts` (WRITE_COMMANDS is a speed bump, not a fence) — INVARIANT: treat the bash write-target scanner as best-effort deterrence, NOT containment; the real containment is `state-manager.ts`'s write ceiling. BREAKS: reading the enumeration as a fence invites the guard-piling pathology (W5b). Verified APPROVED 2026-07-19 against a protected path: `node -e "require('fs').writeFileSync(...)"`, `python3 -c "open(...,'w')"`, `dd of=`, `truncate -s 0`, `install`, `ln -sf`, and any `$VAR`-indirected destination (the tokenizer does no expansion). Do NOT close these by appending interpreter names to `WRITE_COMMANDS` — a shell cannot be fenced by verb enumeration, and each addition raises the false-block surface on read-only invocations. ENFORCE: intentionally none — this entry records a known, accepted limit. PATTERN_SHAPE: `WRITE_COMMANDS` membership growth beyond the destination-arg + in-place-editor families.
- `handlers/config-protection.ts` (chained-command detection) — INVARIANT: `detectProhibitedGitVerb`, `isBashInvokingInstallSh`, and `extractNodeTestPaths` MUST evaluate every `splitShellSegments(command)` segment, not just the leading executable token. BREAKS: `cd sub && git reset`, `git status && git push`, `cd x && bash install.sh`, and `cd x && node --test <expensive>` slip the worker-forbidden-op guards (the CLAUDE.md-mandated `cd <subdir> && git <verb>` pattern makes chaining the common case). `splitShellSegments` is quote-aware so a commit message containing `&& git reset` is not mis-segmented. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js`. PATTERN_SHAPE: `splitShellSegments(` consumed by all three leading-command detectors; no detector reads `tokens[0]` of the raw unsegmented command.
- `handlers/config-protection.ts` (AP-EXT-ITER55-01 the verb read must not depend on git's option surface) — INVARIANT: `findGitVerb` returns the first bare word that NAMES a member of `GATED_GIT_VERBS`; NO table of operand-taking git global options. BREAKS: the old `ARG_CONSUMING_GIT_GLOBAL_OPTIONS` omitted `--config-env`, so `git --config-env x=V reset --hard` read the operand as the verb — all six prohibited verbs APPROVED, chained forms too (12/12 measured against the shipped handler). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER55-01`). PATTERN_SHAPE: one `execNamesIn(rest[i], GATED_GIT_VERBS)` scan of every bare word inside `findGitVerb`; zero git-option-name literals in that file.
- `handlers/config-protection.ts` (quoted-token parity) — INVARIANT: the R-WSRC-GR git-verb chain MUST tokenize each segment quote-aware (matching surrounding quotes stripped). Since AP-EXT-ITER63-02 collapsed the gate into the verb read, that is ONE site: `findGitVerb` tokenizes via `tokenizeShellTokens` — the quoting-carrying form `tokenizeShellCommand` is itself defined in terms of, so the unquoting is the same fold, not a second one. BREAKS: a bare `split(/\s+/)` reads `"git"`/`"reset"` with quotes attached, so `git "reset" --hard` and `"git" reset --hard` (the shell runs both as `git reset --hard`) slip the guard. Mirrors tsc-gate.ts:segmentIsGitCommit which already strips quotes. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js`. PATTERN_SHAPE: `tokenizeShellTokens(` used by `findGitVerb`; no `split(/\\s+/)` in it. An anchor demanding `tokenizeShellCommand(` in `findGitVerb`, or naming `parseFirstShellWord` at all, is stale and matches nothing.
- `handlers/config-protection.ts` (AP-EXT-ITER46-01 quoted-token parity reaches the expensive-test guard) — INVARIANT: `extractNodeTestPathsFromSegment` (renamed by AP-EXT-ITER54-02) tokenizes quote-aware via the `tokenizeShellTokens` family — `tokenizeShellCommand` is that tokenizer's `.value` projection, and AP-EXT-ITER63-05 moved this detector onto the token objects so it can share `execAnchorIndex` — never a bare split. BREAKS: quoted `node --test "<expensive>"` kept its quotes, the file read failed, and the R-CSIS-B1 soak guard APPROVED while the bare twin blocked (4/7 forms measured). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER46-01` cases; mutation-verified 1 RED, zero collateral). PATTERN_SHAPE: zero `split(/\s+/)` code lines (comment prose excluded) in `handlers/config-protection.ts`.
- `shell-exec.ts` (AP-EXT-ITER10-01 `bash -c` command-string unwrap) — INVARIANT: `splitShellSegments` expands a `bash -c '<cmd>'` / `sh -lc "<cmd>"` payload into its own segments via `expandShellCommandStrings` (depth-capped, wrapper segment KEPT), so all three detectors inherit the unwrap from the one seam. BREAKS: the tokenizer keeps `<cmd>` as ONE token, so the only executable a detector saw was the `-c` FLAG — `bash -c "git reset --hard"` and `bash -c "bash install.sh"` APPROVED while their bare twins blocked (proven 2026-07-27; the state-write gate never had the gap — it walks the unwrapped payload as one of `findBashWriteTarget`'s scopes, and since AP-EXT-ITER51-01 that is the ONLY reason, its tokenizer no longer splitting on quotes). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER10-01` cases; 8 mutation-RED, zero collateral). PATTERN_SHAPE: `expandShellCommandStrings(` at the `splitShellSegments` return AND `SHELL_COMMAND_STRING_FLAG_RE` present — both in `shell-exec.ts` since AP-EXT-ITER12-01, NOT `config-protection.ts`. RESIDUAL CLOSED by AP-EXT-ITER14-01 (escape-aware spans) — see that entry. Unescaped nesting (`bash -c 'bash -c "git push"'`) was blocked even pre-fix, which is what proved the recursion live. Correction to this entry's former residual text: the bare `git \"reset\" --hard` approving is CORRECT, not a gap — bash passes git the literal word `"reset"` (quotes included), which git rejects as an unknown subcommand, so no reset ever runs.
- `shell-exec.ts` (AP-EXT-ITER12-01 one segmenter for BOTH handlers — CLOSES AP-EXT-ITER10-02) — INVARIANT: `splitShellSegments` (with its `-c` unwrap) lives in `hooks/shell-exec.ts` and is the ONLY shell segmenter; `config-protection.ts` and `tsc-gate.ts` both import it, neither defines one. Same for `tokenizeShellCommand`/`isShellWrapper`/`ENV_ASSIGNMENT_RE`. BREAKS: each handler carried a private near-identical copy and DID drift — config-protection gained the unwrap (AP-EXT-ITER10-01) while tsc-gate's copy did not, so `segmentIsGitCommit` read `execName(tokens[gitIdx])` = `bash`, classified non-commit, and the R-WACT tsc gate was SKIPPED for every `bash -c "git commit …"` form while the bare twin gated (6/6 bypassed, proven 2026-07-27 against the shipped export). This is the AP-EXT-EXECFOLD failure shape one level up: `execName` was the fold, this is the split. ENFORCE: `extension/tests/tsc-gate.test.js` (`AP-EXT-ITER12-01` behavior cases + the ONE-home structural pin). Mutation-verified both ways: restoring a private non-unwrapping `splitTopLevelSegments` in tsc-gate → 2 RED, zero collateral; deleting the unwrap from the shared seam → 8 config-protection AP-EXT-ITER10-01 cases RED *and* the tsc-gate case, proving the seam is load-bearing for both consumers, not a copy. PATTERN_SHAPE: exactly one `function splitShellSegments(` in `extension/src/hooks/`, in `shell-exec.ts`; zero `function splitTopLevelSegments(`/`function splitShellSegments(` in either handler; both handlers import `splitShellSegments` from `../shell-exec.js`. RESIDUAL CLOSED by AP-EXT-ITER14-01: the inherited escaped-quote tokenizer gap (`bash -c "bash -c \"git commit\""` classifying non-commit) is fixed at the same shared seam.
- `shell-exec.ts` (AP-EXT-ITER27-01 one exec-token PRELUDE for BOTH handlers) — INVARIANT: `skipEnvAssignments` and the `execTokenIndex` prelude built from it (env assignments -> optional shell wrapper -> env assignments) live in `hooks/shell-exec.ts`; `config-protection.ts` and `tsc-gate.ts` import them and neither re-types an `ENV_ASSIGNMENT_RE`-shaped literal. BREAKS: the prelude was the last un-collapsed member of the `execName`/`splitShellSegments` family and had forked FOUR ways — `tsc-gate.ts:segmentIsGitCommit` and `config-protection.ts:extractNodeTestPathFromSegment` each carried a hand-typed `/^[A-Za-z_][A-Za-z0-9_]*=/`, and tsc-gate's copy stopped after the env arm with no wrapper skip, under a comment asserting it was "the identical regex". Same drift shape as AP-EXT-ITER12-01 one level down: a widened shared regex would reach config-protection's detectors and silently skip tsc-gate's, so an env-prefixed commit form classifies non-commit and the R-WACT tsc gate is SKIPPED. ENFORCE: `extension/tests/tsc-gate.test.js` (`AP-EXT-ITER27-01` structural pin + env/wrapper behavior cases). PATTERN_SHAPE: exactly one `function execTokenIndex(` in `extension/src/hooks/`, in `shell-exec.ts`; zero `[A-Za-z_][A-Za-z0-9_]*=` regex literals in either handler; each handler imports its exec-LOCATING helper from `../shell-exec.js` and defines none privately — `config-protection.ts` takes `execTokenIndex`, `tsc-gate.ts` takes `execAnchorIndex` (AP-EXT-ITER63-03). An anchor demanding `execTokenIndex` in BOTH is stale: the one-home rule is about where the helper LIVES, not which one a caller needs.
- `shell-exec.ts` (AP-EXT-ITER14-01 escape-aware quoted spans — CLOSES the ITER10/ITER12 residual) — INVARIANT: ONE pair of quoted-span patterns (`DOUBLE_QUOTED_SPAN`, `SINGLE_QUOTED_SPAN`) backs BOTH scanners (`TOKEN_SCAN_RE`, `SEGMENT_SCAN_RE`). The double-quoted span honors backslash escapes and a double-quoted token is UNESCAPED via `DOUBLE_QUOTE_ESCAPE_RE` (`"`, `\`, `$`, backtick only — bash's real set). The single-quoted span MUST NOT become escape-aware: inside `'…'` bash keeps a backslash literal. Unescaping is scoped to the QUOTED form — a bare `git \"reset\"` runs git with the literal word `"reset"`, which git rejects, so unescaping it would invent a false block. BREAKS: `"[^"]*"` stops at the first escaped quote and desynchronizes every later span, so `bash -c "bash -c \"git reset --hard\""` APPROVED — a live bypass of every worker-forbidden-op guard AND the R-WACT tsc gate (shim-verified that the shell really executes the reset), while both its bare twin and its alternate-quoted twin blocked. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` + `extension/tests/tsc-gate.test.js` (`AP-EXT-ITER14-01` cases). Mutation-verified BOTH halves independently: reverting the span → 7 RED, dropping the unescape → 7 RED, each all-ITER14 with zero collateral. PATTERN_SHAPE: zero `"[^"]*"` quoted-span literals anywhere in `extension/src/`; `DOUBLE_QUOTED_SPAN` consumed by both `TOKEN_SCAN_RE` and `SEGMENT_SCAN_RE` — since AP-EXT-ITER53-01 both reach it through the shared `WORD_PART_SOURCE` alternation rather than naming it directly, so an anchor demanding the literal constant on either scanner's own line reads as a phantom violation; `SINGLE_QUOTED_SPAN` contains no `\\\\`.
- `shell-exec.ts` (AP-EXT-ITER18-01 a GLUED control operator is still a boundary) — INVARIANT: the raw-token split that separates a control operator from its neighbors is DERIVED from `SHELL_SEGMENT_SEPARATORS` (`GLUED_SEPARATOR_RE`), never a hardcoded character — declaring an operator a separator is what makes the tokenizer yield it as a boundary. Alternation order is deliberately uncurated: `&` before `&&` degrades the two-character operator into two adjacent one-character separators, landing the boundary in the same place, so no longest-first sort is needed (verified — removing it changes no behavior). `\n` is excluded because `\S` cannot match one. BREAKS: bash does NOT require whitespace around a control operator — a `git` shim confirms `git status&&git reset --hard` really executes the reset — but the split hardcoded `/(;)/`, so `&&`, `||`, `|`, and `&` survived inside one `\S+` token. The segment's leading command read `git status`, no detector saw the second command, and `git reset --hard` / `push` / `stash` / `rebase` / `bash install.sh` all APPROVED for a worker while their spaced twins blocked, AND `git add -A&&git commit` classified non-commit so the R-WACT tsc gate was skipped (6/6 + 6/6 bypassed, proven 2026-07-27 against the shipped export). Same shape as AP-EXT-ITER12-01 one level down: the set declared six operators, the tokenizer honored two. ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js (`AP-EXT-ITER18-01` cases), extension/tests/tsc-gate.test.js (`AP-EXT-ITER18-01` behavior + derivation pin). Mutation-verified: restoring `/(;)/` → 9 RED, zero collateral. PATTERN_SHAPE: `GLUED_SEPARATOR_RE` built from `[...SHELL_SEGMENT_SEPARATORS]` and consumed by the `.split(` inside `pushWordBoundaryTokens` — the per-word helper `splitShellSegments` delegates to since AP-EXT-ITER53-01; an anchor naming a `raw.split(` inside `splitShellSegments` itself is stale and matches nothing. Zero `raw.split(/(;)/)` in `extension/src/`.
- `shell-exec.ts` (AP-EXT-ITER19-01 a GROUPED command is still a command) — INVARIANT: `SHELL_SEGMENT_SEPARATORS` declares the grouping / command-substitution delimiters `(`, `)`, `{`, `}`, and a backtick alongside the six control operators; `GLUED_SEPARATOR_RE` derives the split from the set, so no second hardcoded regex is added. `$` needs no member — splitting on `(` reduces `$(` to a lone `$` segment. BREAKS: bash starts a command at each of those (shim-verified: `(git reset --hard)`, `{ git reset --hard; }`, `$(git reset --hard)`, and a backtick substitution all really execute the reset), but the set omitted them, so the grouped form stayed ONE segment whose leading token was `(git` — `execName` folds that to `(git`, matching no detector. Every prohibited verb APPROVED when wrapped in parens while its bare twin blocked, and `(git commit …)` classified non-commit so the R-WACT tsc gate was SKIPPED (12/12 + 5/5 bypassed, proven 2026-08-05 against the shipped export). Third instance of the AP-EXT-ITER12-01/ITER18-01 shape: the machinery derives boundaries from one declarative set and the set was incomplete. ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js (`AP-EXT-ITER19-01` cases), extension/tests/tsc-gate.test.js (`AP-EXT-ITER19-01` behavior + declaration pin). Mutation-verified: removing the five members → 9 RED + 2 RED, zero collateral. PATTERN_SHAPE: `SHELL_SEGMENT_SEPARATORS` is a single `new Set([...])` literal containing `'('`, `')'`, `'{'`, `'}'`, and a backtick; no second glued-split regex beside `GLUED_SEPARATOR_RE`.
- `shell-exec.ts` (AP-EXT-ITER53-01 ADJACENT quoting inside one word is still one word) — INVARIANT: both scanners model a bash WORD as one or more ADJACENT parts (`WORD_PART_SOURCE`: a quoted span, a run of ordinary characters, or a lone unmatched quote), never a quoted span OR a bare `\S+` run. `tokenizeShellTokens` folds a word's parts through `foldShellWord`, unquoting each quoted part and marking the word `quoted` only when EVERY part was quoted; `splitShellSegments` decides boundaries per PART via `pushWordBoundaryTokens`, so a quoted operator stays data while an unquoted one still ends the word. Both halves of that quoted-ness rule are load-bearing in OPPOSITE directions: the all-parts reading keeps the redirect pass honest (a value of `>` is data only when the `>` itself was quoted — see AP-EXT-ITER51-01), and the per-part boundary decision keeps `"echo x"&&"git" reset --hard` splitting at the unquoted `&&` that a starts-and-ends-with-a-quote test would swallow. BREAKS: bash concatenates a word's parts, so a PARTIALLY quoted word kept its quote characters in the token and every `execName` / verb / write-anchor compare missed — `ba"sh" install.sh`, `bash insta"ll".sh` and `bash install"".sh` bypassed the R-WSRC manager-only `install.sh` ban; `git rese"t" --hard`, `gi"t" stash` and `git commit --am"end"` bypassed the R-WSRC-GR data-loss guard; `t"ee" state.json` and `s"ed" -i state.json` bypassed the R-WSRC-3 state-write gate. Shim-verified that bash really runs all three forms; measured against the shipped export before the fix. Fourth instance of the AP-EXT-ITER51-01 shape — the scanner's model of a bash word disagreed with bash — and one seam re-opened three guards at once. ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js (`AP-EXT-ITER53-01` cases). Mutation-verified all THREE halves independently, each with zero collateral: reverting the tokenizer fold to whole-token unquoting -> 12 RED; a whole-token quoted test in `pushWordBoundaryTokens` -> 1 RED (the two-quoted-words boundary); restoring the pre-fix `SEGMENT_SCAN_RE` -> 2 RED (the leading- and double-quoted fragments). PATTERN_SHAPE: `TOKEN_SCAN_RE` and `SEGMENT_SCAN_RE` both built as `(?:${WORD_PART_SOURCE})+`; zero `|\\S+` alternatives left in either scanner; `foldShellWord` marking `quoted` with `!sawUnquoted` (never a bare first/last-character test); `pushWordBoundaryTokens` testing `unquoteSpan(part) !== null` per part — AP-EXT-ITER66-01 collapsed the former boolean is-a-quoted-span predicate into that ONE parser so the grammar, the delimiter strip and the unescape cannot disagree about which quoting forms exist, and an anchor naming the retired predicate matches nothing.
- `shell-exec.ts` (AP-EXT-ITER54-01 an option OPERAND is a bare word before `-c`) — INVARIANT (amended by AP-EXT-ITER93-08, which widened the take from the flag's neighbour to the whole tail): `shellCommandStringPayloads` finds `SHELL_COMMAND_STRING_FLAG_RE` by scanning forward from the wrapper — never "the first token that does not start with `-`", and never behind an enumerated operand-taking-option table (that is the AP-EXT-ITER18-01/ITER19-01 incomplete-set shape one option away from the next bypass). BREAKS: bash options take OPERANDS and an operand is a bare word, so the first-bare-word scan quit at `pipefail` before reaching `-c`: `bash -o pipefail -c "git reset --hard"` was never unwrapped, every detector saw only the wrapper, and the reset APPROVED for a worker while its bare twin blocked — AND the same form classified non-commit so the R-WACT tsc gate was SKIPPED (`-o`, `+o`, `-O`, `--rcfile`, `--init-file` all bypassed; measured 2026-08-25 against the shipped export). Fifth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01 shape. Over-reach is fail-safe in this module's existing direction (`bash script.sh -c arg` yields one extra segment; the wrapper segment is kept). ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js (`AP-EXT-ITER54-01` cases), extension/tests/tsc-gate.test.js (`AP-EXT-ITER54-01` behavior + forward-read shape pin). Mutation-verified: restoring the first-bare-word scan → 12 RED, zero collateral. PATTERN_SHAPE (amended by AP-EXT-ITER93-08, then by AP-EXT-ITER93-03 which moved the shape TEST behind a predicate so it could be asked of a witness): `if (isShellCommandStringFlag(tokens[idx])) return tokens.slice(idx + 1);` inside `shellCommandStringPayloads`; zero `startsWith('-')` and zero option-name literals in that function body. RESIDUAL CLOSED by AP-EXT-ITER54-02 (`extractNodeTestPathsFromSegment` now hands the caller EVERY candidate) — see that entry.
- `shell-exec.ts` (AP-EXT-ITER63-01 a non-bash shell is still a shell) — INVARIANT: `isShellWrapper` tests the shell NAMING SHAPE (`SHELL_INTERPRETER_NAME_RE`, `/^[a-z]*sh$/` over the `execName` fold), never a name list; the dot in `install.sh` is what keeps a script from folding to a wrapper. BREAKS: the two-member `bash`/`sh` set was the AP-EXT-ITER54-01 incomplete-declaration shape one member from a bypass, and reached it — `shellCommandStringPayloads` is empty for a rejected token and the `-c` payload is ONE token, so `zsh -c 'git reset --hard'` APPROVED for a worker while its byte-identical `bash`/`sh` twins blocked, and `zsh install.sh` read its exec token as `zsh` (`/bin/zsh`, `/bin/dash`, `/bin/ksh` all present on a stock macOS box; measured 2026-08-26 against the shipped export). Sixth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01 shape. Over-reach is fail-safe in this module's existing direction: `ssh` matches the shape, which only moves the exec-token read onto the host. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER63-01`. Mutation-verified: restoring the two-member set → 11 RED, zero collateral. PATTERN_SHAPE (amended by AP-EXT-ITER93-01, which moved the fold behind a witness): `isShellWrapper` body is one `SHELL_INTERPRETER_NAME_RE.test(shellShapeWitness(execName(token)))`; zero shell-name string literals in that function (the shape's tail constant is not a name list — see that entry). RESIDUAL CLOSED for the git chain by AP-EXT-ITER63-02 (`findGitVerb` reads a `git` ANCHOR, not a position) — see that entry for the three replay residuals still OPEN on the sibling detectors.
- `handlers/config-protection.ts` (AP-EXT-ITER63-02 a POSIX command PREFIX is not the exec — CLOSES the ITER63-01 residual) — INVARIANT: the R-WSRC-GR git chain locates git by ANCHOR, not by position: `findGitVerb` calls the shared `execAnchorIndex(tokens, 'git')` (`shell-exec.ts`), which returns the first token the shell may exec as `git` wherever it sits, demoting a quoted token only when it is not at `execTokenIndex` — the SAME AP-EXT-ITER51-02 quoting rule `findWriteTargetInScope`'s Pass 2 applies, shared rather than restated. The `parseFirstShellWord` gate is DELETED; the anchor read subsumes it. There is NO command-prefix table and must never be one. BREAKS: a POSIX command prefix is an ordinary program that takes a command as its argument and execs it, so it stands in exec position with the real executable behind it. `parseFirstShellWord` read `env` and the whole chain skipped the segment — `env git reset --hard`, `command git push`, `nohup git push`, `nice git stash`, `exec`, `time`, `sudo`, `timeout`, `setsid`, `stdbuf`, `xargs` and chained/wrapped/quoted forms all APPROVED for a worker while their byte-identical bare twins blocked (16 of 17 forms measured 2026-08-26 against the shipped export; `env`/`command`/`nohup`/`nice`/`exec`/`time` each verified to really exec git on this box). Seventh instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01 shape — and the enumerated FIX is the trap, not the bug: the prefix set is open-ended, so any table is one member from the next bypass. Over-reach is fail-safe in this module's direction: a bare unquoted `git` argument to another program (`echo git reset`) now anchors and may over-block, where every prefixed form under-blocked before. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER63-02` (19 prefix block cases, 9 non-tautology approve twins, both quoting directions, an INVENTED prefix no table would carry, and a structural pin that `parseFirstShellWord` stays deleted). Mutation-verified: restoring the positional prelude read -> 20 RED, all AP-EXT-ITER63-02, zero collateral. PATTERN_SHAPE: `execAnchorIndex(tokens, 'git')` in `findGitVerb`; zero `parseFirstShellWord` and zero command-prefix name literals (`'env'`/`'nohup'`/`'nice'`/`'command'`) in `extension/src/hooks/`. RESIDUALS ALL CLOSED (AP-EXT-ITER74-01 correction — this lead-in read RESIDUALS OPEN over three detectors for a full pass after each was fixed): (AP-EXT-ITER63-03) tsc-gate `segmentIsGitCommit` — FIXED 2026-08-26 by the same `execAnchorIndex` collapse; the DIRECT prefixed form this entry claimed was "still open" gates today, measured on the shipped mirror (`env git commit -m x`, `nohup`, `timeout 600`, quoted `env 'git'` — all classify commit; `gh pr create` and `echo hi` still do not); (AP-EXT-ITER63-04) `segmentInvokesInstallSh` — now FIXED 2026-08-26 by a WRAPPER anchor, see its own entry below (residual: the wrapper-less `env ./install.sh` direct exec); (AP-EXT-ITER63-05) `extractNodeTestPathsFromSegment` — now FIXED 2026-08-26, see its own entry below. (AP-EXT-ITER63-06) was the shared root and is now FIXED (see its own entry below); it shrank -03 so only the DIRECT prefixed form (`env git commit`) is still open there — `env bash -c "git commit -m x"` classifies commit again. The write guard's UNQUOTED forms are mitigated by construction — `findWriteTargetInScope`'s Pass 2 scans every token for a `WRITE_COMMANDS` anchor, so `env tee <session>/state.json`, `nohup tee …` and `env sed -i … ` block today (verified) — but its QUOTED forms were NOT: that claim was falsified by the AP-EXT-ITER64-01 replay, which measured `env 'tee' <session>/state.json` APPROVING. Tracked as AP-EXT-ITER64-02, now FIXED (see its own entry below).
- `handlers/config-protection.ts` (AP-EXT-ITER54-02 an option OPERAND hides the test path) — INVARIANT: `extractNodeTestPathsFromSegment` returns EVERY bare word after `--test`; the caller picks the one `isExpensiveTestFile` confirms. BREAKS: first-bare-word stopped on the operand, so `node --test --test-reporter spec <expensive>` APPROVED the soak (8/12 forms). ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js (`AP-EXT-ITER54-02`). PATTERN_SHAPE: `candidates.push(t)` in that scan; zero `return t`/`break` in it; zero `'--test[a-z-]+'` literals in the file.
- `handlers/config-protection.ts` (AP-EXT-ITER19-02 a GROUPED write is still a write — CLOSES the ITER19-01 open gap) — INVARIANT: `findBashWriteTarget` normalizes redirect operators ONCE via `normalizeRedirectOperators`, then walks BOTH the normalized command AND every `splitShellSegments` segment, first hit wins; the scanner's tokenizer does not normalize. Order is load-bearing: `>|` ends in a pipe and `>&<file>` in a background `&`, so segmenting an un-normalized command splits `(echo x >| f)` into `echo x >` and `f` and severs the redirect from its destination. The union (raw + segments) is deliberate — both scopes are fail-closed, so a destination that survives only in the raw token stream keeps its reach. BREAKS: the then-current scanner tokenizer (the since-deleted `tokenizeBashCommand`) split on whitespace and quotes only, so a grouped destination stayed glued to its delimiter (`state.json)`) and matches no protected basename — 10/12 grouped writes APPROVED while their bare twins blocked, proven 2026-08-05 against the shipped export (`(…)`, `{ …; }`, `$(…)`, backtick, `sed -i`, `tee`, `cp`, `mv`, across the state, settings, and config gates). Do NOT fix by appending verbs to `WRITE_COMMANDS` — see the sibling speed-bump entry. ENFORCE: `extension/tests/config-protection-state-files.test.js` (`AP-EXT-ITER19-02` cases). Mutation-verified BOTH halves independently: dropping the segment scopes → 12 RED, segmenting the un-normalized command → 1 RED (clobber-override), each zero collateral. PATTERN_SHAPE: `normalizeRedirectOperators(command)` called before `splitShellSegments` in `findBashWriteTarget`, whose scope list is `[normalized, ...splitShellSegments(normalized)]`; zero redirect `.replace(` calls outside `normalizeRedirectOperators`.
- `handlers/config-protection.ts` (newline segmentation) — INVARIANT: `splitShellSegments` MUST treat an unquoted newline as a segment boundary (`'\n'` ∈ `SHELL_SEGMENT_SEPARATORS`; the tokenizer matches `\n` before `\S+`). BREAKS: `git status\ngit reset --hard`, `git add\n…\ngit reset`, `cd x\nbash install.sh`, and `cd x\nnode --test <expensive>` collapse to one segment and slip the worker-forbidden-op guards (a worker emits sequential commands one per line). Quoted multi-line commit messages stay one token. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js`. PATTERN_SHAPE: `'\n'` present in `SHELL_SEGMENT_SEPARATORS` AND `|\n|` alternative in the `splitShellSegments` tokenizer regex.
- `handlers/tsc-gate.ts` — INVARIANT: only git-commit-class Bash commands trigger the gate; blocking failures emit `tsc_gate_failed`, override bypasses emit `tsc_gate_override_used`, and clean flagged commits auto-clear `allow_tsc_failed_reason` with `tsc_gate_override_consumed` via `StateManager.update`. BREAKS: broken-tsc commits slip through or the manager override persists past the next clean commit. ENFORCE: `extension/tests/tsc-gate.test.js`. PATTERN_SHAPE: `isGitCommitCommand`
- `handlers/tsc-gate.ts` (chained-command detection) — INVARIANT: `isGitCommitCommand` MUST test every `splitShellSegments` segment, not just the cd-stripped leading command, so `git add -A && git commit` (the CLAUDE.md-canonical commit form) still triggers the gate. BREAKS: chained add+commit skips the R-WACT tsc backstop, letting broken-TS commits through. ENFORCE: `extension/tests/tsc-gate.test.js`. PATTERN_SHAPE: `splitShellSegments(command).some(segmentIsGitCommit)` in `isGitCommitCommand`, importing from `../shell-exec.js`.
- `handlers/tsc-gate.ts` (AP-EXT-ITER55-01 parity) — INVARIANT: `segmentIsGitCommit` skips every `-`-prefixed token and classifies on the first bare word that is `commit` or a `NEGATIVE_GIT_SUBCOMMANDS` member; NO operand-taking-option table. BREAKS: the old table omitted `--config-env`, so `git --config-env x=V commit -m x` read the operand as the subcommand, classified non-commit, and SKIPPED the R-WACT tsc gate for a broken-TS commit (measured `false` pre-fix). ENFORCE: `extension/tests/tsc-gate.test.js` (`AP-EXT-ITER55-01`). PATTERN_SHAPE: bare-word scan in `segmentIsGitCommit`; zero git-option-name literals in that file.
- `handlers/tsc-gate.ts` (boolean-flag parity) — INVARIANT: `segmentIsGitCommit` MUST skip ANY leading `-`-prefixed flag before reading the subcommand (a non-arg-consuming git global boolean option `--no-pager`/`-p`/`--paginate`/`--no-optional-locks`/`--bare`, or an `=`-glued arg-option). BREAKS: `git --no-pager commit` reads the boolean flag as the subcommand → classified non-commit → the R-WACT tsc gate is SKIPPED for a broken-TS commit (proven; findGitVerb skips ALL flags so this was a one-sided parity gap). ENFORCE: `extension/tests/tsc-gate.test.js`. PATTERN_SHAPE: generic `if (token.startsWith('-')) continue;` as the ONLY flag arm in `segmentIsGitCommit`, with no per-option `startsWith('--git-dir=')` allowlist.
- `handlers/tsc-gate.ts` (newline segmentation) — INVARIANT: the shared `splitShellSegments` MUST treat an unquoted newline as a segment boundary (`'\n'` ∈ `SHELL_SEGMENT_SEPARATORS`; the tokenizer matches `\n` before `\S+`). BREAKS: `git add -A\ngit commit` collapses to one segment whose subcommand reads `add`, skipping the R-WACT tsc gate so a broken-TS commit slips through (a worker emits sequential commands one per line). Quoted multi-line commit messages stay one token. ENFORCE: `extension/tests/tsc-gate.test.js`. PATTERN_SHAPE: `'\n'` present in `SHELL_SEGMENT_SEPARATORS` AND `|\n|` alternative in the `splitShellSegments` tokenizer regex — both now in `shell-exec.ts`, NOT in `tsc-gate.ts`; an anchor pointing at the handler reads as a phantom violation (AP-EXT-ITER12-01).
- `handlers/tsc-gate.ts` (AP-EXT-ITER52-01 a git-VALID argv, and no shim may rewrite it) — INVARIANT: `checkout-index` carries NO `--stage` flag (default IS stage 0); no `tests/` shim rewrites a production argv. BREAKS: `--stage=0` exits 128, so EVERY TypeScript commit answered `setup_error` — green for a year behind a `git` shim that stripped the flag. ENFORCE: extension/tests/tsc-gate.test.js#AP-EXT-ITER52-01. PATTERN_SHAPE: `--stage` in the `checkout-index` argv, or a `tests/` shim re-exec'ing with argv other than verbatim `"$@"`.
- `handlers/config-protection.ts` (R-WSRC-3 redirect tokenizer) — INVARIANT: `normalizeRedirectOperators` (extracted by AP-EXT-ITER19-02 from the scanner tokenizer AP-EXT-ITER51-01 later deleted, and run by `findBashWriteTarget` BEFORE segmenting) isolates every output-redirect-to-file operator BEFORE the general `>` pass so the dest is never glued: `>|` (clobber-override) and `>&<filename>` (the `&>`-equivalent dup-to-file write) both normalize to ` > `. The `>&` normalization uses negative lookahead `(?![\d-])` so it fires ONLY on the file-write form (`>&state.json`, `>& state.json`) and leaves fd-dup/close untouched (`2>&1`, `>&2`, `>&-`, `1>&2` must NOT be treated as writes or legitimate redirections false-block). BREAKS: a glued redirect operator hides the protected basename and bypasses the state-write guard; dropping the `(?![\d-])` lookahead false-blocks ubiquitous fd-dup redirections. ENFORCE: `extension/tests/config-protection-state-files.test.js`. PATTERN_SHAPE: `.replace(/>\|/g, ' > ')` AND `.replace(/>&(?![\d-])/g, ' > ')` both present before the `(^|[^>])>` pass in `normalizeRedirectOperators` — the only redirect-rewriting site in the file; an anchor pointing at a scanner tokenizer reads as a phantom violation.
- `resolve-state.ts` — INVARIANT: hook cwd matching must compare canonical realpaths via the `sameWorkingDir` helper, not raw `path.resolve()` strings. BREAKS: `/var` vs `/private/var` aliases make active sessions look foreign, so config-protection/tool-error fail open and stop-hook exits early. ENFORCE: `extension/tests/resolve-state.test.js`, `extension/tests/stop-hook.test.js`. PATTERN_SHAPE: `sameWorkingDir(` on hook/session lookup paths.
- `resolve-state.ts` (lookup precedence) — INVARIANT: the exported helpers compose a strict order — `selectScannedStateFile` → `resolveStateFile` → `loadActiveState` — and `loadActiveState` is the single authority feeding the hook `approve` decision shape. Stale-map / dead-pid / wrong-cwd / inactive sessions must demote BELOW a live same-cwd fallback before `approve` returns. BREAKS: skipping any helper makes config-protection / tsc-gate / stop-hook fail open or attach to dead owners. ENFORCE: `extension/tests/resolve-state.test.js`, `extension/tests/config-protection.test.js`, `extension/tests/stop-hook.test.js`. PATTERN_SHAPE: `selectScannedStateFile|resolveStateFile|loadActiveState|approve`.
- `handlers/stop-hook.ts` (AP-EXT-ITER4-01) — INVARIANT: the CLI entry guard compares `path.basename(process.argv[1])`, never a realpath-exact form. BREAKS: `dispatch.ts` builds argv from an un-realpathed `EXTENSION_DIR`, so through a symlinked install root the hook emits nothing and dispatch's fallback approves the stop. ENFORCE: `extension/tests/stop-hook-promise-invariance.test.js`. PATTERN_SHAPE: `process.argv[1]` compared against an `import.meta.url`-derived value.
- `handlers/config-protection.ts` (AP-EXT-ITER51-01 a quoted `>` is DATA, not a redirect) — INVARIANT: `findWriteTargetInScope` tokenizes via `tokenizeShellTokens` (quoted span = ONE token, quoting preserved) and requires Pass 1's ANCHOR — the `>`/`>>` operator — to be UNQUOTED; only the anchor, since a DESTINATION is legitimately quoted (`> "state.json"`). Pass 2's exec anchor is NOT gated on quoting alone — see AP-EXT-ITER51-02 below, which corrected that half: quoting a WORD does not stop bash execing it, so a quoted `WRITE_COMMANDS` token is demoted only when it is not in command position. BREAKS: the deleted `tokenizeBashCommand` split ON quote characters, so a commit message was shredded into bare words and `normalizeRedirectOperators` (quote-blind by design) turned its `->` into an operator whose next word was a protected basename — `git commit -m "worker -> state.json write ordering"` BLOCKED as a state write, refusing the worker's own commit. Reproduced against the shipped hook; 4 lines in this repo's git history trip it, and it blocked a read-only `grep` during the review that found it. Ten prior commits here only ever WIDENED the scanner; none asked whether the `>` was quoted. Do NOT re-fix by making `normalizeRedirectOperators` quote-aware — that NARROWS it and regressed `sh -lc 'echo x >| state.json'` from block to approve (measured), because a `-c` payload's redirects must be isolated before `splitShellSegments` splits `>|` on its trailing `|`. Pass 1's unquoted check is defense-in-depth and currently unreachable (the quote-blind normalizer rewrites any quoted `>` to a spaced form, so no quoted token's value is ever exactly `>`); Pass 2's is live and mutation-pinned. ENFORCE: extension/tests/config-protection-state-files.test.js (`AP-EXT-ITER51-01` cases). Mutation-verified: restoring the quote-splitting tokenizer → 8 RED, zero collateral; dropping the Pass-2 gate → 1 RED (that gate has since been narrowed to non-exec-position by AP-EXT-ITER51-02). PATTERN_SHAPE: any `split(/[\s'\"]+/)` in `extension/src/hooks/` (a tokenizer that splits ON quotes erases the quoted/unquoted distinction its scanner needs); `findWriteTargetInScope` reading `tokens[i]` without a `.quoted` test on the REDIRECT operator anchor (the EXEC anchor carries none by design — AP-EXT-ITER64-02).
- `shell-exec.ts` (AP-EXT-ITER63-06 a command PREFIX hid the whole `-c` payload — the shared root of the positional-exec-read family) — INVARIANT: `shellCommandStringPayloads` anchors the WRAPPER wherever it sits (`tokens.findIndex(isShellWrapper)`), then reads forward for the command-string flag. No positional gate, no prefix table, no separate env-assignment skip — `KEY=value` cannot match the interpreter naming shape, so the prelude was redundant, and collapsing the two same-theme guards into one anchor lowered the branch count. BREAKS: gating on `isShellWrapper(tokens[skipEnvAssignments(tokens)])` failed on the PREFIX, and because the payload is ONE quoted token a failed test hid the ENTIRE command string from every detector at once — measured 2026-08-26 against the shipped export, `env bash -c "git reset --hard"`, `nohup sh -c "git push origin main"`, `command zsh -c …`, `timeout 5 bash -c …` and `env bash -c "bash install.sh"` ALL APPROVED for a worker while their byte-identical unprefixed twins blocked, re-opening the git-verb, install.sh, expensive-test and R-WSRC-3 state-write guards in one stroke. Eighth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02 shape. The widened anchor is a strict SUPERSET of the old read (the post-env index is still scanned, just no longer the only one), so no command that blocked before can stop blocking now — over-reach lands on an extra benign segment, never on a lost block. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER63-06` (7 prefix block cases incl. an INVENTED prefix no table would carry, the install.sh arm, 4 non-tautology approve twins, and a structural pin that the payload resolves AT, BEFORE and AFTER the post-env index). Mutation-verified: restoring the positional read on the compiled mirror -> 9 RED, all AP-EXT-ITER63-06, zero collateral. PATTERN_SHAPE: `tokens.findIndex((token) => isShellWrapper(token))` in `shellCommandStringPayloads`; zero `isShellWrapper(tokens[` positional reads and zero `skipEnvAssignments(` calls in `shellCommandStringPayloads`; zero command-prefix name literals (`'env'`/`'nohup'`/`'nice'`/`'command'`/`'timeout'`) in `extension/src/hooks/`.
- `handlers/config-protection.ts` (AP-EXT-ITER51-02 a quoted COMMAND is still an exec — CLOSES the ITER51-01 over-correction; SUPERSEDED IN PART by AP-EXT-ITER64-02, read that entry with this one) — INVARIANT (as amended): a quoted word in command position is an exec like any other and must anchor Pass 2. The `i !== execTokenIndex(...)` half that used to carry this rule is DELETED — it was itself a bypass (AP-EXT-ITER64-02); Pass 2 now has no quoting arm at all, which enforces this invariant strictly more. Quoting demotes the two anchors asymmetrically because bash treats them asymmetrically: quoting is exactly how you turn a redirect OPERATOR back into data (`echo '>' x` writes nothing), while quoting a WORD changes nothing about execing it. BREAKS: with the anchor gated on quoting ALONE, one pair of quotes re-opened every R-WSRC-3 write guard in the file — `'tee' <session>/state.json`, `"sed" -i '' s/a/b/ <session>/state.json`, `'cp' /tmp/x <session>/state.json`, `"mv" …`, `'/usr/bin/tee' …`, `PICKLE_ROLE=x 'tee' …`, `echo x | "tee" …` all APPROVED for a worker while their bare twins blocked. Reproduced against the shipped hook in BOTH directions (blocked at 551057d, approved at the ITER51-01 commit), and shim-verified that bash really execs `'tee'`. The false positive ITER51-01 removed stays removed: `git commit -m "sed" -i <file>` hands `sed` to git as a commit message, nothing execs it, and it is not at the exec index. ENFORCE: extension/tests/config-protection-state-files.test.js#AP-EXT-ITER51-02 (9 block cases + 2 approve cases). Mutation-verified: restoring the quoting-alone gate on the compiled mirror → 9 RED, zero collateral. PATTERN_SHAPE: zero `.quoted` reads guarding Pass 2's exec test in `findWriteTargetInScope` (the arm this entry once REQUIRED is now forbidden — see AP-EXT-ITER64-02; the test itself became `execNamesIn` at AP-EXT-ITER73-02, so pin the absent quoting read, not the operator). RESIDUAL CLOSED 2026-08-26.
- `handlers/config-protection.ts` (AP-EXT-ITER47-01 `sed` without `-i` is a READER) — INVARIANT: Pass 2 of `findWriteTargetInScope` anchors on the exec name AND its invocation mode: `anchorWritesPositionalArg` returns true for every `WRITE_COMMANDS` member except the `IN_PLACE_ONLY_WRITERS` (`sed`), which anchors only when an arg in scope satisfies `isInPlaceFlag` (`--in-place[=…]`, or a single-dash cluster carrying `i` — `-i`, `-i.bak`, `-i''`, `-ni`; asked of a WITNESS since AP-EXT-ITER93-07, because bash expands an option word too). Membership in `IN_PLACE_ONLY_WRITERS` demands a TOTAL implication "no in-place flag => no positional-arg write"; `perl` is deliberately excluded because `perl -e` opens its own argument with no flag to key on, and the editors always write. BREAKS: anchoring on the bare name blocked pure READS of protected paths — `sed -n '1,200p'`, `sed -e`, `sed -f`, `sed -E -n`, `sed -n … | grep`, `sed --expression='s/a/i/'` — 6/6 measured against the shipped handler across the state, settings AND config gates, while `cat`/`grep`/`head` on the same paths approved. Same over-block class as AP-EXT-ITER51-01's refused worker commit, and it fires on the anatomy-park worker protocol's own mandated `sed -n` read of `~/.claude/pickle-rick/**`. Fail-closed residuals (measured, accepted — do NOT close by teaching the scanner sed's option grammar, that is the enumeration-fence trap): (a) the flag scan spans the whole scope, not the leading flag run, so GNU's permuted `sed 's/a/b/' -i FILE` still blocks AND a later segment's `-i` re-arms an earlier read in the RAW union scope; (b) a glued option value carrying an `i` (`sed -e's/a/i/' FILE`) reads as an in-place cluster and blocks. Both are strictly narrower than the pre-fix state, which blocked EVERY `sed` read. ENFORCE: `extension/tests/config-protection-state-files.test.js` (`AP-EXT-ITER47-01`: 6 read-only approve cases, 9 in-place block cases, 4 unaffected-sibling cases, 2 config-gate parity cases). Mutation-verified: forcing `anchorWritesPositionalArg` to `return true` on the compiled mirror → exactly 7 RED, zero collateral. PATTERN_SHAPE: `anchorWritesPositionalArg(name, argsInScope)` guarding the arg walk in `findWriteTargetInScope`, `IN_PLACE_ONLY_WRITERS` containing `sed` and NOT `perl`, and `WRITE_COMMANDS` membership unchanged (narrow the ANCHOR, never the set — see the sibling `single write-command class` entry).
- `shell-exec.ts` (AP-EXT-ITER64-01 a quoting exception re-admitted the positional exec read) — INVARIANT: `execAnchorIndex` applies ONE uniform test, `execNameIs(tokens[i].value, name)`, over every token; it reads NO exec index and NO `.quoted` flag. Quoting is not a signal an exec guard may key on — the shell strips it before the program runs. BREAKS: the old body spared a quoted token unless it sat AT `execTokenIndex`, and a POSIX command PREFIX stands at exactly that index, so the real exec one token later was demoted to "data" and the scan returned -1 — `findGitVerb` saw no git and the whole R-WSRC-GR chain skipped the segment. Measured 2026-08-26 against the shipped handler: `env 'git' reset --hard`, `env "git" …`, `nohup 'git' push`, `command 'git' stash`, `nice 'git' rebase`, `exec 'git' …`, `sudo "git" push` and `PICKLE_ROLE=x env 'git' push` — 8 of 8 APPROVED for a worker while their byte-identical unquoted twins blocked, all shim-verified to really exec git. Ninth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-06 shape, and the first where the positional read re-entered through a SIDE CONDITION rather than the main read. Removing the exception is a SUBTRACTION, not a widening: the false positive it existed to spare (`echo 'git' reset`) has an unquoted twin (`echo git reset`) that over-blocks anyway — measured — so it suppressed nothing and only taught the bypass to add quotes. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER64-01` (9 prefix block cases incl. an INVENTED prefix no table would carry, 9 non-tautology unquoted twins, a quoting-invariance pin, and a structural pin that the function body reads no exec index). Mutation-verified: restoring the quoting exception on the compiled mirror -> exactly 10 RED, all AP-EXT-ITER64-01, zero collateral. PATTERN_SHAPE: zero `execTokenIndex`, zero `skipEnvAssignments` and zero `.quoted` references inside the `execAnchorIndex` function body (the compare itself moved to `execNameIs` at AP-EXT-ITER73-01 — pin the absent positional read, not the operator); zero command-prefix name literals (`'env'`/`'nohup'`/`'nice'`/`'command'`) in `extension/src/hooks/`. RESIDUAL CLOSED 2026-08-26 by AP-EXT-ITER64-02, which applied the identical collapse to `findWriteTargetInScope`'s Pass 2 and kept Pass 1's redirect anchor unquoted-gated.

- `handlers/config-protection.ts` (AP-EXT-ITER64-02 the same quoting exception re-opened every state-write guard — CLOSES the ITER64-01 residual) — INVARIANT: `findWriteTargetInScope`'s Pass 2 applies ONE uniform test over every token — `execNamesIn(tokens[i].value, WRITE_COMMANDS)` since AP-EXT-ITER73-02 replaced the literal set-membership read this entry used to name; it reads NO exec index and NO `.quoted` flag. Pass 1's `>`/`>>` REDIRECT anchor keeps its UNQUOTED test and is the ONLY `.quoted` read left in the function — the two anchors are asymmetric because bash treats them asymmetrically, and only the EXEC one is safe to make quoting-blind (collapsing both would re-open AP-EXT-ITER51-01). BREAKS: Pass 2 spared a quoted `WRITE_COMMANDS` token unless it sat AT `execTokenIndex`, and a POSIX command PREFIX stands at exactly that index, so `i !== execIndex` was true OF THE REAL EXEC and demoted it to "data". Measured 2026-08-26 against the shipped hook: `env 'tee' <session>/state.json`, `nohup 'cp' …`, `command "mv" …`, `env 'sed' -i '' …`, plus `nice`/`sudo`/`timeout` forms — 7 of 7 APPROVED for a worker while every bare twin BLOCKED, re-opening every R-WSRC-3 protected-state-file write guard to a prefix plus one pair of quotes; four shim-verified to really exec the write command. Tenth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-06/64-01 shape, and the SECOND where the positional read re-entered through a side condition — ITER64-01 predicted this exact sibling in prose and the prediction held. Removing the exception is a SUBTRACTION (the `execTokenIndex` call goes with it): the false positive it existed to spare (`git commit -m "sed" -i <file>`) has an unquoted twin (`git commit -m sed -i <file>`) that over-blocks anyway — measured both directions — so it suppressed nothing and only taught the bypass to add quotes. The false positive that IS real, a write command inside a commit MESSAGE, is one quoted span that `execName` never folds to a `WRITE_COMMANDS` member and needs no quoting arm to spare it. ENFORCE: `extension/tests/config-protection-state-files.test.js#AP-EXT-ITER64-02` (17 prefixed+quoted block cases incl. an INVENTED prefix no table would carry, 5 non-tautology bare twins, a quoting-invariance pin, and 3 read-path approve cases proving the collapse did not turn reads into writes) + the structural pin in `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER64-01` (Pass 2 body reads no exec index; exactly ONE `.quoted` remains, Pass 1's). Mutation-verified: restoring the quoting exception on the compiled mirror -> 19 RED, all AP-EXT-ITER64-02 / ITER51-01 parity, zero collateral. PATTERN_SHAPE: zero `execTokenIndex`, zero `execIndex` and exactly ONE `.quoted` reference (the Pass 1 redirect anchor) inside the `findWriteTargetInScope` function body. RESIDUALS CLOSED — no detector reads the exec POSITIONALLY any more: (AP-EXT-ITER63-03) tsc-gate `segmentIsGitCommit`, (AP-EXT-ITER63-05) `extractNodeTestPathsFromSegment` and (AP-EXT-ITER63-04) `segmentInvokesInstallSh` are all FIXED 2026-08-26, each with its own entry below.
- `handlers/tsc-gate.ts` (AP-EXT-ITER63-03 a POSIX command PREFIX hid the commit from the R-WACT tsc gate) — INVARIANT: `segmentIsGitCommit` locates git by ANCHOR — `execAnchorIndex(tokens, 'git')` — never by position. It reads NO exec index and NO `.quoted` flag, and there is no command-prefix table. The `gh` exclusion the prelude needed is GONE with it: `gh pr create` carries no `git` anchor, so it returns -1 rather than needing its own arm. BREAKS: the old `execTokenIndex(tokens)` read landed on the PREFIX, `execName` folded it to `env`, the segment classified NON-commit, and the R-WACT tsc gate was SKIPPED for a broken-TS commit — `env`/`command`/`nohup`/`nice`/`exec`/`time`/`sudo`/`timeout 600`/`setsid`/`stdbuf -o0`/`xargs`, plus chained, grouped, quoted and `-c`-wrapped forms, 11 of 11 measured 2026-08-26 against the shipped export while every bare twin GATED; `env`/`command`/`nohup`/`nice`/`exec`/`time` each shim-verified to really exec git on this box. Twelfth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-05/63-06/64-01/64-02 shape. Over-reach is fail-safe in this guard's direction: a bare `git` in argument position (`echo git commit`) now merely RUNS the tsc gate, which approves a clean tree. ENFORCE: `extension/tests/tsc-gate.test.js#AP-EXT-ITER63-03` (22 prefix gate cases incl. an INVENTED prefix no table would carry, 8 non-tautology non-commit twins, and a structural pin that the body reads no positional exec index). Mutation-verified BOTH halves independently, each zero collateral: restoring the positional read on the compiled mirror -> exactly 1 RED (the behavior case); restoring it in SOURCE -> exactly 1 RED (the structural pin). PATTERN_SHAPE: `execAnchorIndex(tokens, 'git')` inside `segmentIsGitCommit`; zero `execTokenIndex`, zero `skipEnvAssignments` and zero `.quoted` references in that function body AFTER COMMENT-STRIPPING (this entry's own rationale names `execTokenIndex` in prose to say why it is gone — an un-stripped grep counts that mention and reads as a phantom violation); zero command-prefix name literals (`'env'`/`'nohup'`/`'nice'`/`'command'`/`'setsid'`/`'stdbuf'`) in `extension/src/hooks/handlers/tsc-gate.ts` — `'timeout'` is deliberately EXCLUDED from that list, unlike the sibling config-protection pin, because it is a legitimate `GateFailureKind` union member here and barring it would be an anchor false at birth. RESIDUAL CLOSED — (AP-EXT-ITER63-04) `segmentInvokesInstallSh` was the LAST detector reading the exec POSITIONALLY and is FIXED 2026-08-26. This entry correctly predicted that the `execAnchorIndex` shape would NOT transfer (it would make a read-only `cat install.sh` BLOCK, and the test pinning that approve is outside `scope.json`); the fix anchored the WRAPPER instead and left all four read-only pins green. See its own entry below.
- `handlers/config-protection.ts` (AP-EXT-ITER63-05 a POSIX command PREFIX hid node from the expensive-test guard — CLOSES the last ITER63-02 residual) — INVARIANT: `extractNodeTestPathsFromSegment` locates node by ANCHOR — `execAnchorIndex(tokens, 'node')` — never by position. It reads NO exec index and NO `.quoted` flag, and there is no command-prefix table. BREAKS: the old `execName(tokens[skipEnvAssignments(tokens)]) !== 'node'` gate matched the PREFIX, so the segment yielded NO candidates, `isExpensiveTestFile` was never consulted, and the R-CSIS-B1 soak guard APPROVED — `env`/`command`/`nohup`/`nice`/`exec`/`time`/`sudo`/`timeout 600`/`setsid`/`stdbuf -o0`/`npx` and chained/grouped/quoted forms, 12 of 12 measured 2026-08-26 against the shipped hook, while every bare twin BLOCKED. Eleventh instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-06/64-01/64-02 shape. The anchor is a strict SUPERSET of the old post-env read — an env assignment cannot fold to `node`, so the anchor lands on the same token whenever the old read fired and nothing that blocked before can stop blocking now. Over-reach is fail-safe in this guard's direction: a candidate only blocks when it names a real file whose first line is `// @tier: expensive`. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER63-05` (22 prefix block cases incl. an INVENTED prefix no table would carry, 22 non-tautology benign-file twins, 4 quoted-exec cases, 5 data/npm-entry-point approve cases, and a structural pin that the body reads no positional exec index). Mutation-verified: restoring the positional read on the compiled mirror -> exactly 23 RED, all AP-EXT-ITER63-05, zero collateral. PATTERN_SHAPE: `execAnchorIndex(tokens, 'node')` inside `extractNodeTestPathsFromSegment`; zero `skipEnvAssignments`, zero `execTokenIndex` and zero `.quoted` references in that function body; zero command-prefix name literals (`'env'`/`'nohup'`/`'nice'`/`'command'`/`'timeout'`/`'npx'`) in `extension/src/hooks/handlers/config-protection.ts`. RESIDUALS CLOSED — both remaining positional readers are FIXED 2026-08-26: (AP-EXT-ITER63-03) tsc-gate `segmentIsGitCommit` and (AP-EXT-ITER63-04) `segmentInvokesInstallSh`, each with its own entry.

- `handlers/config-protection.ts` (AP-EXT-ITER63-04 a POSIX command PREFIX hid the deploy script from R-WSRC — CLOSES the LAST positional exec read) — INVARIANT: `segmentInvokesInstallSh` treats install.sh as EXECED when it is the exec token (the old read, RETAINED as a first arm) OR when it follows a shell WRAPPER anchored wherever it sits (`tokens.findIndex((token) => isShellWrapper(token))`), the shape `shellCommandStringPayloads` took in AP-EXT-ITER63-06. There is NO command-prefix table and must never be one. BREAKS: the positional `tokens[execTokenIndex(tokens)]` read landed on the PREFIX and the deploy-script test failed — `env`/`command`/`nohup`/`nice`/`exec`/`time`/`sudo`/`timeout 600`/`setsid`/`stdbuf -o0` before `bash install.sh`, plus chained/grouped/quoted/`-c`-wrapped forms: 13 of 13 measured 2026-08-26 against the shipped compiled hook while both controls (bare and quoted) BLOCKED, re-opening the R-PIPE-3 / R-WSRC hard-forbidden worker deploy op in full. Thirteenth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-03/63-05/63-06/64-01/64-02 shape. The four siblings' `execAnchorIndex(tokens, <name>)` collapse does NOT transfer: they anchor on an EXECUTABLE (`git`, `node`) that no read-only command takes as an argument, while this anchors on a SCRIPT that read-only commands routinely do — `cat install.sh`, `vim install.sh`, `git log install.sh` are pinned APPROVE in `extension/tests/config-protection.test.js` (OUTSIDE this branch's `scope.json`) and `config-protection-state-files.test.js`. Nothing list-free separates them by the script token alone; the WRAPPER is the discriminator that needs no table. The widened test is a strict SUPERSET of the old read, so no command that blocked before can stop blocking. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER63-04` (25 prefixed block cases incl. an INVENTED prefix no table would carry, 8 non-tautology read-only/differently-named approve twins behind the SAME prefixes, and a structural pin that the body no longer reads the exec position ALONE). Mutation-verified BOTH halves independently, each zero collateral: restoring the positional read on the compiled mirror -> exactly 25 RED, all AP-EXT-ITER63-04; restoring it in SOURCE -> exactly 1 RED (the structural pin). PATTERN_SHAPE: `findIndex((token) => isShellWrapper(token))` inside `segmentInvokesInstallSh`; that body must NOT contain `execTokenIndex` WITHOUT also containing `isShellWrapper` (the exec-token arm is retained on purpose — it is what keeps a bare `./install.sh` blocking — so a zero-`execTokenIndex` anchor here would be false at birth); zero command-prefix name literals (`'env'`/`'nohup'`/`'nice'`/`'command'`/`'timeout'`/`'npx'`) in `extension/src/hooks/handlers/config-protection.ts`. RESIDUAL OPEN, reported not claimed closed: a prefixed DIRECT exec with NO wrapper (`env ./install.sh`, `nohup ./install.sh`) still APPROVES — measured this pass. It offers no wrapper to anchor on, and separating it from `cat ./install.sh` provably requires the prefix enumeration this family exists to refuse; the unprefixed `./install.sh` blocks via the exec-token arm. Deliberately NOT pinned as an `approve` test — that would turn a known hole into a contract.

- `shell-exec.ts` (AP-EXT-ITER66-01 bash has FOUR word-quoting forms, the grammar declared TWO) — INVARIANT: `WORD_PART_SOURCE` declares `$'…'` and `$"…"` beside `'…'`/`"…"`; `UNQUOTED_RUN` stops before a `$` that introduces a span; ONE `unquoteSpan` strips all four and decodes `$'…'` numeric escapes. BREAKS: the `$` glues to the span, `$'git'` folds to `$git`, and R-WSRC-GR, the `install.sh` ban and both R-WSRC-3 write gates APPROVE — 10/10 measured, shim-verified. `$'\x67it'` needs the decode. ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER66-01. PATTERN_SHAPE: a quoting form bash accepts that `WORD_PART_SOURCE` does not name, or a `$` `UNQUOTED_RUN` can consume before a quote.
- `shell-exec.ts` (AP-EXT-ITER66-02 a THROW in this scanner is a blanket approve) — INVARIANT: `decodeAnsiCEscapes` resolves a numeric escape only inside the Unicode range and otherwise returns the escape text verbatim; the tokenizer throws on NO input. BREAKS: `String.fromCodePoint` raises past U+10FFFF, `dispatch.ts` fails OPEN, so `$'\UFFFFFFFF' ; git reset --hard` disarmed every guard in config-protection at once (measured, shim-verified) — strictly worse than the bypass AP-EXT-ITER66-01 closed. ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER66-02. PATTERN_SHAPE: any unguarded `String.fromCodePoint` (or a sibling code-point constructor) on parsed input reachable from `tokenizeShellTokens`, or any throw path in a fail-open hook scanner.
- `handlers/config-protection.ts` (AP-EXT-ITER69-01 an EXPORTING handler must not run on import) — INVARIANT: the module-level `main()` sits behind the same basename `process.argv[1]` entry guard both sibling handlers carry, never a bare call. BREAKS: this module exports `detectProhibitedGitVerb` and `PROTECTED_WRITE_GLOBS` "for downstream tools that import the handler for auditing", and the bare call made that documented import run the whole hook — measured on the shipped mirror: with stdin ignored it printed `{"decision":"approve"}` ahead of the importer's own stdout, and with stdin an open never-written pipe (the stdio `node --test` and every spawn-based harness give a child) the import NEVER RETURNED. `tsc-gate.ts` carries this guard under a commit that reads "add CLI guard so test imports do not block on stdin"; `stop-hook.ts` had its hardened as a CRITICAL. This file never had one. Basename compare, NEVER realpath-exact (AP-EXT-ITER4-01) — a guard that misses `dispatch.ts`'s un-realpathed argv emits no decision and dispatch approves, strictly worse than the bug. Mutation-verified both directions on the compiled mirror: unconditional `main()` reds exactly the import case with 368 siblings green; a guard that never fires reds 355 including the anti-disarm control. ENFORCE: extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER69-01. PATTERN_SHAPE: a module-level entry invocation (column-0 `main(`/`run*(`) in `extension/src/hooks/` or `extension/src/bin/` in a file that also has `^export ` lines and no `process.argv[1]` basename guard. Replay swept all of `src/`: the only other unguarded entry calls are `hooks/dispatch.ts`, `hooks/handlers/tool-error.ts` and `bin/test-runner.ts` — each has ZERO exports, so nothing can import them and the unsafe path has no consumer; the first two are also outside this branch's scope.json.
- `shell-exec.ts` (AP-EXT-ITER70-01 `eval` is bash's OTHER word-to-code construct) — INVARIANT: `expandShellCommandStrings` unwraps EVERY construct that hands the shell a WORD as CODE — a wrapper's `-c` operand AND the joined arguments of a word-to-code BUILTIN (`wordToCodeBuiltinPayload`, generalized from the former eval-only extractor by AP-EXT-ITER71-01). BREAKS: `eval` is a BUILTIN, so `isShellWrapper`'s naming shape cannot reach it and the payload stayed ONE opaque token: all 9 gated git verbs, the `install.sh` ban and BOTH R-WSRC-3 write gates APPROVED behind `eval` while every bare twin blocked (measured + shim-verified 2026-08-26). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER70-01`; 23 mutation-RED, zero collateral). PATTERN_SHAPE: `execAnchorIndex(tokens, builtin)` inside `wordToCodeBuiltinPayload`; zero `execTokenIndex` / `.quoted` reads in that body. RESIDUAL (was AP-EXT-ITER70-02, CLOSED below): the here-string half was a WORD all along; what remains open is only code arriving as a program's OUTPUT (`echo '<cmd>' | sh`, `$(echo '<cmd>')`), which leaves no word to unwrap.
- `shell-exec.ts` (AP-EXT-ITER70-02 a here-string operand is a WORD, not fd data) — INVARIANT: `expandShellCommandStrings` also unwraps the here-string operand (`hereStringPayload`), matched on the FOLDED token value so the glued `<<<'<cmd>'` and fd-prefixed `0<<<` forms count, and skipped when the word is wholly quoted. BREAKS: ITER70-01 filed here-strings as fd DATA; 14 forms — `bash <<<`, `sh <<<`, `bash -s <<<`, `source|. /dev/stdin <<<`, glued, fd-prefixed — APPROVED for a worker across all three gates while every bare twin blocked (measured + shim-verified 2026-08-26). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER70-02`; 19/19/4/2 mutation-RED in four directions, zero collateral). PATTERN_SHAPE: `HERE_STRING_OPERATOR_RE.test(token.value)` plus a `token.quoted` skip inside `hereStringPayload`; zero `isShellWrapper` / `execTokenIndex` reads in that body. RESIDUAL CLOSED (AP-EXT-ITER71-01, below): `trap '<cmd>' EXIT` was the OTHER builtin that re-parses a word as code.
- `shell-exec.ts` (AP-EXT-ITER71-01 `trap` is the OTHER word-to-code builtin) — INVARIANT: `WORD_TO_CODE_BUILTINS` declares BOTH `eval` and `trap` and ONE extractor consumes it. BREAKS: 8 `trap '<cmd>' <signal>` forms APPROVED all three gates while their bare twins blocked (shim-verified: the handler really execs git at shell exit). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER71-01`; 6 mutation directions RED, incl. 10 approve-side prose controls). PATTERN_SHAPE: `execAnchorIndex(tokens, builtin)` looping `WORD_TO_CODE_BUILTINS`; the family has exactly ONE extractor — no per-builtin payload function beside it. RESIDUAL: `mapfile -C` is unmeasurable here (bash 3.2 lacks it); `source <file>`, `… | sh`, `$(…)` stay open — no word to unwrap.
- `shell-exec.ts` (AP-EXT-ITER73-01 bash EXPANDS the command word, so a glob NAMES the command) — INVARIANT: every "is this word command X?" compare goes through `execNameIs`, which reads a pattern-bearing word as the PATTERN it is via the ONE shared `shellPatternToRegex` (moved here from `handlers/config-protection.ts`, whose `isProtectedShellPattern` asks the same question of a write DESTINATION); a word of pure wildcards names nothing (`patternNamesACommand`). BREAKS: pathname expansion applies to the command word like any other, so `/usr/bin/gi?`, `g?t`, `gi[t]` really exec git and `bash instal?.sh` really runs the deploy script (shim-verified: `/usr/bin/gi? --version` prints the git version), while `execName` folds them to `gi?`/`instal?.sh` — expansion is NOT quoting, so the FOLD cannot undo it and the COMPARISON must. Measured against the shipped handler: all 9 gated verbs plus the chained, grouped, env-prefixed and `bash -c` carriers APPROVED for a worker while their literal twins blocked (11/11), and `bash instal?.sh` slipped the install.sh ban. Fifteenth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-06/64-01/64-02/66-01/70-01/71-01/72-01 shape, and the first where the missing declaration is an EXPANSION rather than a quoting form. The all-wildcard bound is not a taste call: without it the config guard's block count over the 3815 glob-bearing commands in 8778 real worker Bash commands rose 94 -> 217, 519 anchors being the bare `*` inside a heredoc body — a guard that blocks `cat > f <<EOF` is a reliability cost, not a safety margin. With it: 94 -> 96, zero blocks lost. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER73-01` (15 globbed-verb block cases across every carrier, 3 install.sh forms, 5 non-tautology approve controls from the live corpus, the `execNameIs` predicate matrix incl. the all-wildcard bound, and the one-translator structural pin; mutation-verified BOTH directions on the compiled mirror — reverting the compare to `===` reds exactly the globbed-verb case, dropping the wildcard bound reds exactly the predicate case, zero collateral either way). PATTERN_SHAPE: exactly one `function shellPatternToRegex(` in `extension/src/hooks/`, in `shell-exec.ts`; `execAnchorIndex` and `segmentInvokesInstallSh`'s `isDeployScript` both compare through `execNameIs(`; zero `execName(...) === '<name>'` equality compares in `extension/src/hooks/`. RESIDUALS: (a) is CLOSED 2026-08-29 by AP-EXT-ITER73-02 — Pass 2 reads the set through `execNamesIn` and the ten measured globbed-write forms all block; see its own entry. (b) is CLOSED 2026-08-29 by AP-EXT-ITER93-01 — the shape is asked of a WITNESS, and the ten measured globbed-wrapper forms all block; see its own entry. (c) is CLOSED 2026-08-29 by AP-EXT-ITER73-04 — both write-DESTINATION arms read through the same shared reader and the measured globbed forms all block; see its own entry. Still reported rather than claimed closed: (d) an all-bracket spelling (`/usr/bin/[g][i][t]`) reads as unnamed by (the deliberate) `patternNamesACommand`.
- `shell-exec.ts` (AP-EXT-ITER93-01 a WRAPPER is a SHAPE, and bash expands the command word too) — INVARIANT: `isShellWrapper` asks `SHELL_INTERPRETER_NAME_RE` of a WITNESS (`shellShapeWitness`) — the folded word with each SINGLE-POSITION wildcard (`?`, a bracket expression, a brace alternation) filled with the character the shape wants at that position — never of the raw fold. ONE uniform test, not a literal arm plus a pattern arm: a word carrying no wildcard is its own witness. The shape has ONE declaration, `SHELL_INTERPRETER_NAME_TAIL`, which both the regex and the fill are built from, so the two readings cannot drift. BREAKS: AP-EXT-ITER73-01 taught the exec-NAME compares to read a pattern-bearing word as the pattern it is, but this seam asks a SHAPE, so `execNameIs` could not reach it and the raw-fold regex matched nothing over letters — `/bin/ba?h`, `/bin/b?sh`, `/bin/zs?`, `/bin/[b]ash`, `/bin/{b,z}ash` all really exec a shell (shim-verified on this box) while `shellCommandStringPayloads` came back empty, and the `-c` payload is ONE quoted token, so the ENTIRE command string was hidden from every detector at once: 10/10 measured forms APPROVED for a worker across the R-WSRC-GR git chain, the install.sh ban and BOTH R-WSRC-3 write gates while their literal twins blocked. Sixteenth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-06/64-01/64-02/66-01/70-01/71-01/72-01/73-01 shape, and the AP-EXT-ITER63-06 blast radius reached through the AP-EXT-ITER73-01 seam. The single-position bound is MEASURED, not taste: filling `*` as well turned markdown emphasis inside a `cat > file <<EOF` artifact body (`**`, `**PASS**`, `release/**`) into a wrapper and flipped 3 worker artifact writes from approve to BLOCK over 10126 real worker Bash commands — a blocked artifact write stalls a ticket, the reliability cost `patternNamesACommand` and `execNamesIn` already refuse. With the bound: ZERO of 10126 flip. The `*`-bearing word needs no arm of its own — it survives unfilled into the witness and fails the shape. Quoting is deliberately not consulted, the AP-EXT-ITER64-01 uniform reading. PAIRED FIX, same commit: `segmentInvokesInstallSh`'s wrapper arm takes `tokens.slice(wrapper)`, the anchor token INCLUDED, because one word can now be BOTH readings — `./install?sh` and `./*sh` name the deploy script AND fill the interpreter shape, so the `execTokenIndex` SKIP in the first arm walked past the script and `slice(wrapper + 1)` started one token too late; both blocked before this predicate learned to expand, so including the anchor keeps the pass a widening rather than a trade. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER93-01` (10 globbed-wrapper block cases across every carrier, the install.sh + both write-gate arms, 6 non-tautology approve controls taken from the live corpus, the both-readings block case, the predicate matrix incl. the `*` bound, and a structural pin on the one-declaration shape). Mutation-verified in FOUR directions, zero collateral each: pre-fix predicate on the compiled mirror -> 3 RED; unfilled bound removed -> 2 RED; `slice(wrapper + 1)` restored -> 1 RED; pre-fix predicate in SOURCE only -> 1 RED (the structural pin). PATTERN_SHAPE: `SHELL_INTERPRETER_NAME_RE.test(shellShapeWitness(execName(token)))` is the whole body of `isShellWrapper`; `SINGLE_POSITION_WILDCARD_RE.test(position)` inside `shellShapeWitness` and zero `includes('*')` there; `SHELL_INTERPRETER_NAME_RE` built from `SHELL_INTERPRETER_NAME_TAIL` rather than written as a second literal; `tokens.slice(wrapper).some(isDeployScript)` in `segmentInvokesInstallSh`. RESIDUALS, reported not claimed closed: (a) a `*`-bearing spelling (`ba*`, `*sh`) stays unread — the deliberate price of the 3, same family as AP-EXT-ITER73-06; (b) a multi-character brace alternation (`{bash,zsh}`) counts as ONE position and so fails the shape; (c) a pure-wildcard word (`??`) over-reads AS a wrapper — measured zero decision flips, and over-reach only adds a segment to scan. REPLAY, both shim-verified this pass and left OPEN: AP-EXT-ITER93-02 — the git VERB is still a literal set read (`GATED_GIT_VERBS.has(rest[i].toLowerCase())`, and tsc-gate's `token === 'commit'`), so with a file named `reset` in cwd `git rese?` APPROVES and really hard-resets; AP-EXT-ITER93-03 — an option FLAG is still a literal test (`SHELL_COMMAND_STRING_FLAG_RE`, `isInPlaceFlag`), so with a file named `-c` in cwd `bash -? '<cmd>'` APPROVES and really runs the payload. Expansion applies to EVERY word, and only the command word has learned it.
- `shell-exec.ts` (AP-EXT-ITER72-01 a BACKSLASH is bash's cheapest quoting mechanism) — INVARIANT: the word grammar declares the unquoted backslash escape (`UNQUOTED_ESCAPE`) as `WORD_PART_SOURCE`'s FIRST alternative, `UNQUOTED_RUN` cannot swallow a backslash, and ONE decoder (`unquotedEscapeChar`) is consumed by BOTH `foldShellWord` (so the fold sees the word bash will run) and `pushWordBoundaryTokens` (so an escaped separator stays data). Two rules, both bash's: `\<newline>` is a line CONTINUATION contributing NOTHING, `\<char>` contributes that character. There is deliberately NO table of escaped command names — quoting a non-special character is a no-op for EVERY character, so the grammar needs no enumeration. BREAKS: the backslash fell into `UNQUOTED_RUN` as an ordinary character, so `execName` folded `\git` to `\git`; because `execName` is the subsystem's single exec-token seam, ONE backslash defeated every detector built on it at once — 5/5 gated verbs that block bare APPROVED as `\git <verb>`, `isGitCommitCommand` missed `\git commit` (skipping the R-WACT tsc gate), `execName('\tee')`/`('\sed')` missed every `WRITE_COMMANDS` member (re-opening both R-WSRC-3 write gates), and `isShellWrapper('\bash')` was false so an escaped wrapper's `-c` payload was never unwrapped (measured 2026-08-26 against the shipped mirror; shim-verified that `\git`, `g\it`, `gi\t` and the `gi\<newline>t` continuation form ALL really exec git). Fourteenth instance of the AP-EXT-ITER10-01/12-01/18-01/19-01/54-01/63-01/63-02/63-06/64-01/64-02/66-01/70-01/71-01 incomplete-declaration shape, and the cheapest bypass in the family — it needs no delimiter, no prefix and no builtin. The module already decoded the HARDER grammar correctly (`$'\x67it'` blocked via `decodeAnsiCEscapes`), so this was an inconsistency inside its own decoding. An escaped character is counted UNQUOTED in `foldShellWord` on purpose: the opposite reading would let an attacker demote a real redirect operator by escaping it, where this direction merely over-blocks a `\>` bash would not redirect — over-block, never under-block. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER72-01` (34 cases: all 9 gated verbs escaped, the escape in all three word positions, the escape on the VERB rather than the exec, the line-continuation form, both write gates, the install.sh ban, 9 non-tautology approve twins incl. `find -exec \;` and escaped commit prose, a quoting-invariance pin and a segment-boundary pin; mutation-verified RED in FOUR directions — exact pre-fix grammar 48, decoder-always-null 48, continuation-kept-as-newline 4, escaped-separator-is-a-boundary 2 — zero collateral in every direction). PATTERN_SHAPE: `UNQUOTED_ESCAPE` is the first alternative of `WORD_PART_SOURCE`; `unquotedEscapeChar(` is called in BOTH `foldShellWord` and `pushWordBoundaryTokens`; zero escaped-command-name literals (`'\\git'`/`'\\bash'`/`'\\tee'`/`'\\sed'`) anywhere in `extension/src/hooks/`. RESIDUAL: code arriving as a program's OUTPUT (`echo '<cmd>' | sh`, `$(echo '<cmd>')`) stays open — no word to unwrap — and a `$VAR`-indirected exec is unreachable by construction (the tokenizer does no expansion), the accepted limit the sibling `WRITE_COMMANDS is a speed bump` entry records.
- `shell-exec.ts`; `handlers/config-protection.ts` (AP-EXT-ITER73-02 a globbed WRITE command bypassed both R-WSRC-3 write gates — CLOSES the ITER73-01 residual (a)) — INVARIANT: `findWriteTargetInScope`'s Pass 2 asks `execNamesIn(tokens[i].value, WRITE_COMMANDS)` — a SET read, never a literal membership test of the fold — and the anchor writes when ANY member the word names does (`names.some(anchorWritesPositionalArg)`), so a word naming several resolves fail-closed with no arbitrary pick. `WRITE_COMMANDS` is a LIST, not a Set: `.has` exists to make the literal read fast, so keeping one preserves the seam. `execNamesIn` carries ONE bound `execNameIs` does not — a pattern is read only when EVERY wildcard consumes exactly one position (`?`, bracket, brace), so the word still SPELLS the member. BREAKS: bash expands the command word, so `/usr/bin/t?e`, `/bin/c?`, `/bin/m?`, `/usr/bin/s?d -i`, `te?`, `vi?`, `per?`, `rsyn?`, `{t,q}ee` and `[t]ee` over a protected state path all APPROVED for a worker while every literal twin BLOCKED (10/10 measured against the shipped handler; shim-verified on this box that each glob really execs tee/cp/mv/sed). Sixteenth instance of the AP-EXT-ITER10-01/…/72-01/73-01 shape. The eliding bound is not a taste call and must NOT be pushed down into `execNameIs`: asking TWELVE short names at once makes a `*` hit by accident, and measured over 10122 real worker Bash commands an unbounded set read flipped 21 worker artifact writes from approve to BLOCK — markdown emphasis (`**No`, `*no`, `s**`, `v*`) inside a `cat > file <<EOF` body naming `nano`/`sed`/`vi`. A blocked artifact write stalls a ticket. With the bound: 10/10 bypasses blocked, 0 over-block deltas over the same corpus, 0 blocks lost. Pushing it down would under-block the git seam for nothing (`gi*` really execs git, pinned by ITER73-01). ENFORCE: `extension/tests/config-protection-state-files.test.js#AP-EXT-ITER73-02` (10 globbed-write block cases each with a non-tautology literal twin, 3 corpus-shaped heredoc-prose approve cases, the `execNamesIn` predicate matrix incl. the multi-member and eliding arms and an `execNameIs('gi*')` scoping pin, and the structural pin). Mutation-verified BOTH directions on the compiled mirror: restoring the literal set read reds exactly the 10 globbed-write cases; dropping the eliding bound reds exactly the 3 prose cases plus the matrix — zero collateral either way. PATTERN_SHAPE: `execNamesIn(tokens[i].value, WRITE_COMMANDS)` inside `findWriteTargetInScope`; zero `WRITE_COMMANDS.has(` and zero `new Set(` binding `WRITE_COMMANDS` in `extension/src/hooks/handlers/config-protection.ts`. RESIDUAL, recorded rather than claimed closed: an ELIDING spelling (`t*e`, `s*d`, `c*`) reads as unnamed here and approves — the price of the 21.
- `shell-exec.ts` (AP-EXT-ITER74-01 a docblock outlived the defect it described) — INVARIANT: no docblock here asserts a SIBLING's defect is LIVE; zero `execIndex` and zero `tokens[i].quoted` tokens, live or quoted-as-a-corpse. BREAKS: it claimed Pass 2 "still reads" the arm AP-EXT-ITER64-02 already deleted; a fixer restoring that re-opens every R-WSRC-3 write guard (19 RED). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER74-01`. PATTERN_SHAPE: `execIndex` or `tokens[i].quoted` in `shell-exec.ts`.
- `handlers/config-protection.ts`; `handlers/tsc-gate.ts` (AP-EXT-ITER93-02 the VERB is a pattern) — INVARIANT: `findGitVerb` and `segmentIsGitCommit` read the subcommand via `execNamesIn`, `PROHIBITED_GIT_VERBS_SIMPLE` spread FIRST; `NEGATIVE_GIT_SUBCOMMANDS` stays literal (widening a FALSE arm under-blocks). BREAKS: `git rese? --hard` really hard-resets, `git commi?` really commits — both approved. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER93-02`). PATTERN_SHAPE: `execNamesIn(rest[i], GATED_GIT_VERBS)`, `execNamesIn(token, GIT_COMMIT_SUBCOMMAND)`.
- `handlers/config-protection.ts` (AP-EXT-ITER93-05 an option VALUE is a word, so it is a pattern) — INVARIANT: the GATING FLAG is read through the same shared `execNameIs` the exec word and the git VERB already read with — `afterVerb.some(t => execNameIs(t, '--amend'))`, the `--prune` twin, and the node `--test` flag in `extractNodeTestPathsFromSegment`. `isCheckoutRefOperation`'s `--` deliberately stays literal: that arm returns FALSE (path-mode is ALLOWED), so widening it is the under-block direction — the same reason `NEGATIVE_GIT_SUBCOMMANDS` stays literal. BREAKS: bash applies pathname expansion to an option word like any other, so with a file named `--amend` in cwd `git commit --amen? -m x` really AMENDS (shim-verified 2026-08-29 in a scratch repo: still ONE commit, HEAD sha replaced, subject overwritten) while a `=== '--amend'` compare saw `--amen?`, matched nothing, and APPROVED a history rewrite for a worker; `--prun?`, `--amen[d]` and `node --tes? <expensive>` measured the same (6 of 6 git forms, 3 of 3 node forms APPROVED pre-fix against the shipped handler, every literal twin blocking). Seventeenth instance of the AP-EXT-ITER10-01/…/73-01/93-01/93-02 shape, and the same question one word further right than AP-EXT-ITER93-02 asked of the verb. Cost measured over 10135 unique real worker Bash commands from 557 live session logs: ZERO verdict changes in either direction (115 git-verb blocks identical, and the node candidate list identical for all 10135); per-command cost 146 -> 148 microseconds. `execNameIs`, not `execNamesIn`: this asks ONE long distinctive name, where the measured `*` bound buys nothing and would only under-block. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER93-05`; mutation-verified in THREE directions, zero collateral each — restoring the literal git-flag compares on the compiled mirror -> 1 RED, restoring the literal node-flag compare -> 1 RED, restoring all three in SOURCE only -> 1 RED, the structural pin). PATTERN_SHAPE: `execNameIs(t, '--amend')`, `execNameIs(t, '--prune')` and `execNameIs(t, '--test')` in `handlers/config-protection.ts`; zero `t === '--amend'` / `t === '--prune'` / `t === '--test'` compares in that file; `t === '--'` PRESENT inside `isCheckoutRefOperation` and zero `execNameIs(` in that function. RESIDUAL, recorded rather than guarded: a token that BOTH names the node test flag and is a path (`??test`) is now consumed as the flag instead of pushed as a candidate — zero of 10135 real commands move, and adding an arm to separate them is the case-adding shape this module refuses. REPLAY, both measured this pass and left OPEN: AP-EXT-ITER93-06 — `splitShellSegments` reads the `{`/`}` of a BRACE EXPANSION as command-group delimiters, so `git {reset,--hard}` splits mid-word and no detector sees a verb; it really hard-resets (shim-verified: staged work destroyed) and needs NO crafted filename, 11 of 11 gated forms APPROVE. AP-EXT-ITER93-07 — `isInPlaceFlag` is a SHAPE test, not a name compare, so `execNameIs` could not reach it: `sed -? '' s/a/b/ <session>/state.json` and `sed --in-plac? …` APPROVED past both R-WSRC-3 write gates while `sed -i` blocked. FIXED 2026-08-29 — see its own entry below.
- `shell-exec.ts` (AP-EXT-ITER93-06 a BRACE EXPANSION is a word, not a command group) — INVARIANT: `{` and `}` stay DECLARED in `SHELL_SEGMENT_SEPARATORS` (a standalone one reaches the segment loop as its own token and is still a boundary) but are EXCLUDED from `GLUED_SEPARATOR_RE`, because bash recognizes them as RESERVED WORDS — only when blank-delimited. `expandBraceWord` then runs at `pushWordBoundaryTokens`' flush, where the buffer is one complete bash word, producing the words bash produces (comma list AND `{X..Y}` sequence, nested, prefix/suffix preserved, unquoted empty word dropped, quoted/escaped braces inert via the SAME `unquoteSpan`/`unquotedEscapeChar` parsers the fold uses). BREAKS: the glued split honored `{`/`}` mid-word, so a brace expansion was SHREDDED before any detector ran — `git {reset,--hard}` segmented to `git` + `reset,--hard`, the verb and every flag destroyed. 11 of 11 gated forms APPROVED while every literal twin blocked; shim-verified that `git {reset,--hard}` really hard-resets and `git commit --{amend,amend}` really amends. The sequence arm is equally exploitable: `git {r..r}eset --hard` really resets. Unlike every pathname-expansion sibling in this family (ITER73-01/93-01/93-02/93-05) this needs NO crafted filename — bash expands braces unconditionally — so it is strictly the cheapest bypass of the group. The glued split modeled a construct bash does not have: `{git status;}` is a SYNTAX ERROR, `{ git status;}` runs (both verified), so removing braces from the glued split is a SUBTRACTION toward bash's grammar, not a widening. Cost measured over 10135 unique real worker Bash commands from 557 live session logs: ZERO git-verb verdict flips in either direction (115 blocks identical), segmentation reshaped on 356 (all JS/JSON object literals inside heredoc artifact bodies), and across all four gates ZERO anchors GAINED (no over-block) with ONE lost — a JS fragment `lines[lines.length-1]` that had accidentally matched the wrapper SHAPE, a phantom whose removal is an over-block fix. 125 -> 129 microseconds per command. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js` (`AP-EXT-ITER93-06`; mutation-verified in THREE directions, zero collateral each — restoring braces to the glued split on the compiled mirror -> 3 RED, deleting the flush-time expansion -> 2 RED, restoring the glued split in SOURCE only -> 1 RED, the structural pin). PATTERN_SHAPE: `op !== '\n' && op !== '{' && op !== '}'` in the `GLUED_SEPARATOR_RE` filter; `'('`, `')'`, `'{'`, `'}'` still present in the `SHELL_SEGMENT_SEPARATORS` literal; `tokens.push(...expandBraceWord(buffer))` in `pushWordBoundaryTokens`' flush. RESIDUAL, recorded rather than guarded: a word whose expansion exceeds `BRACE_EXPANSION_STEP_CAP` (4096 recursion steps) falls back UNEXPANDED — the pre-fix reading, so an overflow can never lose a block a non-expanding scanner already had, but a deliberately padded >4096-alternative word (a multi-KB command word) hides its expansion. REPLAY, measured this pass and now FIXED: AP-EXT-ITER93-08 — the payload read walked one token too early past an option RUN; see its own entry below.

- `shell-exec.ts` (AP-EXT-ITER93-08 bash does not stop parsing options at `-c`) — INVARIANT: `shellCommandStringPayloads` returns EVERY word after the command-string flag (`tokens.slice(idx + 1)`), each scanned as its own segment; never one token, never a skip-options scan (that needs an operand table — the AP-EXT-ITER18-01/ITER19-01 incomplete-set shape, and `bash -c -o pipefail "…"` proves any such list short). BREAKS: bash sets a mode flag at `-c` and keeps parsing options, taking the first NON-OPTION argument as the command string, so a repeated or trailing option walked the single-token read one word early — `bash -c -c "git reset --hard"`, `bash -c -x "…"`, `bash -lc -c "…"`, `bash -c -o pipefail "git stash"` all APPROVED while really running the payload (shim-verified; the `-x` trace printed `+ git reset --hard` before executing it). The payload is ONE quoted token, so the miss hid the WHOLE command string from every detector at once — the AP-EXT-ITER63-06 blast radius across the git-verb, `install.sh`, expensive-test and R-WSRC-3 write gates. Needs no crafted filename. The take is a strict WIDENING (yesterday's token is still a member), so nothing that blocked can stop blocking; over-reach lands on the shell's own option words and the payload's positional args, each one more benign segment. Each candidate is scanned as its OWN segment, not joined: bash's `-c` does not concatenate its arguments the way `eval` does (which is why `wordToCodeBuiltinPayload` joins and this must not), and the tokenizer has already stripped each word's quotes, so re-joining manufactures a word sequence bash never produced. It is NOT about reach — `execAnchorIndex` reads no position, so a joined `-x git reset --hard` still anchors (measured). Cost over 10135 unique real worker Bash commands from 557 live logs: ZERO anchors gained and ZERO lost across all four gates; per-command cost unchanged within order-dominated noise (~100-128 microseconds). ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER93-08` (10 option-run block cases across every carrier, the install.sh + state-write + soak arms, 7 non-tautology approve twins incl. a payload with positional args and a bare `-c` with nothing after it, and a monotonicity pin), `extension/tests/tsc-gate.test.js#AP-EXT-ITER54-01` (the shape pin). Mutation-verified BOTH directions, zero collateral each: restoring the single-token read on the compiled mirror -> 14 RED, all AP-EXT-ITER93-08; restoring it in SOURCE only -> 1 RED, the shape pin. PATTERN_SHAPE: `return tokens.slice(idx + 1);` inside `shellCommandStringPayloads`; zero `tokens[idx + 1]` reads and zero `startsWith('-')` in that body; the consumer spreads it (`...shellCommandStringPayloads(segment)`) rather than pushing one payload. RESIDUAL CLOSED 2026-08-29 by AP-EXT-ITER93-07, which asked the in-place SHAPE of a witness built from the same shared primitive; see its own entry below.

- `shell-exec.ts`; `handlers/config-protection.ts` (AP-EXT-ITER93-07 a SHAPE test needs a WITNESS, and there is only ONE fill machine) — INVARIANT: `shellWordWitness(folded, wantedAt)` is the ONE place that splits a word into positions and decides which construct may be FILLED; every shape asked of a word bash will expand goes through it — `shellShapeWitness` (the interpreter tail) supplies only its per-position fill, and `isInPlaceFlag` supplies its two: the long option's own character at that index (`IN_PLACE_LONG_OPTION`, from which `IN_PLACE_LONG_RE` is BUILT — one declaration) and `i` anywhere past the dash for the single-dash cluster. Arms may differ in what they WANT; what must never fork is which construct stands for one position. BREAKS: AP-EXT-ITER73-01/93-02/93-05 taught every NAME compare to read a pattern-bearing word as the pattern it is, and AP-EXT-ITER93-01 did it for the wrapper SHAPE, but `isInPlaceFlag` is a character-class read (`arg.slice(1).includes('i')`, `/^--in-place(=|$)/`), so `execNameIs` could not reach it and the raw fold matched nothing over a wildcard. With a file named `-i` in cwd, `sed -? '' s/a/b/ <session>/state.json` APPROVED for a worker past BOTH R-WSRC-3 write gates while `sed -i` blocked — shim-verified 2026-08-29 on this box: the run really REPLACED the target file's contents. `-n?`, `--in-plac?`, `--in-plac[e]` and `--in-plac?=.bak` measured the same; `-[i]` and `-{i,i}` blocked by ACCIDENT (a bracket body still carries the literal `i`; braces expand upstream via AP-EXT-ITER93-06), and that partial coverage is what made the hole read as closed. Eighteenth instance of the AP-EXT-ITER10-01/…/73-01/93-01/93-02/93-05 shape, and the LAST expansion-blind reader in this subsystem's write path. Extracting the fill rather than adding a second copy is the subtraction the catalog's own `*`-bound history demands: two position-splitters could disagree about `*` and only one would be measured. Cost over 10140 unique real worker Bash commands from 558 live session logs: 12 word-level predicate flips across 481838 words and ZERO in a segment naming `sed` — the predicate is consulted only for `sed` (`anchorWritesPositionalArg` early-returns for every other `WRITE_COMMANDS` member), so zero real commands change verdict in either direction. ENFORCE: `extension/tests/config-protection-state-files.test.js#AP-EXT-ITER93-07` (6 globbed block cases each with a non-tautology literal twin, the settings-gate arm, 5 AP-EXT-ITER47-01 read-only approve controls, the witness matrix incl. the `*` bound, and the one-home structural pin), `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER93-01` (its fill-bound pin re-scoped to the primitive that now owns the rule). Mutation-verified in FOUR directions, zero collateral each: pre-fix predicate on the compiled mirror -> 6 RED; pre-fix predicate in SOURCE only -> 1 RED (the structural pin); the `*` bound removed in the compiled witness -> 1 RED (the matrix); the positions walk re-inlined into `shellShapeWitness` in SOURCE -> 2 RED (both one-home pins). PATTERN_SHAPE: exactly one `SINGLE_POSITION_WILDCARD_RE.test(` in `shell-exec.ts`, inside `shellWordWitness`; `shellShapeWitness` body is one `return shellWordWitness(folded, ` with zero `SHELL_WORD_POSITION_RE`; `shellWordWitness(` called in both arms of `isInPlaceFlag` with zero `arg.slice(1).includes(` and zero `IN_PLACE_LONG_RE.test(arg)` there; `IN_PLACE_LONG_RE` built from `IN_PLACE_LONG_OPTION`. RESIDUALS, reported not claimed closed: (a) a `*`-bearing spelling (`sed -*`, `sed --in-pla*`) stays unread — the deliberate measured bound, same family as AP-EXT-ITER93-01(a); (b) `arg.startsWith('-')`/`('--')` are still literal reads of the leading positions, so `??in-place` approves — widening them would make every wildcard-bearing positional FILE argument a flag candidate, which is the over-block direction this module refuses without measurement. REPLAY, CLOSED by AP-EXT-ITER93-03 (see that entry) — `SHELL_COMMAND_STRING_FLAG_RE` was the one remaining unguarded match of this shape and now asks the same witness. NOT matches, checked: `ENV_ASSIGNMENT_RE` and `HERE_STRING_OPERATOR_RE` are correct as literals — bash recognizes an assignment word and a redirection operator at PARSE time, before expansion, so a globbed spelling of either is not one.

- `shell-exec.ts` (AP-EXT-ITER93-03 an option FLAG is a SHAPE, and bash expands it too) — INVARIANT: `isShellCommandStringFlag` asks `SHELL_COMMAND_STRING_FLAG_RE` of a WITNESS (`shellWordWitness`, the ONE fill machine of AP-EXT-ITER93-07), never of the raw token, and the regex is BUILT from `SHELL_COMMAND_STRING_FLAG_CHAR` — the character the fill supplies — so shape and fill cannot drift. BREAKS: bash pathname-expands an option word like any other, so with a file named `-c` in cwd `bash -? '<cmd>'` really runs the payload (shim-verified 2026-08-29: `-?`, `-[c]` and `-l?` each printed the payload's output) while a literal test matched nothing over letters, `shellCommandStringPayloads` came back empty and the command string stayed ONE opaque token — the AP-EXT-ITER63-06 blast radius, hiding the whole payload from the git-verb chain, the `install.sh` ban and both R-WSRC-3 write gates at once; 9/9 measured forms APPROVED for a worker while their byte-identical literal twins blocked. `-{c,c}` blocked by ACCIDENT (braces expand upstream via AP-EXT-ITER93-06) and that partial coverage is what made the hole read as closed. Nineteenth instance of the AP-EXT-ITER10-01/…/93-01/93-02/93-05/93-07 shape, and the LAST expansion-blind shape test on a shell word in this module — the Phase 2.5 sweep of every remaining literal option read (`startsWith('-')` at `config-protection.ts:339/735/776/893`, `tsc-gate.ts:162`, the `=== '--'` path-mode read) found ZERO further matches: a word starting with a literal `-` cannot expand to one that does not, so those reads are correct as literals and their fail direction is over-scrutiny, not bypass. Cost over 9167 unique real worker Bash commands from 163 live session logs: 35 word-level predicate flips across 330875 words, 4 commands whose SEGMENT SET changed — every one a prior anatomy-park pass's own measurement script — and ZERO handler decision flips in either direction. ENFORCE: `extension/tests/hooks/config-protection-git-boundary.test.js#AP-EXT-ITER93-03` (6 globbed block cases across the git chain, the install.sh + both write-gate arms, 6 non-tautology approve controls taken from the corpus's own flipped words, the segment-level shape matrix incl. the `*` bound, and the one-declaration/shared-witness structural pin). Mutation-verified in three directions: pre-fix predicate on the compiled mirror -> 3 RED; pre-fix predicate in SOURCE only -> 1 RED (the AP-EXT-ITER54-01 call-site pin, re-scoped to the predicate's new spelling); the `*` bound removed in the compiled witness -> 4 RED. PATTERN_SHAPE: `SHELL_COMMAND_STRING_FLAG_RE = new RegExp(`^-[A-Za-z]*${SHELL_COMMAND_STRING_FLAG_CHAR}`)`; `shellWordWitness(` inside `isShellCommandStringFlag` with zero `SHELL_WORD_POSITION_RE` and zero `SINGLE_POSITION_WILDCARD_RE` in that body. RESIDUALS, reported not claimed closed: (a) a `*`-bearing spelling (`bash -*`) stays unread — the deliberate measured bound, same family as AP-EXT-ITER93-01(a)/93-07(a); (b) the leading `-` is a literal position, so `?c` approves — widening it would make every wildcard-bearing positional FILE argument a flag candidate, the over-block direction this module refuses without measurement.

- `handlers/config-protection.ts` (AP-EXT-ITER73-04 a write DESTINATION is a shell word too) — INVARIANT: both state-write arms read the destination through the shared reader — `matchProtectedStateBasename` asks `execNamesIn` of the basename and its tmp-stripped spelling, `isInsideRuntimeRoot` asks `execNameIs` per path COMPONENT. Neither compares a destination literally. BREAKS: bash pathname-expands a redirect destination and a positional file arg like any other word, so `echo x > <session>/stat?.json`, `stat[e].json` and `circuit_break*r.json` really clobber the state file (shim-verified), and `~/.claude/pickle-ric?/...` really writes the deployed runtime tree. Pre-fix on the shipped handler: 12/14 globbed write forms and 3/4 globbed runtime-root forms APPROVED for a worker while every literal twin blocked. Sixteenth instance of the AP-EXT-ITER73-01 expansion family, and the first on a DESTINATION rather than a command word. The runtime-root arm is a COLLAPSE, not a third guard: an exact-equal and a prefix compare were one question asked of two suffixes, and both asked it of a literal. ENFORCE: `extension/tests/config-protection-state-files.test.js#AP-EXT-ITER73-04` (14 globbed write forms x block + literal-twin control, 4 globbed runtime-root forms, 8 over-block controls, and a structural pin on both arms; mutation-verified THREE directions on the compiled mirror — pre-fix behavior reds exactly the 18 hazard cases, source-only pre-fix reds exactly the structural pin, and an always-true root check reds the over-block controls, zero collateral each). Cost measured on 9229 unique real worker Bash commands from 163 live session logs (667070 words): ZERO decision flips either direction. PATTERN_SHAPE: inside `matchProtectedStateBasename` and `isInsideRuntimeRoot` in `extension/src/hooks/handlers/config-protection.ts`, zero `=== candidate` compares and zero `.startsWith(runtimeRoot` prefix compares; `isInsideRuntimeRoot` contains an `execNameIs(` call and `matchProtectedStateBasename` a `wordSpellsProtectedName(` one (AMENDED by AP-EXT-ITER96-01, which moved that arm OFF `execNamesIn` — an anchor still naming `execNamesIn` in this body is stale and reads as a deleted guard). RESIDUALS, measured not asserted: (a) CLOSED 2026-08-29 by AP-EXT-ITER96-01 — the `*`-eliding stem (`stat*.json`) now blocks; see its own entry; (b) a pure-wildcard path component names nothing — no longer merely a bound but a SHIM-VERIFIED bypass, OPEN REPLAY AP-EXT-ITER96-02 below; (c) a Write/Edit `file_path` is not a shell word, so a literal on-disk file named `stat?.json` now over-blocks — fail-closed, override flag remains the escape hatch.
- `handlers/config-protection.ts` (AP-EXT-ITER96-01 a bound is only valid in the domain it was MEASURED in) — INVARIANT: `matchProtectedStateBasename` reads its destination through `wordSpellsProtectedName` — literal equality, else `shellPatternToRegex(word).test(name)` bounded by COVERAGE (`literals * 2 >= name.length`, wildcards and bracket/brace bodies contributing no literal per the AP-EXT-ITER5-01 convention) — never through `execNamesIn`. BREAKS: `execNamesIn` refuses ANY `*`-bearing word, a bound bought for SHORT COMMAND WORDS (`**No` names `nano`) where it is correct; the state domain borrowed it for four LONG DOTTED FILENAMES it was never measured against, so every `*` spelling of a protected file APPROVED for a worker while its `?` twin blocked (AP-EXT-ITER93-03) and the config sibling `isProtectedShellPattern` blocked the same shape through `shellPatternToRegex` — one question, two answers, in adjacent arms of one function. Shim-verified at BOTH ends on the shipped handler: 8/8 forms approved (`stat*.json`, `state.js*n`, `sta*e.json`, `circuit_break*r.json`, `pipeline-stat*.json`, across `>`, `>>`, tee and cp) and bash really clobbers — `echo CLOBBERED > <dir>/stat*.json` overwrote state.json, as did the `tee` form. This is the R-WSRC state gate, so the payoff is a worker rewriting the state file the runtime is mid-read of. COVERAGE, not a blanket `*` read, is MEASURED: over 120,737 tokens of real worker Bash from 26 live session logs the obfuscated spellings score 0.90-0.95 and the accidental globs the bound exists to protect score 0.00-0.35 (bare `*` x501, `**` x41, `c*` x11, `s**`, `pickle_*`), and the coverage-bounded predicate flips exactly THREE tokens repo-wide — all three obfuscated spellings of a protected name, zero worker artifact writes. Reading `*` unconditionally would block all 501: a blocked artifact write stalls a ticket, the reliability cost `execNamesIn`'s bound was bought to avoid. Do NOT push coverage down into `execNamesIn`: at command-word length it re-blocks `v*`/`*no`, the exact 21 writes that bound paid for. ENFORCE: `extension/tests/config-protection-state-files.test.js#AP-EXT-ITER96-01` (6 globbed forms x block, 4 write seams, 5 accidental-glob approve controls taken from the corpus, and the structural pin; mutation-verified BOTH directions on the compiled mirror — restoring `execNamesIn` reds exactly the 10 hazard cases with 261 siblings green, and dropping the coverage bound reds exactly the accidental-glob controls, zero collateral either way, so the guard can neither under- nor over-trigger unseen). PATTERN_SHAPE: `wordSpellsProtectedName(spelling, name)` inside `matchProtectedStateBasename` and zero `execNamesIn(` in that body; `literals * 2 >= name.length` inside `wordSpellsProtectedName`. OPEN REPLAY, same shape, NOT fixed (one fix per iteration): AP-EXT-ITER96-02 — `isInsideRuntimeRoot`'s per-component `execNameIs` inherits `patternNamesACommand`'s all-wildcard bound, so a PURE-wildcard path component names nothing and `echo x > ~/.claude/*/extension/bin/setup.js` APPROVES for a worker; shim-verified this pass — `*` matches `pickle-rick` and bash really clobbered the deployed runtime file. NOT a match, checked: a LEADING pure-wildcard (`~/*/pickle-rick/...`) approves but is not exploitable — bare `*` does not match the dotfile `.claude`, verified by expansion.
