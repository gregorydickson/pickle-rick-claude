## Public Exports

### `index.ts`
`State`, `Backend`, `BackendResolutionSource`, `WorkerBackendResolutionSource`, `BACKENDS`, `ExitReason`, `ALL_EXITS`, `isFailedExit`, `isFailureExit`, `Step`, `ALL_STEPS`, `VALID_STEPS`, `TicketTier`, `TicketTierBudget`, `TICKET_TIER_BUDGETS`, `FALSE_EPIC_THRESHOLD`, `VALID_ACTIVITY_EVENTS`, `ActivityEventType`, `ActivityEvent`, `ActivityLogEntry`, `ActivityEventSource`, `ActivityGatePayload`, `PromiseTokens`, `PROMISE_TOKEN_STRINGS`, `hasToken`, `wrapToken`, `StateError`, `LockError`, `TransactionError`, `SchemaVersionMismatchError`, `InvalidActivityEventError`, `SchemaVersionDeployDriftError`, `VALID_EFFORTS`, `ReasoningEffort`, `ARTIFACT_PREFIXES`, `WorkerRole`, `hasLifecycleArtifact`, `ALL_TICKET_STATUSES`, `TicketStatus`, `StateFlags`, `ReadinessState`, `ReadinessCycleHistoryEntry`, `PhasePersona`, `ProjectContext`, `CourseCorrectionRecord`, `ChangeProposal`, `ErrorRecord`, `DebateRound`, `Defaults`, `STATE_MANAGER_DEFAULTS`, `LATEST_SCHEMA_VERSION`, `StateErrorCode`, `HookInput`, `PostToolUseFailureInput`, `LastToolErrorState`, `PipelineRunnerExitCode`, `IterationExitType`, `IterationExitResult`, `IterationOutcome`, `MUX_ITERATION_REASONS`, `MuxIterationReason`, `RateLimitInfo`, `RateLimitAction`, `SessionMapEntry`, `UpdateCheckCache`, `ReleaseAsset`, `ReleaseInfo`, `UpdateSettings`, `PickleSettings`, `UpdateStatus`, `UpdateResult`, `UpgradeResult`, `MICROVERSE_EXIT_REASONS`, `MicroverseExitReason`, `MICROVERSE_FATAL_REASONS`, `MicroverseFatalReason`, `STATE_SCHEMA_VERSION_AHEAD_EXIT_REASON`, `StateSchemaVersionAheadExitReason`, `isMicroverseFailureExit`, `MicroverseMetric`, `FailureClass`, `StallCategory`, `StallRecoveryAction`, `StallClassification`, `ClassifiedFailure`, `MicroverseHistoryEntry`, `ViolationLedger`, `Violation`, `JudgeResult`, `MicroverseSessionState`, `GateFailure`, `GateMode`, `GateResult`, `GateBaselineFile`, `RemediationResult`, `CreateMicroverseOpts`, `BuildErrorCode`, `Diagnostic`, `ValidationResult`, `BuildError`, `MicroverseOpts`, `WorkspaceOpts`, `StylesheetOverride`, `StylesheetConfig`, `ConvergenceSpec`, `PhaseSpec`, `DefenseMatrix`, `BuildResult`, `BuilderSpec`, `StateManagerOptions`.

### `attractor-schema.fallback.ts`
`AttrScope`, `AttrType`, `AttrDef`, `FallbackSchema`, `ATTRACTOR_SCHEMA_FALLBACK`, `ALL_ATTRS`, `lookupAttr`, `validateAttrType`, `AttrValidation`, `validateAttrs`.

### `engine-keys-registry.ts`
`EngineKeysRegistry`.

### `plumbus-frame-analyzer.ts`
`ContextKeyRow`, `DiamondRoutingRow`, `CycleRow`, `AnalyzerOutput`, `Node`, `Edge`, `Graph`.

## Trap Doors

- `activity-events.schema.json` (R-SJET-3/R-SJET-4 optional gate_payload fields on `judge_measurement_attempted`) — The `gate_payload` object for `judge_measurement_attempted` accepts three OPTIONAL fields added by R-SJET-3 and R-SJET-4; these are NOT in `required` and will be absent on older emitter versions: `nested_claude_detected` (boolean — whether `isNestedClaude()` returned true at spawn time; from R-SJET-3 env-isolation detection), `pre_spawn_env_key_names` (string[] — sorted list of env-var key names present in the sanitized judge env after `buildJudgeSpawnEnv()` stripping, never values; from R-SJET-3), `judge_backend_used` (string — actual resolved backend ('claude'|'codex') selected by `resolveJudgeBackend()` at spawn time, reflects sticky fallback; from R-SJET-4). Emitter: `microverse-runner.ts:measureLlmMetricAttempt`. Schema definition: `extension/src/types/activity-events.schema.json` definitions/judge_measurement_attempted. See `docs/judge-spawn-troubleshooting.md` for operator context. BREAKS: adding these to `required` silently invalidates every existing `judge_measurement_attempted` emission that predates R-SJET-3/R-SJET-4. ENFORCE: extension/tests/activity-event-payload.test.js.
- `activity-events.schema.json` — INVARIANT: prompt catalogs must list every schema-required payload key. BREAKS: analyst ACs cite invalid event contracts. ENFORCE: `spawn-refinement-team` prompt test. PATTERN_SHAPE: docs table row for schema-backed events; INVARIANT: schema event definitions must equal `writeActivityEntry` fields. BREAKS: validators and contract tests bless wrong telemetry. ENFORCE: payload and spawn-path regression tests. PATTERN_SHAPE: emitter fields differ from event definition; INVARIANT: `worker_gate_failed.gate_phase` enum must cover every `runWorkerGate` phase, including `test:integration`. BREAKS: full-tier worker failures emit telemetry the canonical schema rejects. ENFORCE: `extension/tests/spawn-morty-worker-gate.test.js`, `extension/tests/activity-event-payload.test.js`. PATTERN_SHAPE: `worker_gate_failed` schema enum vs `type WorkerGatePhase = 'lint' | 'tsc' | 'test:fast' | 'test:integration'`.
- `activity-events.schema.json` (R-PDD-oneOf) — INVARIANT: every event-type entry under `definitions` (excluding shared enums `backendEnum`, `backendResolutionSourceEnum`, `workerBackendResolutionSourceEnum`) MUST also be referenced from the top-level `oneOf` array via `{ "$ref": "#/definitions/<event>" }`. Definition without oneOf-membership means schema-walking consumers (Ajv `compile(schema)` + validate) reject every emission because no oneOf branch matches; per-definition `validate(payload, defName)` tests still pass, masking the drift. Same regression class as iter-7 (the since-retired ticket_audit_failed event missing schema entirely). All known siblings now guarded at iter-13 HEAD: phantom_done_detected (iter-11), cap_check_skipped_stale_cache (iter-12), standup_session_dropped (iter-13) — pattern grep emits zero lines. BREAKS: a future event added by `definitions[]` without oneOf-update (commit a70db8f0 phantom-Done pattern) silently fails downstream schema validation. ENFORCE: extension/tests/phantom-done-detected-schema-conformance.test.js, extension/tests/cap-check-skipped-stale-cache-schema-conformance.test.js, extension/tests/standup-session-dropped-schema-conformance.test.js (per-event, including the R-PDD-oneOf invariant assertion); extension/tests/time-cap-disabled-default-schema-conformance.test.js, extension/tests/worker-partial-lifecycle-exit-schema-conformance.test.js, extension/tests/install-pkgjson-version-trace.test.js. PATTERN_SHAPE: `node -e "const s=require('./extension/src/types/activity-events.schema.json'); const refs=new Set(s.oneOf.map(o=>o.\$ref.replace('#/definitions/',''))); const SHARED=new Set(['backendEnum','backendResolutionSourceEnum','workerBackendResolutionSourceEnum']); for (const k of Object.keys(s.definitions)) if (!SHARED.has(k) && !refs.has(k)) console.log(k);"` MUST emit zero lines.
- `index.ts` — INVARIANT: `State.effort` values must stay aligned with setup CLI and codex spawn unions, including `xhigh`. BREAKS: persisted effort overrides are rejected or dropped before codex argv emission. ENFORCE: setup/backend-spawn/spawn-morty effort regression tests. PATTERN_SHAPE: `'low' | 'medium' | 'high'` union guarding `state.effort` or `VALID_EFFORTS`.
- `index.ts` (R-PSSS empty-scope events) — INVARIANT: `VALID_ACTIVITY_EVENTS` must include `anatomy_park_empty_scope_skip` and `szechuan_sauce_empty_scope_skip`; both events also need matching `definitions/*` + top-level `oneOf` `$ref` entries in `activity-events.schema.json` and a registration row in the source TS array + compiled `extension/types/index.js` mirror. The emitters live in `src/bin/pipeline-runner.ts:resolveAnatomySubsystems` and `shouldSkipSzechuanForEmptyScope` and write `gate_payload.in_scope_paths` (+ `gate_payload.discovered_subsystems` for anatomy). BREAKS: dropping either event from the registry makes the empty-scope skip silent again — the operator-visible WARN (R-PSSS-1/2 trap door in `src/bin/CLAUDE.md`) loses its activity-log counterpart. ENFORCE: `extension/tests/pipeline-empty-scope-skip-schema-conformance.test.js`, `extension/tests/activity-event-payload.test.js`. PATTERN_SHAPE: `anatomy_park_empty_scope_skip|szechuan_sauce_empty_scope_skip`.
- `index.ts` (R-PSSS-3 PhaseSetupResult) — INVARIANT: `PhaseSetupResult = true | { skipReason: PhaseSkipReason }` and `PhaseSkipReason = 'empty_scope' | 'empty_branch_diff' | 'no_subsystems' | 'setup_error'` are exported from `pipeline-runner.ts` (the per-phase skip contract); `PipelineStatus` carries an additive optional `phase_skips?: Record<string, PhaseSkipReason>` map. Older `pipeline-status.json` files without `phase_skips` MUST still parse. WS-B: `empty_scope` covers a scope that resolved to files of which none are code; the branch-diff case is a DIFFERENT, earlier fact and carries its own `empty_branch_diff`, measured against the session baseline `state.start_commit` (never a branch ref — a single-branch repo has an empty `main...HEAD` diff while the session may have authored commits on top of its baseline) — a run that authored no change ends the phase and says so, and an operator can tell it apart from a doc-only skip. BREAKS: collapsing `PhaseSetupResult` back to `boolean` loses the per-phase skip disposition and the final summary line; folding `empty_branch_diff` back into `empty_scope` makes an empty-diff run indistinguishable from a doc-only one in `phase_skips`. ENFORCE: `extension/tests/anatomy-park-scope.test.js`, `extension/tests/szechuan-scope.test.js`, `extension/tests/pipeline-empty-diff-terminates.test.js`. PATTERN_SHAPE: `PhaseSetupResult|PhaseSkipReason|phase_skips|empty_branch_diff`.
- `src/types/index.ts` (VALID_ACTIVITY_EVENTS) — INVARIANT: `VALID_ACTIVITY_EVENTS` is a SUPERSET of the event names `activity-events.schema.json` defines: every `definitions/*` entry that pins `properties.event` to a `const`/`enum` must appear in the array AND be reachable through the top-level `oneOf` `$ref`. The array is the runtime allowlist (`log-activity.js:136`, `state-manager.ts:614`) and the source of `ActivityEventType`; the schema only constrains payload SHAPE, so a schema-only name is a name the runtime refuses. BREAKS: `ticket_audit_manual_edit` and `bundle_2026_05_04_closer_done` shipped with a definition, a `oneOf` `$ref` and a payload spec but never reached the array — `log-activity.js` answered "Unknown event type" for names the schema and the suite both blessed, and `spawn-refinement-team.ts:221` advertised one of them to PRD analysts under "Do NOT invent event names". The pre-existing conformance test could not see it: it set-equals `schema.definitions` against a hand-maintained `EVENT_NAMES` literal in the same file, never against the runtime array. ENFORCE: extension/tests/activity-event-payload.test.js, extension/tests/activity-logger.test.js. PATTERN_SHAPE: the two conformance tests derive BOTH sides — `[...schemaEventNames()].filter((n) => !registered.has(n))` against `new Set(VALID_ACTIVITY_EVENTS)`, and the `oneOf` reachability filter — and recognise an event definition STRUCTURALLY via `def.properties.event` rather than by subtracting a named list of non-event defs; a hardcoded event-name literal on either side does not satisfy this shape.
- `index.ts` (const-list membership is the source of truth) — INVARIANT: an exported membership list and its union derive one way — `export const X = [...] as const;` then `export type T = typeof X[number];`. A list annotated AS its union (`export const X: readonly T[] = [...]`) is the reverse and lets a union member missing from the array typecheck, so every runtime validator built on the array silently rejects it (`BACKENDS`/`Backend`: a new backend fell back to `'claude'`). BREAKS: re-annotating, or dropping `as const` (the union widens to `string`). ENFORCE: extension/tests/pipeline-runner.test.js#backend-derives-from-backends-rather-than-being-a-parallel-hand-maintained-union and #NO_PROGRESS_FAILURE_REASONS derives from a total FAILURE_REASONS classification, which asserts the PATTERN_SHAPE below over the WHOLE file rather than one named const (AP-EXT-ITER77-01 — the shape had a live second occurrence while this entry read green). PATTERN_SHAPE: `^export const [A-Z_]+: readonly \w+\[\] =` in `index.ts`.
- `index.ts` (AP-EXT-ITER77-01 a SUBSET of a union is DERIVED, never restated) — INVARIANT: a const naming some-but-not-all members of a union is computed from a TOTAL `Record<Union, …>` classification (`FAILURE_REASON_IS_NO_PROGRESS` -> `NO_PROGRESS_FAILURE_REASONS = FAILURE_REASONS.filter(...)`), never hand-listed. BREAKS: the hand-listed form annotated `: readonly TicketFailureReason[]` compiled CLEAN with a 4th `FAILURE_REASONS` member omitted (mutation-probed), and `mux-runner.ts:isOversizedNoProgressFailed` then answers false for it, silently stripping that reason's retry-exemption / selection semantics. Total Record makes the omission TS2741. ENFORCE: extension/tests/pipeline-runner.test.js#NO_PROGRESS_FAILURE_REASONS derives from a total FAILURE_REASONS classification (mutation-verified RED, zero collateral). PATTERN_SHAPE: an exported union-member list in `index.ts` initialized with a `[...]` literal rather than a `FAILURE_REASONS.filter`-style derivation, or a classification map typed looser than `Record<TicketFailureReason, …>`.
- `src/types/index.ts` (ExitReason / isFailureExit) — INVARIANT: `isFailureExit` and `isFailedExit` are the canonical predicates for classifying exit_reason values; inline string comparisons against the `ExitReason` union are forbidden. BREAKS: new exit reasons added to `ExitReason` but not to `isFailureExit` make `auto-resume.sh` R-CNAR-4(c) misclassify terminal exits and loop on non-failure cases. ENFORCE: extension/tests/auto-resume-stop-conditions.test.js.

- `activity-events.schema.json` (AP-EXT-ITER80-01 a JSON enum mirroring a TS union needs a DERIVED pin) — INVARIANT: every schema `enum` that mirrors a TS union/const is set-equal to it by a test that IMPORTS the union, never by a second hand-written literal. BREAKS: `backendEnum` held 3 members while `BACKENDS` grew to 7, so the four newer backends (deepseek/grok/kimi/gemini) emitted schema-invalid telemetry across 9 event definitions — and the guarding test hardcoded the same stale 3, so neither mirror could see the other drift. ENFORCE: extension/tests/activity-event-payload.test.js#activity-event-payload-schema-backendenum-equals-the-backends-const-it-mirrors (mutation-verified RED on the 3-member enum). PATTERN_SHAPE: an `enum` array in `activity-events.schema.json` whose members are a strict subset of the TS union it mirrors, with no test importing that union; replayed over all 33 schema enums — `tier_phase_skipped.tier` omits `large` and is correct ONLY while `TIER_LIFECYCLE.large` stays total over `ALL_LIFECYCLE_PHASES`.
- `index.ts` (AP-EXT-ITER199-01 the artifact-name contract has ONE reader) — INVARIANT: `matchesArtifactPrefix(file, prefix)` is the only expression of the gated-artifact naming rule (`<prefix>.md` OR any `<prefix>_*`) in this module; `newestArtifactFile` and `hasLifecycleArtifact` are both built on it, and no consumer re-implements either. BREAKS: a second hand-written copy drifts silently — a reader gating on an exact `'<prefix>.md'` and a reader counting the prefix PRESENT then disagree about the SAME file, which is how `checkPartialLifecycleExit` left a silently-dead worker unclassified (`src/bin/CLAUDE.md`, AP-EXT-ITER199-01). ENFORCE: extension/tests/worker-produced-everything-but-commit.test.js (`AP-EXT-ITER199-01`, 5 cases; mutation-verified — collapsing the rule to exact-name-only reds 5 here and 5 in mux-runner.test.js). PATTERN_SHAPE: under extension/src/**/*.ts, a strict-equality against a template literal of the form ${prefix} followed by .md, in the same expression as a startsWith of the same prefix — measured at HEAD the shape occurs at exactly 3 sites: `types/index.ts` (canonical), `bin/retry-ticket.ts:isLifecycleArtifact` (NOT a match, the union over all prefixes IS the intent) and `services/artifact-validation.ts:findMissingPrefixes` (AP-EXT-ITER199-02, byte-identical today, fence-blocked). A FOURTH site is a new fork.
