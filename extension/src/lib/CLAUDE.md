## Trap Doors

- `process-liveness.ts` (AP-EXT-ITER33-01 EPERM is not proof of death; AP-EXT-ITER109-01 name the ONE errno that proves death, never the ones that do not) — INVARIANT: `isProcessAlive` is the ONE pid-liveness probe (state-manager re-exports it; recoverable-json, resolve-state, pipeline-runner and stop-hook import it, none re-inlines one), and it answers positive-death-only: `ESRCH` is its sole `false`; every other throw is unaccountable and defers to LIVE. A non-integer/non-positive pid also reads alive — `kill(0)`/`kill(-n)` address the caller's own process GROUP and must never reach the probe. BREAKS: four of the five shipped copies collapsed `EPERM` into death, and that false licenses a destroy at every consumer — `isDeadPidPayload` evicts a LIVE holder's `state.json` lock (two writers in the critical section, the catastrophe `lock-steal-live-holder.test.js` exists to prevent), `orphan-reaper` group-SIGKILLs a live sibling worker, `pickle-utils` demotes a live session out of the map. Repairing that by testing FOR the survivor rebuilt the same defect one member over: Node rejects a pid above 2^31-1 with `ERR_INVALID_ARG_TYPE` BEFORE the syscall runs, and every reader bounds a pid at positive-integer and nothing tighter (`isDeadPidPayload`, `isStaleLockSnapshot`, `state.pid`), so a corrupt on-disk pid read as PROVABLY DEAD. Test AGAINST the one proof of death; a survivor list is one errno from the next destroy licence. ENFORCE: extension/tests/integration/lock-steal-live-holder.test.js (`AP-EXT-ITER33-01` + `AP-EXT-ITER109-01` — both assert the STEAL VERDICT and the lockfile on disk; the 109 pair drives the REAL `process.kill`, since an injected-errno oracle is what the survivor-list predicate already satisfied). PATTERN_SHAPE: exactly one `function isProcessAlive(` in `extension/src/`, in `lib/process-liveness.ts`, whose catch reads `err.code !== 'ESRCH'`; zero `process.kill(<pid>, 0)` liveness probes outside it (a `kill(pid,0)`-then-SIGTERM teardown is not a probe); generally, a catch-handler verdict enumerating the failures that do NOT prove it and defaulting to the CLAIM. REPLAY, re-measured 2026-08-30 — the earlier "same shape live in" wording overstated BOTH: `probeCatFile` (AP-EXT-ITER109-03) was live and is now FIXED as AP-EXT-ITER111-01 (`src/services/CLAUDE.md`); classifyGitProbeError (AP-EXT-ITER109-02) carried the shape but was LATENT — zero production call sites, a dead export pinned only by 4 direct unit tests — and was DELETED rather than guarded, since a dead survivor-list classifier is one wiring away from becoming the next wrong verdict. Backticks are dropped on that name deliberately: it no longer resolves. Re-run the call-site count before restating any sibling as live.

- `salvage-ticket.ts` (AP-EXT-ITER10-01 partial dep injection MERGES) — INVARIANT: `salvageTicket` takes `Partial<SalvageDeps>` and spreads it over `defaultDeps` (undefined-valued keys dropped); callers MUST NOT cast a partial to `SalvageDeps`. BREAKS: replacement left every unsupplied dep `undefined`, so `deps.ffReattach` threw into the best-effort catch — `pickle-recover --reset-ticket` archived nothing, reset nothing, and still exited 0 reporting success. ENFORCE: extension/tests/salvage-partial-deps-merge.test.js. PATTERN_SHAPE: `depsIn: Partial<SalvageDeps> = {}` + `{ ...defaultDeps, ...definedOverrides }` in `salvageTicket`; no `as SalvageDeps` in `src/` **on a non-comment line** — a bare `grep -rn "as SalvageDeps" src/` returns 1, and that hit is the `pickle-recover.ts` comment NAMING the retired cast, so the un-filtered form reports a phantom violation. Strip `//` comment lines before counting.
- `salvage-ticket.ts` (aafc633a `resetTodo` is the ONLY automatic Todo writer — a stubbed-out seam strands a runnable roster) — INVARIANT: `resetTodo` (`salvage-ticket.ts:115-117`, `updateTicketFrontmatter(... { status: 'Todo', completion_commit: null })`) is the SOLE automatic `status: Todo` writer in the whole salvage runtime. A caller that builds a full (non-`Partial`) `SalvageDeps` object for `salvageTicket` — e.g. `mux-runner.ts:routeExitPathSalvage`'s shared dep set, consumed by all three `routeExitPathSalvage` call sites (`:11351`/`:11444`/`:11520`) — MUST implement `resetTodo` for real (or omit the key so the `Partial<SalvageDeps>` merge above falls through to the default); it MUST NOT stub it to a no-op "for behavior parity". `archive` MAY legitimately stay a no-op at a seam that intentionally leaves gate-failing dirty work in place (that does not touch frontmatter and is a separate, orthogonal decision). BREAKS: a no-op `resetTodo` reachable from a `failed_flip_suppressed` hold (`mux-runner.ts:evaluateFailedFlipSuppression`/`readActiveFailedFlipHolds`, whose release requires the LITERAL frontmatter status `todo`, never `Failed`/`Done`/anything else) makes the hold permanent — `findNextPendingTicketId` excludes the ticket forever, `noRunnableTicketsRemain` returns `true` for an otherwise-runnable lone-ticket roster, and the L5 `empty_roster_all_failed_no_runnable` terminal fires over work that should still be in flight (live incident: exactly this shape at `mux-runner.ts:6459-6460`, ticket aafc633a). ENFORCE: extension/tests/salvage-ticket-matrix.test.js (`routeExitPathSalvage: shared dep-set release wiring (aafc633a)` block — mutation-verified RED against the stubbed no-op). PATTERN_SHAPE: a `resetTodo: () => { ... }` (or any body that never calls `updateTicketFrontmatter`) inside a full `SalvageDeps` object literal passed to `salvageTicket`.
- `context-key-matrix.ts` — INVARIANT: RunContext keys may contain dots. BREAKS: edge-reader rows truncate nested keys and hide real consumers. ENFORCE: `data-flow-trace-a.test.js` dotted-key analyzer contract. PATTERN_SHAPE: `condition="context.<key.with.dots>=<value>"`; INVARIANT: supplemental `ATTRACTOR_CTX` scans must preserve dotted keys in tool and prompt text. BREAKS: writer/reader matrix drops nested keys emitted outside `context_on_*`. ENFORCE: `plumbus-frame-analyzer-contract.test.js` dotted-key contract. PATTERN_SHAPE: `ATTRACTOR_CTX:<key.with.dots>=...|${ATTRACTOR_CTX_<key.with.dots>}`; INVARIANT: edge reader extraction must scan every `&&` context clause, including `!=`. BREAKS: later-clause readers vanish from the matrix. ENFORCE: `plumbus-frame-analyzer-contract.test.js` multi-clause contract. PATTERN_SHAPE: `condition="context.<k>=... && context.<k2>!=..."`.
- `diamond-routing.ts` — INVARIANT: Diamond conditions must parse dotted RunContext keys as one key. BREAKS: routing cells disappear for nested-key gates. ENFORCE: `data-flow-trace-a.test.js` dotted-key analyzer contract. PATTERN_SHAPE: `^context\\.[\\w.]+=(.+)$`; INVARIANT: enumeration must union edge-expected values with observed writer values per key. BREAKS: edge-only branch states vanish from covered/stuck cells. ENFORCE: `data-flow-trace-a.test.js` edge-only-value contract. PATTERN_SHAPE: `keySetsForEdges(...)` deriving values from observed map without `edge.condition.expectedValue`; INVARIANT: edge routing must evaluate every `&&` context clause and honor `!=`. BREAKS: multi-clause routes disappear or forbidden states look routable. ENFORCE: `plumbus-frame-analyzer-contract.test.js` multi-clause contract. PATTERN_SHAPE: `condition="context.<k>=... && context.<k2>!=..."`.

## Module Export Catalog (R-CMD B-AUDIT)

Per-module entry points exported from `extension/src/lib/`. Each entry pins a one-line invariant; deeper invariants for the principal exports live in the trap-door entries above.

- `context-key-matrix.ts` → `buildContextKeyMatrix` — analyzer pass: cross-frame RunContext key writer/reader matrix (see trap door above).
- `diamond-routing.ts` → `buildDiamondRouting` — analyzer pass: diamond-condition routing matrix (see trap door above).
- `cycles.ts` → `buildCycles` — analyzer pass: strongly-connected components in the attractor graph; trivial single-node cycles excluded.
- `engine-keys-registry.ts` → `loadEngineKeysRegistry`, `isEngineWritten`, `isUserWritten` — typed registry loader at `extension/data/engine-injected-keys.json` is the source of truth for engine-vs-user RunContext-key classification across `plumbus-frame-analyzer`. ENFORCE: `extension/tests/engine-keys-registry.test.js`.
- `process-liveness.ts` → `isProcessAlive`, `SignalProbe` — the single positive-death-only pid probe; the `SignalProbe` seam exists so tests can drive real errnos (see trap door above).
- `is-record.ts` → `isRecord` — canonical `value is Record<string, unknown>` boundary type-guard for parsed JSON.
- `findings.ts` (shared analyzer types) → `Finding`, `FrameId`, `Severity`, `maxSeverity`, `structuralEqual`, `selectFix` — `Severity` is `'P0'|'P1'|'P2'|'P3'|'P4'`; `maxSeverity` returns the strictest (lowest-numbered); `structuralEqual` dedupes findings by semantic identity (file + rule + location), not object identity; `selectFix` picks the highest-priority candidate fix from a list.
- `generative-audit.ts` → `shouldRunGenerativeAudit` — kill-switch helper that returns `false` whenever `PLUMBUS_GENERATIVE_AUDIT === 'off'`, honouring the `pickle-rick-claude/CLAUDE.md` Override 6 environment variable.
- `linear-comment.ts` → `emitCrossTicketRegressionLinearComment` — emits Linear comments via the configured `PICKLE_LINEAR_COMMAND` parsed argv (no shell). No-op when the command is absent. ENFORCE: `extension/tests/linear-integration.test.js`.
- `monitor-respawn.ts` → `respawnMonitorWindowForMode` — phase-boundary respawn always passes the inferred monitor mode (`pickle|council|refinement|szechuan-sauce|anatomy-park`); never defaults to `'pickle'` for microverse phases. ENFORCE: `extension/tests/monitor-mode-resilience.test.js`.

- `reconcile-ticket-truth.ts` (AP-EXT-ITER49-02 OPEN REPLAY — a fabricated clean tree routes a dirty ticket into the clean-tree Done back-fill) — INVARIANT (VIOLATED, unlanded): `defaultDeps.dirtyPaths` / `defaultDeps.isDirty` must not map a failed git probe to `[]` / `false`. Today both swallow, so `reconcileTicketTruth` publishes `dirty: false` inside a struct named `TicketTruth`. BREAKS: `salvage-ticket.ts:192` reads `if (!truth.dirty) return salvageCleanTree(...)`, and `salvageCleanTree` flips the ticket **Done** with `backfilled_clean_tree` whenever `completionCommitSha` is set, `truth.headSha` resolves, and the ticket is non-terminal — so a git-status failure over a genuinely dirty tree yields a false Done while the ticket's uncommitted work stays on the floor, AND the dirty branch (gate -> `commitScoped`) is skipped. `headSha` does NOT fail with it: measured on a corrupt `.git/index`, `git status` exits 128 while `git rev-parse HEAD` exits 0, so both back-fill preconditions hold at once. Same family as AP-EXT-ITER49-01 (`src/bin/CLAUDE.md`), one severity higher — that one skipped a salvage attempt, this one manufactures a completion claim. ENFORCE: none today (gap). Fix belongs at the PRODUCER (the two `defaultDeps` probes, mirroring `probeTreeDirty`: distinguish a measured non-repo from an unmeasurable one and let `dirty` carry the absence), NOT at the `salvage-ticket.ts` consumer. `extension/src/lib/reconcile-ticket-truth.ts` is OUTSIDE this branch's `scope.json` allowed_paths, so it is knowledge-only inside this fence. PATTERN_SHAPE: `catch { return []; }` in `dirtyPaths` or `catch { return false; }` in `isDirty` within `defaultDeps`.
