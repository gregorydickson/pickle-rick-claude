## Subsystem invariants

Five contracts apply to every script in `bin/`. Per-file trap doors (below) enforce these at the file level — this section makes the subsystem-wide contract findable before diving into per-file detail.

1. **CLI guard** — Every `bin/` script is a CLI entry-point and MUST have a `if (process.argv[1] && path.basename(process.argv[1]) === '<script>.js')` guard before executing any side-effectful logic. Enforced per-file via `audit-trap-door-enforcement.sh`; canonical `PATTERN_SHAPE` is in the `check-flake-budget.ts` trap door.

2. **StateManager.read()** — Every entry-point script that reads `state.json` MUST go through `StateManager.read()` (not raw `JSON.parse(fs.readFileSync(...))`). The only documented exception is the explicit crash-path `forceWrite` fallback in `mux-runner.ts`. Enforced via the consolidated **State-reader coverage** trap door in `extension/CLAUDE.md` (lists the covered readers + their ENFORCE tests) plus the `mux-runner.ts` `(crash fallback)` raw-parse exception trap door; `audit-trap-door-enforcement.sh` verifies ENFORCE-reachability.

3. **Finite spawn timeout** — Every script that spawns subprocesses MUST pass a finite `timeout` to `spawnSync` / `execFileSync` / `spawn`. Enforced per-file by: `test-runner.ts` trap door, `plumbus-frame-analyzer.ts` trap door (`BUN_TIMEOUT_MS`), R-APMW-6 (`mux-runner.ts`), `spawn-morty.ts` timeout-CLI trap door.

4. **Module Export Catalog** — Every helper module exported from `bin/` that is imported by other modules MUST appear in the `## Module Export Catalog` below. The catalog is the canonical index for import surface; add new exports when you add them to source. Enforced by `audit-subsystem-claude-md.sh` (R-CMD-4 trap door).

5. **Command files can be load-bearing runtime templates** — files under `.claude/commands/` and `extensionRoot/templates/` may be consumed at runtime as the manager-prompt template, not just as user slash-commands: `mux-runner.ts`, `pipeline-runner.ts`, and `jar-runner.ts` resolve `state.command_template` (default `pickle.md`) through `composeManagerPromptFromSkill` (which strips Setup + Step-One). NEVER delete or rename such a file without first grepping `command_template` + `composeManagerPromptFromSkill` for a runtime consumer — deleting `pickle.md` on the "it's just the bare `/pickle` command" premise bricked dispatch (Finding #77 / B-PNTR). Hard enforcement (template-parity + no-bare-`/pickle`) lands with B-PNTR.

## Trap Doors

- `check-update.ts` — INVARIANT: extraction containment is INHERITED from tar's defaults, not implemented here; the extract args MUST NOT carry `-P`/`--absolute-names`, and extraction MUST stay a real `tar` spawn. BREAKS: `..` and write-through-symlink members escape the `mkdtemp` prefix, then `runReleaseInstallScript` runs `install.sh` from the polluted dir. ENFORCE: extension/tests/integration/check-update-extraction-containment.test.js. PATTERN_SHAPE: `-P|--absolute-names` in `extractInstallablePayload`'s `tarArgs`, or `spawnSync('tar'` replaced by a tar library call.
- `correct-course.ts`; `generate-debate-personas.ts` (CLI entry guard) — INVARIANT: guard on `path.basename(process.argv[1]) === '<file>.js'` (subsystem contract #1). BREAKS: Node realpaths `import.meta.url` but never `argv[1]`, so a realpath-exact guard stops firing through a symlinked invocation path — the CLI exits 0 having done nothing (personas unwritten, correction unapplied). ENFORCE: none — the regression test is fence-blocked (both test files sit outside `scope.json` allowed_paths); the enforced sibling is the `verify-bundle.js` entry in `bin/CLAUDE.md`. PATTERN_SHAPE: `process\.argv\[1\]` compared against `fileURLToPath\(import\.meta\.url\)` (bare, `path.resolve`-wrapped, or via `new URL(\`file://…\`).href`) instead of `path\.basename\(process\.argv\[1\]\)`. OPEN GAP: `calibrate.ts:105`, `debate.ts:711`, `archaeology.ts:406`, `auto-fill-completion-commit.ts:156`, `hooks/handlers/stop-hook.ts:827` still carry the shape — all outside the fence, unfixed.

`types/index.ts` exports `CRASH_FLOOR_EXIT_REASONS` (3-member pickle-phase crash floor) and `EXIT_REASONS` (the runtime-iterable superset mux-runner's `ExitReason` type derives from) — see the trap door immediately below.
- `pipeline-runner.ts` (B-CRASHFLOOR pickle-arm crash floor) — INVARIANT: `isFatalPhaseFailure`'s `phase === 'pickle'` arm halts on exactly two conditions: a missing `start_commit`, and `state.exit_reason` being a member of `CRASH_FLOOR_EXIT_REASONS` (`types/index.ts`, beside `MICROVERSE_FATAL_REASONS`) via the mirrored `isCrashFloorExitReason` predicate — never `FAILURE_EXIT_REASONS`/`isFailureExit` (mux-runner.ts), which is a quality/measurement-verdict set CLAUDE.md binds to park-and-flag. `EXIT_REASONS` (also `types/index.ts`) is the runtime-iterable source of truth mux-runner's `ExitReason` type derives from (`typeof EXIT_REASONS[number]`). The trailing `catch` in `isFatalPhaseFailure` fails OPEN (`return false`), not closed. BREAKS: widening the pickle-arm halt predicate to `isFailureExit` re-adds 12 non-crash-floor abort conditions (error, stall, circuit_open, rate_limit_exhausted, ...), each a new way for reliability and quality to both hit zero per the root CLAUDE.md's no-stop-gates thesis. ENFORCE: extension/tests/pipeline-runner.test.js. PATTERN_SHAPE: `CRASH_FLOOR_EXIT_REASONS` consumed via `isCrashFloorExitReason(runnerState.exit_reason)` inside the `phase === 'pickle'` branch; `isFailureExit` absent from that branch.
- `spawn-refinement-team.ts` — INVARIANT: source-AC matches must overwrite analyst `source_prd`/`source_section` with repo-root-relative PRD metadata. BREAKS: readiness flags stale absolute or parent-relative PRD paths and blocks bundle launch. ENFORCE: `extension/tests/refinement-source-prd-relative.test.js`. PATTERN_SHAPE: `source_prd: ticket\\.source_prd \\?\\? sourcePrds\\.join`; INVARIANT: source-AC enrichment must walk reachable `peer_prds` and `composes:` closure before preserving analyst fallbacks. BREAKS: nested PRD provenance drops and readiness re-flags wrong source paths. ENFORCE: `extension/tests/refinement-source-prd-relative.test.js`. PATTERN_SHAPE: `composedPrdPaths\\(|peerPrdDeferredPaths\\(`.

- `spawn-refinement-team.ts` (relative `--prd` fail-late) — INVARIANT: `parseAndValidateArgs` absolutizes `--prd` via `path.resolve` before `existsSync`, so `prdPath` is absolute everywhere downstream. BREAKS: `enrichManifestTicketsFromSourcePrds` hard-throws on a relative parent path at manifest build — after all analyst cycles ran — discarding the whole refinement. ENFORCE: extension/tests/refinement-relative-prd-resolve.test.js. PATTERN_SHAPE: `prdPath` returned from `parseAndValidateArgs` without a `path.resolve(` wrap.
- `spawn-refinement-team.ts` (R-ACSG matcher + verdict shape) — INVARIANT: `isParametrizedTicket` and `hasJustificationBlock` MUST read the JOINED text of `title`+`acceptance_test`+`justification` (cross-field, via `ticketShapeText(`), NOT a single hard-coded field each; `runAcShapeEnforcement` MUST honor `--skip-ac-shape-gate <reason>` (emitting `ac_shape_gate_bypassed`) and its rejection message MUST name the `ac_id`+ticket id plus a fix template or the override path (`--skip-ac-shape-gate "<reason>"`); the gate is NORMATIVE on analyst ticket shape and ADVISORY on operator PRD prose shape. BREAKS: reverting to `title`-only quantifier / `acceptance_test`-only `describe.each` re-opens the LOA-727 false-reject (~30 min + ~9 worker quotas burned per incident); removing the bypass strands operators when the gate is wrong. ENFORCE: extension/tests/refinement-ac-shape-gate.test.js, bash extension/scripts/audit-trap-door-enforcement.sh. PATTERN_SHAPE: `ticketShapeText(` (or equivalent joined-field helper) consumed by BOTH `isParametrizedTicket` and `hasJustificationBlock`; `--skip-ac-shape-gate` parsed in `parseAndValidateArgs`.
- `spawn-refinement-team.ts` (R-RSU-2/R-RSU-1 bundle-of-bundles over-collapse guard) — INVARIANT: when the input PRD frontmatter declares `composes:` with ≥2 entries AND ≥1 composed source carries a `## Atomic decomposition` section, the analyst prompt MUST carry `BUNDLE_OF_BUNDLES_FANOUT_SECTION` (one ticket per R-code, no umbrella collapse) AND `main()` MUST run `detectBundleOfBundlesOverCollapse` before `writeManifestAtomic`, emitting `refinement_over_collapse_detected` + a `bundle_of_bundles_over_collapse` ticket-quality warning when `manifest.tickets.length <= composedPrdPaths(...).length`; the guard is observability-only (never throws, never changes exit code). BREAKS: removing the prompt section re-opens the section-umbrella over-collapse (incident `2026-05-13-ba01c135` ticket `3ab68cdd`, 67-min wedge); making the guard throw bricks refinement on a false positive. ENFORCE: extension/tests/spawn-refinement-team-bundle-of-bundles.test.js. PATTERN_SHAPE: `BUNDLE_OF_BUNDLES_FANOUT_SECTION` consumed in `buildWorkerPrompt` AND `detectBundleOfBundlesOverCollapse(` called in `main` before `writeManifestAtomic`.
- `spawn-refinement-team.ts` (manifest root-key schema drift) — INVARIANT: every `RefinementManifest` root key is declared in `refinement-manifest.schema.json` `properties` (root is `additionalProperties: false`). BREAKS: one undeclared key fails ajv over the WHOLE manifest, not the one field. ENFORCE: extension/tests/spawn-refinement-team-checker.test.js#AP-RMS-6. PATTERN_SHAPE: key in the `RefinementManifest` interface absent from schema `properties`. OPEN GAP: `decomposition_quality_flags`, `prd_advisory_shape_concerns` are undeclared — the schema file sits outside the scope fence; both are pinned in `KNOWN_UNDECLARED_MANIFEST_KEYS` so a THIRD key fails the suite.
- `spawn-refinement-team.ts` (unattributed `ticket_quality_warnings.ticket_id`) — INVARIANT: every `TicketQualityWarning` writer with no owning ticket uses `UNATTRIBUTED_TICKET_ID` (`'<prd>'`); `refinement-manifest.schema.json` declares `ticket_id` required, `minLength: 1`. BREAKS: `''` fails ajv over the WHOLE manifest, not the one entry — and the analyst-path scanner runs every refinement. ENFORCE: extension/tests/spawn-refinement-team-checker.test.js#AP-RMS-3. PATTERN_SHAPE: `ticket_id:\s*''` absent from `spawn-refinement-team.ts`.
- `spawn-refinement-team.ts` (warning-item schema drift) — INVARIANT: every `TicketQualityWarning` key is declared in `refinement-manifest.schema.json` `ticket_quality_warnings.items.properties` (also `additionalProperties: false`). BREAKS: an undeclared item key fails ajv over the WHOLE manifest; `required`-only checks are blind to it. ENFORCE: extension/tests/spawn-refinement-team-checker.test.js#AP-RMS-8. PATTERN_SHAPE: key in a TS interface absent from its strict subschema `properties` — check EVERY strict object, not just the root. OPEN GAP: `analyst`, `cycle` undeclared (schema fenced), pinned in `KNOWN_UNDECLARED_WARNING_ITEM_KEYS`.

- `spawn-refinement-team.ts`; `spawn-morty.ts` (citation line-count off-by-one — THREE consumers, ONE oracle) — INVARIANT: every content-line count routes through the ONE exported `countContentLines` oracle (strips a single trailing newline, CRLF-aware, `'' -> 0`). Consumers: `findStaleAnchorWarnings` + `checkAnalystOutputPaths` (citation staleness) and `statCodegraphFile` (codegraph node staleness, feeding `line <= info.lineCount` in `isNodeLocationFresh`). BREAKS: a bare newline-split length counts a phantom trailing empty element on every POSIX file, so a ref exactly ONE line past EOF reads in-range — inflating the operator-facing citation count by one, and (spawn-morty) passing a DEAD codegraph symbol ref into the worker prompt while `dropped_stale` under-counts the drop the codegraph efficacy metric rides on. CRLF is worse: a bare `'\n'` split also leaves `\r` in every element. ENFORCE: extension/tests/spawn-refinement-team-checker.test.js#AP-RMS-10, extension/tests/codegraph-context-section.test.js (boundary `line == count` FRESH vs `line == count + 1` STALE, LF + CRLF + no-trailing-newline control; both the injected `dropped_stale` and the `stale_refs` skip branch). PATTERN_SHAPE: `countContentLines(` called in `statCodegraphFile`; and, after stripping comments, the ONLY newline-split-immediately-followed-by-`.length` in either file is `countContentLines`' own one-line body in `spawn-refinement-team.ts` (the oracle IS that expression — it is the sanctioned occurrence, not a violation; an anchor demanding ZERO reads false on sight). Count only NON-comment lines: both files name the forbidden shape in prose, so a bare grep counts ITSELF (the documented self-matching falsification mode). A `.split(<newline>).filter(<non-empty>).length` form is NOT a match — the filter discards the phantom element by construction (the four surviving sites in `pipeline-runner.ts`, `audit-ticket-bundle.ts`, `hooks/dispatch.ts`, `state-manager.ts` are all that form, verified 2026-07-26).

- `spawn-refinement-team.ts` (citation suffix resolution crosses `/`) — INVARIANT: `resolveTrackedSuffixMatches` treats `git ls-files -- '*<token>'` as a PREFILTER only; a match counts solely on a path boundary (`matchesOnPathBoundary`: whole path, or preceded by `/`). Git's wildmatch runs without `WM_PATHNAME`, so `*` crosses `/`. BREAKS: fabricated `manager.ts` resolves to the real `services/state-manager.ts` — no `path_not_found` (fabrication detection defeated), and `manager.ts:9` emits `line_out_of_range` read off a file the analyst never named. ENFORCE: extension/tests/spawn-refinement-team-checker.test.js#AP-RMS-12. PATTERN_SHAPE: a `ls-files -- '*<token>'` result consumed without a `(?:^|/)<token>$` / `endsWith('/'+token)` boundary filter. Correct sibling: `check-readiness.ts:322`.

- `spawn-refinement-team.ts` (subprocess-timeout enumeration drift) — INVARIANT: subsystem contract #3 is enforced by ONE uniform scan over every `execFileSync`/`execSync`/`spawnSync` call in the file, never a per-callsite assertion list. The async worker `spawn` is exempt by construction (SIGTERM/SIGKILL escalation + hangGuard) and that escalation is itself asserted. BREAKS: hand-enumerated pins defend only remembered callsites — `readHeadFile`'s `git show HEAD:<path>` (main path, once per PRD citation) shipped unbounded past two such pins; an unbounded git wedges refinement with no output. ENFORCE: extension/tests/spawn-refinement-team-checker.test.js#AP-RMS-9. PATTERN_SHAPE: a sync-spawn call expression in `spawn-refinement-team.ts` whose balanced-paren body lacks `timeout:\s*[A-Za-z0-9_]`.
- `mux-runner.ts` (R-ICDM-1 detectManagerMaxTurnsExit turn-budget check) — INVARIANT: `detectManagerMaxTurnsExit(outcome, logFile, maxTurns)` MUST compare actual `num_turns` (falling back to `turn_count`) from the last `result` event against `maxTurns`; it MUST return `false` when either value is `null`/`undefined` (conservative). Callers at lines ~1721 and ~4065 in `mux-runner.ts` and at line ~1412 in `classifyManagerRelaunchExit` MUST pass the real `maxTurns` budget, never `null` as a permanent override. The `.claude/commands/anatomy-park.md`, `.claude/commands/szechuan-sauce.md`, and `.claude/commands/plumbus.md` templates MUST NOT carry `Do NOT output any promise tokens` without a complementary `<promise>TASK_COMPLETED</promise>` instruction. BREAKS: any claude worker that completes cleanly without a legacy promise token gets reclassified as `completion: 'error'` → anatomy-park / szechuan-sauce on claude tears down on iteration 1 (session 2026-05-13-e58dcc1d incident). ENFORCE: extension/tests/integration/mux-runner-claude-iteration-classifier.test.js. PATTERN_SHAPE: `detectManagerMaxTurnsExit\(` callable without a `maxTurns` argument; or any `.claude/commands/*.md` containing `Do NOT output any promise tokens` without a complementary `TASK_COMPLETED` instruction.
- `mux-runner.ts` (R-MMTR-3 claude max-turns relaunch) — INVARIANT: when the manager subprocess on the claude backend exits via `--max-turns` exhaustion (clean Claude CLI `end_turn` exit with non-empty pending-ticket queue), the `mux-runner.ts:3696-3730` relaunch branch MUST call `evaluateManagerRelaunch` and respawn up to `Defaults.CLAUDE_MANAGER_RELAUNCH_CAP` (`CLAUDE_MANAGER_RELAUNCH_CAP=20`) times rather than logging `Subprocess error. Exiting loop.`. BREAKS: long pickle phases on claude backend (>=10 tickets per mux iteration) tear down at the 400-turn boundary, stranding remaining tickets; operator must manually `bash launch.sh` to resume. ENFORCE: extension/tests/mux-runner-claude-max-turns-relaunch.test.js, extension/tests/manager-relaunch.test.js.
- `mux-runner.ts` (R-APMW-6 timeout-close ordering) — INVARIANT: timeout resolution must wait for child `close` (or a bounded fallback after `SIGTERM`) before resolving or closing `tmux_iteration_<n>.log`. BREAKS: timeout exits drop shutdown output and can overlap the next manager spawn with a still-dying child. ENFORCE: extension/tests/mux-runner.output-stall.test.js. PATTERN_SHAPE: `proc.kill\\('SIGTERM'\\)` followed by immediate `resolve\\(` or `fs\\.closeSync\\(logFd\\)` outside a `close`-gated path.
- `mux-runner.ts` (R-WSRC-2 schema-ahead graceful exit) — INVARIANT: every non-cap-check `sm.read(statePath)` call site MUST route through `readRunnerState()` which handles `SchemaVersionAheadError`/`SCHEMA_MISMATCH` by calling `recordExitReason('state_schema_version_ahead') + safeDeactivate + process.exit(3)`. BREAKS: a forward-schema state.json (e.g., from a malformed worker write OR mid-deploy schema bump) causes mux-runner to retry-loop at 1 warn/sec indefinitely (R-QGSK-3 wedge incident class). ENFORCE: extension/tests/mux-runner-schema-ahead-graceful-exit.test.js. PATTERN_SHAPE: `grep -c "sm\\.read(statePath)" extension/src/bin/mux-runner.ts` == 1, and that one occurrence is the `return sm.read(statePath)` INSIDE `readRunnerState`; the count IS the guard, because a second raw read is by definition an unwrapped site. Do NOT restore the old `count MUST equal grep -c "readRunnerState(" + grep -c "classifyCapCheckReadError("` equality: it compares ONE raw read against the wrapper's CALL count, which grow in opposite directions, and it was already false (1 vs 21) at its own authoring commit `e0d37d1c` — 1 vs 27 today.
- `mux-runner.ts` (orphan-session detection) — INVARIANT: `detectOrphanSessions` must read sibling `state.json` via `readRecoverableJsonObject(...)` before checking `parent_session_hash` / `invocation_source` / `working_dir`. BREAKS: a newer dead-owner `state.json.tmp.*` hides a real orphan manager subprocess and suppresses the iteration-boundary orphan warning. ENFORCE: extension/tests/integration/orphan-session-detection.test.js. PATTERN_SHAPE: `JSON\\.parse\\(fs\\.readFileSync\\(siblingStatePath`.
- `mux-runner.ts` (R-WMW-5 oversized-wedge auto-skip) — INVARIANT: `runMuxRunnerMain` MUST snapshot `countWorkerArtifacts(ticketDir)` BEFORE each `runIteration` call and persist the delta via `recordWorkerArtifactProgress` into `state.worker_artifact_progress[ticketId]` (schema v5, survives R-MMTR manager-relaunch boundary); at `PICKLE_WMW_OBSERVE_K` (default 3) consecutive zero-delta spawns emit exactly one `worker_artifact_progress_zero` event; at `PICKLE_WMW_SKIP_K` (default 5) consecutive zero-delta spawns flip the ticket to `Failed` with `failed_reason: 'oversized_no_progress'` and emit `worker_auto_skip_oversized`. `oversized_no_progress` is operator-recoverable (set `status: Todo` to re-queue). BREAKS: removing the cross-spawn persistence from `state.worker_artifact_progress` resets the zero-progress counter on each R-MMTR manager-relaunch, allowing a genuinely wedged oversized ticket to avoid auto-skip indefinitely and block the pipeline. ENFORCE: extension/tests/integration/worker-manager-wedge-oversized.test.js. PATTERN_SHAPE: `recordWorkerArtifactProgress(statePath, sessionDir, apTicketId, apBeforeCount` called after `runIteration` inside `runMuxRunnerMain`.
- `mux-runner.ts` (B-PCOMP #b736337f exit-path bystander stash) — INVARIANT: `commitGatePassingDeliverableOnExitPath` MUST NEVER over-stage a whole-tree `git add -A` under the exiting ticket's completion commit. When the dirty set has un-attributable foreign paths it routes through the shared salvage seam (`salvageDirtyTree`, B-1SEAM WS-3) and stages ONLY the owned paths; when the ownership probe throws it stashes the remainder and returns `{committed:false, reason:'clean-ticket-tree'}` (commits nothing). `stashUnattributableRemainder` now LIVES in `services/dirty-tree-salvage.ts` (re-exported from `mux-runner.ts` to preserve the import surface): it anchors the WHOLE dirty tree (tracked + untracked) under `refs/pickle/salvage/<session>` via a THROWAWAY `GIT_INDEX_FILE` (no real-index/worktree mutation, no `state.json` write — schema-neutral), returning null on a clean tree or any git failure (best-effort). BREAKS: the whole-tree-add fallback commits a sibling ticket's work as THIS ticket's Done (false Done); mutating the real index in the stash path corrupts the caller's subsequent scoped commit. ENFORCE: extension/tests/exit-path-bystander-stash.test.js, extension/tests/dirty-tree-salvage-callsites.test.js. PATTERN_SHAPE: `salvageDirtyTree(` on the `foreign.length > 0` branch and `stashUnattributableRemainder(` on the probe `catch`; no whole-tree `git add -A` fallback under a `ticketId`.
- `mux-runner.ts` (R-CMWL-1..4 codex-continuation invariant) — INVARIANT: codex `Session inactive` exit is a relaunchable class gated by `CODEX_MANAGER_RELAUNCH_CAP`; a progressing-incomplete pickle (tickets remain pending after a relaunch) is non-fatal — the outer loop continues; interrupted-ticket work MUST be reset via `resetInterruptedTicketWorkForRelaunch` at the relaunch boundary before the next worker spawn; 2 consecutive zero-progress codex relaunch passes (`codex_manager_consecutive_no_progress >= 2`) MUST halt with `exit_reason: 'codex_manager_no_progress'`. BREAKS: removing the `Session inactive` relaunch branch strands remaining tickets permanently; absent no-progress guard allows infinite codex relaunch loops when no tickets advance. ENFORCE: extension/tests/integration/codex-manager-fixed-wall-continuation.test.js.

- `microverse-runner.ts` (R-SJET-1a stdio invariant) — INVARIANT: judge spawn sites (`measureLlmMetricAttempt` and `probeJudgeCliAvailability`) MUST use `stdio: ['ignore', 'pipe', 'pipe']` in the non-legacy async path. The `PICKLE_JUDGE_LEGACY_SPAWN=1` kill-switch is the ONLY permitted exception and restores `['pipe', 'pipe', 'pipe']` for emergency rollback. No other judge spawn site may use `['pipe', 'pipe', 'pipe']` for stdin. BREAKS: reopens the stdin-hang bug where Claude CLI reads stdin waiting for EOF before producing output (180s × 4 attempts × 2 sessions). ENFORCE: extension/tests/bin/microverse-judge-probe.test.js. PATTERN_SHAPE: `stdio: \['ignore', 'pipe', 'pipe'\]` at judge async spawn sites; `stdio: \['pipe', 'pipe', 'pipe'\]` permitted only inside `PICKLE_JUDGE_LEGACY_SPAWN === '1'` branch.
- `microverse-runner.ts` (R-SJET-3 judge env hygiene) — INVARIANT: both judge spawn sites (`measureLlmMetricAttempt` and `probeJudgeBackendAvailability`) MUST build env via `getJudgeEnvForAttempt(...)`, then layer backend-specific overrides. BREAKS: raw `process.env` leaks `PICKLE_*` or `CLAUDECODE*` session markers into nested judge probes and reopens outer-session contamination/hang regressions. ENFORCE: extension/tests/bin/microverse-judge-probe.test.js. PATTERN_SHAPE: `env: \{ \.\.\.process\.env` at judge spawn/probe call sites.
- `mux-runner.ts`; `pipeline-runner.ts`; `microverse-runner.ts` — INVARIANT: CLI session preflight must accept tmp-only recoverable `state.json` via `readRecoverableJsonObject(...)`, not raw `existsSync(state.json)`. BREAKS: crash-recoverable sessions die at Usage before runner recovery starts. ENFORCE: `extension/tests/runner-entry-recovery.test.js`. PATTERN_SHAPE: `existsSync\(path\.join\(sessionDir, 'state\.json'\)\)` in CLI entry blocks.
- `metrics.ts` (HS-13 backend column completeness) — INVARIANT: per-backend output columns in both daily and weekly tables derive from `BACKENDS` via `buildBackendColumns`, never a hardcoded `claude`/`codex`/`hermes` subset. BREAKS: a backend added to `BACKENDS` (deepseek/grok/kimi/gemini drift) is silently dropped from the breakdown, so columns no longer reconcile to the `Output` total. ENFORCE: extension/tests/metrics.test.js (`metrics.backend-columns` tests). PATTERN_SHAPE: `buildBackendColumns(` used in both `printDailyTable` and `printWeeklyTable`; no literal `'Claude'`/`'Codex'`/`'Hermes'` table headers in `metrics.ts`.

- `status.ts` (pipeline-status recoverable read) — INVARIANT: `readPipelineStatus` reads `pipeline-status.json` via `readRecoverableJsonObject`, never raw `JSON.parse(fs.readFileSync)`, so an interrupted tmp-rename leaves a `.tmp.<pid>` snapshot promoted like the sibling readers in `monitor.ts`/`pipeline-runner.ts`. BREAKS: a crash-orphaned tmp makes the operator recap show stale/empty phases (`X/0`). ENFORCE: extension/tests/status.test.js (`recovers pipeline-status.json from orphan tmp`). PATTERN_SHAPE: `readRecoverableJsonObject(statusPath)` in `readPipelineStatus`; no `JSON.parse(fs.readFileSync(` of `pipeline-status.json` in `status.ts`.

- `microverse-runner.ts` (R-SJET-1b typed-error instanceof-first invariant) — INVARIANT: `classifyJudgeError` MUST check `instanceof JudgeMeasurementTimeout` and `instanceof JudgeMeasurementSpawnFailed` BEFORE any regex fallback. The ETIMEDOUT regex fallback (`/\bETIMEDOUT\b/i`) is the catch-all for raw errors from the async callback path that bypass typed-error wrapping. Callers MUST NOT wrap callback errors in typed classes — raw errors are intentional so the regex fallback preserves existing telemetry assertions (`probe_kind: 'timeout'`, `timeout_class: 'probe_timeout'`). BREAKS: wrapping ETIMEDOUT callback errors in `JudgeMeasurementSpawnFailed` bypasses the regex fallback and reclassifies them as `spawn_failed`, breaking `baseline-attempt-timeout.test.js` and `microverse-baseline-classification.test.js` assertions. ENFORCE: extension/tests/bin/microverse-judge-probe.test.js. PATTERN_SHAPE: `instanceof JudgeMeasurementTimeout` check before `isMissingCliError` and ETIMEDOUT regex in `classifyJudgeError`.
- `microverse-runner.ts` — INVARIANT: dirty-tree preflight rollback must restore the exact pre-existing cached diff after auto-commit failure; only runner-added staging may be undone. BREAKS: hook or identity failures clear operator-staged changes from the index and hide the intended manual commit set. ENFORCE: `extension/tests/microverse.test.js`. PATTERN_SHAPE: `preflightAutoCommit` catch path calling bare `git reset` without restoring a pre-stage cached diff snapshot.
- `microverse-runner.ts` (R-SJWT-1/R-SJWT-TD scoped judge prompt) — INVARIANT: scoped microverse runs (`allowed_paths` non-empty) build the judge prompt over `allowed_paths` via the `allowedPaths.length > 0` branch in `buildJudgePrompt`, never the whole TARGET; the "Target path:" instruction is omitted and replaced with "Review ONLY these paths:" followed by each allowed path enumerated as `- <path>`. BREAKS: re-introducing the whole-tree "Target path:" for scoped runs causes the judge to score out-of-scope violations, producing a non-zero score plateau instead of converging to 0 once all in-scope violations are fixed (HS-12 held at score=8 for 4 iterations pre-fix). ENFORCE: extension/tests/microverse-convergence.test.js (R-SJWT-3 tests). PATTERN_SHAPE: `allowedPaths.length > 0` branch in `buildJudgePrompt` enumerates paths with `'Review ONLY these paths:'` header; `'Target path:'` string absent from scoped-path prompt output; R-SSOC L1 ADDS a `'Count ONLY violations located within these paths'` scoring-constraint line inside the same branch (additive — does not weaken the header/enumeration/Target-path-absent pins).

- `microverse-runner.ts` (R-SSOC #129 post-iteration scope audit) — INVARIANT: `handleWorkerMode` MUST call `auditPostIterationScope(ctx, state)` immediately after `ctx.postIterSha = _deps.getHeadSha(ctx.workingDir)`. The audit diffs the iteration's committed files (`git diff --name-only --no-renames preIterSha..postIterSha` via `_deps.spawnSync`, finite 15_000ms timeout) and routes them through the EXPORTED `checkScopeDiff` (REUSE — never reimplement scope matching, so the #128 `CLAUDE.md` carve-out flows through); on `status: 'outside_scope'` it emits `worker_edit_outside_scope` via `_deps.logActivity` with the AC-APWS-1 quartet (`scope_json_path`, `staged_paths_outside_scope`, `head_ref`, `suggested_remediation`) + `ticket_id` from `state.current_subsystem`. This fires INDEPENDENTLY of the worker running the prompt-level preflight (a codex worker bypasses both the prompt instruction and PreToolUse hooks → 7 off-scope commits landed silently with 0 events, session 2026-06-19-2b1e2707). Observability ONLY — it NEVER reverts/blocks/halts; the whole body is best-effort try/catch (telemetry never crashes the runner or changes exit behavior); absent `scope.json` / equal pre==post HEAD / empty diff are no-ops. BREAKS: removing the call re-opens the silent off-scope-commit drift class for non-preflight (codex) workers — pollution is then caught only by manual `git log` at the ship gate. Do NOT re-word this to say the audit makes drift visible at `/pickle-status`: it does not, and never did (see the microverse-prompt EXECUTION CONTEXT trap door below) — `renderScopeDrift` keeps only `ticket_id`s present in `collectTickets`, and this producer's `ticket_id` is a SUBSYSTEM name. The event's real home is the activity JSONL; reimplementing scope matching instead of reusing `checkScopeDiff` drops the #128 carve-out and re-fences trap-door `CLAUDE.md` catalog commits. ENFORCE: extension/tests/microverse-post-iteration-scope-audit.test.js. PATTERN_SHAPE: `auditPostIterationScope(ctx, state)` called in `handleWorkerMode` after the `postIterSha` capture; `checkScopeDiff(` invoked inside `auditPostIterationScope` with `_getStagedPaths`; `event: 'worker_edit_outside_scope'` emitted via `_deps.logActivity` in that function.

- `microverse-runner.ts` — INVARIANT: `worker_backend_resolved.source` must be the worker-backend precedence source from backend resolution. BREAKS: schema-backed activity logs reject or misclassify remediator backend telemetry. ENFORCE: `extension/tests/microverse.test.js` remediator activity-event assertion. PATTERN_SHAPE: `event: 'worker_backend_resolved'` with `source` not sourced from `workerBackendResolution.source`; INVARIANT: handoff must advertise `gap_analysis.md` only when the file exists at `gap_analysis_path`. BREAKS: workers are told to read a dead path and lose the real checklist. ENFORCE: `extension/tests/microverse.test.js` gap-analysis handoff coverage. PATTERN_SHAPE: `gap_analysis_path` truthiness check without `fs.existsSync(...)`; INVARIANT: structured judge payloads with numeric `score` must be accepted before backoff/retry logic. BREAKS: full `{score,violations,...}` outputs loop through timeout/failed paths and never advance ledger classification. ENFORCE: `extension/tests/microverse-helpers.test.js` structured-ledger runtime coverage. PATTERN_SHAPE: `extractScore(...)` on judge output without JSON `score` fallback.
- `audit-worker-backends.ts` — INVARIANT: backend audits must apply per-ticket `worker_spawn_backend_override` before inferring expected worker backend from manager-resolution events, but only in real activity order so a stale override cannot outlive a newer `worker_backend_resolved`. BREAKS: intentional codex overrides are reported as cross-backend mismatches, or old overrides hide later claude/hermes mismatches. ENFORCE: `extension/tests/audit-worker-backends.test.js` override-order regression. PATTERN_SHAPE: per-event-type filtering that replays `worker_spawn_backend_override` after later `worker_backend_resolved`; INVARIANT: per-log audits must prefer `worker_spawn_backend_resolved {ticket,pid,backend}` over ticket-level fallback because `worker_session_<pid>.log` is the exact spawn artifact. BREAKS: mixed-backend retries on one ticket are false-flagged or missed when the last ticket-level backend is applied to every historical log. ENFORCE: `extension/tests/audit-worker-backends.test.js` mixed-backend retry regression. PATTERN_SHAPE: `worker_session_<pid>.log` scanned without joining `worker_spawn_backend_resolved.pid`; INVARIANT: Codex-banner detection is a mismatch for every non-Codex expected worker, including Hermes. BREAKS: Hermes sessions silently pass when a Codex worker wrote the log. ENFORCE: `extension/tests/audit-worker-backends.test.js` Hermes mismatch regression. PATTERN_SHAPE: `expectedWorkerBackend === 'claude' && patternsFound.length > 0`.
- `audit-worker-backends.ts` — INVARIANT: session backend audits must read `state.json` through `readRecoverableJsonObject(...)` before replaying `activity` backend events. BREAKS: a newer dead-owner `state.json.tmp.*` can hide codex overrides/resolutions and false-flag or miss worker-log mismatches. ENFORCE: `extension/tests/audit-worker-backends.test.js`. PATTERN_SHAPE: `JSON\\.parse\\(fs\\.readFileSync\\(path\\.join\\(sessionDir, 'state\\.json'\\)`.
- `council-publish.ts` (composeBody markdown-cell escaping) — INVARIANT: every free-text finding cell (`file`, `description`, `rule`, `recommendation`) interpolated into the findings table MUST pass through `escapeTableCell` (`|`→`\|`, CR/LF→`<br>`), AND every free-text trap-door field (`path`, `constraint`, `why_it_breaks`, `what_must_hold`) in the trap-door bullet block MUST pass through `collapseCellNewlines` (CR/LF→`<br>`). BREAKS: a raw `|` or newline in LLM-authored findings splits/misaligns the PR-comment table, misattributing severity/file/rule columns; a raw newline in LLM-authored trap-door text breaks the single bullet and injects arbitrary block-level markdown (headings, lists, `</details>` breakout) into the posted PR comment. ENFORCE: extension/tests/council-publish.test.js (`pipe and newline in finding cells`, `newline in trap-door free text cannot inject block markdown` tests). PATTERN_SHAPE: `escapeTableCell(` wrapping each free-text cell AND `collapseCellNewlines(` wrapping each trap-door field in `composeBody`.
- `test-runner.ts` — INVARIANT: the delegated `node --test` child MUST run under an explicit `spawnSync(..., { timeout })`, with `PICKLE_TEST_RUNNER_TIMEOUT_MS` override clamped to a sane bound. BREAKS: one hung test process wedges `npm run test:*`, gate verification, and release wiring indefinitely. ENFORCE: `extension/tests/bin/test-runner-tier-discovery.test.js`. PATTERN_SHAPE: `spawnSync\\(process\\.execPath, nodeArgs, \\{[^}]*stdio: 'inherit'` without `timeout: getRunnerTimeoutMs\\(\\)`.
- `auto-fill-completion-commit.ts` — INVARIANT: completion-commit inference must read `start_time_epoch` through `readRecoverableJsonObject(statePath)` without any `existsSync(statePath)` pre-gate before filtering git evidence. BREAKS: a newer recoverable `state.json.tmp.*` can hide the real session start and auto-fill a prior-session commit. ENFORCE: `extension/tests/auto-fill-completion-commit.test.js`. PATTERN_SHAPE: `existsSync\\(statePath\\)|JSON\\.parse\\(fs\\.readFileSync\\(statePath, 'utf8'\\)\\)`.
- `cancel.ts` — INVARIANT: `cancelSession` must promote tmp-only recoverable `state.json` via `readRecoverableJsonObject(statePath)` before missing/unreadable checks. BREAKS: `/eat-pickle` rejects live recoverable sessions as missing and leaves the loop active. ENFORCE: `extension/tests/cancel.test.js`. PATTERN_SHAPE: `existsSync\\(statePath\\)` pre-gate before `sm.read\\(statePath\\)` in `cancelSession`.
- `cancel.ts` (index.lock is git's, not ours) — INVARIANT: cancel REPORTS a leftover `.git/index.lock`, never unlinks it. BREAKS: git closes the fd before it renames, so an unheld probe reading is not proof of unowned; deleting there lets a second git take the same lock — two writers in the index critical section. ENFORCE: `extension/tests/cancel-index-lock-preserved.test.js`. PATTERN_SHAPE: no `unlinkSync(lockPath` and no `probeLockHolder`/`lsof`/`pgrep` in `cancel.ts`.
- `setup.ts` — INVARIANT: resume-time `--worker-backend` must persist to `state.worker_backend` whenever the flag is explicit. BREAKS: `resolveWorkerBackendFromStateFile` keeps routing workers to the stale persisted backend after `/resume`. ENFORCE: `extension/tests/setup.test.js` resume worker-backend override regression. PATTERN_SHAPE: `config.explicitFlags.has('worker-backend')` without a matching `s.worker_backend = config.workerBackend`.
- `setup.ts` (paused-orphan scan) — INVARIANT: `scanPausedOrphans` must read stale session state via `readRecoverableJsonObject(statePath)` before checking `active`/`pid`/`working_dir`, so a newer dead `state.json.tmp.*` snapshot cannot hide a paused orphan from `--paused` demotion. BREAKS: setup skips warning and demotion even though the recoverable snapshot still has `active=true` with no claimant. ENFORCE: `extension/tests/bin/setup-paused-orphan.test.js`. PATTERN_SHAPE: `JSON\\.parse\\(fs\\.readFileSync\\(statePath, 'utf-8'\\)\\)` inside `scanPausedOrphans`.
- `setup.ts`; `src/services/state-manager.ts` (R-PTSB phantom-leak invariant) — INVARIANT: (1) tests invoking session-writing bins (setup.js, mux-runner.js, spawn-morty.js, jar-runner.js) MUST set PICKLE_DATA_ROOT to a tmp directory — enforced by audit-test-isolation.sh's audit_session_bin_sandbox check; (2) setup.ts MUST stamp pid = process.pid into state.json when writing an active session so dead-pid recovery can identify and demote stale entries; (3) state-manager.ts recoverStaleActiveFlag MUST auto-demote active sessions whose pid is null with no live claimant, recording exit_reason = 'orphan-phantom-demoted'. BREAKS: (1) unsandboxed tests pollute the real data root and produce pid-null phantom sessions that block install.sh finalization; (2) missing pid stamp leaves pid-null sessions permanently active with no runtime recovery path; (3) removing the pid-null demotion path in recoverStaleActiveFlag leaves phantoms immune to recovery until the babysitter manual band-aid runs. ENFORCE: extension/scripts/audit-test-isolation.sh, extension/tests/state-manager.test.js.
- `spawn-gate-remediator.ts` (R-GRLS WS-1 — the sixth lock reclaims a dead holder; proof of death ONLY) — INVARIANT: `acquireLockfile` does NOT hand-roll an acquire/release. It takes the lock via `acquireLockFile(lockfilePath, String(process.pid))` — a BARE-pid payload, the one encoding `isDeadPidPayload` reads — reclaims a strand through `reclaimDeadRemediatorLock` (`withStealRight` + `isDeadPidPayload` + `stealLockFile`), retries the acquire ONCE, and releases identity-bound via `releaseLockFile(lockfilePath, lock.held)`. It MUST NOT grow an age-based steal arm: a remediator legitimately holds for minutes while it drives a worker turn, so an age verdict would evict a LIVE holder. A still-held lock after the reclaim attempt means a provably live owner and keeps the lockout (exit 0, `LOCKOUT_PATH=`). BREAKS: it hand-rolled `openSync(O_CREAT|O_EXCL|O_WRONLY)` + immediate `closeSync` — publishing an EMPTY payload — and released only via `process.on('exit')`, which SIGKILL/SIGTERM skip. One abrupt death stranded `gate/remediator.lockfile`, and because the EEXIST branch returns exit code **0**, every later remediator for that session exited CLEAN having edited nothing — indistinguishable from a successful remediation (the false-GREEN class: a red gate reported as handled). The empty payload also silently defeats any steal bolted on later (`isDeadPidPayload('')` → NaN → never dead), which is why the payload encoding IS the fix, not an add-on. ENFORCE: extension/tests/integration/lock-steal-live-holder.test.js (`spawn-gate-remediator:` block — the strand case asserts `BRIEF_PATH=` + exactly one brief on disk, NOT merely exit 0, because exit 0 is what the bug already returned; both mutations confirmed RED). PATTERN_SHAPE: `acquireLockFile(lockfilePath, String(process.pid))` + `reclaimDeadRemediatorLock(` + `releaseLockFile(lockfilePath, lock.held)` in `spawn-gate-remediator.ts`; no `O_EXCL` and no `existsSync`/`unlinkSync` lock cleanup in the file. The reclaim-side and write-side tests are COMPLEMENTARY, not redundant — the strand case seeds a bare-pid lock by hand, so it stays GREEN if the remediator's own payload regresses to `{pid,ts}`; only the published-payload case catches that. RESIDUAL: a lock stranded by a PRE-R-GRLS binary has a 0-byte payload and is therefore never reclaimed (deliberate — a steal on empty payloads would evict a live holder under the old create-then-write shape). Bounded and one-time: `gate/` is session-scoped and post-fix every published lock carries a pid.
- `spawn-gate-remediator.ts` — INVARIANT: `worker_spawn_backend_resolved.source` must stay within `BackendResolutionSource` enum semantics (`env` or `default` here). BREAKS: schema-backed remediator telemetry becomes invalid and downstream payload validation drifts. ENFORCE: `extension/tests/bin/spawn-gate-remediator.test.js` activity-event regression. PATTERN_SHAPE: `event: 'worker_spawn_backend_resolved'` with source outside schema enum.
- `log-activity.ts` — INVARIANT: persona CLI emissions must reject events whose schema-required top-level or `gate_payload` fields are absent; `subtool_backend_override` still requires `--backend codex` in `.claude/commands/codex-rescue.md` and `.claude/commands/send-to-morty.md`. BREAKS: shell emitters append schema-invalid activity rows that downstream validators reject. ENFORCE: `extension/tests/log-activity-gate-payload.test.js`, `extension/tests/log-activity-backend-flag.test.js`. PATTERN_SHAPE: `log-activity.js` event payload built without schema-required field validation before `logActivity(...)`.
- `mux-runner.ts` (R-WSE-2 worker_partial_lifecycle_exit ts) — INVARIANT: `checkPartialLifecycleExit` emits via `writeActivityEntry`, which validates only the event NAME and does NOT auto-stamp `ts` (unlike `logActivity` in `services/activity-logger.ts`). The producer MUST pass `ts: new Date().toISOString()` explicitly so the JSONL line satisfies schema `required: ['event','ts','ticket','gate_payload']`. BREAKS: removing the explicit `ts` reverts the iter-9 producer/schema disconnect — payload becomes schema-non-conformant, downstream timestamp correlation breaks, and any future Ajv runtime validation on `oneOf` rejects every emission. Same regression class as iter-7 (`ticket_audit_failed`) and iter-8 (`time_cap_disabled_default`). ENFORCE: extension/tests/worker-partial-lifecycle-exit-schema-conformance.test.js, extension/tests/integration/worker-partial-lifecycle-exit.test.js. PATTERN_SHAPE: `event:\s*['"]worker_partial_lifecycle_exit['"]` inside a `writeActivityEntry(...)` call in `checkPartialLifecycleExit` MUST be accompanied by `ts:\s*new Date\(\)\.toISOString\(\)` in the same call.
- `mux-runner.ts` (R-WSDO worker_produced_nothing breadcrumb) — INVARIANT: the MAIN-path breadcrumb fires ONLY when all three hold — `plExit === null` (mutual exclusion with `worker_silent_death` by construction), zero raw artifact-count delta (`apProgressResult.lastArtifactCount - apBeforeCount === 0`, apples-to-apples because both counts share `creditEarlyPhases`), AND `classifyWorkerSessionLogs` returns `subClass === 'log_empty'`; it emits `worker_produced_nothing` via `writeActivityEntry` with explicit `ts: new Date().toISOString()` (no auto-stamp) and is observability-only (never alters reap/salvage). BREAKS: dropping the `plExit === null` gate double-emits alongside `worker_silent_death` (the exact case R-WSDO was built to carve out); dropping the explicit `ts` makes the payload schema-non-conformant (same class as the R-WSE-2 / R-CCPM-1 ts disconnects). ENFORCE: extension/tests/worker-produced-nothing.test.js, extension/tests/activity-event-payload.test.js. PATTERN_SHAPE: `event:\s*['"]worker_produced_nothing['"]` inside a `writeActivityEntry(...)` call gated by `plExit === null` and accompanied by `ts:\s*new Date\(\)\.toISOString\(\)`.
- `mux-runner.ts` (AC-WMFF-2A archive-on-ladder-flip consistency) — INVARIANT: EVERY Failed-flip site archives a dirty tree BEFORE its frontmatter write. All three — head-regression, wmw-auto-skip, and `advanceOrExitOnLadderExhaustion` — call `archiveDirtyTreeBeforeFlip({ workingDir, sessionDir, ticketId, log })` first; `advanceOrExitOnLadderExhaustion` therefore REQUIRES a `workingDir` input (it had none, which is precisely why it was the one site that could not archive). BREAKS: the B-DURA T10 boundary committer is `!isTerminalTicketStatus(...)`-guarded and skips an already-Failed ticket, so a ladder-exhaustion flip over a dirty tree leaves a verified worker diff with NO commit AND no archive — the runner's downstream reset then destroys it unexamined (2026-07-11 incident, session `2026-07-11-a19aa731` ticket `6317933b`). Making `workingDir` optional silently re-opens the gap AND lets `archiveBeforeDestructive` run with `cwd: undefined` (git in `process.cwd()`). ENFORCE: extension/tests/worker-produced-everything-but-commit.test.js (AC-WMFF-2A cases), extension/tests/rrh-workstream-a.test.js. PATTERN_SHAPE: `archiveDirtyTreeBeforeFlip(` call-site count in `mux-runner.ts` >= 4 (orphan-reattach pre-ff-only + the 3 Failed-flip sites), and `advanceOrExitOnLadderExhaustion`'s input type declares a required `workingDir: string` consumed by an `archiveDirtyTreeBeforeFlip(` call placed BEFORE its `updateTicketFrontmatter(... status: 'Failed' ...)`.
- `mux-runner.ts` (AC-WMFF-2B worker_produced_everything_but_commit breadcrumb) — INVARIANT: **at most ONE production breadcrumb reaches `state.json` per iteration**, and the emission lives in the exported `emitWorkerProductionBreadcrumb` — the one seam a test can cross to reach it. The mux loop passes the measured terms in (`partialLifecycleExit`, `artifactDelta`) and does nothing else; the predicates, their overlap, and the R-WSDO-first control flow are all inside the function. Do NOT re-inline it: for as long as it was inline, its only "spec" was a test-local re-derivation of the `if / else if` that never invoked the runtime and could not be reddened by ANY production mutation. The breadcrumb is a structural `else if` BELOW the R-WSDO `worker_produced_nothing` branch — R-WSDO FIRST, mutual exclusion in CONTROL FLOW (never a "disjoint by construction" claim: all-prefixes-present forces `plExit === null` and R-WSDO's middle term is an iteration DELTA, so a respawned worker with prior-iteration artifacts + zero delta + 0-byte log + a dirty tree satisfies BOTH predicates). THREE guards enforce the exclusion and a real double-fire must defeat ALL THREE: (1) the second arm is computed LAZILY (`wsdoFires ? null : claim(...)`), so a winning R-WSDO leaves it `null`; (2) the R-WSDO branch `return`s early; (3) the `else if`. Swapping ONLY the `else if` for a sibling `if` therefore does NOT double-fire — the earlier trap-door text asserted that it does, which is false, and a spec that pins any single guard can be satisfied while the invariant is violated. **Pin the invariant where it lives: exactly one event in `state.json.activity`, asserted against the real emitter.** `claimWorkerProducedEverythingButCommit` is claim-once per `(ticket, iteration)` and fires only when: the ticket reads Failed; `findMissingPrefixes(files, requiredTierArtifactPrefixes(tier))` is empty with `tier` read from the TICKET FRONTMATTER `complexity_tier` (NEVER the `state.current_ticket_tier` cache — R-CNAR-1 staleness trap); AND either `collectDirtyInScopePaths` (which REUSES the exported `checkScopeDiff` via its `_getStagedPaths` seam so the #128 `CLAUDE.md` carve-out flows through, falling back to scope-unfiltered `listWorkingTreeDirtyPaths` when no `scope.json` exists) reports paths, OR an unreferenced commit exists in `preIterSha..HEAD` (unreferenced = not any ticket's `completion_commit`, and the walk starts at `preIterSha` — NEVER the session `start_commit`, which would resurrect an earlier iteration's commit as this one's unclaimed work). `dirty_in_scope_paths` is capped at `EVERYTHING_BUT_COMMIT_PATH_CAP` (20) with `truncated` + `total_count` carrying the truth. ARCHIVE PARITY: the dirty set MUST equal the set `archiveBeforeDestructive` would save — `collectDirtyInScopePaths` drops codegraph artifacts via the SAME exported `isCodegraphArtifact` the archive uses (`.codegraph/` lives in the working dir and is git-ignored only through the local, unversioned `.git/info/exclude`, so on a fresh clone it is plain untracked dirt). The payload claims "this work is still on the floor" and the archive is what makes it recoverable; if they disagree the breadcrumb sends the operator after a `pre_reset_*.patch` that was never written. Producer stamps `ts: new Date().toISOString()` explicitly (`writeActivityEntry` does NOT auto-stamp). OBSERVABILITY ONLY: the emit path performs no reap, salvage, status, or frontmatter write. BREAKS: eager-computing the second arm AND dropping the early return AND swapping the `else if` for a sibling `if` double-fires on the overlap fixture (verified: the overlap spec goes RED on that mutation, and RED again on the nastier variant that double-writes but still returns the correct value — so the `state.json` assertion is load-bearing on its own, not dead weight behind the return-value check); re-inlining the emitter into `runMuxRunnerMain` re-opens the untestable region (a 2000-line async function has no seam, which is exactly how the original shipped with a self-certifying spec); reading `state.current_ticket_tier` instead of the frontmatter mis-tiers the prefix set on a stale cache; re-implementing scope matching instead of reusing `checkScopeDiff` drops the #128 carve-out; removing the cap re-opens the 20MB-state.json class; dropping the `isCodegraphArtifact` filter counts the runtime's own codegraph index as recoverable worker output (fires over a tree the archive correctly declines to archive); widening the window walk to `start_commit..HEAD` reports a prior iteration's commit as this iteration's unclaimed work; any mutating call on the emit path turns a breadcrumb into an unreviewed recovery action. ENFORCE: extension/tests/worker-produced-everything-but-commit.test.js, extension/tests/activity-event-payload.test.js, extension/tests/activity-logger.test.js. PATTERN_SHAPE: both `writeActivityEntry(` emissions live inside `export function emitWorkerProductionBreadcrumb(`, and `runMuxRunnerMain` reaches them ONLY through a call to it (zero `event: 'worker_produced_nothing'` / `'worker_produced_everything_but_commit'` literals in the loop body); the lazy `wsdoFires\s*\?\s*null\s*:\s*claimWorkerProducedEverythingButCommit\(` + the `return 'worker_produced_nothing';` early return + `} else if (everythingButCommit) {` all three present in that function; the overlap spec asserts `deepEqual(readAllActivity(statePath).map(e => e.event), ['worker_produced_nothing'])` against the REAL emitter — never a test-local re-derivation of the branch; `listWorkingTreeDirtyPaths(workingDir).filter((p) => !isCodegraphArtifact(p))` in `collectDirtyInScopePaths`; `rev-list` bounded by `` `${preIterSha}..HEAD` `` in `findUnreferencedWindowCommit`; `event: 'worker_produced_everything_but_commit'` accompanied by `ts: new Date().toISOString()`; `claimWorkerProducedEverythingButCommit` body free of `updateTicketFrontmatter|writeTicketStatus|markTicketDone|salvage|writeActivityEntry`; registered at ALL touchpoints — `VALID_ACTIVITY_EVENTS` (`src/types/index.ts`), the schema `definitions` block AND the top-level `oneOf` `$ref` (`src/types/activity-events.schema.json` — a definition without the `$ref` is INERT), plus the compiled mirrors `extension/types/index.js` + `extension/bin/mux-runner.js`.
- `pipeline-runner.ts` (AC-CWRR-6 resume counter seed) — INVARIANT: the crash-resume start index and the seeded `counters.completed`/`counters.skipped` come from ONE `readResumePhasePlan` read of `pipeline-status.json`; skipping phases without carrying their counts makes `finalizePipeline`'s `(completed + skipped) < phases.length` permanently true. BREAKS: a fully-successful resumed pipeline finalizes FAILED, the closer `install()`+`tag()` (gated `!pipelineFailed`) is skipped, and it exits 1 — driving auto-resume retry loops. ENFORCE: extension/tests/b-crsr-resume-counter-seed.test.js. PATTERN_SHAPE: a `resumeStartIndex > 0` branch in `main()` with no `counters.completed =` / `counters.skipped =` assignment, or a second parse of `pipeline-status.json` beside `readResumePhasePlan`.
- `pipeline-runner.ts` (WS-A2 all-Done incompleteness stamp) — INVARIANT: `reportPhaseIncomplete`'s `statusUnfinished === 0` fall-through, NOT the completion oracle, is what stamps an all-Done bundle whose ticket exited `done_without_commit_evidence`. The oracle CANNOT do it: `resolveUnfinishedTickets` filters `status !== 'done'` FIRST, so a Done-but-unevidenced ticket is invisible to oracle exclusion, and `collectPicklePhaseProgress.pendingCount` is pure status with no oracle. BREAKS: gating the stamp on the oracle, or widening the `statusUnfinished > 0` early return, lets mux-runner's "already marked Done by model" exit reach `graduating` and report the phase completed successfully. ENFORCE: extension/tests/pipeline-runner-done-without-commit-evidence-fatal.test.js, extension/tests/pipeline-runner-halt-on-incomplete.test.js. PATTERN_SHAPE: `if (statusUnfinished > 0 && unfinished.length === 0)` in `reportPhaseIncomplete`; `.filter(t => (t.status || '').toLowerCase() !== 'done')` as the FIRST filter in `resolveUnfinishedTickets`.
- `pipeline-runner.ts` — INVARIANT: persisted phase transitions MUST append canonical `{step,timestamp}` entries to `state.history`, not just stamp `state.step`. BREAKS: `verify-recapture-fired.js` cannot derive the anatomy window, so a real `baseline_recapture_attempted` event is misreported as `phase-window-missing`. ENFORCE: extension/tests/pipeline-runner-phase-history.test.js. PATTERN_SHAPE: `event:\s*['"]phase_transition['"]` or `s\.step = phaseConfig\.name` without a same-transaction `s\.history` append.
- `pipeline-runner.ts` (W5c gate_skipped sink) — INVARIANT: `executeCitadelPhase`'s skip branch emits `gate_skipped` (source `citadel-mechanical`) via `logActivity` to the activity-dir jsonl, NEVER `sm.update`/`s.activity=[...]` (the state.json sink the W5c skip-flag scanner ignores). BREAKS: the purpose-built `citadel-mechanical::skip_quality_gates` budget reads 0 forever, disarming subtract-before-add governance. ENFORCE: extension/tests/pipeline-runner-citadel-mechanical-skip.test.js. PATTERN_SHAPE: `event: 'gate_skipped'` inside `logActivity(` in `executeCitadelPhase`, not `s.activity = [`.
- `pipeline-runner.ts` (R-PSSS Finding #49 empty-scope skip observability) — INVARIANT: when `resolveAnatomySubsystems` skips because the scope filter excluded every subsystem, it MUST emit `formatEmptyScopeWarn('anatomy-park', ...)` to the log AND a `anatomy_park_empty_scope_skip` activity event; when `setupSzechuanSauce`'s effective scope is non-empty but code-free (`isCodeFreeScope`), `shouldSkipSzechuanForEmptyScope` MUST emit the WARN + `szechuan_sauce_empty_scope_skip` event and skip BEFORE the `init-microverse.js` spawn. An UNSCOPED szechuan run (empty `effectiveAllowedPaths`) MUST still run. Both events carry all 7 registration touchpoints (R-PDD-oneOf). R-PSSS-3: phase setup returns `PhaseSetupResult` (`true` | `{skipReason}`); `runConfiguredPhase` threads `skipReason` and `runPhaseIteration` records it into `counters.phaseSkips` → `pipeline-status.json:phase_skips` (an additive, optional field — omitted when empty). BREAKS: a doc-only / fixture-only branch diff makes anatomy-park / szechuan-sauce a silent no-op the operator can only discover by reading raw logs; collapsing `PhaseSetupResult` back to `boolean` loses the per-phase skip disposition. ENFORCE: extension/tests/anatomy-park-scope.test.js, extension/tests/szechuan-scope.test.js, extension/tests/pipeline-empty-scope-skip-schema-conformance.test.js. PATTERN_SHAPE: `formatEmptyScopeWarn|anatomy_park_empty_scope_skip|szechuan_sauce_empty_scope_skip|PhaseSetupResult`.
- `pipeline-runner.ts` (R-OMTD orphan-mux subtree reap) — INVARIANT: `spawnRunner` spawns mux-runner children `detached: isMuxRunnerInvocation(args)` so they LEAD their own process group, and tracks that fact in the module-local `activeChildLeadsGroup` (reset to `false` on child `close`/`error`). Both teardown paths — `installShutdownHandlers().handleShutdown` and the `armChildMuxRunnerHeartbeat` no-progress kill — MUST route through `reapChildSubtree(child, leadsGroup, signal)`, which sends the signal to the WHOLE group via the shared negative-PID primitive `killProcessGroup` (`services/orphan-reaper.ts`, AC-CXHANG-3) when `leadsGroup`, falling back to a direct `child.kill()` for non-detached children or when the group signal fails. BREAKS: a bare `child.kill()` on the mux-runner PID leaves the grandchild worker subtree to re-parent to PID 1 and outlive the pipeline (overnight budget burn); spawning the mux child WITHOUT `detached:true` makes the negative-PID group reap a silent no-op. ENFORCE: extension/tests/pipeline-runner-orphan-mux-teardown.test.js, extension/tests/single-group-kill-implementation.test.js. PATTERN_SHAPE: `detached: leadsGroup` in `spawnRunner`; `reapChildSubtree(` called in BOTH `handleShutdown` and `armChildMuxRunnerHeartbeat`; `killProcessGroup(child.pid` inside `reapChildSubtree`.
- `pipeline-runner.ts` (dead watcher-banner gate) — INVARIANT: no gate may test file content against `◤ FEED TERMINATED ◢`; watcher panes emit it to stdout only (`wrapWithStderrRedirect` captures `2>>` stderr), so no log can contain it. BREAKS: `pass = (matches.length === 0)` is unfalsifiable — AC-DR-05 shipped a green "watcher liveness verified" bundle checkmark that could never fail. Not repairable by teeing the banner: R-MWR-6 reserves it for NORMAL session end, so a working version false-REDs every clean session. Delete such a gate. ENFORCE: extension/tests/watcher-banner-is-pane-only.test.js. PATTERN_SHAPE: `.includes(<banner literal or const alias>)` on file content anywhere under `extension/src/**` or `bin/**`.

- `.claude/commands/anatomy-park.md`; `.claude/commands/szechuan-sauce.md` (AP-EXT-ITER10-01 microverse prompts have no EXECUTION CONTEXT) — INVARIANT: a microverse command prompt MUST NOT key behaviour off `TICKET_ID`, `TICKET_DIR`, or `FIREWALL_DETECTED`. `spawn-morty.ts:1100-1102` is the sole emitter of that block, for pickle-phase workers only; `microverse-runner.ts` emits none (0 occurrences in a live iteration log). BREAKS: `--ticket-id "$TICKET_ID"` interpolates a phantom, so `worker_edit_outside_scope` ships with NO `ticket_id` (42/110 real events on 2026-07-23; the raw activity sink reads ~3.5k but 97% of that is `check-scope-diff` test-fixture leakage — filter `gate_payload.scope_json_path` for tmp dirs before citing any ratio off it) and `status.ts:renderScopeDrift` — which keeps only `collectTickets` ids — drops it; the sibling R-SSOC producer keys the same event by SUBSYSTEM name and is dropped too. Both prompts claimed `/pickle-status` would surface the drift; it never could. `FIREWALL_DETECTED` gates a Session-Knowledge-Transfer skip that can never fire (benign — the section's own inaccessibility fallback covers it). ENFORCE: extension/tests/skill-prompt-shape/fom-infusion-prompts.test.js (`scope-drift claim:` block — 5/5 mutation-RED, incl. widening the consumer gate). PATTERN_SHAPE: `(?:value of|from|appears in)\s+(?:the |your )?`?\$?(TICKET_ID|EXECUTION CONTEXT)` or `If `?FIREWALL_DETECTED=true`? appears` in a microverse-driven prompt (`anatomy-park.md`, `szechuan-sauce.md`, `plumbus.md`, `pickle-microverse.md`); pickle-phase surfaces (`send-to-morty*.md`, `morty-{implementer,reviewer}.md`, `_pickle-manager-prompt.md`) legitimately carry these and are NOT matches. OPEN GAP: the consumer half is unfixed — `status.ts:renderScopeDrift` still intersects with `collectTickets`, so microverse-phase scope drift has NO operator surface; `status.ts` sits outside this session's `scope.json` fence. The ENFORCE test pins the gate so widening it re-reddens and forces the prompts back in step.

## Heal-via-edit-then-resume

Operator workflow to skip or re-classify a ticket mid-session without abandoning the whole epic:

1. Stop the pipeline (Ctrl-C or tmux detach).
2. Edit the ticket's frontmatter: set `status: "Skipped"` (or `"Done"`), add `skipped_reason: "<reason>"` and `completion_commit: "<sha>"`.
3. Run `setup.js --resume <SESSION_ROOT>` — WITHOUT `--force-ticket-status-sync`. The resume honors your edit, emits `setup_resume_ticket_status_preserved`, and continues to the next ticket.

If you want the legacy behavior (auto-correct frontmatter to match `state.current_ticket`), add `--force-ticket-status-sync`. This emits `setup_resume_overrode_ticket_status` and is useful after an unexpected desync where `state.current_ticket` is authoritative.

- `setup.ts` (resume orphan-reattach STRICT descent) — INVARIANT: `shaDescendsFromHead` MUST require strict descent — HEAD is an ancestor of the stamped sha AND the sha is not HEAD — before `tryResumeOrphanReattach` recovers. Compare through git (a stamp may be short, HEAD full), never as strings. BREAKS: git counts a commit as its own ancestor and `merge --ff-only HEAD` exits 0 ("Already up to date"), so an ancestor-OR-EQUAL probe reads a pure no-op back as a successful reattach and resurrects a Failed ticket to Done. ENFORCE: extension/tests/setup.test.js (`completion_commit equal to HEAD is NOT an orphan`, short-sha variant, `REAL orphaned commit is still ff-reattached`). PATTERN_SHAPE: `merge-base', '--is-ancestor'` (or `isHeadAtOrBelowCommit`) used as an orphan/regression discriminator with no reverse probe and no equality reject.
- `setup.ts` (resume-reattach Done flip is R-CWGE fail-closed, R-WDTF-TO WS-2/WS-3) — INVARIANT: the `status: 'Done'` write in `tryResumeOrphanReattach` — one of only two raw terminal-Done writes in `src/` — MUST be gated on THREE sequential guards, checked in this order: (1) R-WDTF-TO WS-2 — `resolveWorkerGateVerdict(...).computedVia !== 'between_ticket_gate'` (a verdict RECOMPUTED by `recomputeAbsentWorkerGateVerdict` — eslint+tsc only, R-WGFR — is not proof the tier's full gate ran and must not authorize Done, even when green); (2) R-CWGE — `.verdict === 'green'` (imported from `mux-runner.ts`, the SAME authority every mux Done-flip path consults; never a second bespoke check), with the ADVISORY carve-out decided by the shared exported predicate `isAdvisoryWorkerGateVerdict(gate.verdict, workingDir)` — NEVER a bespoke verdict literal. TWO verdicts are advisory and BOTH reach this site: `not_run` (no `extension/` in the target repo, the gate could not run at all) and a `red` authored by a TARGET repo's own toolchain (reachable only since B-OFFREPO AC-OFFREPO-2a made the off-repo gate actually RUN and persist its result). Both take the gate-exempt route (flip proceeds, `emitAdvisoryWorkerGateResidual` records it — an advisory `red` files as `worker_gate_target_repo_red`/`computed_via: target_repo_gate`, NOT as `not_run`, because a red that ran and failed is a different fact), exactly as `guardCompletionCommitBeforeDone` routes them. `absent` stays fail-closed, and so does pickle-rick's OWN `red` — the predicate returns false when `extension/` exists. The advisory branch early-returns, so guard (3) is skipped on it: behaviour-neutral, since an off-repo `not_run` persists `worker_gate_tests_verdict: not_run`, which `readTicketWorkerGateTestsVerdict` already reads as `null`; (3) R-WDTF-TO WS-3 — `readTicketWorkerGateTestsVerdict(...) !== 'red'` (a PERSISTED green verdict, `computedVia === 'worker_gate'`, still only proves eslint+tsc per R-WGFR — the sibling `worker_gate_tests_verdict` field records whether THIS turn's own test run genuinely failed, and a clean lint/tsc reading must not resurrect a real test failure to Done). The ff-reattach itself stays unconditional across all three: preserve the commit, gate only the flip. BREAKS: narrowing guard (2)'s carve-out back to a bespoke `gate.verdict === 'not_run'` literal re-opens the live asymmetry this entry's own prose caused — `guardCompletionCommitBeforeDone` treats a TARGET-repo `red` as advisory and flips Done, while this site refuses it, so ONE pre-existing failing test in a target repo's suite strands every ticket whose work is already committed and ff-reattached (the R-AICF accept-here-refuse-there class; mutation-verified RED at `actual: 'Failed', expected: 'Done'`). Widening it to skip the check entirely, or dropping the `workingDir` argument, lets pickle-rick's OWN red authorize Done. Widening guard (2) back to a bare `!== 'green'` refusal wedges every off-repo session — the gate returns `not_run`, the flip is refused, and the ticket stays re-selectable with its commit already reattached (this is how B-OFFREPO reddened `rrh-resume-reattach.test.js` and `setup.test.js`); dropping guard (2), a worker whose gate went RED — Failed with its `completion_commit` still stamped — is resurrected to terminal Done on the next `--resume`, over code the gate rejected; dropping guard (1) lets an eslint/tsc-only recompute (test:fast never ran) authorize Done; dropping guard (3) lets a clean-lint/tsc persisted verdict authorize Done over a genuine same-turn test failure. In every case `isPendingMuxTicket` never re-selects the ticket and the epic finalizes on it. AC-D4 allowlists `setup.ts` by LOCATION only, so it cannot see this. ENFORCE: extension/tests/setup.test.js (`a REAL orphan whose worker gate went RED is reattached but NOT flipped Done` — its fixture MUST `mkdirSync(<repo>/extension)` or the predicate reads that red as a TARGET-repo red and the pin greens over its own bug; `a TARGET-repo RED is advisory — reattached, flipped Done, residual recorded`), extension/tests/resume-reattach-recomputed-verdict.test.js (`AC-WDTFTO-1-4` recomputed-verdict + `worker_gate_tests_verdict: red` cases). PATTERN_SHAPE: a `status: 'Done'` frontmatter write in `setup.ts` not dominated by ALL THREE of an `isAdvisoryWorkerGateVerdict(gate.verdict, workingDir)` carve-out over a `resolveWorkerGateVerdict(` green check, a `computedVia === 'between_ticket_gate'` refusal, and a `readTicketWorkerGateTestsVerdict(` `'red'` refusal; or ANY verdict-literal comparison (`=== 'not_run'`, `=== 'red'`) standing in for that predicate at a Done-flip authority — the two authorities (`guardCompletionCommitBeforeDone`, `resumeReattachDoneRefusal`) MUST branch on the one shared predicate, and `grep -c "isAdvisoryWorkerGateVerdict(" extension/src/bin/{mux-runner,setup}.ts` is 2 and 1 respectively (definition aside).
- `setup.ts` (R-SRTS-1 resume status gating) — INVARIANT: `reconcileTicketStateDesyncOnResume` MUST NOT call `writeTicketStatus(winner, 'In Progress')` unless `forceSync === true`. When `forceSync === false` and the winner ticket's frontmatter disagrees with `state.current_ticket` status, the operator edit is preserved and `setup_resume_ticket_status_preserved` is emitted. BREAKS: reverting the flag gate means `setup --resume` silently overwrites operator-edited ticket frontmatter (R-PPPG ship incident). ENFORCE: `extension/tests/setup-resume-ticket-status-preserved.test.js`. PATTERN_SHAPE: `forceSync` guard before `writeTicketStatus(sessionDir, winner, 'In Progress')` in `reconcileTicketStateDesyncOnResume`.

- `mux-runner.ts` (R-PIWG-1 HEAD pin) — INVARIANT: every iteration in `runMuxRunnerMain` re-checks `state.pinned_branch`/`state.pinned_sha` via `checkHeadPinMismatch` before ticket selection; an external branch switch sets `exit_reason='working_tree_modified_externally'`, emits `head_mismatch_detected`, and calls `safeDeactivate`. Pipeline-internal commits on a named branch do NOT trigger this (branch name unchanged). BREAKS: removing the pre-ticket check allows a parallel `git checkout` to silently shift the branch under a live pipeline. ENFORCE: extension/tests/integration/head-pin-mismatch-detection.test.js. PATTERN_SHAPE: `checkHeadPinMismatch\(` before ticket selection in `runMuxRunnerMain`.
- `mux-runner.ts` (B-RRH C2 producer wiring — ONE shutdown handler, and it is the registered one) — INVARIANT: `installShutdownSignalHandlers` is the SOLE shutdown handler in the file: it owns the only `SIGTERM`/`SIGINT`/`SIGHUP` registrations (one each), it stamps the B-RRH C2 sentinel via the file's only `writePickleIncompleteSentinelIfRemaining(sessionDir, statePath, log)` callsite BEFORE `safeDeactivate`, and `runMuxRunnerMain` reaches the teardown ONLY by calling it — passing its session-scoped handles (phantom-Done watchers, codegraph) in as `releaseSessionResources`, never by re-inlining a handler. BREAKS: this shipped BROKEN for the life of the gate. The handler was duplicated — an exported `setupSignalHandlers` that stamped the sentinel but which NOTHING ever called, and an inline twin inside `runMuxRunnerMain` that was registered but omitted the stamp. `pickle_incomplete.json` was therefore never written in production, so pipeline-runner's C1 robustness gate (`maybeStampPickleIncompleteRobust`) read a file no producer created and its `sentinelPresent` branch was permanently unreachable: an externally-SIGTERM-killed mux exits 0 and was read as a clean completion (the B-XSPA bug C1 exists to catch). Re-inlining the handler at the call site re-opens the divergence, because the two copies then drift exactly as they did before. ENFORCE: extension/tests/rrh-pickle-incomplete.test.js (`B-RRH C2: the REGISTERED shutdown handler stamps the sentinel when a ticket remains` — drives the REAL returned handler and asserts both registration and the written sentinel; mutation-verified RED when the stamp is removed, while the five sibling C1 tests stay GREEN because they hand-write the sentinel and are blind to the producer by construction). PATTERN_SHAPE: in `src/bin/mux-runner.ts`, `grep -c "process.on('SIGTERM'"` == 1 (same for `SIGINT`/`SIGHUP`) and that registration is inside `installShutdownSignalHandlers`; `writePickleIncompleteSentinelIfRemaining(sessionDir, statePath, log)` appears exactly once as a call, in that function, above its `safeDeactivate(statePath)`; `setupSignalHandlers` absent.
- `mux-runner.ts` (b1089e97 codegraph summary counts) — INVARIANT: `codegraph_session_summary` `injected`/`skipped`/`degraded_ops` MUST derive from persisted `state.activity` via `countCodegraphContextEvents(...)` / `countCodegraphDegradedEvents(...)`, NEVER `cgService.getSessionCounters()`; those counters are incremented only inside `buildCodegraphContextSection` in the per-spawn spawn-morty PROCESS and read 0 in mux-runner. `index_status` is the DELIBERATE exception — it stays on the in-memory counters as this process's service-health enum. `createCodegraphSession` is EXPORTED with an optional `deps` seam (`resolveSettings`/`createService`, both defaulting to production) so a test can drive the real `emitSummary`; do NOT re-privatize it. BREAKS: every summary reports `injected:0`/`skipped:0` regardless of real injections. Until 2026-08-05 this PATTERN_SHAPE was the only thing over the emit site and the helpers it names are PURE — mutation-verified: reverting the emit site to `ctrs.*` left all 8 pre-existing tests GREEN and reddened only the behavioral pin. A grep passes on unreachable code and fails on a reformat. ENFORCE: extension/tests/codegraph-session-summary-counts.test.js (`createCodegraphSession.emitSummary (b1089e97 wiring)` — drives the REAL `emitSummary` with persisted activity and in-memory counters set to DIFFERENT values, so each field can only pass by reading the correct source). PATTERN_SHAPE: `countCodegraphContextEvents(` + `countCodegraphDegradedEvents(` feed the `injected`/`skipped`/`degraded_ops` fields of the `codegraph_session_summary` `writeActivityEntry`; `ctrs.injected`/`ctrs.skipped`/`ctrs.degraded` absent from that payload; `export function createCodegraphSession(` present.
- `spawn-morty.ts` (b1089e97 injection truthfulness — TWO branches, 2e632f9a) — INVARIANT: two distinct empty-content guards exist, each with its own skip reason and neither may be collapsed into the other. (1) Node-level staleness: `buildCodegraphEntries` runs `isNodeLocationFresh(node, workingDir)` fs verification BEFORE `nodeLocation` renders (upstream of any render step); when `ranked.length > 0` but zero located nodes survive (every node's cited file/line is stale, or none existed), `buildCodegraphContextSection` MUST `emitSkipped('stale_refs', droppedStale)` and return '' — Location-less `Summary:` lines are discarded too (they never render standalone) since a Summary-only "injection" with no located evidence is not productive. This is gated on `workingDir` being provided (absent/empty → no staleness filtering, legacy behavior for callers/tests that don't wire a real working tree). (2) Render-empty (UNCHANGED PATTERN_SHAPE): `recordContextInjected()` + the `codegraph_context_injected` emit fire ONLY when `renderCodegraphSection(...)` returns a non-empty section; an empty render (no entry fits `context_max_bytes`, checked AFTER the node-level filter above) MUST `emitSkipped('zero_hits')` and return ''. BREAKS: a phantom `codegraph_context_injected` with `bytes:0` inflates the codegraph efficacy metric the default-on decision rides on; conflating `stale_refs` into `zero_hits` (or vice versa) hides whether skip pressure comes from index staleness (actionable — lower `staleness_max_age_minutes` or reindex) or from a repo with no matching symbols. ENFORCE: extension/tests/codegraph-context-section.test.js, extension/tests/codegraph-context-events-schema-conformance.test.js. (3) Render ORDERING is what makes guards (1) and (2) honest AFTER the render: `buildCodegraphEntries` MUST emit located entries FIRST and `Summary:` prose LAST, because `renderCodegraphSection` enforces `context_max_bytes` by popping TRAILING entries. Both guards run PRE-render and count entries BUILT, not entries that SURVIVE the cap — so a leading Summary starves out every symbol under a tight cap and still returns a non-empty section, slipping past both and reaching `recordContextInjected()` with `hits_count: <all ranked>` over ZERO rendered symbols. Ordering, not a third guard, is the fix: with payload leading, a non-empty section always carries >= 1 located entry. BREAKS: reversing the ordering re-opens the phantom Summary-only injection that inflates the codegraph efficacy metric the default-on decision rides on — and does it invisibly, since the two existing guards still "pass". ENFORCE: extension/tests/codegraph-context-section.test.js (`cap: long Summary must not starve out every symbol entry`). PATTERN_SHAPE: `if (section.length === 0) { emitSkipped(` between `renderCodegraphSection(` and `recordContextInjected(` in `buildCodegraphContextSection` (branch 2, untouched); `if (built.locatedSurvivors === 0) { emitSkipped('stale_refs'` immediately after the `buildCodegraphEntries(` call (branch 1, upstream of branch 2); in `buildCodegraphEntries`, `const entries: string[] = [...locatedEntries]` precedes the `Summary: ` push loop (branch 3 — payload leads, prose trails).
- `spawn-morty.ts` (B-1SEAM WS1b R-AICF sanctioned runner-authored amend + claimed-sha verification) — INVARIANT: `reconcileWorkerCommitAttribution` runs in `finalizeWorkerTurn` BEFORE `persistWorkerOutcomeStatus` and is the ONLY sanctioned `git commit --amend` in the runtime; the amend fires only when ALL THREE preconditions hold: (1) the verified commit carries no PARSED `Pickle-Ticket` trailer (git's `%(trailers:key=Pickle-Ticket,valueonly)` view, key-presence not value-equality — see the AP-EXT-ITER4-01/02 entry below), (2) the verified commit IS the single-commit-window unpushed tip (windowSize === 1 AND HEAD === verifiedSha, re-verified via a second `rev-parse HEAD` immediately before amending — parallel-session race guard), (3) the index is clean (`git diff --cached --quiet`). A worker-claimed completion sha (frontmatter stamp / COMPLETION_COMMIT_RECORDED ack / gate autofix sha) that is hallucinated, unreachable, or outside preWorkerHead..HEAD is DISCARDED and replaced by the newest in-window commit touching the worker's changed files, normalized to full 40-char; `persistWorkerOutcomeStatus` consumes verifiedSha ?? getHeadSha. Every git spawn uses `execFileSync` with finite `RECONCILE_GIT_TIMEOUT_MS` (5000 ms). spawn-morty's own execFileSync git is NOT PreToolUse-hook-mediated, so R-WSRC-GR's commit--amend block does not apply to this runner-authored callsite. BREAKS: dropping verification re-opens the R-AICF hallucinated-full-sha class (live incident session 2026-07-01-9e922602 ticket c46045a6: done-guard accepted / phantom watcher reverted / Done-flip guard fataled on the same fake stamp); amending without the three preconditions sweeps staged foreign work into the commit or rewrites multi-commit/pushed history. ENFORCE: extension/tests/spawn-morty-commit-attribution.test.js. PATTERN_SHAPE: `reconcileWorkerCommitAttribution(` called in `finalizeWorkerTurn` before `persistWorkerOutcomeStatus(`; `windowSize !== 1` skip + `git diff --cached --quiet` probe + double `rev-parse HEAD` re-verify inside `maybeAmendTicketTrailer`; `Pickle-Ticket: ` trailer literal; `timeout: RECONCILE_GIT_TIMEOUT_MS` on every reconcile git spawn.

- `spawn-morty.ts` (AP-EXT-ITER4-01/02 the trailer producer shares the consumer's oracle, both directions — LANDED) — INVARIANT: `maybeAmendTicketTrailer` decides "already attributed?" from git's PARSED trailer view (`readParsedTicketTrailers`, `%(trailers:key=Pickle-Ticket,valueonly)`, key-presence not value-equality — a DIFFERENT-id trailer also suppresses the amend, AC-LAND-10), NEVER a word-boundary regex over `%B`; it WRITES via `buildTrailerAmendedMessage` (`git interpret-trailers --if-exists addIfDifferentNeighbor` piped to `commit --amend -F -`), falling back to the legacy `-m message -m trailer` append ONLY when interpret-trailers cannot run (degraded: keeps attribution, accepts trailer-demotion risk on that path). git parses trailers from the LAST paragraph only, so a raw-text guard/writer fails in OPPOSITE directions: a `%B` word-boundary guard reads a PROSE mention (`fix(<id>):`, `(ticket <id>)`) as attribution and skips the stamp, leaving the trailer scan — the only git-log arm after B-GITATTR WS-3 — with nothing; an unconditional two-`-m` amend opens a NEW paragraph that demotes every pre-existing trailer (`Co-Authored-By`, `Signed-off-by`, `Resolves`) to body prose. This is the sibling of the `git-trailer-hooks.ts` AP-EXT-ITER2-01 guard-oracle entry (hook side collapsed onto `interpret-trailers --parse` in iteration 2); this producer now shares the same oracle on both the guard and the writer. BREAKS: reverting the guard to raw-`%B` reopens the historic incident — 7 consecutive commits on `release/v2.1-beta` carried `(ticket 6b7c3b82)` in prose with ZERO parsed trailer, so evidence read `absent` and the Done-flip refused `done_without_commit_evidence` over a commit whose ticket id was visible in `%B` (mutation-verified RED, ticket ce0a4f46); reverting the writer to the unconditional two-`-m` append demotes pre-existing trailers to prose (also mutation-verified RED). ENFORCE: extension/tests/spawn-morty-commit-attribution.test.js (`a PROSE-only ticket-id mention is not attribution — the tip IS amended`, `a DIFFERENT-id Pickle-Ticket trailer is left alone — exactly ONE parsed value survives`, `pre-existing trailers survive the amend instead of being demoted to prose`). PATTERN_SHAPE: `readParsedTicketTrailers(workingDir, verifiedSha)` feeding the already-attributed guard in `maybeAmendTicketTrailer` (no `new RegExp(...).test(message)` word-boundary guard reachable on the guard path); `buildTrailerAmendedMessage(` invoked before `commit --amend -F -`, with the unconditional `-m message -m trailer` two-arg amend reachable ONLY inside the `amended === null` interpret-trailers-unavailable fallback branch.

## Resume-time ticket runnability contract

Precedence: ticket frontmatter `status:` is authoritative for runnable/skipped decisions. The runner reads it fresh per iteration via `getTicketStatus(sessionDir, ticketId)` (`extension/src/services/pickle-utils.ts`) inside `isPendingMuxTicket` (`extension/src/bin/mux-runner.ts:471-480`). All other sources are advisory:
1. ticket frontmatter `status:` — AUTHORITATIVE; values `Todo` / `In Progress` are runnable, `Done` / `Skipped` are not (`isPendingMuxTicket` short-circuits via `normalizeTicketStatus`)
2. `state.current_ticket` — single-active-ticket lock for the in-flight iteration; cleared between tickets, does NOT gate runnability of other tickets
3. `pipeline.json.completed_phases` — phase-advance ledger, not a per-ticket gate
4. `refinement_manifest.json[].status` — informational metadata only; NOT consulted at runtime for runnability (the runner reads ticket frontmatter, not the manifest)

There is no `state.failed_tickets` / `state.blocked_tickets` / `state.skipped_tickets` field in this codebase — a parallel runnability set would be a regression. R-RMBS-3 emits `ticket_runnability_resolved` per iteration to surface the runner's decision for observability without changing the underlying mechanism.

Heal flow recipe: To make a previously-failed or skipped ticket runnable again:
1. Stop the pipeline (Ctrl-C or `eat-pickle`).
2. Edit the ticket's frontmatter file at `<SESSION_ROOT>/<hash>/rick_ticket_<hash>.md`: set `status: Todo`.
3. (Optional Finding #36 workaround if `state.current_ticket` points at this same ticket): set `state.current_ticket = null` BEFORE running setup.js — only needed when running an older deployed setup.js that lacks R-SRTS-1.
4. Re-run `setup.js --resume <SESSION_ROOT>`. The runner will pick up the ticket via `findNextPendingTicketId` (frontmatter read). Do NOT edit `refinement_manifest.json` for this purpose — the runner does not consult it for runnability.

Naming convention: internal worker artifacts live on disk as `rick_ticket_<hash>.md` files; do not confuse them with real Linear ticket identifiers.

- `mux-runner.ts` (R-RMBS-1/R-RMBS-3 frontmatter authority) — INVARIANT: `isPendingMuxTicket` is the canonical pendingness check; it MUST read ticket frontmatter via `getTicketStatus(sessionDir, ticketId)` and return `false` only for `done`/`skipped` statuses. NO field in `state.json` may parallel this decision — `state.failed_tickets`, `state.blocked_tickets`, `state.skipped_tickets` are FORBIDDEN field names that would re-introduce the heal-flow papercut (Master Plan Finding #41). Per-iteration `ticket_runnability_resolved` event emits `{ ticket_id, frontmatter_status, runnable, reason }` for observability. BREAKS: reintroducing a parallel set in State means an operator who edits ticket frontmatter back from Skipped to Todo cannot make the ticket runnable again without also clearing the parallel set, defeating the heal flow recipe. ENFORCE: extension/tests/runnability-contract-doc-shape.test.js, extension/tests/integration/runnability-frontmatter-authoritative.test.js. PATTERN_SHAPE: `state\.(failed|blocked|skipped)_tickets` MUST NOT appear in `extension/src/bin/mux-runner.ts` or `extension/src/types/index.ts`.

- `mux-runner.ts` (R-CXOR-2 baseline-rejection activation) — INVARIANT: every completion decision site MUST feed `readEvidence`'s `isBaselineSha` rejection with the session baselines so a `completion_commit === start_commit`/`pinned_sha` stamp classifies `absent` (`baseline_sha`). B-1SEAM WS-1 made the wiring compile-time-required: `CompletionDecisionCtx` declares `startCommit`/`pinnedSha` as REQUIRED (nullable but explicit) fields, so no `evaluateCompletionEvidence` caller can silently omit them; in `mux-runner.ts` they are wired centrally by `buildCompletionCtx` via `resolveSessionBaselineShas(args.sessionDir)`, and `auto-fill-completion-commit.ts` pulls `start_commit`/`pinned_sha` from its recoverable `state.json` read. (The rejection was added to `readEvidence` in 1cbe4078 but originally reached only the Done-flip guard; the former "sibling callers still omit" replay is CLOSED by this collapse.) BREAKS: reverting the ctx fields to optional re-opens the codex orphan-reset false-Done class (ticket Done with `completion_commit` = session baseline) at whichever site forgets the wiring. ENFORCE: extension/tests/guard-completion-commit-baseline-rejection.test.js, extension/tests/completion-predicate-single-seam.test.js. PATTERN_SHAPE: `resolveSessionBaselineShas(args.sessionDir)` inside `buildCompletionCtx` feeding REQUIRED `startCommit`/`pinnedSha` on `CompletionDecisionCtx`.
- `mux-runner.ts` (R-RIC-EXPLICIT-4 phantom-revert reachability) — INVARIANT: BOTH phantom-Done watchers — the batch loop (`batchLoopPhantomDoneKind`) AND the single-file watcher (`applyInspectPhantomDoneDecision`, path-7) — MUST decide keep-vs-revert via `gateForPhantomDoneRevert` over a `buildCompletionCtx(..., 'phantom-watch')` ctx (predicate-backed since B-1SEAM WS-1), NOT a bespoke `source !== 'absent'` check or a bare field-presence keep. The path-7 watcher's old field-presence-without-git-probe keep is GONE: a stamped `completion_commit` is git-probed like every other site, so a hallucinated/unreachable stamp is REVERTED unless the R-AICF scan/inferred fallback attributes real work (the live R-AICF accept-here-revert-there split died with it). The batch loop returns `'explicit-reachable'` on keep, `'absent'` on revert, and `'inferred'` ONLY on the persist-failure edge where its caller-side `promoteInferredToExplicit` writes the explicit field the predicate's promote-once failed to persist; a lingering `completion_commit_inferred` line after predicate-side promotion is inert (explicit-wins). BREAKS: collapsing either gate back to a lenient presence check lets a ticket with a stamped-but-unreachable `completion_commit` (typo, foreign repo, dropped or hallucinated sha) keep status Done forever, defeating phantom detection (R-CCR-1 regression caught at v1.80.3 release gate; R-AICF live incident session 2026-07-01-9e922602). ENFORCE: extension/scripts/audit-phantom-done-call-sites.sh, extension/tests/mux-runner.test.js (R-CCR-1 block), extension/tests/characterization/completion-commit-cluster/path-7-phantom-done-watcher-backfill.test.js. PATTERN_SHAPE: `gateForPhantomDoneRevert(` in `batchLoopPhantomDoneKind` returning `'inferred'` / `'explicit-reachable'` / `'absent'`; `gateForPhantomDoneRevert(` before `writeTicketStatus(` in `applyInspectPhantomDoneDecision`.
- `check-readiness.ts` (R-RTPS-1 tilde-path skip) — INVARIANT: `extractContractReferences` MUST skip backticked tokens that begin with `~/`, `$HOME/`, or `${HOME}/` before either PATH_RE or symbol-shape extraction runs; these are runtime deploy paths (verified by R-CLOSER-3 via `bash install.sh` parity check) and have no source-tree counterpart. BREAKS: ticket text citing runtime deploy paths produces spurious `file_path` findings (live incident 2026-05-10 session `2026-05-10-84ad0873`, 7 false positives across tickets `010f5c8b` and `4dcf9b43`). ENFORCE: extension/tests/check-readiness-tilde-paths.test.js. PATTERN_SHAPE: `~/|\$HOME/|\$\{HOME\}/` skip predicate inside `extractContractReferences` backtick loop.

## R-CCPM-1 codex manager payload scrub

INVARIANT: every codex-manager-subprocess detection path that fires `codex_manager_self_bootstrap_attempted` MUST stamp `ts: new Date().toISOString()` explicitly before calling `writeActivityEntry` (or `logActivity`), because `writeActivityEntry` does NOT auto-stamp `ts`. The `action_taken` field MUST be the literal string `'logged'` (not `'blocked'` or `'aborted'`) — it records what the guard DID, not what it prevented. The `ticket` field is nullable: pass `state.current_ticket ?? null`, never omit the key. The `attempted_argv` field MUST be the actual `process.argv` slice passed to the subprocess, trimmed of the node binary but preserving all flags — never a reconstructed or abbreviated form.

PATTERN_SHAPE: `event: 'codex_manager_self_bootstrap_attempted'` in any emitter MUST be accompanied by `ts: new Date().toISOString()`, `action_taken: 'logged'`, and `ticket: state.current_ticket ?? null`.

BREAKS: omitting `ts` makes the payload schema-non-conformant (same class as R-WSE-2 `worker_partial_lifecycle_exit` iter-9 disconnect). Using `action_taken: 'blocked'` fails the `const: 'logged'` schema constraint. Omitting `ticket` violates the `required` array and fails `activity-event-payload.test.js` drop-field tests.

ENFORCE: `extension/tests/codex-manager-self-bootstrap-attempted-schema-conformance.test.js`, `extension/tests/activity-event-payload.test.js`.

## Probe vs Measurement Timeout Distinction (R-MJCP-8)

`probeJudgeCliAvailability` is a fail-fast existence check (≥5s default via `PICKLE_JUDGE_PROBE_TIMEOUT_MS`, max 60s). Only `kind: 'missing'` (ENOENT-class) short-circuits to `judge_cli_missing`. `kind: 'timeout'` or `kind: 'failed'` fall through to the measurement loop so a slow cold-start does not kill the convergence run.

`measureLlmMetricAttempt` is the correctness-check with 4-attempt retry-with-backoff (10s/30s/60s between attempts). ENOENT here also produces `failureKind: 'cli_missing'`; ETIMEDOUT produces `failureKind: 'timeout'`. The shared `classifyJudgeError` helper is used in both paths — no duplicate ENOENT/ETIMEDOUT branches.

## Manager-turn freshness contract (R-SJLAG)

The session state file's mtime advances at least once per artifact-write during a manager turn. When `mux-runner.ts` detects that artifact files (`research_*.md`, `plan_*.md`, `conformance_*.md`) under the current ticket dir gained a newer mtime, it calls `fs.utimesSync` on the state file (mtime bump only — no content write) and emits a `manager_turn_progress` activity event.

Operator signal: a state file that is >N min stale with NO recent `manager_turn_progress` activity event indicates a genuine wedge, not a slow-but-working turn.

## Module Export Catalog

Machine-maintained export index for the `bin/` subsystem. The audit script treats these names as the minimum subsystem surface that this document covers.

- `archaeology.ts` -> `ArchaeologyArgs`, `ArchaeologyRunOptions`, `ArchaeologyRunResult`, `buildArchaeologyPrompt`, `normalizeProjectContext`, `parseArgs`, `runArchaeology`
- `audit-ticket-bundle.ts` -> `auditSession`, `checkMissingAuditComment`, `checkPathDrift`, `detectCrossDocNamingDrift`
- `audit-worker-backends.ts` -> `scanSession`
- `auto-fill-completion-commit.ts` -> `AutoFillCompletionCommitInput`, `AutoFillCompletionCommitResult`, `autoFillCompletionCommit`
- `calibrate.ts` -> `main`, `parseArgs`, `runCalibrate`
- `cancel.ts` -> `cancelSession`
- `check-flake-budget.ts` -> `checkFlakeBudgetMain`
- `check-gate.ts` -> `CheckGateMainOpts`, `checkGateMain`
- `check-readiness.ts` -> `ReadinessArgs`, `ReadinessFinding`, `countUnresolvedReferences`, `extractAcceptanceCriteria`, `extractContractReferences`, `findReadinessFindings`, `isMachineCheckable`, `loadReadinessAllowlist`, `parseArgs`, `runHistory`, `runReadiness`
- `codegraph-efficacy-probe.ts` -> `CorpusTicket`, `PROBE_SPAWN_TIMEOUT_MS`, `ProbeArgs`, `buildEfficacySample`, `captureWorkerDiff`, `consumerFileJaccard`, `diffTouchedFiles`, `hallucinatedRefCount`, `loadCorpus`, `main`, `parseArgs`, `runProbe`
- `check-scope-diff.ts` -> `CheckScopeDiffOpts`, `ScopeDiffResult`, `checkScopeDiff`
- `check-update.ts` -> `BlockedDowngradeError`, `CheckForUpdateOptions`, `PerformUpgradeOptions`, `checkForUpdate`, `compareSemver`, `downloadRelease`, `extractAndInstall`, `getCurrentVersion`, `getLatestRelease`, `isCacheStale`, `parseVersion`, `performUpgrade`, `readCache`, `readSettings`, `writeCache`
- `correct-course.ts` -> `CorrectCourseArgs`, `CorrectCourseRunOptions`, `CorrectCourseRunResult`, `CourseCorrectionProposalValidationInput`, `CourseCorrectionProposalValidationResult`, `buildCorrectCourseBrief`, `main`, `parseArgs`, `runCorrectCourse`, `validateCourseCorrectionProposal`, `validateDiscovery`
- `council-publish.ts` -> `CouncilPublishError`, `PrListParse`, `PublishOptions`, `PublishReport`, `PublishResult`, `composeBody`, `parsePrList`
- `debate.ts` -> `DebateArgs`, `DebateRunOptions`, `DebateRunResult`, `DebateSettings`, `buildDebateBrief`, `main`, `parseArgs`, `runDebate`
- `finalize-gate.ts` -> `FinalizeGateOpts`, `FinalizeGateSettings`, `finalizeGateMain`, `loadFinalizeGateSettings`, `resolveFinalizeSettingsRoot`
- `generate-debate-personas.ts` -> `DEBATE_PERSONAS`, `DebatePersonaDefinition`, `DebatePersonaName`, `debateAgentFilename`, `generatedDebatePersonas`, `renderDebatePersona`, `writeDebatePersonas`
- `get-session.ts` -> `getSessionPath`
- `jar-runner.ts` -> `IntegrityResult`, `RunTaskResult`, `SpawnResult`, `TaskMeta`, `buildJarNotification`, `discoverMarinatingTasks`, `handleTaskEnoent`, `loadJarTaskTimeout`, `skipTaskWithReason`, `validateTaskIntegrity`
- `log-watcher.ts` -> `formatToolUse`, `processLine`
- `metrics.ts` -> `parseMetricsArgs`
- `microverse-runner.ts` -> `ExitOutcome`, `IterationClassification`, `JudgeMeasurementSpawnFailed`, `JudgeMeasurementTimeout`, `MetricSnapshot`, `NoCommitExitClassification`, `PerIterationGateDeps`, `RunContext`, `StallClassifierInput`, `_deps`, `appendGapAnalysisFixedBlock`, `applyTestBackendOverrideFromEnv`, `autoRescueDirtyTree`, `buildEfficiencySection`, `buildFailureDistribution`, `buildJudgePrompt`, `buildMicroverseHandoff`, `classifyJudgeError`, `classifyNoCommitExit`, `classifyStall`, `currentExitForFailureHistory`, `deactivateRunnerState`, `emitJudgeLedgerDiagnostic`, `emitJudgeLegacyShapeDiagnostic`, `emitJudgeParseDiagnostic`, `ensurePerIterationGateBaseline`, `ensureRunnerStateActive`, `executeGapAnalysis`, `executeMainLoop`, `finalizeMicroverseRun`, `extractScore`, `getBestScore`, `handleIterationOutcome`, `handleNoCommitStall`, `handleRateLimit`, `handleWorkerManagedIteration`, `injectRecoveryGuidance`, `loadConvergenceGateSettings`, `loadFailureClassificationFlag`, `loadPassModelOverrides`, `main`, `mapBaselineMeasureExitReason`, `markMicroverseFatalError`, `maybeEmitConsecutiveNoProgressWarning`, `measureAndClassifyIteration`, `measureLlmMetric`, `measureLlmMetricWithBackoff`, `measureMetric`, `parseLlmJudgeOutput`, `preflightAutoCommit`, `probeJudgeBackendAvailability`, `readRunnerState`, `resetGapAnalysisForAmnesiacBreaker`, `resolvePassModelOverride`, `runPerIterationGateHook`, `runRemediatorForIteration`, `writeFinalReport`, `classifyAnatomyNonConvergence`, `maybeHaltAnatomyNonConvergent`, `countComplexityRuleFailures`, `classifyComplexityRegression`, `maybeEmitComplexityRegression`
- `monitor.ts` -> `MONITOR_STDERR_CAP_BYTES`, `MONITOR_STDERR_LOG_NAME`, `MONITOR_STDOUT_WATCHDOG_MS`, `MonitorMode`, `MonitorWriteSink`, `RESPAWN_WATCHDOG_INTERVAL_MS`, `appendMonitorStderrLog`, `buildMonitorStderrHeader`, `buildTicketLines`, `checkAndSwapMode`, `createMonitorStderrCapture`, `formatCurrentField`, `inferModeFromStep`, `isRespawnWatchdogDisabled`, `readPipelineLifecycle`, `renderDashboard`, `renderElapsedField`, `renderMicroverseDashboard`, `renderMicroverseTrend`, `shouldMonitorExit`, `sparkline`, `startRespawnWatchdog`, `summarizeLine`, `writeWithWatchdog`
- `morty-watcher.ts` -> `classifyArtifact`, `discoverArtifacts`
- `mux-runner.ts` -> `ApplyAutoTicketCompletionInput`, `AutoTicketCompletionValidation`, `BetweenTicketGateFailure`, `BetweenTicketGateResult`, `COMMIT_PENDING_HANDOFF_TEXT`, `CapCheckReadDecision`, `CircuitBreakerBudget`, `CommitGatePassingDeliverableInput`, `CommitGatePassingDeliverableReason`, `CommitGatePassingDeliverableResult`, `CommitPendingProbeInput`, `CommitPendingProbeResult`, `CorrectPhantomDoneTicketsInput`, `EpicCompletionDecision`, `EvaluateEpicCompletionInput`, `ExitReason`, `LoopAction`, `LoopContext`, `MuxReadinessGateInput`, `OrphanDetectionResult`, `OrphanedFastTestRunner`, `PhantomDoneInspectResult`, `PhantomDoneKind`, `SparkSmokeGateAction`, `SparkSmokeGateDecision`, `SparkSmokeGateRule`, `TicketAuditGateInput`, `TicketAuditGateResult`, `TicketDesyncResolution`, `TimeoutCounterInput`, `TimeoutCounterState`, `TimeoutHaltContext`, `appendPipelineRunnerMarker`, `applyAllTicketsDoneCompletion`, `applyAutoTicketCompletionValidation`, `applyTicketTierBudget`, `applyTimeoutCounter`, `buildTmuxNotification`, `buildWorkerGateFailureSummary`, `checkFailedAfterResearchApproved`, `checkHeadPinMismatch`, `checkIterationLogForCodexSelfBootstrap`, `checkPartialLifecycleExit`, `classifyCapCheckReadError`, `classifyCompletion`, `classifyGitProbeError`, `classifyIterationExit`, `classifyManagerRelaunchExit`, `classifyTicketCompletion`, `clearStalePerTicketCacheAtIterationStart`, `clearStaleDoneWithoutCommitEvidence`, `clearStaleTicketCacheFields`, `commitGatePassingDeliverableOnExitPath`, `stashUnattributableRemainder`, `commitPendingProbe`, `computeRateLimitAction`, `correctPhantomDoneTickets`, `detectManagerMaxTurnsExit`, `detectMultiRepo`, `detectOrphanSessions`, `detectOutputFormat`, `detectPkgJsonVersionDrift`, `detectRateLimitInLog`, `detectRateLimitInText`, `evaluateCloserTerminalState`, `evaluateEpicCompletion`, `evaluateManagerRelaunch`, `evaluateMuxIdleStallWatchdog`, `evaluateSparkSmokeGate`, `executeTimeoutHalt`, `extractAssistantContent`, `findPendingNonCurrentTickets`, `getCircuitBreakerBudget`, `guardCompletionCommitBeforeDone`, `hasStalePerTicketCacheFields`, `hasSubstantiveManagerHandoff`, `inspectPhantomDoneTicketFile`, `isGenuineCrashOrSpawnFailure`, `isSchemaVersionAheadError`, `isValidPerTicketCapCache`, `killCurrentChild`, `loadRateLimitSettings`, `observeCodexToolCallStream`, `parseBetweenTicketFastGateFailures`, `parseOrphanedFastTestRunnersFromPs`, `processCompletionBranch`, `processIterationOutcome`, `processRateLimitCycle`, `readRunnerState`, `reapOrphanedFastTestRunnersOnStartup`, `recordManagerRelaunch`, `repairZeroWorkerTimeout`, `repopulateNoProgressCapFromFrontmatter`, `resolveIdleStallThresholdSeconds`, `resolveTicketDesyncWinner`, `runBetweenTicketFastGate`, `runBetweenTicketFastTests`, `runIteration`, `runMuxReadinessGate`, `runTicketAuditGate`, `installShutdownSignalHandlers`, `stalePerTicketCacheDiagnostic`, `stripSetupSection`, `truncateTaskNotes`, `validateAutoTicketCompletion`, `validateStartupState`, `writeHandoffAtomic`, `writeRecoveryHandoffArtifact`, `maybeEmitManagerTurnProgress`, `detectAndRecoverHeadRegression`, `FAILED_FLIP_SUPPRESSED_STRATEGY`, `FailedFlipCallsite`, `FailedFlipEvidenceKind`, `FailedFlipSuppressionDecision`, `FailedFlipSuppressionInput`, `evaluateFailedFlipSuppression`, `readActiveFailedFlipHolds`, `countCodegraphContextEvents`, `countCodegraphDegradedEvents`, `EXIT_DRAIN_FALLBACK_ENV_VAR`, `resolveExitDrainFallbackMs`, `recomputeAbsentWorkerGateVerdict`, `APNC_MAX_PASSES_ENV_VAR`, `resolveApncMaxPassesWithoutClean`, `WorkerProducedEverythingButCommitPayload`, `claimWorkerProducedEverythingButCommit`, `advanceOrExitOnLadderExhaustion`, `WorkerProductionBreadcrumb`, `WorkerProductionBreadcrumbInput`, `emitWorkerProductionBreadcrumb`, `createIterationSpawnEnv`, `MainLoopRateLimitParkInput`, `MainLoopRateLimitParkOutcome`, `runMainLoopRateLimitPark`, `CodegraphSessionSettings`, `CodegraphSessionDeps`, `createCodegraphSession`
- `pickle-recover.ts` -> `ParsedRecoverArgs`, `RecoverDeps`, `RecoverSubcommand`, `RecoverTransition`, `RecoverArgError`, `RunRecoverResult`, `parseArgs`, `runRecover`, `selectLowestRunnableTodo`
- `pipeline-runner.ts` -> `BackendSource`, `BundlePreflightError`, `ChildMuxRunnerHeartbeatHandle`, `CloserReleaseActions`, `CloserReleasePlan`, `MainOpts`, `MicroverseHaltDecision`, `PhaseConfig`, `PhaseName`, `PhaseSetupResult`, `PhaseSkipReason`, `SetupArgs`, `SetupScopeArgs`, `SpawnRunnerFn`, `SpawnRunnerResult`, `__setCitadelRemediationDepsForTests`, `__setCloserReleaseActionsForTests`, `__setSpawnRunnerForTests`, `applyEpochResetOnReconstruction`, `applyStrictPhasesOverride`, `armChildMuxRunnerHeartbeat`, `assertCleanWorkingTree`, `assertCodexRequiredBackend`, `buildCloserReleasePlan`, `buildPipelineCompletePanel`, `claimPipelineRunnerActive`, `classifyMicroverseHaltDecision`, `ResumePhasePlan`, `readResumePhasePlan`, `cleanPhaseArtifacts`, `discoverSubsystems`, `enterPicklePhase`, `executeCitadelPhase`, `executeCloserReleasePlan`, `executePhaseRunner`, `installShutdownHandlers`, `isFatalPhaseFailure`, `isTestFile`, `logPhaseContinueReason`, `logPhaseHaltReason`, `main`, `parsePipelineConfig`, `postPhaseCleanup`, `readBundlePrdBackend`, `readCitadelReport`, `recordRecoverablePhaseFailure`, `resetInterruptedTicketWorkForRelaunch`, `resetStateForPhase`, `resolveBackendWithSource`, `runBundlePreflight`, `samplePhaseHistoryTimestamp`, `setupAnatomyPark`, `setupPhase`, `setupScope`, `setupSzechuanSauce`, `shouldHaltAfterPhase`, `writePipelineStatus`, `writeSkippedByScope`, `SCOPE_AUTO_EXTEND_MAX`, `ScopeAutoExtension`, `computeScopeAutoExtension`, `maybeAutoExtendScope`, `scopeByteOrder`
- `prune-activity.ts` -> `pruneActivity`
- `raw-morty.ts` -> `processLineRaw`
- `retry-ticket.ts` -> `retryTicket`
- `setup.ts` -> `CodegraphIndexAtSetupDeps`, `DEFAULT_MANAGER_IDLE_BACKOFF_FALLBACK_MS`, `McpSnapshotFetchFn`, `SetupArgs`, `codexVersionSatisfiesRange`, `countManifestTickets`, `displaySetupSummary`, `evaluateLaunchSizing`, `handleResumeSession`, `initializeNewSession`, `parseArguments`, `precleanPausedOrphansBeforeCreate`, `resolveCodexVersionForSetup`, `resolveManagerIdleBackoffFallbackMs`, `resolvePipelineContinueOnPhaseFailSetting`, `runCodegraphIndexAtSetup`, `runMcpSnapshot`, `runSetupOrphanReap`, `scanPausedOrphans`
- `spawn-gate-remediator.ts` -> `SpawnGateRemediatorOpts`, `spawnGateRemediatorMain`
- `spawn-morty.ts` -> `BuildWorkerPromptOptions`, `CodegraphContextOptions`, `ParsedArgs`, `ReconcileCommitAttributionOpts`, `TicketSpec`, `WorkerProcessContext`, `buildCodegraphContextSection`, `buildTierLifecycleSections`, `buildTierResumeTable`, `buildWorkerPrompt`, `checkGitEdits`, `computeChangedLoc`, `deriveCodegraphTerms`, `isPhasePersonasEnabled`, `parseAndValidateArgs`, `parseBackendOverrideArg`, `reconcileWorkerCommitAttribution`, `renderCodegraphSection`, `resolveCodexModel`, `resolveEffectiveTierForTicket`, `resolveEffectiveTimeout`, `resolvePhasePersonaModel`, `resolveWorkerGateTier`, `resolveWorkerModelFromTierAndPersona`, `runWorkerGate`, `runWorkerGateTestCommand`, `runWorkerProcess`, `tierToModel`, `tierUsesGraphContext`, `buildWorkerSpawnEnv`
- `spawn-refinement-team.ts` -> `ACTIVITY_EVENT_SCHEMA_SECTION`, `AcShapeSmell`, `AcShapeViolation`, `AnchorCitation`, `CycleResults`, `PathVerificationWarning`, `PortalContext`, `RefinementArgs`, `RefinementManifest`, `RefinementSettings`, `RefinementTicketManifestEntry`, `StaleAnchorWarning`, `SymbolAuditCategory`, `SymbolAuditFinding`, `SymbolAuditReference`, `SymbolAuditReport`, `TicketQualityWarning`, `UNATTRIBUTED_TICKET_ID`, `__resetRefinementBackendWarning`, `buildRefinementEnv`, `buildRefinementManifest`, `buildRefinementWorkerInvocation`, `buildWorkerPrompt`, `checkAnalystOutputPaths`, `countContentLines`, `emitStaleAnchorWarnings`, `enrichManifestTicketsFromSourcePrds`, `evaluateAcShapeAdvisory`, `evaluateAcShapeEnforcement`, `evaluateSymbolAudit`, `extractAnchorCitations`, `findStaleAnchorWarnings`, `isParametrizedTicket`, `loadRefinementSettings`, `orchestrateCycles`, `parseAcShapeSection`, `parseAndValidateArgs`, `renderSymbolAuditMarkdown`, `resolveComposesChain`, `reportSymbolAuditFindings`, `runAcShapeEnforcement`, `runReadinessGate`, `scanAnalystOutputsForUnverifiedPaths`, `warnIfCodexRequested`, `writeManifestAtomic`, `writeSymbolAudit`
- `standup.ts` -> `GitCommitEntry`, `GitOnlyCommit`, `classifyStandupNoise`, `deduplicateCommits`, `formatOutput`, `getCurrentUserEmail`, `getGitCommits`, `parseArgs`, `readActivityFiles`
- `status.ts` -> `computeConsecutiveNoProgress`, `showStatus`
- `update-state.ts` -> `updateState`
- `validate-teams-ticket.ts` -> `ParsedArgs`, `main`, `parseArgs`

- `mux-runner.ts` (AP-EXT-ITER6-01 a whole-tree stage excludes the runtime's OWN index) — INVARIANT: both whole-tree `git add -A` sites in this file — `commitAndContinueDoneFlip`'s `stagePaths`-absent branch and `executeConvergedPlanAdapter`'s per-Phase `commitPhase` — spread the ONE exported `CODEGRAPH_PATHSPEC_EXCLUDES` (`services/git-utils.ts`), never a hand-copied pathspec. `.codegraph/` is written INTO `<workingDir>/.codegraph/` and is git-ignored only through the local, unversioned `.git/info/exclude`, so in any freshly-cloned TARGET repo it is plain untracked dirt. Every sibling staging path already drops it via `isCodegraphArtifact` (`archiveBeforeDestructive`, `collectDirtyInScopePaths`, the exit-path committer); these two did not. BREAKS: B-PCOMP (#b736337f) built the ownership/salvage guard at the exit-path CALL SITE and left the committer's default a bare `add -A` — so the recovery-ladder rung-1 caller `attemptRecoveryBeforeTerminal`, which passes NO `stagePaths`, committed the runtime's own code-index database into the user's repository and stamped THAT commit as the ticket's `completion_commit` on the Done flip (repo-agnostic operation is the north star; this pollutes every target repo it recovers in). Assert the COMMIT CONTENT (`git show --name-only HEAD`), never the return value: the guard and Done flip run AFTER the commit lands, so a return-value pin greens over the pollution. ENFORCE: extension/tests/worker-produced-everything-but-commit.test.js (`AP-EXT-ITER6-01` + `AP-EXT-ITER6-01 (replay)` — both mutation-verified RED with the spread removed, zero collateral). PATTERN_SHAPE: an `'add', '-A'` argv in `src/bin/mux-runner.ts` not followed by `...CODEGRAPH_PATHSPEC_EXCLUDES`. OPEN GAP: rung 1 still has no OWNERSHIP partition — `commitAndContinueDoneFlip`'s no-`stagePaths` branch stages every non-codegraph dirty path under this ticket's `completion_commit`, while the exit-path caller partitions and stashes via `salvageDirtyTree`. Collapsing that guard into the committer is the right fix and is NOT possible here: `extension/tests/dirty-tree-salvage-callsites.test.js` pins `salvageDirtyTree(` at exactly 2 call sites and `'add', '-A'` in this file at exactly 2, and that test sits outside this session's `scope.json` — the AP-EXT-ITER20-02 shape (forced deferral, not a chosen one).

- `pipeline-runner.ts` (AP-EXT-ITER7-01 the phase-runner transport decodes on the STREAM) — INVARIANT: `spawnRunner` calls `child.stdout?.setEncoding('utf-8')` AND `child.stderr?.setEncoding('utf-8')` BEFORE attaching its `'data'` handlers; the per-chunk `chunk.toString()` inside those handlers is then a no-op on an already-decoded string. BREAKS: an OS pipe boundary is a BYTE offset, so a multi-byte UTF-8 character straddles it and each half decodes to U+FFFD — mojibake in the echoed phase output, in `pipeline-runner.log`, and in the accumulated `SpawnRunnerResult.stdout`/`stderr`, at every 64KB boundary. This runner's own log lines are non-ASCII-dense (`—`, `◤`, `✓`), so it fires on any phase that out-talks a pipe buffer. Full rationale, the sibling-parity list, and the behavioral proof live in the `codegraph-query-runner.ts` half of this entry in `src/services/CLAUDE.md`. ENFORCE: extension/tests/pipeline-runner.test.js (`AP-EXT-ITER7-01 replay`) — a STRUCTURAL pin only: the real `spawnRunner` needs a `phaseRunnerContext` that only `main()` installs, so the end-to-end round-trip proof is `extension/tests/codegraph-degradation.test.js`'s `AP-EXT-ITER7-01 multi-byte round-trip` over the identical transport. Do not read the source pin as an independent behavioral guarantee. PATTERN_SHAPE: `child.stdout?.setEncoding(` precedes `child.stdout?.on('data'` inside `spawnRunner`, with a matching `child.stderr?.setEncoding('utf-8')`.

- `pipeline-runner.ts`; `check-readiness.ts`; `microverse-runner.ts` (AP-EXT-ITER8-01 replay — an UNBOUNDED patch or repo enumeration carries an explicit `maxBuffer`) — INVARIANT: `isDesignSafeBranch`'s bundle-wide `git diff <startCommit>..HEAD`, the `git log --oneline` commit count, `gitTrackedFiles`' whole-repo `git ls-files`, and `captureCachedDiffPatch`'s `git diff --cached --binary` each declare a 64 MB ceiling instead of inheriting Node's 1 MB default. BREAKS: `captureCachedDiffPatch` is the worst of the four — `preflightAutoCommit`'s rollback runs `git reset` FIRST, unstaging everything, and then re-applies the captured patch, so a truncated capture means the operator's staged index is restored PARTIALLY (silent) or `git apply` rejects and the restore aborts having already unstaged the whole set (loud); this is the same silent-loss shape the `microverse-runner.ts` preflight-rollback trap door claims to restore "the EXACT pre-existing cached diff" against. `gitTrackedFiles` truncated reads as "file not tracked" and manufactures false-positive `contract`/`file_path` readiness findings; the design-safe diff truncated under-counts its visual stat and silently flips `design_safe`. ENFORCE: extension/tests/pickle-utils.test.js (`AP-EXT-ITER8-01` block pins the shared `runCmd` primitive; these direct spawns are pinned structurally only). PATTERN_SHAPE: a `spawnSync`/`execFileSync` in these files reading an unbounded patch or whole-repo file list whose options object carries no `maxBuffer`. OPEN GAP: `restoreCachedDiffPatch` still passes the patch back as a `string` under `encoding: 'utf-8'` — the AP-EXT-ITER25-02 byte-exactness defect is untouched here; a ceiling stops truncation, it does not stop the decode.

- `pickle-recover.ts` (AP-EXT-ITER10-01 un-terminalizing RELEASES the dead pid) — INVARIANT: the `reactivate` transition's single StateManager write DELETES `s.pid` alongside `{active:true, step:'research', exit_reason:null, current_ticket}`. `finalizeTerminalState` never clears `pid`, so a terminal state carries the DEAD pid of the runner that finalized it, and `StateManager.recoverStaleActiveFlag` demotes any `active:true` state whose finite pid is dead — writing back to disk. BREAKS: the one field this command exists to set is reverted by the very next `StateManager.read` and the revert is PERSISTED, so the sole sanctioned terminal→runnable transition (`CUJ-RECOVER-EPIC-01`, and the writer `audit-un-terminalize-single-path.sh` allowlists) is inert for `active` — the operator sees `{active:false}` and no error. `null` is the truthful claim (no process owns it yet); the next runner stamps its own pid, and the paused-orphan arm still demotes it after 300s if none ever does. `delete`, not `= null`: `State.pid` is `pid?: number`, so absence is the schema encoding of "unclaimed" and `recoverStaleActiveFlag` takes the same branch for both. Do NOT stamp `process.pid`: this is a short-lived CLI, so its own pid is dead the moment it exits and the demotion returns. Assert what a LATER `StateManager.read` sees, never the mutator's effect — `tests/bin/pickle-recover-reactivate.test.js` injects a fake `updateState` that applies the mutator to a plain object and is blind BY CONSTRUCTION to this whole class. ENFORCE: extension/tests/setup.test.js (`AP-EXT-ITER10-01` cases — durability through the real StateManager, plus a live-pid control; mutation-verified RED on the compiled mirror with `delete s.pid` removed, control GREEN). PATTERN_SHAPE: an `s.active = true` write in a SHORT-LIVED CLI that neither stamps `s.pid = process.pid` (long-lived owner) nor clears it. OPEN GAP: two replay matches carry the identical shape and are unfixed — `retry-ticket.ts:91` (its own `sm.read` six lines later demotes it before the operator can run the spawn command it prints) and `lock-scope.ts:135` (`patchStateJson` logs "state.json patched — active=true" for a flag the next read drops). Both files are outside this session's `scope.json` (the AP-EXT-ITER20-02 forced-deferral shape). NOT matches: `jar-runner.ts:113`, `pipeline-runner.ts:1435` (`claimPipelineRunnerActive`), `microverse-runner.ts:3137`, `mux-runner.ts:9777` — each stamps `s.pid = process.pid` in the same write and is the live owner.
