## Trap Doors

- `transaction-ticket-ops.ts` (AP-EXT-ITER12-01 apply-ledger reversal completeness) — INVARIANT: every ledger replay reverses a `started`-only step. The two `selectForwardEntries` consumers are `reverseAppliedEntries` (operator `--recover-from-ledger`) and `forwardReplayEntries` (`--recover --force`) — NOT a pair of rollback paths; the third replay, the in-transaction `replayReverseLedger` that `rollbackAndHaltApply` calls, reaches the same result by filtering NOTHING (`parseLedgerContent` keeps every status, and per-step duplicate entries are idempotent). `reverseAppliedEntries` takes NO `throughStep` ceiling. A `started`-only entry IS the crash window — the file was already overwritten at `applyPlannedWrite`'s `writeFileSync` before the `applied` append — and its `beforeContent` is the only copy. Reversal is idempotent both directions (write never landed → identical bytes; create never landed → `existsSync` skips). `lastSuccessfulStep` stays `applied`-only for REPORTING and must not gate reversal. BREAKS: an `applied`-only filter makes a SIGKILL/OOM mid-write unrecoverable while `recoverCourseCorrectionFromLedger` returns success + `course_correct_recovered` — verified-work destruction on the sanctioned recovery surface. ENFORCE: extension/tests/transaction-ticket-ops.test.js (started-only crash-window, failed partial-write, create-step no-op). PATTERN_SHAPE: `selectForwardEntries(entries)` inside `reverseAppliedEntries`; no `status === 'applied'` filter and no `throughStep` param there; `replayReverseLedger` keeps `parseLedgerContent` (a status filter there WOULD drop the crash window). Do NOT re-word this to "BOTH rollback paths select via `selectForwardEntries`" — that read false from birth (`d1e6302c`) and sends a reviewer hunting a guard `replayReverseLedger` never had.
- `pickle-utils.ts` (R-CIFB-A session-ranking dead-pid demotion + deterministic recency) — INVARIANT: `selectScannedSessionPath` demotes a dead-pid (finite-integer `pid` && `!isProcessAlive(pid)`) `active:true` session out of the `activeMatch` lane into the `inactiveMatch` lane (eligible only when `!requireActive`); an `active:true` session with no/non-finite pid stays a live candidate. `preferNewerSession` MUST NOT use `sessionPath.localeCompare` — recency ties prefer a parseable `started_at`, then keep the incumbent (stable, first-seen-wins on iteration order). `isProcessAlive` is IMPORTED from `state-manager.js`, never re-inlined. BREAKS: a dead-pid active session shadows a live same-cwd session on Linux coarse-mtime ties; lexical path order silently picks the wrong session. ENFORCE: `extension/tests/get-session.test.js` (`:320` mapped dead-pid case + the equal-mtime lexical-swap-stability cases). PATTERN_SHAPE: `isProcessAlive(` imported in `pickle-utils.ts` and called (via `isDeadPidState`) inside `selectScannedSessionPath`; `localeCompare` ABSENT from `preferNewerSession`.
- `recoverable-json.ts`; `state-manager.ts` (R-CIFB-B orphan-tmp equal-mtime promotion) — INVARIANT: orphan-`.tmp.<pid>` recovery treats an equal `mtimeMs` as "tmp wins" in BOTH `recoverable-json.ts` `parseDeadTmp` (discard predicate `mtimeMs < baseMtimeMs`, NOT `<=`) and `state-manager.ts` `isStateSnapshotNewer` (mtime tie-break `candidateMtimeMs >= currentMtimeMs` in the equal-iteration and both-iterations-absent branches). A `.tmp.<pid>` is written AFTER its base, so on a Linux coarse-mtime TIE it is the more-recent intent. Iteration-first precedence (higher `iteration` wins regardless of mtime) and the `shouldSkipLiveTmp` live-writer guard are PRESERVED — only DEAD-pid orphan tmps reach the mtime tie-break. Among multiple equal-mtime competing dead tmps, `recoverable-json.ts` winner-selection keeps strict `>` (first-seen-wins on readdir order). BREAKS: reverting to `<=` / strict `>` drops a valid recovery candidate on Linux ties → lost recoverable state (R-CIFB cluster). ENFORCE: `extension/tests/recoverable-json.test.js`, `extension/tests/state-manager.test.js`, `extension/tests/verify-recapture-fired.test.js` (each with an equal-forced-mtime promotion case). PATTERN_SHAPE: `mtimeMs < baseMtimeMs` (not `<=`) in `parseDeadTmp`; `candidateMtimeMs >= currentMtimeMs` in `isStateSnapshotNewer`.
- `git-utils.ts` (`probeConcurrentGitAccess` advisory contract — R-PIWG-5) — INVARIANT: `probeConcurrentGitAccess` is ADVISORY — its result MUST only trigger a warn log plus a `concurrent_git_access_detected` activity event; it MUST NEVER be used as a hard launch block that causes `setup.ts` to abort or return a non-zero exit; every `spawnSync`/`execFileSync` subprocess spawned inside the function MUST carry a finite positive `timeout` option (current default 5_000 ms). BREAKS: converting the advisory warn into a hard guard causes legitimate launches (e.g. a background git fetch holding the lock momentarily) to fail; dropping a subprocess timeout re-opens the wedged-launch-probe class where `lsof` or `pgrep` hangs indefinitely, blocking session startup. ENFORCE: extension/tests/probe-concurrent-git-access.test.js, extension/tests/setup.test.js. PATTERN_SHAPE: `TRAP DOOR: probeConcurrentGitAccess advisory-probe`.
- `git-utils.ts` (AP-HS6-1 completion_commit add-path) — INVARIANT: `updateTicketFrontmatter` MUST apply `applyCompletionCommitField` to the frontmatter SLICE (which ends in the closing `---`), never the full document. `setFrontmatterField`'s add-path fallback `/\n---(\r?\n?)$/` is `$`-anchored and only matches when the closing delimiter is at end-of-string. BREAKS: an absent `completion_commit` is silently dropped on the `status:Done` success path (`spawn-morty.ts`), stamping Done with no completion evidence (`done_without_commit_evidence`). The sibling `upsertFrontmatterField` (pickle-utils) is the safe reference — it inserts via `lastIndexOf('\n---', fm.end - 1)`. ENFORCE: extension/tests/git-utils-ticket-frontmatter.test.js (`completion_commit when the field is ABSENT`). PATTERN_SHAPE: `applyCompletionCommitField(updatedBase, patch)` on the frontmatter-only slice, NOT `applyCompletionCommitField(updatedBase + original.slice(fm.end), …)`.
- `git-utils.ts` (AP-EXT-ITER8-01 the archive must APPLY, not merely mention) — INVARIANT: every `archiveBeforeDestructive` diff spreads `ARCHIVE_DIFF_BASE` (`['diff','--binary']`) — staged, unstaged, and per-file untracked alike. BREAKS: without `--binary` git writes a contentless `Binary files a/x and b/x differ` stub, and `git apply` is ALL-OR-NOTHING — ONE such stub rejects the WHOLE patch, so a single dirty binary destroys the co-archived TEXT work too while `pre_reset_diff_archived` still reports it saved. A patch that MENTIONS the work is not a patch that RESTORES it: the sibling AC-WMFF-2A case asserts `assert.match(patch, /worker edit/)`, an oracle the stub satisfies — assert the round trip instead. Correct sibling: `microverse-runner.ts:captureCachedDiffPatch` already carries `--binary`. ENFORCE: extension/tests/worker-produced-everything-but-commit.test.js (`AP-EXT-ITER8-01: the pre-reset archive must APPLY`). PATTERN_SHAPE: a `runArchiveGitBytes([` call in `git-utils.ts` whose first element is a bare `'diff'` literal instead of `...ARCHIVE_DIFF_BASE`. Scope the anchor to `runArchiveGitBytes(` ONLY — AP-EXT-ITER25-01 renamed the patch-building calls off `runArchiveGit(`, which is now the TEXT wrapper and carries only the `ls-files` path read; an anchor still naming `runArchiveGit([` matches nothing and would read as satisfied while covering none of the patch sections. `getDiffFiles`' `runGit(['diff', …'--name-status', '-M100', '-z'])` is parsed for filenames and never applied, so it MUST NOT carry `--binary`; a bare `['diff'` anchor matches it and reads as a phantom violation. SCOPE: `--binary` fixes only the class git CALLS binary — the non-UTF-8 TEXT class it cannot reach is AP-EXT-ITER25-01 below.
- `git-utils.ts` (AP-EXT-ITER25-01 the archive pipe is BYTE-exact — a decode rewrites the work it saves — LANDED) — INVARIANT: every patch section is read through `runArchiveGitBytes` (no `encoding`, returns `Buffer`) and concatenated + written as a `Buffer`; the decoding `runArchiveGit` wrapper is for PATH LISTS only (`listUntrackedPaths`' `ls-files`). Git calls a file TEXT when its first 8000 bytes hold no NUL and then emits its RAW bytes, so a UTF-8 decode turns every invalid sequence into U+FFFD and the archive restores DIFFERENT bytes than `resetToSha` destroyed. `--binary` (AP-EXT-ITER8-01) does NOT cover this and cannot: it reaches only files git already calls binary, which base85 to pure ASCII and survive a decode — the two entries fix disjoint classes, so neither may be collapsed into the other. BREAKS: the corruption lands ONLY in added lines, so context still matches and `git apply` exits **0** — an rc check, a `files`/`pre_reset_diff_archived` reading, or a `/worker edit/` match all go GREEN over silently mangled recovery content. Measured live against the shipped `services/git-utils.js`: a latin-1 note archived as `caf\xe9` came back `caf\xef\xbf\xbd`, rc 0. Assert BYTE EQUALITY of the round trip (`Buffer.equals`), never rc and never a substring. `diff.length` is also now the TRUE byte size, so the untracked cap stops measuring post-corruption bytes. ENFORCE: extension/tests/worker-produced-everything-but-commit.test.js (`AP-EXT-ITER25-01` — tracked + untracked non-UTF-8 TEXT, with a fixture guard asserting no NUL so the case cannot pass via the base85 path; mutation-verified: a type-clean decode-then-re-encode reddens exactly this case, zero collateral, ITER8's binary case stays green). PATTERN_SHAPE: `encoding:` ABSENT from `runArchiveGitBytes`' `spawnSync` options, `buildArchivePatch` returning `Buffer.concat(`, and `writePatchFileSync(patchPath: string, content: Buffer)`. Do NOT anchor on "no `encoding: 'utf-8'` in `git-utils.ts`" — `listWorkingTreeDirtyPaths`, `lookupCommandForPid` and the text wrapper legitimately decode, so a whole-file zero-count reads as a phantom violation.
- `../bin/microverse-runner.ts` (AP-EXT-ITER25-02 the preflight index snapshot is BYTE-exact — LANDED) — INVARIANT: `captureCachedDiffPatch` returns a `Buffer` (no `encoding` on the `execFileSync`) and `restoreCachedDiffPatch` takes a `Buffer` and passes it straight through as `input` (no `encoding`), so the operator's staged index round-trips byte-exactly. Same class as AP-EXT-ITER25-01 and identically un-covered by the `--binary` this call already carried: git calls a file TEXT when its first 8000 bytes hold no NUL and then emits its RAW bytes, so a UTF-8 decode rewrites every invalid sequence to U+FFFD, while `--binary` reaches only files git already calls binary (which base85 to pure ASCII and survive a decode). The empty-patch short-circuit is `patch.length === 0`, not a decoded `.trim()`. BREAKS: `preflightAutoCommit`'s rollback runs `git reset` FIRST, unstaging everything, then re-applies — so a staged non-UTF-8 TEXT file comes back with U+FFFD in the operator's index (silent), or the mangled bytes land in a CONTEXT line, `git apply` fails, and the restore aborts having already unstaged the whole set (loud). Measured live against the shipped `bin/microverse-runner.js` before the fix: a staged latin-1 `caf\xe9 note` was restored as `caf\xef\xbf\xbd note` and the rollback reported success. Assert BYTE EQUALITY of the staged blob (`git show :<path>` as a Buffer, `Buffer.equals`), never the staged NAME list and never "did it throw" — the pre-fix restore succeeded and its `git diff --cached --name-only` reading was already correct, which is exactly why the sibling name-list case is blind to this by construction. Fix by keeping both halves `Buffer`, never by re-encoding in JS. ENFORCE: extension/tests/microverse.test.js (`AP-EXT-ITER25-02: rollback restores a non-UTF-8 TEXT staged file byte-exactly` — its fixture asserts the bytes are NUL-free so the case cannot pass via the base85 binary path; mutation-verified RED on the compiled mirror by restoring `encoding: 'utf-8'` to the capture, with both sibling `preflightAutoCommit` cases staying GREEN). PATTERN_SHAPE: `encoding:` ABSENT from BOTH `captureCachedDiffPatch`'s and `restoreCachedDiffPatch`'s `execFileSync` options, `captureCachedDiffPatch(): Buffer`, and `restoreCachedDiffPatch(workingDir: string, patch: Buffer)`. Do NOT anchor on "no `encoding: 'utf-8'` in `microverse-runner.ts`" — many unrelated readers in this file legitimately decode, so a whole-file zero-count reads as a phantom violation. NOT matches (parsed, never re-applied, so a decode cannot rewrite work): `pipeline-runner.ts:isDesignSafeBranch`'s bundle diff, `convergence-gate.ts:getChangedExportedSymbols`, and every `--name-only`/`--numstat`/`--stat` reader. The sibling producer of restorable patches, `git-utils.ts:archiveBeforeDestructive`, is already `Buffer` (AP-EXT-ITER25-01); after this fix `git apply` has exactly one call site in `src/`.
- `git-utils.ts` (AP-EXT-ITER32-01 a TRUNCATED archive refuses the destroy) — INVARIANT: `resetToSha` routes the archive through `assertArchiveProtectsTree`, which raises `ArchiveAbortError` on `filesTruncated`; a write failure and a truncated patch reach the caller on the SAME wire, so "did the archive fully protect this tree?" is one question with one answer. `collectUntrackedDiffs` stops at the first untracked file crossing `ARCHIVE_UNTRACKED_BYTE_CAP` and omits it AND every later one, so the returned flag is the only signal that the patch cannot restore the tree. BREAKS: pre-fix the result was DISCARDED (`if (archive) archiveBeforeDestructive(archive);`) and `reset --hard` + `clean -fd` ran anyway — deleting untracked work no patch holds while `pre_reset_diff_archived` reported it saved. The sibling `pipeline-runner.ts:archiveOrFatalOnTruncation` already states this invariant and FATALs; `resetToSha` is the primitive BOTH destructive callers use (`spawn-morty.ts` gate-fail reset, `microverse-runner.ts` regression rollback), and it was the unguarded one. Assert the FILE SURVIVES and HEAD is unmoved — the pre-fix run set the flag and emitted the event correctly, so a flag/event oracle greens over its own bug. ENFORCE: extension/tests/guarded-reset.test.js (`AP-EXT-ITER32-01`; mutation-verified RED on restoring the discarded call, zero collateral). PATTERN_SHAPE: `assertArchiveProtectsTree(archiveBeforeDestructive(archive), archive)` in `resetToSha`; no bare `archiveBeforeDestructive(` whose result is dropped ahead of a `reset --hard`/`clean`.
- `backend-spawn.ts` — INVARIANT: `resolveBackendFromStateFileWithSource(cliBackend)` must honor CLI override before reading persisted `state.backend`. BREAKS: relaunch/spawn callers silently route to stale session backend instead of the operator-selected backend. ENFORCE: `backend-spawn.test.js` CLI override precedence coverage. PATTERN_SHAPE: exported resolver with `cliBackend?: Backend` and `source: 'cli-flag-override'`.
- `backend-spawn.ts` (AC-8 MCP forwarding invariant) — INVARIANT: `buildClaudeWorkerInvocation` and `buildClaudeManagerInvocation` MUST push `--mcp-config <path>` into the spawned `args` array whenever `resolveMcpConfigPath(opts.settingsBag)` returns a non-null, non-undefined path; both functions follow the pattern `const mcpCfg = opts.mcpConfig ?? resolveMcpConfigPath(opts.settingsBag); if (mcpCfg) args.push('--mcp-config', mcpCfg)`. BREAKS: removing or short-circuiting the `if (mcpCfg) args.push('--mcp-config', mcpCfg)` conditional silently drops MCP forwarding for all worker and manager claude-backend spawns — workers can no longer access operator-configured MCP servers (Linear, GitHub, etc.) even when `worker_mcp_config_path` is set in `pickle_settings`. ENFORCE: `extension/tests/services/backend-spawn-mcp.test.js`, `extension/tests/integration/worker-mcp-access.test.js`. PATTERN_SHAPE: `'--mcp-config'` literal present in `buildClaudeWorkerInvocation` body.
- `backend-spawn.ts` (R-CXOR-3 codex git-boundary parity — chosen option (a)) — INVARIANT: the post-iteration `detectAndRecoverHeadRegression` audit in `mux-runner.ts` is the authoritative git-boundary guard for codex-backend workers. `buildCodexInvocation` spawns codex via `codex exec --dangerously-bypass-approvals-and-sandbox`; the codex subprocess runs bash commands directly and NEVER routes through Claude Code's PreToolUse hook dispatch. Therefore `isGitVerbBlockedByRWSRCGR` (which checks `PICKLE_ROLE` and blocks prohibited git verbs only for claude-backend workers) CANNOT intercept `git reset --hard` from a codex worker. The authoritative recovery path is: after every worker iteration, `mux-runner.ts` calls `detectAndRecoverHeadRegression({ ticketId, workingDir, startCommit, completionCommitSha, ... })` in the post-Done-detection branch; this function compares HEAD to `start_commit` and either ff-reattaches the orphaned commit or marks the ticket Failed — no silent orphan is possible. Option (b) (wrapper-git on PATH injected per codex spawn) was rejected: requires compiling a PATH-shim, adds a new ENOENT failure mode, duplicates `detectProhibitedGitVerb` logic outside its home module, and is redundant with the already-wired R-CXOR-1 audit. Option (c) (reference-transaction git hook installed per session) was rejected: codex uses `--skip-git-repo-check`, git hook inheritance under `--ephemeral` and `--ignore-user-config` is undefined, and the setup path is fragile. Claude-backend behavior is UNCHANGED: `detectProhibitedGitVerb` + `isGitVerbBlockedByRWSRCGR` in `hooks/handlers/config-protection.ts` remain authoritative for claude workers via the PreToolUse hook; these functions must not be modified. BREAKS: removing `detectAndRecoverHeadRegression` from the "ticket already marked Done" branch of `runMuxRunnerMain` (currently at `mux-runner.ts:5549-5563`) eliminates the only git-boundary enforcement for codex workers, allowing a codex worker to silently orphan commits via `git reset --hard <baseline>` and have the pipeline accept Done-at-baseline as valid completion evidence. ENFORCE: extension/tests/codex-git-boundary-parity.test.js. PATTERN_SHAPE: `detectAndRecoverHeadRegression(` called in the "ticket already marked Done" branch of `runMuxRunnerMain` guarded by `previousTicketStartCommit`; `detectProhibitedGitVerb(` present and unmodified in `hooks/handlers/config-protection.ts`.
- `git-trailer-hooks.ts` (B-GITATTR WS-1 trailer-hooks materialization) — INVARIANT: `materializeTrailerHooks` NEVER mutates the target repo — no `.git/hooks` write, no `git config` write — it writes only inside the caller-supplied `managedDir`; `resolvePreExistingHooksDir` MUST discard a candidate equal to `managedDir` (a self-match seen through an inherited `GIT_CONFIG_*` fragment in a nested spawn) and fall through to the repo default, or every hook gets rewritten to `exec <itself>` — an infinite exec loop that hangs the commit forever. The `prepare-commit-msg` script stamps the trailer via `git interpret-trailers --in-place --trailer` (git's own trailer writer, symmetric with the consumer's `%(trailers:...)` reader) and falls back to a `printf` append ONLY if `interpret-trailers` cannot run; an unconditional `printf` append would open a new paragraph and silently demote every pre-existing trailer (`Co-Authored-By`, `Signed-off-by`, `Resolves`) to body prose. Unset/empty/whitespace-only `PICKLE_TICKET_ID` all no-op identically (stripped form is a guard input only; the original value is what gets written). Resolution failure materializes NOTHING and returns `{ok:false, reason}` (degrading to read-side attribution) rather than silently disabling a target repo's own hooks (e.g. husky pre-commit). BREAKS: a self-match candidate not discarded hangs every commit in the target repo; an unconditional trailer-append corrupts pre-existing trailers on every stamped commit; mutating the target repo's real `.git/hooks` or config disables the operator's own hooks. ENFORCE: `extension/tests/integration/gitattr-trailer-producer.test.js`, `extension/tests/integration/gitattr-hook-forwarding.test.js`. PATTERN_SHAPE: `samePath(resolved, managedDir)` discard inside `resolvePreExistingHooksDir`; `git interpret-trailers --in-place --trailer` attempted before the `printf` fallback in `buildTrailerHookScript`.
- `git-trailer-hooks.ts` (AP-EXT-ITER2-01 idempotence guard reads the consumer's oracle) — INVARIANT: the hook's already-stamped guard tests `git interpret-trailers --parse "$1"`, never a whole-file `grep '^Pickle-Ticket:'`. git parses trailers from the LAST paragraph only, so a mid-body `Pickle-Ticket:` line satisfies a raw grep, the hook skips, and `%(trailers:…)` — the reader `scanGitLogByTrailer` uses — sees nothing. BREAKS: attribution silently lost with the id visible in `%B`; WS-3 left the trailer scan as the only git-log arm, so evidence reads `absent` and the Done-flip refuses `done_without_commit_evidence` (live: 271587ae). ENFORCE: extension/tests/integration/gitattr-trailer-producer.test.js#a-mid-body-Pickle-Ticket-line-does-not-suppress-the-real-trailer. PATTERN_SHAPE: `interpret-trailers --parse` feeds `_pickle_trailer_view` in `buildTrailerHookScript`; no `grep -q '^Pickle-Ticket:' "$1"` guard.
- `git-trailer-hooks.ts` (AP-EXT-ITER19-01 hooks live in the COMMON dir) — INVARIANT: `resolvePreExistingHooksDir`'s second arm derives from `rev-parse --git-common-dir`, never `--git-dir`. Hooks are a per-REPOSITORY resource; in a linked worktree `--git-dir` is `.git/worktrees/<name>`, which has no `hooks` child at all. `--git-common-dir` is identical to `--git-dir` in the main-worktree and `--separate-git-dir` layouts and equally ignores `core.hooksPath`, so the swap keeps the two arms non-redundant (the self-match rejection the first arm performs is not re-derived) — it is strictly a widening. BREAKS: the fallback resolves nothing inside a linked worktree, `materializeTrailerHooks` returns `{ok:false, reason:'pre-existing hooks dir unresolvable'}`, `buildTrailerHooksEnvFragment` returns `{}`, and NO commit made in that worktree carries a `Pickle-Ticket` trailer. WS-3 deleted the message-inference passes, so the trailer scan is the only git-log arm left: evidence reads `absent` and the Done-flip refuses `done_without_commit_evidence` — the whole attribution mechanism vanishes silently, for every ticket, with `{ok:false}` swallowed by the caller's warn-once. The JSDoc CLAIMED "honoring separate-git-dir/worktree layouts" while only the first half was true; assert the round trip (commit in a worktree, read the trailer back), not the resolver's return shape. ENFORCE: extension/tests/integration/gitattr-trailer-producer.test.js#a-linked-worktree-still-stamps-the-trailer. PATTERN_SHAPE: `[['rev-parse', '--git-common-dir'], 'hooks']` in `resolvePreExistingHooksDir`; no `'--git-dir'` argv literal in `git-trailer-hooks.ts`.
- `agent-md-loader.ts` — INVARIANT: agent frontmatter list fields (`tools`, `principles`) must accept both CSV scalars and YAML `- item` lists. BREAKS: standard override markdown crashes `loadAgentMd`, blanking phase personas and debate agents. ENFORCE: `agent-md-loader.test.js` YAML-list load coverage. PATTERN_SHAPE: `tools:` or `principles:` followed by indented `- ` items.
- `pickle-utils.ts` (AP-EXT-ITER7-01 the iteration-number namespace RESETS every phase) — INVARIANT: `latestIterationLog` ranks `tmux_iteration_*.log` by mtime FIRST, iteration number only as tiebreak. BREAKS: `/pickle-pipeline` runs three phases in ONE session dir and mux-runner + microverse-runner each number from 1, so max-by-number returns the longest-dead phase's log. Measured live (session `2026-08-20-54c74299`, anatomy-park iter 7): it returned the pickle phase's `tmux_iteration_8.log`, 2h14m stale — all three watcher panes (`monitor.ts`, `log-watcher.ts`, `raw-morty.ts`) tailed a finished phase for the whole run, `log-watcher` printing `▸ ITERATION 8`. Zero tests existed. Assert the SELECTED FILE against a real mtime layout; a non-null oracle greens over it. Correct sibling: `pipeline-runner.ts:resolveChildMuxLivenessMtime` takes MAX MTIME across the same family, for the same reason. ENFORCE: `extension/tests/pickle-utils.test.js` (AP-EXT-ITER7-01 block — the phase-reset case mutation-verified RED against the pre-fix compiled mirror; the ascending-single-phase, mtime-tie and empty-dir controls stay GREEN). PATTERN_SHAPE: a `.sort(` over a parsed `tmux_iteration_` number whose last element is selected — i.e. any max-by-iteration-NUMBER pick over an artifact namespace a phase boundary resets; rank by mtime instead.
- `activity-timeline-verifier.ts` (AP-EXT-ITER6-01) — INVARIANT: `findOverlapViolations` terminals come from `WORKER_TERMINAL_EVENTS` — manager-side, post-worker-exit — and MUST carry the clean branch `boundary_commit_resolved`, never live-stdout `worker_completion_commit_announced`. BREAKS: every cleanly-Done ticket reads as an unresolved overlap; exit 1 on a healthy session. ENFORCE: `extension/tests/activity-timeline-verifier.test.js` (AP-EXT-ITER6-01 pair). PATTERN_SHAPE: a bare `event === 'worker_gate_failed'` terminal filter inside `findOverlapViolations`; (AP-EXT-ITER39-01 the terminal must prove the CURRENT run ended) — INVARIANT: the lookup is bounded at BOTH ends, `t.ts >= lastSpawnMs && t.ts <= spawn.ts`. BREAKS: relaunch is routine (6/8 tickets re-spawned in session 2026-08-22-a1e33756), so run-1's terminal vouches for every later run and a live overlap reads `OVERLAP: none`, exit 0. ENFORCE: `extension/tests/activity-timeline-verifier.test.js` (AP-EXT-ITER39-01 block). PATTERN_SHAPE: a `t.ts <= spawn.ts` filter in `findOverlapViolations` carrying no `lastSpawnMs` lower bound.
- `activity-logger.ts` — INVARIANT: buffered activity writes flush only into the current `getActivityDir()` root; entries captured under a different data root must be dropped instead of retried forever. BREAKS: one unwritable temp root contaminates later sessions with permanent stderr spam and dead-path retries. ENFORCE: `activity-logger.test.js` stale-root buffer-drop coverage. PATTERN_SHAPE: `pendingBuffer.splice(0)` flushed without `path.dirname(entry.filepath) === activityDir`.
- `state-manager.ts` (WS4 dir-scanner emit sink) — INVARIANT: any event a metrics dir-scanner reads (`REFUSED_RECOVERED_EVENT_NAMES`, `SKIP_FLAG_EVENT_NAMES`, `readiness_false_positive_suppressed`) MUST be emitted to the `getDataRoot()/activity/<day>.jsonl` sink (inline `appendFileSync` or `logActivity`), NEVER via `writeActivityEntry` / `s.activity=[...]` (the `state.json.activity` sink the scanner never reads). BREAKS: dashboard/budget count stuck at 0 forever. ENFORCE: extension/tests/recurrence-refused-counts.test.js. PATTERN_SHAPE: a dir-scanner event name routed through `writeActivityEntry(` or `s.activity = [` instead of the activity-dir jsonl.
- `pickle-utils.ts` — INVARIANT: `inferMonitorMode` must keep the layout-selector `MonitorMode` union in `extension/src/services/pickle-utils.ts` pinned to `'pickle' | 'council' | 'refinement' | 'szechuan-sauce' | 'anatomy-park'` (the `'meeseeks'` member was retired with the meeseeks subsystem — B-RSHM WS-2); this trap door covers the layout-selector union only, not the separate render-mode `MonitorMode` in `extension/src/bin/monitor.ts`. BREAKS: future workers collapse `inferMonitorMode` back to a partial union, dropping `'refinement'` or microverse layout modes by confusing it with the distinct render-mode union. ENFORCE: `extension/tests/monitor-mode-resilience.test.js` R-MWCL-1 mode-inference coverage. PATTERN_SHAPE: `export type MonitorMode = 'pickle' | 'council' | 'refinement' | 'szechuan-sauce' | 'anatomy-park';` adjacent to `inferMonitorMode(...)`.
- `pickle-utils.ts` (R-MWCL-1 command_template mode inference) — INVARIANT: `inferMonitorMode(sessionDir, log?)` resolves monitor mode from `state.json.command_template` via startsWith globs (`pickle*→pickle`, `council*→council`) and exact matches (`anatomy-park.md→anatomy-park`, `szechuan-sauce.md→szechuan-sauce`, `refinement.md→refinement`); a retired/unrecognized template (e.g. the deleted `meeseeks.md`) or missing/empty `command_template` defaults to `pickle` and emits WARN via the optional `log` callback with prefix `[ensureMonitorWindow]`. `ensureMonitorWindow` passes its `log` function to `inferMonitorMode` so mode-inference warnings surface in the runner log alongside the `created 4-pane monitor (mode=...)` line. BREAKS: dropping the `log` pass-through silences mode-inference regressions; collapsing the startsWith globs to exact matches drops `council-of-ricks.md` (council* glob) and future pickle variants. ENFORCE: `extension/tests/pickle-utils-ensure-monitor-window-mode.test.js`. PATTERN_SHAPE: `inferMonitorMode(opts.sessionDir, log)` (not bare `inferMonitorMode(opts.sessionDir)`) in `ensureMonitorWindow` context initialization.
- `pickle-utils.ts` (R-WTB-4 worker-timeout / R-PTG floor pin) — INVARIANT: `TICKET_TIER_BUDGETS.medium.worker_timeout_seconds` must stay at or above the documented R-PTG worker lifecycle floor because medium-tier workers must cover research, plan, implement, the per-ticket fast-test gate, type/lint checks, artifact writes, and commit overhead before timeout; the compiled default is `60 * 60` (`3600`) seconds (raised from `2400` in R-WTB-A2 to give 60-minute headroom; b04f41d6 incident precedent). Two-layer design: R-WTB-A1's artifact-progress detector (`extension/CLAUDE.md` R-WTB-A1 trap-door) owns the "no halt while productive" guarantee; this pin is the static floor. BREAKS: lowering the medium-tier default below the R-PTG floor silently turns compliant medium tickets into timeout-prone runs during the required worker gate, even when the code change itself is small. ENFORCE: `extension/tests/integration/worker-timeout-tier-budget.test.js` R-WTB-3 timeout-budget regression coverage. PATTERN_SHAPE: `TICKET_TIER_BUDGETS.medium.worker_timeout_seconds` in `extension/src/services/pickle-utils.ts` remains `>= 60 * 60` with `worker-timeout-tier-budget.test.js` asserting the documented medium-tier default.
- `pickle-utils.ts` — INVARIANT: session-map prune and same-cwd lookup must recover tmp-only `state.json` before phantom/readable checks. BREAKS: cancel/status/retry/worker-setup prune or miss live recoverable sessions. ENFORCE: `extension/tests/cancel.test.js`. PATTERN_SHAPE: `fs\\.accessSync\\(.*state\\.json` or `new StateManager\\(\\)\\.read\\(statePath\\)` inside session discovery helpers.
- `state-manager.ts` — INVARIANT: orphan tmp promotion must reject snapshots missing core fields or newer than the runtime schema before `renameSync`. BREAKS: partial or future-schema tmp overwrites readable `state.json`, then older runtimes brick on recovery. ENFORCE: `state-manager.test.js` orphan tmp recovery coverage. PATTERN_SHAPE: `state.json.tmp.<pid>` JSON object promoted via `isStateSnapshotNewer(...)`; INVARIANT: reset-step snapshots with `step: null` are valid recoverable state during phase handoff. BREAKS: corrupt-base recovery drops newer `clearExitReason` tmp state and loses phase-reset progress. ENFORCE: `state-manager.test.js` reset-step orphan tmp coverage. PATTERN_SHAPE: `clearExitReason(..., { resetStep: true })` followed by orphan tmp recovery.
- `pickle-utils.ts` (tmux ownership is derived from the (name, sessionDir) pair, never resolved) — INVARIANT: `isForeignTmuxSession(sessionName, sessionDir)` decides ownership by comparing the trailing `-`-delimited hash of the ambient tmux name against the trailing hash of `path.basename(sessionDir)`, and MUST fail CLOSED (any name it cannot tie to our own session dir is foreign). It MUST NOT resolve the tmux name against `getDataRoot()/sessions` to look up an owner. ALL THREE functions that resolve a tmux target from the ambient `#S` call it before acting: `restartDeadWatcherPanes` before ANY tmux send-keys/split-window, `ensureMonitorWindow` before ANY kill-window/new-window, and `respawnMonitorWindowForMode` before ANY respawn-pane. Count the consumers — do not trust this sentence to have kept up: it read "BOTH" while there were already three, and the ungated third was the most destructive of them. BREAKS: a data-root lookup falls OPEN for every caller whose data root does not contain the ambient session — exactly the case for a worker/test child with a sandboxed `PICKLE_DATA_ROOT`. The fast tier then typed 30 `send-keys ... Enter` per run into the operator's LIVE monitor pane (`pipeline-<hash>:monitor.0`); at c=8 the keystrokes interleaved and a mangled redirect wrote a junk `extension/bash` file. Gating only `restartDeadWatcherPanes` leaves the WORSE half open: `ensureMonitorWindow` issues `kill-window` on a `@pickle_monitor_mode` mismatch, so an un-gated call DESTROYS a stranger's monitor window and rebuilds it pointed at our session dir — it survived only because the foreign windows happened to already carry a matching mode. `respawnMonitorWindowForMode` (the phase-boundary path, called from `pipeline-runner.ts` at EVERY phase transition) is worse still and shipped ungated: `_tmuxRespawnPane` issues `respawn-pane -k`, which KILLS pane 0 of the target window unconditionally — no mode mismatch needed to arm it. `validateSessionDirOrSkip` is NOT this guard: it proves OUR sessionDir is coherent, never that the tmux target is ours. Ownership must be derived from what we already know (our own session-dir hash), not looked up. Fixtures MUST pair a session dir whose trailing hash matches the tmux name their fake `#S` advertises — production always names the tmux session `<prefix>-<session-hash>` for the dir it manages, so a fixture pairing `session` with `pickle-abc12345` exercises a layout that cannot exist. ENFORCE: `extension/tests/restart-dead-watcher-panes-sessiondir-validation.test.js` (foreign-hash and absent-from-our-data-root cases assert ZERO send-keys; the `ensureMonitorWindow` mode-mismatch cases assert ZERO kill-window against a foreign session and that our OWN stale window is still replaced; the `respawnMonitorWindowForMode (pipeline)` cases assert ZERO respawn-pane against a foreign session and that our OWN pane is still respawned — each foreign case was confirmed RED against the pre-guard body). PATTERN_SHAPE: `isForeignTmuxSession` body compares `sessionHashOf(sessionName)` to `sessionHashOf(path.basename(sessionDir))`; NO `getDataRoot()` / `readdirSync` inside it; `isForeignTmuxSession(` called in ALL THREE of `restartDeadWatcherPanes`, `ensureMonitorWindow`, and `respawnMonitorWindowForMode`. OPEN GAP: `src/lib/monitor-respawn.ts` exports a same-named `respawnMonitorWindowForMode` fork that resolves the ambient `#S` and issues its own `respawn-pane -k` with NO ownership guard. It is unreachable — zero importers in `extension/src/` (the pipeline calls the `pickle-utils.ts` one) — so it is not a live hazard, but four test files drive the fork and therefore guard nothing. Delete the fork and repoint its tests; until then, a reader who greps the export name finds the dead copy first.
- `citadel/audit-runner.ts` — INVARIANT: Citadel header state reads must call `readRecoverableJsonObject(state.json)` without pre-gating on `existsSync(state.json)` before inspecting `recoverable_phase_failure` activity. BREAKS: orphan or newer dead-owner tmp snapshots hide pickle failure headers and false-green the audit preamble. ENFORCE: `extension/tests/citadel-cross-phase-fixture.test.js`. PATTERN_SHAPE: `buildCitadelReportHeader\\(` with `existsSync\\(statePath\\)` or raw `JSON.parse\\(readFileSync\\(statePath`; INVARIANT: cross-phase artifact reads must promote recoverable `anatomy-park.json` / `szechuan-sauce.json` tmp snapshots before missing/corrupt checks. BREAKS: Citadel drops fresh replay findings or emits stale `anatomy-park:missing`. ENFORCE: `extension/tests/citadel-cross-phase-fixture.test.js`. PATTERN_SHAPE: `readPhaseFindings\\(` with `existsSync\\(artifactPath\\)` or raw `JSON.parse\\(readFileSync\\(artifactPath`.
- `citadel/trap-door-coverage-audit.ts` — INVARIANT: scoped runs emit orphan findings only for changed tests/docs, but `referencedFiles` still derives from all `CLAUDE.md`. BREAKS: repo-wide orphan noise or changed tests bypass unchanged ENFORCE docs. ENFORCE: `extension/tests/citadel/trap-door-coverage-audit.test.js`. PATTERN_SHAPE: scoped `auditTrapDoorCoverage(diff)` + repo-wide `referencedFiles`; INVARIANT: bare subsystem `ENFORCE:` refs resolve from `extension/tests/`, not repo root. BREAKS: valid trap doors false-report `orphan-enforce` and `orphan-test-file` drift. ENFORCE: `extension/tests/citadel/trap-door-coverage-audit.test.js`. PATTERN_SHAPE: `ENFORCE: \`?<name>.test.js` in subsystem `CLAUDE.md` without `extension/tests/` prefix.
- `citadel/audit-runner.ts` — INVARIANT: malformed `composes:` graphs must fail Citadel; never downgrade `parseWithComposes(...)` errors to `parsePrdMarkdown(...)`. BREAKS: bad composed PRD paths audit as false-clean root-only graphs. ENFORCE: `extension/tests/citadel-audit-runner.test.js`. PATTERN_SHAPE: `catch (err) { if (err instanceof ComposesError) parsedPrd = parsePrdMarkdown(...) }`.
- `citadel/prd-parser.ts` (walkComposeChain diamond) — INVARIANT: cycle detection MUST use a DFS recursion-path set (`onPath`, add-on-enter/remove-on-exit), NOT a cumulative `visited` set; a node reached via two branches is a benign diamond (skip via `processed`), not a cycle. BREAKS: a valid diamond compose graph throws ComposesCycleError, crashing the whole UNwrapped Citadel audit. ENFORCE: `extension/tests/citadel/prd-parser.test.js`. PATTERN_SHAPE: `onPath.add(realPath)` before recurse + `onPath.delete(realPath)` after in walkComposeChain.
- `citadel/divergence-reconciliation.ts` — INVARIANT: `changedLineEvidence` MUST try/catch its `readFileSync` and return `[]` on failure, because `reconcileDivergences` runs UNwrapped by `safeRunAnalyzer` in `audit-runner.ts`. BREAKS: one changed file unreadable from the working tree (deleted/moved post-`head`, broken symlink, perms) throws out of `buildCitadelAuditReport` and crashes the entire Citadel audit. ENFORCE: `extension/tests/citadel-divergence-reconciliation.test.js`. PATTERN_SHAPE: `readFileSync(path.join(repoRoot, file.path)` inside `changedLineEvidence` without an enclosing `try`.
- `citadel/ac-shape-audit.ts` (`readManifestText`) — INVARIANT: every session-manifest read MUST be try/catch (return `''` on failure), NOT `existsSync`-filter + `readFileSync`-map; `auditAcShape` runs UNwrapped by `safeRunAnalyzer`. `existsSync` proves existence, not readability — EACCES/EISDIR or a TOCTOU atomic-rename of `refinement_manifest.json` throws out of `buildCitadelAuditReport` and crashes the whole audit. ENFORCE: `extension/tests/citadel-ac-shape-manifest-unreadable.test.js`. PATTERN_SHAPE: `existsSync(X)` then unguarded `readFileSync/statSync/readdirSync(X)` in an UNwrapped analyzer (audit-runner unwrapped set: ac-shape, diff-hygiene `fileSize`, sibling-auth, rule-set-invariant, divergence, project-shape, diff-walker, parseWithComposes). Working-tree changed-file reads MUST be guarded; the PRD-input read (`ac-shape:72`, `audit-runner:79`) and `parseWithComposes` throw BY DESIGN (mandatory precondition, fail-Citadel).
- `citadel/diff-hygiene.ts` (`fileSize`) — INVARIANT: `fileSize` MUST try/catch `statSync` (return 0), NOT `existsSync`-then-`statSync`, because `auditDiffHygiene` runs UNwrapped by `safeRunAnalyzer`. BREAKS: a TOCTOU-removed or unstattable added file throws out of `buildCitadelAuditReport`, crashing the whole audit. ENFORCE: `extension/tests/citadel-diff-hygiene-filesize-unreadable.test.js`. PATTERN_SHAPE: `statSync(fullPath)` in `fileSize` without an enclosing `try`.
- `citadel/diff-walker.ts` (`getChangedLineRanges`/`summarizeBlame`) — INVARIANT: per-file `runGit` calls MUST pass `check=false` (fail soft to `''`), NOT the default `check=true`, because `walkDiff` runs UNwrapped by `safeRunAnalyzer` (audit-runner.ts:84) and its output feeds every downstream analyzer. `getDiffFiles` (the aggregate diff) stays `check=true` BY DESIGN as the mandatory precondition, like `parseWithComposes`. BREAKS: a gitlink/submodule pointer or type-change in the diff makes per-file `git blame`/`git diff` exit non-zero ("fatal: no such path"); an unguarded throw crashes the entire Citadel audit over one un-blameable file. ENFORCE: `extension/tests/citadel-diff-walker-gitlink-unblameable.test.js`. PATTERN_SHAPE: per-file `runGit([...], repoRoot)` inside `walkDiff` helpers without the trailing `false` check arg.
- `citadel/pattern-conformance-audit.ts` (R-PCPS PATTERN_SHAPE arm subtracted) — INVARIANT: this analyzer emits SQL-clobber findings ONLY; it MUST NOT grep trap-door `PATTERN_SHAPE:` declarations as required literals. BREAKS: PATTERN_SHAPE is LLM-authored prose carrying negative assertions, shell commands and cross-file symbols; requiring it PRESENT inverts every MUST-NOT guard (green only once a shipped bug is reintroduced) and reports literally-present code as absent, because presence was tested regex-first — `(` and `.` in `if (counterNext.halt)` parse as a group and a wildcard, so the pattern cannot match itself. It emitted 41/41 false High findings on a clean tree. Curated enforcement lives in `extension/scripts/audit-trap-door-enforcement.sh` (release gate) and `citadel/trap-door-coverage-audit.ts`, which escapes anchors before embedding them. ENFORCE: `extension/tests/citadel/pattern-conformance-audit.test.js`. PATTERN_SHAPE: `findPatternShapeViolations` and `extractPatternShapes` ABSENT from `pattern-conformance-audit.ts`; (SQL-clobber list scan) — INVARIANT: the `ON CONFLICT` detector splits the WHOLE `DO UPDATE SET` list on top-level commas and tests EVERY assignment. BREAKS: anchoring `\w+\s*=` to `DO UPDATE SET` pins the match to column 1, so a correct `EXCLUDED.<col>` in position 1 false-GREENs every clobber in columns 2..N — the canonical upsert shape (propagate most columns, clobber one). ENFORCE: `extension/tests/citadel/pattern-conformance-audit.test.js`. PATTERN_SHAPE: `splitTopLevelCommas(` applied to the `ON_CONFLICT_SET_RE` capture; no `DO\s+UPDATE\s+SET\s+\w+\s*=` anchor in `pattern-conformance-audit.ts`; (SQL-clobber RHS is matched POSITIVELY) — INVARIANT: `CLOBBER_ASSIGN_RE` matches the assignment's RHS against the literal-CONSTANT set (`CLOBBER_LITERAL_RHS`: quoted string / number / `NULL` / `TRUE` / `FALSE`), with a trailing `(?![\w.(])` boundary so `NULL` cannot match the head of `NULLIF(`. It MUST NOT go back to testing the RHS negatively (`(?!\s*EXCLUDED\.)\S` — "anything that is not `EXCLUDED.<col>`"). The two are not the same set, and the difference is EVERY function-valued assignment: a clobber DISCARDS the incoming row's value, while `NOW()` / `CURRENT_TIMESTAMP` COMPUTE one. This is the column-scan's twin — one half picks the columns, the other picks which assignments count, and widening either one is what manufactures false Highs. BREAKS: the negative form fired a High on `updated_at = NOW()` — the single most common CORRECT upsert idiom — carrying advice (*"use `EXCLUDED.updated_at` instead"*) that is precisely what you must NOT write there; stamping the update time is the entire point of `NOW()`. It reached every changed `.sql` in any target repo, and it is the same false-High class the R-PCPS subtraction above deleted the `PATTERN_SHAPE` arm for ("41/41 false High findings on a clean tree"). Real clobbers in columns 2..N are still caught — narrowing the RHS does not blind the scan. ENFORCE: `extension/tests/citadel/pattern-conformance-audit.test.js` (the `function-valued RHS is not a const clobber:` cases + `narrowing the RHS does not blind the scan`; all confirmed RED against the pre-fix body). PATTERN_SHAPE: `CLOBBER_LITERAL_RHS` present and consumed by `CLOBBER_ASSIGN_RE`; no `(?!\s*EXCLUDED\.)` in `pattern-conformance-audit.ts`.
- `convergence-gate.ts` — INVARIANT: test-script safety must inspect delegated leaf commands before blocking `integration`-named wrappers. BREAKS: `npm test` chains like `test -> test:integration -> node bin/test-runner.js` false-green by skipping tests entirely. ENFORCE: `extension/tests/services/convergence-gate-test-safety.test.js`. PATTERN_SHAPE: raw `scripts.test` regex block without recursive `npm|pnpm|yarn run <script>` leaf resolution.
- `convergence-gate.ts`; `../bin/spawn-morty.ts` (AP-EXT-ITER14-01 one spawn-safety classifier) — INVARIANT: both consumers of `data/gate-commands.json` that SPAWN a project's `test` command ask the exported `classifyTestScriptSafety` first — `runGate` via `canRunTestScript`, the off-repo worker gate via `runOffRepoTestDimension`. A refused script is `not_run`. BREAKS: `isUnrunnableCheckResult` classifies a command that ALREADY ran, so it cannot stop an `e2e`/`playwright`/`hardhat` leaf launching browsers and real services once per ticket on an arbitrary target repo. ENFORCE: extension/tests/worker-gate-offrepo-runs.test.js (`an unsafe (smoke/e2e-class) target test script is NOT spawned`, `the spawn decision uses the convergence gate ONE classifier`). PATTERN_SHAPE: a `commands.test`/`cmdMap[...tests]` spawn not dominated by a `classifyTestScriptSafety(` call; or `playwright|cypress|hardhat` DECLARED outside `convergence-gate.ts` — count only NON-comment lines. `spawn-morty.ts`'s own JSDoc names those tokens in prose, so a bare grep counts ITSELF (returns 1, not 0) and reads as a phantom violation; strip `//` and `/* */` first, as the ENFORCE test does. The sole real declaration is `UNSAFE_TEST_SCRIPT_REGEX` in `convergence-gate.ts`.
- `convergence-gate.ts`; `../bin/spawn-morty.ts` (AP-EXT-ITER26-01 ONE reader of the gate-command map) — INVARIANT: `data/gate-commands.json` is READ in exactly one place — the exported `loadGateCommands` in `convergence-gate.ts`, which owns the table AND its module-relative path resolution. The off-repo worker gate consumes it by IMPORT, never by re-resolving the path itself. Only the read is shared: each consumer keeps its own failure POLICY — `runGate` fails fast (`GateError('GATE_COMMANDS_UNREADABLE')`, naming the resolved path), while `loadWorkerGateCommands` catches and degrades to `{}` so no project resolves and the gate reports `not_run`, never a synthesized green. That split is the point; do not "unify" it by pushing either policy into the loader. BREAKS: a second loader hand-copies the `path.resolve(__dirname, '../data/…')` depth assumption, so `bin/` and `services/` silently disagree the moment either file changes depth — and the pre-collapse pair had ALREADY diverged on failure semantics (one threw bare with no path in the message, one swallowed to `{}`), so one unreadable map produced a crash on the gate path and a silent skip on the worker path. The sibling AP-EXT-ITER14-01 above forbids a second unsafe-script CLASSIFIER for the same reason; this forbids the second READER. ENFORCE: extension/tests/worker-gate-offrepo-runs.test.js (`no per-stack adapter matrix` — both assertions mutation-verified RED: forking `loadGateCommands` out of the import, and re-introducing a hand-rolled second reader; zero collateral). PATTERN_SHAPE: `loadGateCommands` present in the `../services/convergence-gate.js` import in `spawn-morty.ts`; and, after stripping comments, ZERO `readFileSync(…gate-commands.json)` on the gate path. Count only NON-comment lines and do NOT anchor on the bare filename — `spawn-morty.ts` and `mux-runner.ts` both name `gate-commands.json` in PROSE, so a filename grep is satisfied by a comment and reads GREEN with every real reader deleted (the AP-EXT-ITER15-01 self-matching mode; that is exactly how the pre-collapse assertion had to be retargeted).
- `convergence-gate.ts` (R-FGNC Finding #18 gate classifier) — INVARIANT: `buildFailures` uses the subprocess EXIT CODE as the sole pass/fail signal (`exitCode === 0` → zero failures, regardless of stderr); it scans BOTH stdout+stderr combined (never `stderr || stdout` — real tsc/eslint errors land on stdout while pnpm env-noise lands on stderr) and runs `stripEnvNoise` to drop pnpm `WARN  Issue while reading "<file>"` config-read lines BEFORE failure-line parsing. BREAKS: a pnpm `.npmrc` `${TOKEN}` WARN on stderr masks the real TS/lint failures, the classifier reports only the benign WARN, and the remediator exhausts its cap on noise while real toolchain debt ships silently. ENFORCE: `extension/tests/services/convergence-gate-env-noise.test.js`. PATTERN_SHAPE: `stripEnvNoise|ENV_NOISE_WARN_RE`.
- `convergence-gate.ts` (AP-EXT-ITER20-01 ONE failure identity — the ordinal and the fingerprint share it) — INVARIANT: `assignOccurrenceIndices`' grouping key and `buildFingerprint` BOTH derive from `failureIdentityKey`, which carries `check`. BREAKS: `buildFailures`' unparsed fallback keys every check identically (`file: pkgDir`, `ruleOrCode: String(exitCode)`, `line: 0`) and `tests` has NO granular parser, so it always takes that shape when red — a repo whose suite is red at baseline capture has a coarse `tests` entry standing by to absorb the phase's first coarse `typecheck`/`lint` failure, and the gate reports GREEN over it (measured live against the shipped export: `new_failures_vs_baseline: 0`; the `<timeout>`/`GATE_CHECK_TIMEOUT` failure collides the same way). An occurrence index is only meaningful inside the identity space it is counted in, so a fingerprint the grouping does not share is the second half of the bug, not a separate one: cross-check grouping handed occurrence 0 to the NEW typecheck failure (subtracted) and 1 to the unchanged tests failure (reported) — red for the wrong reason, at the wrong check. Do NOT try to discriminate coarse failures by `message`: it carries run-to-run counts and timings, so every baselined red check would re-report as new. ENFORCE: extension/tests/convergence-gate-skip-not-pass.test.js (AP-EXT-ITER20-01 block — 4/4 mutation-RED, the pre-existing-still-subtracted control stays GREEN). PATTERN_SHAPE: `failureIdentityKey(` occurs 3× in `convergence-gate.ts` — its definition plus a call inside EACH of `buildFingerprint` and `assignOccurrenceIndices`; and `grep -c 'f\.file}::\${f\.ruleOrCode}'` on that file is 1 and equals `grep -c 'f\.check}::\${f\.file}::\${f\.ruleOrCode}'` — i.e. the only key literal in the file is the check-qualified one. Do NOT word this anchor as "zero `${f.file}::${f.ruleOrCode}` literals": the qualified key CONTAINS that substring, so the zero form reads as a phantom violation against its own fix (the documented self-matching falsification mode). CLOSED (AP-EXT-ITER15-01, below): `subtractBaseline`'s `selfGuard` is now armed at `resolveBaselineResult`. ACCEPTED COST: a `gate/baseline.json` written by the PRE-fix build stores cross-check ordinals, so if it holds coarse failures from ≥2 checks in one package the first post-deploy iteration can report the pre-existing one as new (one-iteration false red). Bounded — baselines are session-scoped and recaptured on staleness, and that state is exactly where the gate was already mis-attributing. Sibling doc-drift, out of fence: `tests/services/convergence-gate-baseline.test.js:29`'s test NAME still says "per (file, ruleOrCode)"; its fixtures are single-check so it passes unchanged.
- `convergence-gate.ts` (AP-EXT-ITER15-01 the no-disown guard is ARMED at its one production call site) — INVARIANT: `resolveBaselineResult` passes `buildNoDisownContext(opts)` as `subtractBaseline`'s third argument, deriving the R-ORSR-6 context from `opts.since` — the phase's own diff base, which the per-iteration gate already carries (`runChangedPerIterationGate` passes `preIterSha`). Absent `since`, or a diff with neither changed files nor changed exported symbols, yields `undefined`, i.e. byte-identical pre-guard subtraction — the guard can only ever KEEP a failure the iteration caused, never invent one. BREAKS: shipped with `selfGuard` optional and passed by ZERO production callers, so a failure the iteration's own diff introduced was disowned by any coincidental baseline fingerprint match — reachable in practice via the coarse-failure collision of AP-EXT-ITER20-01 above. The whole-repo `microverse-runner.ts:runInterfaceChangeSweep` is convergence-time only and does NOT cover the per-iteration gate. Every pre-existing case drives `subtractBaseline` DIRECTLY with a hand-built context, so all five stayed GREEN with the wiring absent: cover the WIRING seam (drive `runGate` against a real repo, assert the gate STATUS), never the helper alone. ENFORCE: extension/tests/convergence-gate-no-disown-wiring.test.js (mutation-verified RED on removing the third argument; the genuinely-pre-existing control stays GREEN so no false red is fabricated). PATTERN_SHAPE: `grep -c "buildNoDisownContext(" extension/src/services/convergence-gate.ts` == 2 — its definition plus the single `subtractBaseline` call site. Do NOT anchor on `buildNoDisownContext(opts)`: the definition reads `(opts: RunGateOpts)`, so that form counts 1 and reads as a phantom violation against its own fix.
- `convergence-gate.ts` (AP-EXT-ITER8-02 ONE path space for the no-disown file axis) — INVARIANT: `changedFiles` holds git's REPO-ROOT-relative spelling, so `NoDisownContext.workingDir` is the repo root: `buildNoDisownContext` walks `opts.workingDir` up `--show-prefix`'s depth, never `--show-toplevel` (realpath-resolved — under a symlinked root `path.relative` escapes to `../..`). ONE membership test, via `changedFileKey`. BREAKS: R-SZGB-A rewrites `opts.workingDir` to a package dir one level down (this repo: no root marker, `extension/` below), so the file axis never matches and a failure in a file THIS iteration changed is disowned as pre-existing — fake green. Measured: nested root GREEN, flat root RED, one fixture. ENFORCE: extension/tests/convergence-gate-no-disown-wiring.test.js (AP-EXT-ITER8-02; both mutations RED, controls GREEN). PATTERN_SHAPE: `--show-prefix` present, `--show-toplevel` absent, `changedFiles.has(` exactly once. CLOSED (AP-EXT-ITER34-01, below): the same shape in `selectWorkspaceTargetDirs` now resolves against the repo root too.
- `convergence-gate.ts` (AP-EXT-ITER34-01 ONE path space for the workspace target selection) — INVARIANT: `selectWorkspaceTargetDirs` resolves BOTH of its repo-root-relative inputs — `getChangedSince`'s output and `opts.allowedPaths` (`scope.json:allowed_paths`) — against `resolveLexicalRepoRoot(opts.workingDir)`, never against `opts.workingDir` itself. Target dirs stay ABSOLUTE, so the package-dir spelling never reaches what gets run. BREAKS: R-SZGB-A rewrites `opts.workingDir` to a package dir one level down, so on a monorepo whose workspace root sits below the git root every candidate filters out and the gate runs ZERO checks — `status: green`, `total_raw_failure_count: 0`, and NO `gate_skipped`, i.e. indistinguishable from an executed pass. Measured live on one fixture: nested root GREEN over a real failing package, flat root RED, both axes independently (changed-files and allowedPaths). Sibling AP-EXT-ITER8-02 above is the same root cause on the no-disown file axis. NOT matches, do not "fix" them: `getWorkspacePackages`/`resolveWorkspaceGlobs` and `detectProjectType` read markers and globs DECLARED at `workingDir`, so the package-dir base is correct there by construction; `applyFlakeFilter` mixes spaces too but has no production producer for `known_flake_files` and fails CLOSED. ENFORCE: extension/tests/convergence-gate-skip-not-pass.test.js (AP-EXT-ITER34-01 block — this file's thesis exactly: a gate that runs NOTHING must not report an executed green. 3/4 cases mutation-RED under the full pre-fix revert, zero collateral across the 8 pre-existing cases; the 4th is the narrowing control that stays GREEN both ways, so a fix cannot pass by simply running every package. Do NOT re-home these in tests/services/convergence-gate-workspaces.test.js: every fixture there is flat-rooted, which is the blind spot itself.) PATTERN_SHAPE: inside `selectWorkspaceTargetDirs`, `path.resolve(`/`path.relative(` whose base is `opts.workingDir` rather than a `resolveLexicalRepoRoot` result — expect exactly 3 `repoRoot` bases and ZERO `opts.workingDir` bases there. Do NOT anchor on a whole-file count of `opts.workingDir`: it is the correct argument for every git `cwd:`, for `getChangedSince`, and for the non-workspace `targetDirs` arm, so a file-wide zero reads as a phantom violation against its own fix. OPEN GAP, same shape, fence-blocked: `bin/finalize-gate.ts:90 splitByScope` relativizes absolute gate failures against `state.working_dir`, then matches them against `allowed_paths` — which `bin/resolve-scope.ts:16` (R-RSBI-2) anchors at `--show-toplevel` REGARDLESS of which working_dir invoked it, and nothing asserts the two agree. Measured live against the shipped `filterByScope`, one failure and one fence: in-scope from the git root, OUT of scope from one level down — and an empty `inScope` prints `all failures are out-of-scope` and EXITS 0 over a red finalize gate, worse than the workspace case because it needs no monorepo. `extension/src/bin/finalize-gate.ts` sits outside this session's `scope.json` (the AP-EXT-ITER20-02 forced-deferral shape). NOT matches: `microverse-runner.ts:3163`/`:3945` feed `filterByScope` from `git status --porcelain -z`, repo-root-relative by contract (documented at `microverse-runner.ts:477`).
- `microverse-state.ts` — INVARIANT: `violation_ledger` is the live judge-visible set, so each full judge result must replace it and drop resolved entries. BREAKS: stale prior violations suppress re-reported regressions and skew set-op classification. ENFORCE: `extension/tests/microverse-helpers.test.js` resolved-ledger regression. PATTERN_SHAPE: `updateViolationLedger(...)` mutates/appends without rebuilding from current `judgeResult.violations`; (AP-EXT-ITER22-01 an EMPTY prior ledger is NOT prior context) — INVARIANT: `compareMetric` gates the set-ops branch on `hasPriorLedgerContext` — a non-empty CURRENT `resolved` (the judge can only resolve an id it was shown, so that is positive proof of prior context) ELSE a POPULATED `previousLedger` via `hasLedgerContext` — never on bare `previousLedger !== undefined` (presence). The `resolved`-first arm is load-bearing: `microverse-llm-judge-non-determinism-recovery.test.js` R-SLLJ-8 case (b) pins set-ops on a `resolved`-carrying snapshot whose `previousLedger` fixture is EMPTY. Do NOT widen that arm to `remaining`: `microverse-state-iteration-regressions.test.js` pins the half-ledger fallback on a `remaining`-carrying snapshot with NO previous snapshot, and widening routes it into set-ops. Both pins sit outside the anatomy scope fence, so a fix here is verified by RUNNING them, never by editing them. `microverse-runner.ts:3544` builds `previousLedger` from `state.violation_ledger`, which is empty until the first `updateViolationLedger` — whose ONLY callsite is the very next line, so the baseline never populates it. The count-vs-score arm below it IS the intended no-prior-ledger handler; a presence gate makes it unreachable, since both snapshots are assigned together in the same `shape === 'full'` block. `hasLedgerContext` is total (non-array fields answer false), which is what let it replace both arms' `try/catch` fall-throughs rather than add a third guard. BREAKS: the judge's own contract (`buildJudgePrompt`: "when there is no such list, `resolved` and `remaining` are `[]` and every id goes in `new`") makes a first pass read as `new > resolved` -> 'regressed' HOWEVER much the score improved — firing `guardedMicroverseRollback`, writing a false "score dropped from X to Y" into `failed_approaches` (which the next worker is handed as a denylist), and incrementing `stall_counter` toward convergence-by-stall. Work destruction is averted only downstream, by `wouldResetOrphanCommit` preserving a HEAD the worker already committed to — do not read that guard as making the classification harmless. The extension/CLAUDE.md R-SLLJ-4 entry ALREADY said "populated"; the doc was right and the code silently disagreed, so re-derive this gate from the ledger contents, never from the parameter's presence. ENFORCE: extension/tests/microverse-convergence.test.js (AP-EXT-ITER22-01 block — the presence-gate and the dropped-`Array.isArray` mutations are each RED on exactly one case, zero collateral). PATTERN_SHAPE: `hasPriorLedgerContext(currentLedger, previousLedger)` dominating `compareMetricSetOps(` in `compareMetric`, with `current.resolved.length > 0` as its first arm and `hasLedgerContext(previous)` (never `previous !== undefined`) as its fallback; `grep -c 'previousLedger !== undefined' microverse-state.ts` == 0; and NO `try {` inside `compareMetric`'s own body — scope that one to the function, since the file legitimately carries two unrelated `try` blocks (`readCommandTemplate`, `readMicroverseState`) and a whole-file `== 0` anchor reads as a phantom violation on sight.
- `bundle-state-integrity.ts` — INVARIANT: `statePathsForBundle` must include `microverse_*` children when `state.json` exists only as a newer recoverable `state.json.tmp.*`, not just when the base file exists. BREAKS: AC-BUNDLE-03 skips child relaunch-cap violations and false-greens exhausted bundles. ENFORCE: `extension/tests/pipeline-runner.test.js` tmp-only microverse relaunch-cap coverage. PATTERN_SHAPE: `entry.name.startsWith('microverse_')` with child enrollment gated only by `existsSync(childStatePath)`.
- `jar-utils.ts` — INVARIANT: `addToJar` must treat `state.json.tmp.*` as live session state before any "not found" preflight. BREAKS: tmp-only recoverable sessions cannot be jarred, so metadata/deactivation never happen. ENFORCE: `extension/tests/jar-utils.test.js`. PATTERN_SHAPE: `existsSync(statePath)` guard before `StateManager.read(statePath)` in `addToJar`.
- `src/services/pickle-utils.ts`; `src/lib/monitor-respawn.ts`; `src/bin/monitor.ts` (R-MMRT-1/R-MMRT-2 sessionDir validation) — INVARIANT: `restartDeadWatcherPanes(sessionDir, ...)` and `respawnMonitorWindowForMode(sessionDir, ...)` MUST call `validateSessionDirOrSkip(sessionDir, caller)` BEFORE any tmux send-keys/respawn-pane; `startRespawnWatchdog({ sessionDir })` MUST call `validateSessionDirOrSkip(opts.sessionDir, 'startRespawnWatchdog')` BEFORE scheduling. Invalid sessionDir (empty/non-string, path missing, `state.json` absent, or `path.resolve(state.session_dir) !== path.resolve(sessionDir)`) produces zero spawns and one `monitor_respawn_session_dir_invalid` event per (caller, sessionDir, reason) tuple per session (deduped via `_sessionDirInvalidEmitted`). `startRespawnWatchdog` returns `null` on fail. BREAKS: stale/injected `sessionDir` reaches `tmux respawn-pane`/`send-keys`, hits wrong window, corrupts pipelines. ENFORCE: `extension/tests/restart-dead-watcher-panes-sessiondir-validation.test.js`, `extension/tests/monitor-watchdog.test.js`, `extension/tests/integration/monitor-respawn-sessiondir-cascade.test.js`. PATTERN_SHAPE: `validateSessionDirOrSkip(sessionDir` or `validateSessionDirOrSkip(opts.sessionDir` at function top before any tmux spawn; `monitor_respawn_session_dir_invalid` emitted with `gate_payload: { caller, sessionDir, reason }`.
- `scope-resolver.ts` (scope.json orphan-tmp recovery is delegated, never hand-rolled) — INVARIANT: `refreshScope` reads `scope.json` via `readRecoverableJsonObject`, the ONE recovery primitive; it MUST NOT re-implement the tmp scan. The deleted local scan re-forked both halves of the delete rule: it unlinked from a `catch` that also wrapped the tmp's own `readFileSync` (an EACCES tmp read as garbage) and discarded on `mtimeMs <= baseMtimeMs` (killing the equal-mtime tmp that R-CIFB-B says WINS). BREAKS: a killed initial scope writer leaves the only valid scope in a sibling `.tmp.<pid>`; a hand-rolled scan destroys it, and the session loses its fence. ENFORCE: extension/tests/verify-recapture-fired.test.js (`scope.json orphan tmp:` unreadable-preserved + equal-mtime-promoted). PATTERN_SHAPE: `readRecoverableJsonObject(scopePath)` in `refreshScope`; no `readdirSync` + `\.tmp\.(\d+)` scan in `scope-resolver.ts`. CLOSED by AP-EXT-ITER40-01 (see `../bin/CLAUDE.md`): `check-scope-diff.ts` raw-read `scope.json` (`existsSync` + `JSON.parse`), so a tmp-only scope read as `no_scope` at exit 0 — a FAIL-OPEN no-op of the worker's pre-commit fence. It now resolves `allowed_paths` through `readRecoverableJsonObject` and decides absence below that read. ENFORCE: extension/tests/check-scope-diff-preflight.test.js#AP-EXT-ITER40-01.
- `scope-resolver.ts` (AP-EXT-ITER21-01 / AP-EXT-ITER24-01 ONE diff, TWO git contracts — `numstat -` means UNDIFFABLE, not binary — LANDED) — INVARIANT: `computeAllowedFromDiff`'s exclusion probe reads the diff through the SAME contract as the enumeration it subtracts from — `listUndiffablePaths` runs `--numstat -M100 -z`, matching `getDiffFiles`' `--name-status -M100 -z` — and a repo-DECLARED `-diff` is NOT git's binary DETECTION: `getDeclaredNoDiffPaths` (`git check-attr -z diff`) removes declared paths from the exclusion set, so ONLY auto-detected binary is fenced out. A declared-`-diff` BINARY is therefore admitted too; that is deliberate and fail-SAFE — the fence is permissive, and a file that IS in the diff being editable is correct, whereas under-exclusion FATALs. `-z` is the load-bearing half of the argv (it fixes both the C-quoted non-ASCII path and the combined `old.bin => new.bin` rename form); `-M100` is contract PARITY only and is pinned by no test — mutation-verified, dropping it alone reddens nothing. BREAKS: a TEXT source file the target repo marks `-diff`/`binary` in `.gitattributes` — the routine lockfile / generated / snapshot shape — is dropped from `allowed_paths` while sitting in the branch diff, so `check-scope-diff` refuses the worker's commit (`worker_edit_outside_scope`) over a file the diff CONTAINS, and `filterBySubsystem` silently drops a subsystem whose only changed files carry the attribute. A branch whose changes are ALL so marked resolves to an EMPTY fence: `resolveScope` throws `SCOPE_EMPTY_DIFF` ("No files changed between …") and `refreshScope` throws `SCOPE_EMPTY_POST_BUILD` ("the build phase produced no review surface") — both factually false, the latter FATAL at the anatomy-park boundary via `refreshPhaseScope`'s `throwOnEmptyScope`. The same un-`-z`/un-`-M100` contract also makes a RENAMED binary unmatchable (enumeration yields `new.bin`; the probe captures the combined `old.bin => new.bin`), so it under-excludes exactly what it was written for. Measured live against the shipped `services/scope-resolver.js`, not cited: a 2-file branch fenced to `["src.ts"]`, and the generated-only branch threw `SCOPE_EMPTY_DIFF`. Do NOT widen the capture regex — "what changed" and "what is undiffable" must come from one contract, and `-diff` TEXT belongs in the fence. This repo tracks no `.gitattributes`, so it is not self-exposed; the exposure is repo-agnostic operation against target repos. Assert the RESOLVED FENCE (`resolveScope(...).allowed_paths`), never the probe's return — the probe was always internally consistent; it was consistent with the wrong contract. ENFORCE: extension/tests/compute-review-base.test.js (`AP-EXT-ITER24-01` block — 4 cases: declared-`-diff` TEXT kept WITH a genuine-binary control, all-`-diff` branch resolving instead of throwing, non-ASCII binary still excluded, renamed binary excluded by its POST-rename name; mutation-verified — dropping the carve-out → 2 RED, dropping `-z` → 2 RED, mis-picking the rename path → 1 RED, zero collateral). PATTERN_SHAPE: `listUndiffablePaths`' `runGit(['diff', '--numstat', '-M100', '-z', …])` argv carries `-z`, and `getBinaryPathSet` subtracts `getDeclaredNoDiffPaths(` from it. NOTE ON THE HOST: those cases live in `compute-review-base.test.js` because every canonical `scope-*.test.js` is OUTSIDE this session's `scope.json:allowed_paths`; that file is the in-fence scope-resolver unit file with real-git fixtures. Move them to `scope-resolver.test.js` when a fence carries it — do NOT read their location as evidence they are about `computeReviewBase`.
- `../bin/check-scope-diff.ts` (AP-EXT-ITER24-02 the fence's OWN reader has the same two-contract split) — INVARIANT: `getStagedPaths` must read staged paths through the same contract that produced `allowed_paths`. It does NOT: it runs `git diff --staged --name-only --no-renames` with NO `-z`, while `allowed_paths` is built from `--name-status -M100 -z`. `core.quotePath` defaults ON, so git C-quotes any non-ASCII path and the reader yields `"caf\303\251.ts"` — a literal-quote-wrapped, backslash-escaped string that matches NOTHING in `allowed_paths`. BREAKS: a worker staging an in-scope file whose path carries any non-ASCII byte is told it is OUT of scope — `checkScopeDiff` returns `outside_scope`, the Phase-2 preflight refuses the commit, and `worker_edit_outside_scope` fires over a file the fence EXPLICITLY allows. Measured live against the shipped `bin/check-scope-diff.js`, not cited: `allowed_paths: ['café.ts','seed.txt']` with `café.ts` staged returned `status: 'outside_scope', staged_paths_outside_scope: ['"caf\\303\\251.ts"']`. Same shape and same harm class as AP-EXT-ITER24-01 — a path set compared against a diff enumeration read through a DIFFERENT git contract. The sibling producer `microverse-runner.ts:auditPostIterationScope` (`--name-only --no-renames`) shares the defect but is observability-only, so it mis-reports rather than blocks. Fix by adding `-z` and splitting on NUL (or setting `-c core.quotePath=false`), NOT by un-quoting in JS. CLOSED by AP-EXT-ITER31-01 (see `src/bin/CLAUDE.md`) — `getStagedPaths` now carries `-z`, and the same pass fixed the two sibling readers (`convergence-gate.ts:getChangedSince`, `microverse-runner.ts:listCommittedFilesInRange`). The iteration-24 deferral was FORCED by that session's fence, not chosen; this session ran unscoped. ENFORCE: extension/tests/check-scope-diff-preflight.test.js (`AP-EXT-ITER31-01` block). PATTERN_SHAPE: a `--name-only` argv whose output is compared against `scope.json:allowed_paths` without `-z`.
- `recoverable-json.ts` (AP-EXT-ITER29-01 delete authority is read-success) — INVARIANT: `readJsonObjectFile` judges read and parse separately: any `readFileSync` throw is `unreadable` (preserved); only a successful read of unparseable/non-object bytes is `invalid`, the one input `parseDeadTmp` unlinks on. No errno allowlist. BREAKS: `74f24aa8` listed {EACCES,EPERM,EISDIR}, so EMFILE/EIO/ELOOP still deleted a valid snapshot; under fd exhaustion base and tmp both throw and the session's only state is destroyed by the recovery primitive. ENFORCE: extension/tests/recoverable-json.test.js. PATTERN_SHAPE: bare `catch { return { kind: 'unreadable' }; }` around `readFileSync` in `readJsonObjectFile`; zero `err.code` in `recoverable-json.ts`. Assert survival via `lstatSync` — `existsSync` follows a symlink loop and reports false.
- `orphan-reaper.ts` (AP-EXT-ITER30-01 an unreadable state.json is not an absent one) — INVARIANT: `readOwningSessionState` returns three states — `missing` / `unaccountable` / `state` — and `isOwningSessionLive` treats `unaccountable` as LIVE. Only a positive ENOENT/ENOTDIR proves absence; every other errno, and an unparseable base, spares the proc. BREAKS: `readRecoverableJsonObject` collapses "no such file" and "the read threw" (EMFILE/ENFILE/EIO/EACCES/EISDIR — the c=8 fd-exhaustion class of AP-EXT-ITER29-01) into one `null`, and the pre-fix `if (!state) return false` read that as death, then group SIGTERM→SIGKILLed a LIVE sibling pipeline's worker mid-build — the one thing this file's positive-ownership trap door says can never happen. Assert the KILLS, not the return shape: the pre-fix call also returned `{scanned:1}`, with the proc dead. ENFORCE: extension/tests/orphan-worker-reaper.test.js (`AP-EXT-ITER30-01` — EISDIR fixture, uid-independent so it cannot silently pass as root, plus the unparseable case; mutation-verified RED on `unaccountable → false`, and the missing-state.json reap control stays GREEN). PATTERN_SHAPE: `if (read.kind === 'unaccountable') return true;` in `isOwningSessionLive`; no `if (!state) return false` there.
- `recoverable-json.ts` — INVARIANT: `readRecoverableJsonObject(path)` is the sole JSON recovery primitive for files that may have `.tmp.<pid>` partial writes; direct `fs.readFileSync` + `JSON.parse` at sites that read mutable session artifacts (state.json, microverse.json, anatomy-park.json, pipeline-status.json, scope.json, settings) is forbidden because it bypasses orphan-tmp promotion and dead-pid demotion. BREAKS: interrupted writes produce corrupted reads, the runtime ignores valid recoverable snapshots, and pipelines silently strand on stale base files while a newer `.tmp.<pid>` sibling holds the live state. ENFORCE: `extension/tests/get-session.test.js`, `extension/tests/resolve-state.test.js`, `extension/tests/state-manager.test.js`. PATTERN_SHAPE: `readRecoverableJsonObject(...)` at every mutable-session-artifact read site; no `JSON.parse(fs.readFileSync(...))` in services that read those artifacts.
- `promise-tokens.ts` — INVARIANT: `scrubForbiddenWorkerTokens(text)` must be applied to all worker output before promise-token detection; `FORBIDDEN_WORKER_TOKENS` is the authoritative list of orchestrator-only tokens (`EPIC_COMPLETED`, etc.) that workers must never emit, and `PROMISE_TOKENS` is the authoritative detected-token set. BREAKS: orchestrator-only tokens emitted by workers escape detection, falsely advance the lifecycle (e.g. a worker accidentally emitting `EPIC_COMPLETED` would short-circuit the manager loop), and corrupt session forensics. ENFORCE: `extension/tests/activity-event-payload.test.js`, `extension/tests/promise-tokens.test.js`. PATTERN_SHAPE: `scrubForbiddenWorkerTokens(` invoked on every worker stdout/stderr stream before classifier/promise-token detection paths.
- `pickle-utils.ts` (R-MWCL-3 collapsed-layout-repair) — INVARIANT: when `readPaneCurrentCommand` returns `null` for any watcher pane, `restartDeadWatcherPanes` MUST first probe `tmux list-panes -t <session>:monitor`; on probe failure (window entirely gone), it MUST call `ensureMonitorWindowFn` (injectable, defaults to `ensureMonitorWindow`) and return without any `split-window`; on probe success (window survives), `recreateMissingWatcherPane` MUST issue split-window then send-keys then `select-layout tiled`, and every repair log line MUST contain the literal tag `collapsed-layout-repair`. BREAKS: when `remain-on-exit off` removes a dead pane entirely (collapsed layout), pane repair silently skips with no `select-layout` and no escalation when the whole window is gone — monitor layout stays corrupted for the rest of the pipeline lifetime. ENFORCE: `extension/tests/pickle-utils-restart-dead-watcher-panes-collapsed.test.js`. PATTERN_SHAPE: `list-panes` probe before `recreateMissingWatcherPane`; `select-layout tiled` after successful `send-keys`; `collapsed-layout-repair` in every repair log line; `ensureMonitorWindowFn` parameter on `restartDeadWatcherPanes`.
- `pickle-utils.ts`; `src/bin/monitor.ts` (R-MWCL bundle — monitor spawn-mode + collapsed-layout repair) — INVARIANT 1: `ensureMonitorWindow` MUST infer monitor mode from `state.command_template` when no explicit mode argument is given; defaulting to `pickle` for non-pickle templates causes immediate `render()` crashes and full monitor death. INVARIANT 2: `restartDeadWatcherPanes` MUST handle the case where a target pane index has been collapsed away by tmux (`readPaneCurrentCommand === null`) by creating a new pane via `tmux split-window`, not by logging-and-continuing. INVARIANT 3: `render()` in `monitor.js` MUST treat mode-mismatch errors as recoverable (`{ active: false }` + retry) rather than `process.exit(2)`. BREAKS: inferred-mode regression silently routes non-pickle pipelines into the pickle layout, crashing the dashboard for anatomy-park and szechuan-sauce phases; collapsed pane layouts persist for the rest of the pipeline lifetime without `split-window` fallback; mode-mismatch kills the entire monitor dashboard irreversibly instead of recovering. ENFORCE: extension/tests/integration/monitor-collapsed-layout-respawn.test.js (introduced by ticket ce1642b1). PATTERN_SHAPE: `ensureMonitorWindow\(.*['\"]pickle['\"]` called with a non-pickle `state.command_template`; `if \(currentCommand === null\) \{[\s\S]*continue;` in `restartDeadWatcherPanes` without a `tmux split-window` fallback above it; `setInterval\(tick, 30_000\)` in `startRespawnWatchdog` without a synchronous `tick()` invocation immediately following.
- `state-manager.ts` (R-POD — paused-orphan demotion AND-vs-OR predicate) — INVARIANT: `getPausedOrphanDemotion` MUST require BOTH `ageMs >= 300_000` AND `deadMappedPid` (`shouldDemote: ageMs >= 300_000 && deadMappedPid`). The 5-minute bound is an INLINE literal — there is no `STATE_STALE_THRESHOLD_MS` constant anywhere in `src/`, so do not re-introduce that name here: grepping it returns nothing and reads as if the guard were deleted. Either condition alone is insufficient signal to demote a session — fresh state with a stale session-map PID is a healthy session whose launch-shell PID has merely rolled over, and stale state with a live mapped PID is a slow but still-owned session. BREAKS: reverting `&&` to `||` demotes healthy sessions on PID rollover (fresh state, stale map) or keeps orphaned sessions active indefinitely (stale state, live map). ENFORCE: `extension/tests/services/resolve-state-paused-orphan.test.js` covers all 4 cells of the `(age, mapped-pid)` matrix. PATTERN_SHAPE: `||` between an age-staleness check and a process-liveness check in any demotion / orphan-detection predicate — should be `&&`. TICKET_TRACEABILITY: R-POD-1 (`0ff20931`), R-POD-2 (`df11df4d`), R-POD-3 (`2a92a614`) from `prds/archive/bug-reports/p3-state-manager-paused-orphan-demotion-or-aggression.md`.
- `src/services/ticket-completion-evidence.ts` (R-RIC-EXPLICIT — readEvidence explicit-source) — INVARIANT: `readEvidence` (B-DURA T70 deleted the deprecated `hasCompletionCommit` shim from `pickle-utils.ts` entirely; this is the sole oracle) MUST honor an explicit `completion_commit:` SHA from `<sessionDir>/<ticketId>/rick_ticket_<ticketId>.md` BEFORE its inferred-field and git-log-scan branches, returning `kind: 'committed'` (`via: 'explicit'`) when the SHA is git-reachable — explicit-reachable-wins is unchanged. B-1SEAM WS-1 (R-AICF): an explicit SHA that is present but UNREACHABLE (hallucinated/dropped stamp) is NO LONGER hard-absent — it falls through to the inferred-field and git-log-scan branches so real untagged work stays attributable (`absentReason: 'unreachable_explicit_unattributable'` when nothing attributes); ONLY baseline-rejected (`baseline_sha`, R-CXOR-2) and foreign-attributed (`foreign_attribution`, R-OMA) explicit SHAs stay hard-absent short-circuits. BREAKS: returning `kind: 'absent'` for a reachable explicit field fatals `guardCompletionCommitBeforeDone` mid-bundle (R-RIC regression, MASTER_PLAN #83; 2026-05-26 ea04b6f8 incident); restoring the unreachable-explicit hard-absent re-opens the R-AICF class where a hallucinated stamp buries a real untagged commit (live incident session 2026-07-01-9e922602 ticket c46045a6). ENFORCE: `extension/tests/has-completion-commit-explicit-source.test.js`, `extension/tests/trap-door-completion-evidence-conformance.test.js`. PATTERN_SHAPE: `normalizeCompletionCommitField(readFrontmatterField(content, 'completion_commit'))` in `readEvidence`, evaluated on `resolveTicketPath`/`ticketFilePath`-resolved content before `scanGitLog`; `unreachableExplicit` fall-through (no `return` on the unreachable-explicit branch) ahead of the inferred/scan branches.
- Naming convention: internal session artifacts are `rick_ticket_<hash>.md` files on disk; references to real Linear tickets remain external-tracker terminology, not local artifact paths.
- `ticket-completion-evidence.ts` (B-1SEAM WS-1 completion-predicate single seam) — INVARIANT: `evaluateCompletionEvidence(ctx: CompletionDecisionCtx)` is the ONE completion predicate — the single policy ladder (readEvidence explicit-reachable-wins → R-CCGR single backoff re-read on absent → R-CCEM announcement recovery → R-WUWC promote-once persist+re-probe → R-CWGE worker-gate verdict fail-closed, consulted ONLY when `decision === 'done-flip'`) answering "may this ticket's completion evidence be acted on?" for every decision site. `readEvidence(` callsites outside `ticket-completion-evidence.ts` MUST be 0 (the guard's refusal string literal `readEvidence().kind` is prose tests grep, not a callsite — audits count call sites); `evaluateCompletionEvidence(` callsites are pinned EXACTLY: 6 in `mux-runner.ts` (all via its local `buildCompletionCtx`) + 1 in `auto-fill-completion-commit.ts`; importer files of `ticket-completion-evidence` in `extension/src/` stay exactly {`mux-runner.ts`, `auto-fill-completion-commit.ts`} (R-AFCC-CALLER-ENUMERATION); `gateForPhantomDoneRevert` stays a thin adapter over the predicate (`{ decision: 'phantom-watch', rereadBackoffMs: 0 }` — the re-read runs, the sleep is skipped). A new predicate consumer is a deliberate two-place pin bump (`completion-predicate-single-seam.test.js` + the `audit-trap-door-enforcement.sh` B-1SEAM block). BREAKS: a bypassing `readEvidence` caller or a per-site ladder copy re-opens the R-AICF accept-here-revert-there split — three sites judging the SAME stamp three ways (live incident session 2026-07-01-9e922602 ticket c46045a6: done-guard accepted, phantom watcher reverted, Done-flip guard fataled `done_without_commit_evidence`, stranding the pipeline 12/16). ENFORCE: extension/tests/completion-predicate-single-seam.test.js, extension/tests/ticket-completion-evidence-predicate.test.js; bash extension/scripts/audit-trap-door-enforcement.sh (B-1SEAM block). PATTERN_SHAPE: `evaluateCompletionEvidence(` inside `guardCompletionCommitBeforeDone`, `defaultDoneGuard`, and `gateForPhantomDoneRevert`; no `readEvidence(` callsite outside `ticket-completion-evidence.ts`.
- `ticket-completion-evidence.ts` (B-GTRUTH WS-A1 zero-diff completion arm) — INVARIANT: "complete, and correctly produced no diff" is representable WITHOUT a SHA. `CompletionDecision`'s success arm is a UNION (`CompletionCommittedAccept` | `CompletionZeroDiffAccept`); the zero-diff arm carries `sha?: undefined` + `via: 'zero-diff'` and is produced ONLY by `zeroDiffAccept`, which requires ALL THREE of: a ticket-DECLARED `zero_diff_intent` in the closed set {`verification`,`audit`,`already-satisfied`} (read via the `ctx.zeroDiffIntent?: () => string | null` resolver; membership validated HERE, never at the wiring site), the ticket's own `complexity_tier` lifecycle artifacts all on disk (`requiredTierArtifactPrefixes` + `findMissingPrefixes` — an absent/unrecognized tier REFUSES rather than riding `normalizeTicketComplexityTier`'s silent `medium` default), and — on `done-flip` only — a GREEN R-CWGE verdict (whose own refusal is RETURNED, so a corroborated ticket reports `worker_gate_red`, not the useless `no_evidence`). Decision-kind reach is NOT uniform and each arm is load-bearing: `done-flip` accepts; `phantom-watch` accepts as a KEEP with the verdict exempt (the existing keep-decision asymmetry, R-DSAN never-discard); `attribution` REFUSES, which is what keeps `isTicketOracleCommitted` → `reportPhaseIncomplete`'s unfinished roster honest and is what makes the `CompletionAttributionDecision` overload (non-nullable `sha` for attribution consumers) sound. Hard-absent evidence (`baseline_sha` R-CXOR-2, `foreign_attribution` R-OMA) is NEVER laundered by a declaration. The arm sits after every attribution branch and before `refuseAbsent`, so committed evidence always wins and the declaration is consulted last. NO sentinel sha, NO empty-marker commit, NO write of any kind on the path. `guardCompletionCommitBeforeDone` stays policy-free — it maps the sha-less accept mechanically (`decision.sha ?? null`) and contains zero zero-diff logic. BREAKS: dropping the `attribution` refusal lets a frontmatter field HIDE a genuinely-unfinished ticket from the operator's roster — relocating the proxy-over-truth bug instead of removing it; dropping the `phantom-watch` KEEP makes the whole arm INERT BY CONSTRUCTION, because a zero-diff Done carries no `completion_commit` and `batchLoopPhantomDoneKind` reverts it on the next sweep (flip Done, lose it seconds later); dropping the artifact or verdict corroboration turns the field into a free-form self-certification ("any ticket may declare itself done"); fabricating a sentinel sha is the empty-marker-commit forgery moved into a string. ENFORCE: extension/tests/zero-diff-completion-arm.test.js (20 cases, 13 negative — incl. the attribution-refusal and phantom-watch-keep pins), extension/tests/completion-decision-3way-parity.test.js. PATTERN_SHAPE: `zeroDiffAccept(ctx, evidence)` called at the FIRST `!isAcceptedEvidence(evidence)` return inside `evaluateCompletionEvidence`, ahead of `refuseAbsent`; `if (ctx.decision === 'attribution') return null;` as `zeroDiffAccept`'s first statement; `ZERO_DIFF_INTENTS` defined in this file and consumed only here; zero `sha: '` literals in `ticket-completion-evidence.ts`.
- `ticket-completion-evidence.ts` (R-OMASD — a session-root dir is not a ticket) — INVARIANT: `enumerateSiblingTicketIds` admits a directory ONLY when it holds a `rick_ticket_*.md` (`isTicketDir`). A session root also holds `gate`, `archive`, `refinement`, `microverse_*`, and anatomy-park subsystem dirs (`bin`, `extension`) — ordinary English words that word-boundary-match routine commit subjects (7/30 recent subjects here). BREAKS: unfiltered, R-OMA reads a ticket's OWN reachable stamped commit as `foreign_attribution` → hard-absent → Done-flip refused (`done_without_commit_evidence`) or phantom-watcher reverts shipped work. ENFORCE: extension/tests/r-omasd-non-ticket-sibling-dirs.test.js. PATTERN_SHAPE: `isTicketDir(` called inside `enumerateSiblingTicketIds`; no bare `out.push(entry.name.toLowerCase())` guarded only by `isDirectory()`.
- `ticket-completion-evidence.ts` (AP-EXT-ITER16-01/-02 every accept arm shares ONE rejection gate) — INVARIANT: all three `readEvidence` accept arms (explicit / inferred / scan) pass their SHA through the single composed gate `rejectsAccept` — which applies BOTH rules, `rejectsAsBaseline` (R-CXOR-2) and `isForeignAttributedExplicitSha` (R-OMA) — before returning `kind:'committed'`; neither rule may be called directly from an arm, or the next rule added lands on two arms and misses the third. `commitExists` substitutes for NEITHER: a baseline SHA is git-reachable BY CONSTRUCTION and a foreign-attributed SHA is a real commit, just someone else's. Each arm owns only its `absentReason` — the hard reason (`baseline_sha` / `foreign_attribution`) for the stamped fields (explicit, inferred), the scan arm's deliberate `no_evidence` downgrade. BREAKS: the inferred arm gated on `commitExists` alone, so an announced baseline (ITER16-01) or a borrowed sibling sha (ITER16-02) classified `committed` — ONE sha drawing THREE verdicts by which field carried it. The Done-flip survived only BY ACCIDENT — promote-once persisted the SHA and the RE-PROBE's explicit arm rejected it — after writing a durable `completion_commit:` stamp that `mux-runner.ts:resolveAttributableFrontmatterSha` (R-RASO, suppresses silent-death respawn) and `hasPresentCompletionCommitField` (B-RRH `signal_committed`) both read with NO baseline or attribution check. Do not "fix" this by trusting the re-probe: it launders the write, it does not prevent it. ENFORCE: extension/tests/ticket-completion-evidence-predicate.test.js (`AP-EXT-ITER16-01` classification + never-persist cases and `AP-EXT-ITER16-02` foreign-inferred + explicit/inferred arm-agreement cases, each with its own accept-still-works control; all mutation-RED against the pre-fix arms, zero collateral). PATTERN_SHAPE: a `return { kind: 'committed'` inside `readEvidence` not dominated by a `rejectsAccept(` call on that same sha; or a `rejectsAsBaseline(`/`isForeignAttributedExplicitSha(` call anywhere outside `rejectsAccept`.
- `ticket-completion-evidence.ts` (AP-EXT-ITER27-01 the trailer scan's window is bounded on the SAME axis as its filter — LANDED) — INVARIANT: `scanGitLogByTrailer` bounds its `git log` on the epoch axis (`--since @<startTimeEpoch>`) whenever that bound exists, matching the per-entry `e.epoch < startEpoch` filter it feeds; the count ceiling `TRAILER_SCAN_MAX_COMMITS` survives ONLY as the no-epoch arm, and the spawn carries `UNBOUNDED_READ_MAX_BUFFER` (AP-EXT-ITER8-01) now that the output is no longer capped at 50 lines. A count cap is a DIFFERENT axis, and the difference is not cosmetic: the scan's predicate is EXACT ticket-id equality against one trailer value, which is NOT monotone in log order, so a cap can skip the one matching commit. Contrast the safe sibling `spawn-morty.ts:checkGitEdits` (`-n 10`), whose predicate is "any commit newer than the epoch" — reverse-chronological order makes that monotone, so its cap is mitigated by construction and MUST NOT be "fixed" by copying this change. BREAKS: at `-n 50` an early ticket's correctly-trailered commit falls out of the window as soon as the bundle authors 50 more commits — this repo has logged 143 commits in a single day, and `auto-fill-completion-commit.ts` re-scans EVERY ticket in the session late, in bulk, precisely when history is deepest. WS-3 deleted every message-inference fallback, so this scan is `readEvidence`'s only git-log arm: the miss reads `absent` and the Done flip is refused `done_without_commit_evidence` over work that shipped and is correctly attributed. Assert the ATTRIBUTION of a commit pushed deep into history (`readEvidence(...).kind === 'committed'`), never the scan's arg list — an argv oracle greens the moment someone re-tunes the number instead of the axis. ENFORCE: extension/tests/ticket-completion-evidence-predicate.test.js (AP-EXT-ITER27-01 block — deep-history attribution on both the epoch and no-epoch arms, plus a pre-session-commit case proving the widened window still fences; mutation-verified by restoring `-n 50` in the compiled mirror: exactly the two positive cases RED, the fence case and all 25 siblings GREEN). PATTERN_SHAPE: `grep -c "'-n', '50'" extension/src/services/ticket-completion-evidence.ts` == 0, and `--since` reached via `windowArgs` in `scanGitLogByTrailer` with `TRAILER_SCAN_MAX_COMMITS` on the else arm. Do NOT anchor on "no `'-n'` literal in the file" — the no-epoch arm legitimately emits one, so a zero-count on the bare flag reads as a phantom violation against its own fix.
- `ac-phase-gate.ts` (R-PVTA host-tool preflight) — INVARIANT: `runCriterion` calls `detectMissingTools(criterion.command)` BEFORE any `spawnSync` and short-circuits with a `tool not installed: <X>` reason for absent non-guaranteed tools. BREAKS: phase/final gates silently skip toolchain checks and report `tool not installed` as a spurious criterion failure instead of a clear preflight block. ENFORCE: `extension/tests/services/ac-phase-gate-tool-preflight.test.js`. PATTERN_SHAPE: `detectMissingTools(` before any `spawnSync(` in `runCriterion`.
- `ac-phase-gate.ts` (R-VSGE glob safety) — INVARIANT: string-form criterion command never runs with bare `shell: true`; it runs argv-form or under `set -f` (glob-disabled shell). BREAKS: unquoted glob characters in AC commands expand against the filesystem and contaminate or bypass the test/lint gate. ENFORCE: `extension/tests/services/ac-phase-gate-glob-safety.test.js`, `extension/scripts/audit-ac-command-glob-safety.sh`. PATTERN_SHAPE: `shell: true` absent from the criterion-command branch.
- `verify-command-safety.ts` (R-PVTA/R-VSGE shared predicate) — INVARIANT: `NON_GUARANTEED_TOOLS`, `detectMissingTools`, `containsUnquotedGlobHazard` are defined ONLY in `verify-command-safety.ts`; `ac-phase-gate.ts`, `convergence-gate.ts`, and `audit-ac-command-glob-safety.sh` import from it. BREAKS: divergent inline copies of the predicate drift silently — host-tool detection or glob-hazard classification in ac-phase-gate and convergence-gate diverge from the shared definition without detection. ENFORCE: `extension/tests/services/verify-command-safety.test.js`.
- `dirty-tree-salvage.ts` (B-1SEAM WS-3 shared dirty-tree salvage seam) — INVARIANT: the dirty-tree rescue safety MECHANISM is single-implementation here: `salvageDirtyTree` anchors any foreign remainder via `stashUnattributableRemainder` (whole dirty tree snapshotted under `refs/pickle/salvage/<session>` through a THROWAWAY `GIT_INDEX_FILE`; no real-index/worktree/`state.json` mutation) and returns ONLY the owned set as stageable; `stageOwnedPaths` stages per-path `git add --`; no whole-tree `git add -A`/`-u` exists in this module outside the throwaway-index stash. Ownership POLICY stays at the call sites (mux-runner exit path: ticket-dir attribution; microverse rescue/preflight: `AUTO_COMMIT_DIRT_EXCLUDES` + scope), and the call-site set is pinned (mux-runner.ts + microverse-runner.ts only). BREAKS: a second staging implementation or an in-module whole-tree add re-opens the B-PCOMP false-Done/bystander-sweep class from either runner. ENFORCE: extension/tests/services/dirty-tree-salvage.test.js, extension/tests/dirty-tree-salvage-callsites.test.js. PATTERN_SHAPE: throwaway `GIT_INDEX_FILE` in `stashUnattributableRemainder`; `salvageDirtyTree(` call-site count pinned by dirty-tree-salvage-callsites.test.js.
- `orphan-reaper.ts` (R-CXHANG orphaned-worker-proc reaper) — INVARIANT: `reapOrphanedWorkerProcs` (setup-time bootstrap via `setup.ts:runSetupOrphanReap`) kills a worker-shaped proc ONLY under positive ownership: argv `--add-dir` resolves to a session under the sessions root AND that session is provably not live (state.json missing, `active !== true`, or dead finite pid) AND the proc is older than the min-age floor (default 600s); un-attributable procs are NEVER killed and there is deliberately NO ppid==1-only reap branch. Kill path is group SIGTERM → grace → group SIGKILL via `killProcessGroup`, the ONE negative-PID group-kill primitive (spawn-morty `killProcessTree` and pipeline-runner `reapChildSubtree` delegate to it; `hooks/dispatch.ts` keeps its local fail-open copy BY DESIGN). Best-effort: never throws, never blocks a launch; `PICKLE_ORPHAN_REAP=off` (literal) → inert. BREAKS: reaping without positive ownership kills a live sibling pipeline's worker mid-build; dropping the collector re-opens the B-SIGFH orphan-accumulation machine-starvation class. ENFORCE: extension/tests/orphan-worker-reaper.test.js, extension/tests/setup-orphan-worker-reap.test.js, extension/tests/single-group-kill-implementation.test.js. PATTERN_SHAPE: `owningSessionDir === null` reject + min-age reject in `isReapableOrphan`; `env[ORPHAN_REAP_ENV_VAR] === 'off'` checked before any ps scan.
- `orphan-reaper.ts` (AP-EXT-ITER47-01 self-protection is the whole GROUP, not just pid/ppid) — INVARIANT: `isReapableOrphan` rejects via ONE membership test, `rt.selfIds.has(cand.pid) || rt.selfIds.has(cand.pgid)`; `selfIds` = `{process.pid, process.ppid, selfPgid}`, where `selfPgid` is read off the SAME census by `parseSelfPgidFromPs` (Node exposes no `getpgrp()`; a second `ps` is a second timeout to get wrong). A new identity to spare is a `.add`, never a new branch. BREAKS: the two pre-fix guards compared pid and pgid against self/parent only — adequate while `owningSessionDir === null` rejected every unattributed candidate, but the WS-1 `tmp_fixture` class is gated by AGE ALONE, so a same-group fixture passed every guard. `npm run test:fast` -> `posttest:fast` -> `sh -c` -> `node bin/reap-orphans.js` leaves the group led by the npm job leader, TWO levels up and therefore neither pid nor ppid; `kill(-thatPgid)` SIGTERMs then SIGKILLs the entire test run, reaper included. Same shape at `setup.ts:runSetupOrphanReap` and both `mux-runner.ts` sites, where the group is the PIPELINE's. Reproduced live against the shipped `services/orphan-reaper.js` under a `bash -> sh -> node` chain: `KILLS: [[<ownPgid>,'SIGTERM']]`. Assert the KILLS against a census that CONTAINS the reaper's own row — every pre-existing fixture omits it, which is why all 38 stayed GREEN. ENFORCE: extension/tests/orphan-worker-reaper.test.js (AP-EXT-ITER47-01 block — mutation-verified twice: restoring the two pid/ppid guards, and dropping only the pgid `.add`, each redden exactly the self-group case; the foreign-group and kin controls stay GREEN both ways, so a fix cannot pass by sparing everything). PATTERN_SHAPE: `rt.selfIds.has(` occurs in `isReapableOrphan` and `cand.pgid === process.pid` occurs ZERO times in `orphan-reaper.ts`; a `killProcessGroup(`/`process.kill(-` whose target pgid is not proven distinct from the caller's own group. NOT matches: the five `killProcessGroup(child.pid` sites (`spawn-morty.ts`, `pipeline-runner.ts`, `microverse-runner.ts`, `spawn-refinement-team.ts`, `hooks/dispatch.ts`) all target a child spawned `detached`, which leads its OWN group by construction. REPLAY (AP-EXT-ITER47-02, OPEN — same shape, unmitigated): `escalateFixtureGroup` group-signals raw PIDs read from `fixture_pid_registry.json`, a file a PREVIOUS run wrote, with NO self-group check and NO identity re-verification — `readRegistryPids` filters only `Number.isInteger(pid) && pid > 0`, and `reapPreviousRunFixtures` admits any registry under 24h, a window in which PIDs recycle. Confirmed live: a `sleep 30` this run never spawned was killed by PID alone. Fix by recording an identity alongside the pid (POSIX identity is the (pid, start-time) pair) and re-verifying before signalling — never by widening `selfIds`. Currently reachable only from `suite-level-fixture-registry.test.js`: the exported registry API has ZERO production callers, and the fixture's own producer (`sigterm-ignoring-sleeper.js`, newline-delimited text at `PICKLE_FIXTURE_PID_REGISTRY`) writes a format `readRegistryPids`' `JSON.parse` cannot read, so PRD AC-1's producer/consumer pair never joins.
- `state-manager.ts` (a lock and its payload are published together; identity is the BYTES, not the inode number) — INVARIANT: `acquireLockFile` writes a per-acquisition nonce header + the caller's payload to a staging file and publishes the lock by `linkSync`-ing it into place, so a lock never exists without the pid holding it; it returns a `LockHandle {ino, raw}` and BOTH `releaseLockFile` and `stealLockFile` evict only on `sameLock` (inode AND full bytes). The inode NUMBER is not identity — ext4 recycles it, so a rival's new lock can wear the dead holder's number and an ino-only check evicts a LIVE holder (R-LSPC-2). `LockSnapshot.payload` stays exactly the caller's bytes, so each caller's own decoder (bare pid, `{pid,ts}` JSON) is untouched. All six acquire sites (gate, restructure, state.json, steal-rights, session-map, gate-remediator) route through it — count the callers rather than trusting this number, which read "five" while the remediator was still hand-rolled. BREAKS: a create-then-write publish leaves an EMPTY payload when the holder dies in the window; `isDeadPidPayload` cannot prove death from it, so the gate and restructure locks — which carry no age-based arm by design — wedge forever. Do NOT answer that by stealing empty payloads: a LIVE holder is empty in that same window, so such a steal evicts it. ENFORCE: extension/tests/integration/lock-steal-live-holder.test.js. PATTERN_SHAPE: `fs.linkSync(staging, lockPath)` in `acquireLockFile`; no `openSync(lockPath, 'wx')` in `state-manager.ts`.
- `transaction-ticket-ops.ts` (restructure lock reclaims a dead holder — proof of death ONLY) — INVARIANT: `acquireFileLock` (the fourth lock) does NOT hand-roll an acquire/release: it takes the lock via `acquireLockFile(lockFile, String(process.pid))` — a BARE-pid payload — reclaims a strand through `reclaimDeadRestructureLock` (`withStealRight` + `isDeadPidPayload` + `stealLockFile`), releases identity-bound via `releaseLockFile(lockFile, held)`, and still refuses a live/unaccountable holder with an `EEXIST`-coded error (the `applyCourseCorrectionRestructure` refusal contract). It MUST NOT grow an age-based steal arm: a restructure legitimately holds while it rewrites every ticket in the session, so an age verdict would evict a LIVE holder mid-transaction. THE PAYLOAD ENCODING IS THE FIX: `isDeadPidPayload` parses ONLY a bare integer pid — the prior `{pid,ts}` JSON payload (like the remediator's empty one) reads back as NaN, so a steal bolted onto it silently never fires. BREAKS: it hand-rolled `openSync(O_EXCL)` + a `{pid,ts}` payload nothing read back + a bare `unlink` release, so one SIGKILL stranded `restructure.lock` and EVERY later course correction for that session died on EEXIST — permanently, with no runtime recovery; a bare unlink also evicts a successor that stole ours. ENFORCE: extension/tests/integration/lock-steal-live-holder.test.js (dead-holder reclaimed; LIVE holder never stolen). PATTERN_SHAPE: `acquireLockFile(lockFile, String(process.pid))` + `reclaimDeadRestructureLock(` + `releaseLockFile(lockFile, held)` in `acquireFileLock`; no `O_EXCL` / `fs.writeSync(fd, JSON.stringify({ pid` / bare `unlinkSync(lockFile)` in `transaction-ticket-ops.ts`.
- `codegraph-service.ts` (AP-EXT-ITER6-01 the head-sha sidecar's lifetime follows the db's) — INVARIANT: `quarantine` calls `discardIndexedHeadSha(dbPath)` after BOTH arms (injected `deps.quarantine` and the default `renameSync`) — the sidecar is an oracle about the db AT `dbPath`, not about itself. BREAKS: the rebuild's `CodeGraph.init()` never passes `options.index`, so a surviving sidecar makes `setup.ts:cgResolveIndexAction` read sha-match + fresh mtime, resolve `noop`, and feed every worker an EMPTY graph with no degrade event. Every pre-existing quarantine fixture injects `deps.quarantine`, so the default arm had zero coverage. ENFORCE: extension/tests/codegraph-staleness.test.js (AC-GTRUTH-B1-4). PATTERN_SHAPE: `discardIndexedHeadSha(dbPath)` reached after the if/else in `quarantine`; no `return` inside the `this.deps.quarantine` branch.
- `codegraph-query-runner.ts` (R-CGST child-exit stdout flush) — INVARIANT: `runChild` exits via the `process.stdout.write(payload, () => process.exit(0))` callback, never a bare `write(...); process.exit(0)`. BREAKS: a batch JSON past the ~64KB pipe buffer truncates on exit → parent `JSON.parse` throws `unparseable-stdout` → codegraph degrades on large result sets. ENFORCE: extension/tests/codegraph-degradation.test.js (R-CGST source guard + large-payload round-trip). PATTERN_SHAPE: `process.stdout.write(JSON.stringify({ searches, callers }), () =>` in `runChild`.

## Module Export Catalog

Machine-maintained export index for the `services/` subsystem. The audit script treats these names as the minimum subsystem surface that this document covers.

- `ac-phase-gate.ts` -> `AC_PHASE_MANIFEST`, `AcEvaluationPhase`, `AcPhaseCriterion`, `AcPhaseGateFailure`, `AcPhaseGateResult`, `runAcPhaseGate`
- `activity-logger.ts` -> `_clearPendingBuffer`, `_getPendingBuffer`, `_setRetryDelayMs`, `getActivityDir`, `logActivity`, `pruneActivity`
- `agent-md-loader.ts` -> `AgentMdFrontmatter`, `AgentModel`, `LoadAgentMdOptions`, `LoadedAgentMd`, `defaultAgentsDir`, `loadAgentMd`, `parseAgentMdFrontmatter`, `resolveAgentMdPath`
- `artifact-validation.ts` -> `findMissingPrefixes`, `listRickTicketFiles`, `requiredTierArtifactPrefixes`
- `backend-spawn.ts` -> `AddDirOutsideSandboxError`, `BackendPreSpawnAssertion`, `JudgeInvocationOptions`, `ManagerInvocationOptions`, `ReasoningEffort`, `SpawnInvocation`, `TrailerHooksSpawnOpts`, `WorkerBackendResolution`, `WorkerInvocationOptions`, `__resetBackendWarnings`, `assertAddDirsUnderTmpdirIfTestMode`, `assertBackendPreSpawn`, `backendEnvOverrides`, `buildJudgeInvocation`, `buildManagerInvocation`, `buildWorkerInvocation`, `isBackend`, `loadBackendFromSession`, `resolveBackend`, `resolveBackendFromStateFile`, `resolveBackendFromStateFileWithSource`, `resolveWorkerBackendFromState`, `resolveWorkerBackendFromStateFile`, `sessionStampEnv`, `shouldIsolateSessionGroup`
- `bundle-finalize.ts` -> `BundleFinalizeTicket`, `BundleTestFloorContribution`, `BundleTestFloorInput`, `BundleTestFloorResult`, `computeBundleTestFloor`, `parseCommitTestDelta`, `parseRefinementBaseline`, `renderMorningSummaryFrontmatter`
- `bundle-state-integrity.ts` -> `RelaunchCapAuditResult`, `RelaunchCapAuditViolation`, `auditCodexManagerRelaunchCaps`
- `calibration-corpus.ts` -> `CALIBRATION_SCHEMA_VERSION`, `CALIBRATION_SUITES`, `CalibrationBaseline`, `CalibrationDriftEntry`, `CalibrationDriftReport`, `CalibrationFixtureResult`, `CalibrationSuite`, `DEFAULT_CALIBRATION_DRIFT_THRESHOLD_PCT`, `assertKnownSuite`, `buildCalibrationBaseline`, `calibrationBaselinePath`, `checkCalibrationDrift`, `loadCalibrationThresholdPct`, `writeCalibrationBaseline`
- `circuit-breaker.ts` -> `CircuitBreakerConfig`, `CircuitBreakerState`, `CircuitState`, `CircuitTransition`, `IterationResult`, `ProgressResult`, `canExecute`, `countFilesChanged`, `detectProgress`, `extractErrorSignature`, `initCircuitBreaker`, `isConstraintDiscoverySignature`, `loadSettings`, `normalizeErrorSignature`, `readCircuitBreakerState`, `recordIterationResult`, `resetCircuitBreaker`
- `classifier-utils.ts` -> `detectOutputFormat`, `extractAssistantContent`, `observeCodexToolCallStream`
- `convergence-defaults.ts` -> `DEFAULT_ADVERSARY_MODEL`, `DEFAULT_ADVERSARY_PROMPT`, `DEFAULT_ADVERSARY_SEALED_FROM_SOURCE`, `DEFAULT_BODY_MAX_VISITS`, `DEFAULT_BUILD_API_CMD`, `DEFAULT_BUILD_UI_CMD`, `DEFAULT_COMMIT_PUSH_TIMEOUT`, `DEFAULT_CONVERGENCE_EPSILON`, `DEFAULT_CONVERGE_MAX_VISITS`, `DEFAULT_CONVERGE_TIMEOUT`, `DEFAULT_FIX_BACKEND_HARNESS`, `DEFAULT_FIX_BACKEND_MODEL`, `DEFAULT_FIX_BACKEND_PROMPT`, `DEFAULT_FIX_FRONTEND_HARNESS`, `DEFAULT_FIX_FRONTEND_MODEL`, `DEFAULT_FIX_FRONTEND_PROMPT`, `DEFAULT_FIX_TIMEOUT`, `DEFAULT_FP_VERIFY_CMD`, `DEFAULT_GOAL_GATE_MAX_VISITS`, `DEFAULT_GOAL_GATE_TIMEOUT`, `DEFAULT_LINT_CMD`, `DEFAULT_MAX_ITERATIONS`, `DEFAULT_MECHANICAL_BUILD_TIMEOUT`, `DEFAULT_MECHANICAL_MAX_VISITS`, `DEFAULT_MECHANICAL_TESTS_TIMEOUT`, `DEFAULT_REPRO_VERIFY_CMD`, `DEFAULT_REVIEW_BE_MODEL`, `DEFAULT_REVIEW_BE_PROMPT`, `DEFAULT_REVIEW_FE_MODEL`, `DEFAULT_REVIEW_FE_PROMPT`, `DEFAULT_REVIEW_INT_MODEL`, `DEFAULT_REVIEW_INT_PROMPT`, `DEFAULT_REVIEW_TIMEOUT`, `DEFAULT_TESTS_API_CMD`
- `convergence-gate.ts` -> `BaselineMissingError`, `BaselineStaleError`, `BaselineWriteFailedError`, `GATE_REMEDIATION_EVENT_NAMES`, `GateCommandMap`, `GateError`, `GateTimeoutError`, `RunGateOpts`, `assertBaselineFresh`, `assignOccurrenceIndices`, `buildFailures`, `detectProjectType`, `filterByScope`, `getWorkspacePackages`, `loadGateCommands`, `runGate`, `stripEnvNoise`, `subtractBaseline`
- `council-fanout.ts` -> `StackTier`, `SubagentSpec`, `planFanOut`
- `council-schema.ts` -> `BranchEntry`, `CodexBranchResult`, `CodexVerdictValue`, `CouncilSchemaError`, `Directive`, `Finding`, `KNOWN_CATEGORIES`, `KnownCategory`, `StackOverview`, `SubagentPayload`, `TrapDoor`, `validateDirective`, `validateSubagentPayload`
- `dirty-tree-salvage.ts` -> `SalvageDirtyTreeInput`, `SalvageDirtyTreePlan`, `salvageDirtyTree`, `stageOwnedPaths`, `stashUnattributableRemainder`
- `dot-builder.ts` -> `BUILD_ERROR_CODES`, `BuildError`, `BuildErrorCode`, `BuildResult`, `BuildResultNs`, `BuilderSpec`, `BuilderSpecNs`, `DefenseMatrix`, `DefenseMatrixNs`, `Diagnostic`, `DiagnosticNs`, `DotBuilder`, `MicroverseOpts`, `MicroverseOptsNs`, `PhaseSpec`, `PhaseSpecNs`, `StylesheetConfig`, `StylesheetConfigNs`, `ValidationResult`, `ValidationResultNs`, `WorkspaceOpts`, `WorkspaceOptsNs`, `_parseConvergenceSpec`, `_parsePhases`
- `git-trailer-hooks.ts` -> `MaterializeTrailerHooksOpts`, `MaterializeTrailerHooksResult`, `materializeTrailerHooks`
- `git-utils.ts` -> `ConcurrentGitHolder`, `DiffEntry`, `DiffStatus`, `TicketFrontmatterPatch`, `getBranchName`, `getDiffFiles`, `getGithubUser`, `getHeadBranch`, `getHeadSha`, `getMergeBase`, `isGitIgnoredPath`, `isWorkingTreeDirty`, `listWorkingTreeDirtyPaths`, `lookupCommandForPid`, `probeConcurrentGitAccess`, `resetToSha`, `runGit`, `updateTicketFrontmatter`, `updateTicketStatus`
- `jar-utils.ts` -> `addToJar`
- `judge-spawn-env.ts` -> `JudgeBackend`, `buildJudgeEnv`, `getJudgeEnvForAttempt`, `isNestedClaude`
- `linear-integration.ts` -> `emitBundleLinearComments`, `syncLinearTicketStatus`
- `manager-relaunch.ts` -> `ManagerRelaunchDecision`, `ManagerRelaunchExitKind`, `currentManagerRelaunchCount`, `evaluateManagerRelaunch`, `managerRelaunchCap`, `managerRelaunchCapForBackend`, `recordManagerRelaunch`
- `metrics-utils.ts` -> `BackendTokenTotals`, `DEFAULT_SKIP_FLAG_BUDGET`, `DailyLOC`, `DailyTokens`, `MetricsCache`, `MetricsReport`, `MetricsRow`, `MetricsTotals`, `ProjectSummary`, `SKIP_FLAG_BUDGETS`, `SKIP_FLAG_EVENT_NAMES`, `SkipFlagBudgetEntry`, `SkipFlagBudgetReport`, `SkipFlagEvent`, `buildReport`, `buildSkipFlagBudgetReport`, `extractSkipFlagUse`, `formatNumber`, `parseGitLogOutput`, `parseSessionLine`, `scanGitRepos`, `scanSessionFiles`, `scanSkipFlagEvents`, `shortenSlug`
- `microverse-state.ts` -> `LedgerSnapshot`, `assertMicroverseStateShape`, `classifyFailure`, `clearAmnesiacExits`, `compareMetric`, `createMicroverseState`, `findLastAcceptedEntry`, `generateViolationId`, `getLastAcceptedScore`, `isConverged`, `readMicroverseState`, `readRecoverableJsonObject`, `recordAmnesiacExit`, `recordFailedApproach`, `recordIteration`, `recordStall`, `resolveStallLimit`, `updateViolationLedger`, `writeMicroverseState`
- `orphan-reaper.ts` -> `ORPHAN_REAP_ENV_VAR`, `ReapMatchClassCounts`, `ReapOrphanedWorkerProcsOpts`, `ReapSweepResult`, `ReapSweepSkipReason`, `WorkerProcCandidate`, `killProcessGroup`, `parseSelfPgidFromPs`, `parseWorkerProcsFromPs`, `reapOrphanedWorkerProcs`
- `orphan-reaper.ts`; `../bin/reap-orphans.ts`; `../bin/setup.ts` (AP-EXT-ITER44-01 a sweep that never RAN is not an empty census) — INVARIANT: every not-run path returns `sweepNotRun(reason)`, so `skipped === null` is the only reading in which `scanned: 0` is evidence of a quiet box; both census printers branch on `skipped` before rendering counts. BREAKS: the kill-switch, the win32 no-op and the best-effort catch each returned their own zero tuple, so a `ps` that was absent, timed out, or overflowed `maxBuffer` printed byte-identically to a genuinely quiet box — the very line `prds/MASTER_PLAN.md` cites as post-deploy proof ("reports `scanned=0 reaped=0` against a genuinely empty census") and that R-ORCG calls binding ("an empty census is NOT evidence"). The THIRD consumer, `../bin/mux-runner.ts:runPipelineOrphanWorkerReap` (AP-EXT-ITER45-01), fires per-iteration, so it reports `sweep_failed` ONLY -- `kill_switch`/`unsupported_platform` are run constants and restating them each iteration is noise; it cannot borrow its siblings' throw-into-the-caller's-catch signal because the producer never throws. NOT matches: the two sibling reapers in `../bin/mux-runner.ts` THROW on scan failure and their callers log the error text, so the reason is named, not laundered. ENFORCE: extension/tests/reap-orphans.test.js (AP-EXT-ITER45-01 block -- pinned from BOTH sides: dropping the branch reddens the 2 sweep_failed cases, widening it to any `skipped` reddens the run-constant case; AP-EXT-ITER44-01 block — producer collapse reddens 4/4, reverting only the CLI render reddens exactly the operator-line case). PATTERN_SHAPE: a `scanned: 0` object literal in `orphan-reaper.ts` outside `sweepNotRun`, or a `reapOrphanedWorkerProcs(` consumer that prints `scanned=` without first branching on `.skipped` (all 3 consumers are `grep -rn 'reapOrphanedWorkerProcs' src/`).
- `pickle-utils.ts` -> `CompletionCommitEvidence`, `ComposeManagerPromptOpts`, `DEFAULT_WORKER_TEST_GATE_TIMEOUT_MS`, `DisplayMacNotificationOptions`, `EnsureMonitorWindowOptions`, `EnsureMonitorWindowResult`, `MANAGER_ROLE_FRAMING_BLOCK`, `MatrixStyle`, `MissingTicketError`, `MonitorMode`, `NOTIFICATION_TIMEOUT_MS`, `RAIN_CHARS`, `RetryLockOptions`, `Style`, `TICKET_TIER_BUDGETS`, `TIER_DIFF_ENVELOPE`, `TicketComplexityTier`, `TicketInfo`, `TicketStatus`, `TicketTierBudget`, `TierCapPartial`, `TierCapsConfig`, `VALID_TICKET_COMPLEXITY_TIERS`, `_resetExtensionDirFallbackForTests`, `buildHandoffSummary`, `clearTicketCacheFields`, `clearTicketResolutionTimestamps`, `collectTickets`, `composeManagerPromptFromSkill`, `detectLogTruncation`, `displayMacNotification`, `drainLog`, `drainStreamJsonLines`, `ensureMonitorWindow`, `extractFrontmatter`, `findSessionPathForCwd`, `formatLocalDateKey`, `formatTime`, `getDataRoot`, `getExtensionRoot`, `getHeight`, `getMicroverseSettings`, `getTicketStatus`, `getTicketTierBudgetWithOverrides`, `getWidth`, `inferMonitorMode`, `isoCompactStamp`, `latestIterationLog`, `loadPickleSettingsBag`, `markTicketDone`, `markTicketSkipped`, `markTicketWithStatus`, `matrixSeparator`, `monitorModesCompatible`, `normalizeTicketComplexityTier`, `parseTicketFrontmatter`, `printMinimalPanel`, `pruneOldSessions`, `pruneOrphanedMapEntries`, `readFrontmatterField`, `readPickleSettingsTierCaps`, `readStateTierCapOverrides`, `resolveJudgeBackend`, `resolveScopeSettings`, `ScopeSettings`, `resolveSessionPath`, `resolveWorkerTestGateTimeoutMs`, `restartDeadWatcherPanes`, `runCmd`, `safeErrorMessage`, `sleep`, `sleepSync`, `statusSymbol`, `stripSetupSection`, `stripStepOneBlock`, `ticketFilePath`, `ticketInfoBudget`, `ticketTierBudget`, `topoSortTickets`, `updateState`, `upsertFrontmatterField`, `withRetryLock`, `wrapText`, `writeStateFile`
- `pr-factory.ts` -> `createPR`
- `project-type-classifier.ts` -> `ClassifyProjectTypeOptions`, `PROJECT_TYPE_CATEGORIES`, `ProjectTypeCategory`, `ProjectTypeClassification`, `ProjectTypeConfidence`, `ProjectTypeDefinition`, `ProjectTypeScore`, `classifyProjectType`, `getProjectTypesCsvPath`, `loadProjectTypeDefinitions`
- `promise-tokens.ts` -> `FORBIDDEN_WORKER_TOKENS`, `PROMISE_TOKENS`, `PromiseToken`, `ScrubResult`, `scrubForbiddenWorkerTokens`
- `recoverable-json.ts` -> `readRecoverableJsonObject`
- `scope-resolver.ts` -> `ParsedScope`, `RefreshEntry`, `RefreshScopeOpts`, `ScopeArgs`, `ScopeError`, `ScopeErrorCode`, `ScopeJson`, `ScopeMode`, `ScopeStrategy`, `buildScopeV1Schema`, `computeOneHop`, `computeReviewBase`, `filterByPaths`, `filterBySubsystem`, `parseScope`, `refreshScope`, `resolveScope`
- `state-manager.ts` -> `ClearExitReasonOpts`, `FinalizeOpts`, `InvalidActivityEventError`, `LockSnapshot`, `SchemaVersionAheadError`, `SchemaVersionDeployDriftError`, `SchemaWriteOpts`, `StateManager`, `TimeoutStubMeta`, `WithLockOptions`, `acquireLockFile`, `assertSchemaVersionDeployParity`, `clearExitReason`, `finalizeTerminalState`, `inspectLockFile`, `isDeadPidPayload`, `isProcessAlive`, `readMappedPid`, `recordExitReason`, `releaseLockFile`, `safeDeactivate`, `stealLockFile`, `withLock`, `withStealRight`, `writeActivityEntry`, `writePipelineAutoResumedEvent`, `writeTimeoutStub`
- `ticket-completion-evidence.ts` -> `CompletionAttributionDecision`, `CompletionCommittedAccept`, `CompletionDecision`, `CompletionDecisionCtx`, `CompletionDecisionKind`, `CompletionDecisionRefusalReason`, `CompletionRefusal`, `CompletionZeroDiffAccept`, `EvidenceAbsentReason`, `EvidenceCtx`, `EvidenceKind`, `EvidenceResult`, `EvidenceVia`, `PersistOpts`, `PersistResult`, `RevertDecision`, `RevertPolicy`, `ZeroDiffIntent`, `evaluateCompletionEvidence`, `gateForPhantomDoneRevert`, `persistEvidence`, `readEvidence`
- `transaction-ticket-ops.ts` -> `ApplyCourseCorrectionRestructureInput`, `ApplyCourseCorrectionRestructureResult`, `CourseCorrectionApplyLedgerEntry`, `CourseCorrectionBranch`, `CourseCorrectionRecoveryClass`, `CourseCorrectionRecoveryMode`, `MaterializeTicketFileSpec`, `MaterializeTicketSpec`, `MaterializedTicketPlan`, `PlannedFileWrite`, `PlannedTicketWrite`, `RecoverCourseCorrectionInput`, `RecoverCourseCorrectionResult`, `ReverseLedger`, `ReverseLedgerEntry`, `TicketStatus`, `TicketTransactionContext`, `applyCourseCorrectionRestructure`, `materializeNewTicket`, `recoverCourseCorrectionFromLedger`, `replayReverseLedger`, `updateTicketStatusInTransaction`
- `worker-shutdown.ts` -> `flushAndExit`
- `ticket-completion-evidence.ts` (AP-EXT-ITER23-01 judge BEFORE you persist) — INVARIANT: `recoverFromAnnouncement` passes the announced sha through the shared `rejectsAccept` gate BEFORE `upsertFrontmatterField`, and returns the hard reason like its stamped-field sibling `readInferredArm`. BREAKS: re-probing AFTER the write classifies a bad sha but does not CONTAIN one — the durable `completion_commit_inferred:` stamp survives the refusal, and `mux-runner.ts`'s `resolveAttributableFrontmatterSha` / `hasPresentCompletionCommitField` read that field with NO baseline or attribution check. A baseline sha is git-reachable BY CONSTRUCTION and a borrowed one is a real commit, so both pass the git-only probe: silent-death recovery reads `hold` (respawn suppressed) and the Failed-flip is suppressed, wedging a ticket that did no work. `readAnnouncedCompletionSha` shape-validates only, so this is the one place the rule can apply. Returning the rejection rather than null also stops `zeroDiffAccept` laundering it into a declared zero-diff accept. ENFORCE: extension/tests/ticket-completion-evidence-predicate.test.js (AP-EXT-ITER23-01 block — assert the ON-DISK STAMP, never the verdict: the pre-fix verdict was ALREADY `refuse(baseline_sha)`/`refuse(foreign_attribution)`, so a verdict-only pin greens over its own bug; both cases mutation-RED, control green). PATTERN_SHAPE: `rejectsAccept(announced, ctx, raw)` dominating `upsertFrontmatterField(raw, 'completion_commit_inferred'` in `recoverFromAnnouncement`. RESIDUAL: a stamp written by a PRE-fix binary is untouched — this stops NEW bad stamps, it does not scrub existing ones, so a ticket already carrying a baseline/borrowed `completion_commit_inferred` keeps suppressing its respawn and Failed-flip. Bounded and one-time (ticket files are session-scoped and ephemeral); no migration, deliberately.

- `codegraph-query-runner.ts` (AP-EXT-ITER5-01 async-EPIPE parent kill) — INVARIANT: a `child.stdin` writer registers an `'error'` listener BEFORE writing; the sync try/catch alone is dead cover. BREAKS: a >64KB batch flushes on a later tick, the broken pipe raises an unhandled stream error, and the uncaught exception kills the PARENT (spawn-morty), not the child. ENFORCE: `extension/tests/codegraph-degradation.test.js` AP-EXT-ITER5-01 (mutation-RED: `write EPIPE`). PATTERN_SHAPE: `child.stdin?.write(` with no `child.stdin?.on('error'` earlier in the same function.

- `codegraph-query-runner.ts`; `../bin/pipeline-runner.ts` (AP-EXT-ITER7-01 a pipe boundary is a BYTE offset — decode on the STREAM, never per chunk) — INVARIANT: every child-stdout reader that ACCUMULATES text calls `setEncoding('utf-8')` on the stream BEFORE attaching its `'data'` handler; `String(chunk)` / `chunk.toString()` per Buffer is forbidden on such a reader. An OS pipe delivers a >64KB payload in byte-sized chunks, so a multi-byte UTF-8 character straddles the boundary and each half decodes to U+FFFD; `setEncoding` routes the stream through a StringDecoder, which withholds a partial sequence until its continuation bytes arrive. The correct siblings already existed — `spawn-morty.ts:1354`, `microverse-runner.ts:2140`/`2350` — these two were the outliers. BREAKS: the corruption is SILENT: U+FFFD is legal inside a JSON string, so `JSON.parse` succeeds and `status` reads `ok`. The codegraph batch returns mangled node paths/summaries into the worker prompt (a mangled path is then discarded by `isNodeLocationFresh`, so it reads as index staleness, not as transport corruption); the phase-runner echo writes mojibake into `pipeline-runner.log` and the operator's terminal at every 64KB boundary, and this runner's own log lines are non-ASCII-dense (`—`, `◤`, `✓`). Assert the ROUND TRIP (`blob === '—'.repeat(N)`), never `status`/parseability — a status check greens over its own bug (measured live against the shipped `services/codegraph-query-runner.js`: `'—'.repeat(350000)` came back with U+FFFD at index 43673, `status: 'ok'`). ENFORCE: extension/tests/codegraph-degradation.test.js (`AP-EXT-ITER7-01 multi-byte round-trip` — drives the REAL parent transport; mutation-verified RED before the fix, and the pre-existing R-CGST large-payload case stays GREEN because its blob is ASCII-only, which is exactly why it never caught this). PATTERN_SHAPE: a `child.stdout?.on('data'` handler that appends a decoded string, not preceded by `child.stdout?.setEncoding(` in the same function. NOT a match: `mux-runner.ts:attachStreamHandlers` passes the raw Buffer through with no decode (byte-exact by construction), and `spawn-morty.ts:2977`'s `ackLineBuf` is consumed only by the ASCII `COMPLETION_COMMIT_ACK_RE` — a mangled non-ASCII byte cannot alter a sha token, so it is mitigated by construction. The `pipeline-runner.ts` half is pinned STRUCTURALLY (`extension/tests/pipeline-runner.test.js`, `AP-EXT-ITER7-01 replay`) rather than behaviorally: its real `spawnRunner` needs a `phaseRunnerContext` only `main()` installs, so the end-to-end proof of the shape is the codegraph case above — do not read the source pin as an independent behavioral guarantee. OPEN GAP: `src/hooks/dispatch.ts:222-223` carries the same `data.toString()` shape and is unfixed — `hooks/dispatch.ts` is outside this session's `scope.json` (forced deferral, the AP-EXT-ITER20-02 shape). Harm there is bounded: the child writes one hook-decision JSON, far under a pipe buffer, so no boundary is reached in practice.
- `pickle-utils.ts` (AP-EXT-ITER8-01 `runCmd` output is complete or an error, NEVER a truncated prefix — LANDED) — INVARIANT: `runArgvCmd` and `runShellCmd` both pass `maxBuffer: UNBOUNDED_READ_MAX_BUFFER` (the ONE 64 MB ceiling, declared in `types/index.ts` and shared by every unbounded reader — the former per-file `RUN_CMD_MAX_BUFFER`/`GIT_STATUS_MAX_BUFFER`/`GIT_ENUMERATION_MAX_BUFFER`/`IMPORT_WALK_MAX_BUFFER`/`GIT_LS_FILES_MAX_BUFFER`/`GIT_UNBOUNDED_MAX_BUFFER`/`ARCHIVE_GIT_MAX_BUFFER` forks are collapsed into it; do NOT re-declare a local copy) and both route their result through `assertNotTruncated`, which throws on `ENOBUFS` for EVERY caller — `check: false` included. That arm is the load-bearing half and is deliberately asymmetric with the rest of the `check: false` contract: those callers degrade to `''` on a command that FAILED, whereas ENOBUFS is the harness losing bytes from a command that SUCCEEDED. `check: true`'s throw also interpolates `result.error?.message`, because a spawn that emits no stderr otherwise raises a bare unattributable `Command failed: …\nError: ` with nothing after it. Narrow BY DESIGN: `ETIMEDOUT` still degrades as before — its partial output is empty-or-tiny and the `check: false` probes (`rev-parse`, `merge-base`, `@{upstream}`) are written to expect that, so widening the throw to all of `result.error` converts the 30 s hang-guard into a fail-CLOSED abort. BREAKS: Node's 1 MB default SIGTERMs the child and still hands back the first megabyte, so a `check: false` caller consumes a truncated string it cannot distinguish from a complete one — `scope-resolver.ts:listTrackedAndUntracked` (`ls-files -co -z`) feeds `resolveAllowedFromPaths` → `scope.json:allowed_paths`, so a partial repo enumeration becomes a partial scope FENCE (`worker_edit_outside_scope` on a file the fence should allow, or a false `SCOPE_EMPTY_PATHS`), and `citadel/diff-walker.ts`'s per-file `diff --unified=0` / `blame --line-porcelain` silently lose the tail of their range. 1 MB is INSIDE the operating envelope, not outside it: `ls-files -co -z` is 507 KB at 7.4k paths (octy) so ~15k paths overflows, and full-file `blame --line-porcelain` on this repo's own `mux-runner.ts` is 6.28 MB. Measured live against the shipped `services/pickle-utils.js` before the fix: a 2 MB write returned 1,114,112 bytes with `check: false`, no signal. Assert the BYTE COUNT of the round trip, never "did it throw" or "is it non-empty" — a liveness or parseability oracle greens over exactly this bug. ENFORCE: extension/tests/pickle-utils.test.js (`AP-EXT-ITER8-01` block — argv/shell byte-complete round trips, the over-ceiling throw for BOTH `check` values, and the stderr-less attribution case; 5/5 mutation-verified RED against the pre-fix body, zero collateral). PATTERN_SHAPE: `maxBuffer: UNBOUNDED_READ_MAX_BUFFER` in BOTH `runArgvCmd`'s `spawnSync` options and `runShellCmd`'s `execSync` options, each followed by an `assertNotTruncated(` call reached before any `return`; and a `64 * 1024 * 1024` literal appearing in `src/` ONLY in `types/index.ts` (`src/bin/audit-ticket-bundle.ts` is the one un-collapsed pre-existing holdout). Do NOT anchor on "no `spawnSync` without `maxBuffer` in `pickle-utils.ts`" — the notification/tmux/`ps` shell-outs in this file are bounded by construction and legitimately carry none, so a whole-file zero-count reads as a phantom violation.
- `git-utils.ts`; `metrics-utils.ts`; `convergence-gate.ts`; `scope-resolver.ts` (AP-EXT-ITER8-01 replay — an UNBOUNDED enumeration carries an explicit `maxBuffer`) — INVARIANT: every git/rg/grep spawn in these files whose stdout is an unbounded whole-tree or whole-branch enumeration declares a 64 MB ceiling: `listWorkingTreeDirtyPaths` (`status --porcelain -z`), `scanGitRepos` (`log --shortstat` window), `getChangedSince` (`diff --name-only`), `getChangedExportedSymbols` (`diff <sha>..HEAD -- '*.ts'` — a whole-branch PATCH, the largest of the set; it carried a local `32 * 1024 * 1024` fork until AP-EXT-ITER47-01), `workerModeSkipResult` (`status --porcelain`), and both import walks (`rg -l` / `grep -rl`). BREAKS: each truncates differently and NONE of them says so — `listWorkingTreeDirtyPaths` throws on the resulting `status: null` with an EMPTY stderr, and on the salvage/rescue paths that call it a throw means the worker's dirty work is not salvaged at all; `scanGitRepos`' `(status ?? 1) !== 0` guard drops the whole repo from `/pickle-metrics` silently, so under-counted LOC reads as real data; `getChangedSince` returns `[]`, silently collapsing the gate's changed-file scope. NOT matches (bounded by construction, do not "fix" them): single-line `rev-parse`/`cat-file -e`/`show -s`/`--version` probes, `ps -p <pid>`/`lsof -t`/`pgrep` reads, path-scoped `status`/`diff --numstat` restricted to an explicit pathspec set, and any spawn whose stdout is never consumed (`stdio: 'ignore'`, `commit`, `apply`, `reset`). ENFORCE: extension/tests/pickle-utils.test.js (`AP-EXT-ITER8-01` block covers the shared primitive these sites' siblings route through; these direct spawns are pinned structurally, not behaviorally — do not read the primitive's tests as an independent behavioral guarantee for them). PATTERN_SHAPE: a `spawnSync`/`execFileSync` in these files whose stdout is split/parsed and whose options object carries no `maxBuffer`.
- `backend-spawn.ts` (AP-EXT-ITER9-01 the R-WSRC-4 sandbox assertion lives at the DISPATCHER, not in one arm — LANDED) — INVARIANT: `assertAddDirsUnderTmpdirIfTestMode(opts.addDirs)` is the FIRST statement of BOTH `buildWorkerInvocation` and `buildManagerInvocation`, so every backend arm inherits it; no arm-local copy exists. BREAKS: the assertion sat inside `buildClaudeWorkerInvocation`, so the codex worker arm — which appends the SAME `--add-dir` list to `codex exec --dangerously-bypass-approvals-and-sandbox`, a strictly wider grant than claude's `--dangerously-skip-permissions` — built an argv pointed at the operator's real repo and returned it clean. A test fixture rooting `working_dir` at REPO_ROOT plus a spawn timeout (R-MRWG-2) then orphans a full-write subprocess over the operator's tree — the exact leak class R-WSRC-4 exists to catch, wearing a different backend. Assert the ARGV, not just "did it throw": the pre-fix codex arm SUCCEEDED and carried the repo path, so a throw-only oracle on the claude arm greens over the whole defect. NOT a match: `buildJudgeInvocation` — both its arms are read-only by construction (`--allowedTools Read,Glob,Grep` / `-s read-only`), so it deliberately carries no assertion; `hermes`/`grok`/`kimi`/`gemini` emit no `--add-dir` at all. ENFORCE: extension/tests/services/backend-spawn-trailer-env.test.js (`AP-EXT-ITER9-01` block — per-arm refusal, the tmpdir-rooted control, the production passthrough, and the manager replay; mutation-verified against BOTH the pre-fix shape (assertion back inside the claude helper → exactly the codex case RED) and the manager-guard deletion → exactly the replay case RED, zero collateral). PATTERN_SHAPE: `assertAddDirsUnderTmpdirIfTestMode(opts.addDirs)` present as the first statement of `buildWorkerInvocation` and ABSENT from every `build<Backend>*Invocation` helper. OPEN GAP: `buildManagerInvocation` carries the identical `--add-dir <workingDir>` argv under the identical bypass flags (`spawn-morty.ts:3051` is the worker; `mux-runner.ts:3464`/`6121` and `jar-runner.ts:161` are the manager, each passing the live `working_dir`) and is STILL unguarded. The one-line dispatcher fix was built and reverted this pass: it reddens `tests/iteration-outcome.test.js` (`fractional mux max-turn settings fall back before spawning manager`), which runs under `PICKLE_TEST_MODE=1` with an unsandboxed `PICKLE_DATA_ROOT` — a REAL fixture leak the guard correctly flags, not a false positive. Land the guard together with that fixture's sandboxing; both that test file and `tests/backend-spawn-add-dir-sandbox.test.js` are outside this session's `scope.json`, so the deferral is forced, not chosen (the AP-EXT-ITER20-02 shape). The replay case in the ENFORCE file pins the CURRENT defective argv so the gap cannot go silent — flip it to a refusal assertion when the fix lands. NOTE ON THE HOST: these cases live in the trailer-env file because it is the only in-fence backend-spawn test file for this session's `scope.json`; move them beside the other R-WSRC-4 cases in `tests/backend-spawn-add-dir-sandbox.test.js` when a fence carries it — do NOT read their location as evidence they are about trailer env.

- `transaction-ticket-ops.ts` (AP-EXT-ITER8-02 the reversal reads the `action` discriminant, never content-nullness) — INVARIANT: both replay paths (`replayReverseLedger`, `reverseAppliedEntries`) route through the single `planReversal(entry)`, which deletes ONLY on `action: 'create'|'created'`; a non-create entry with no recorded prior content is `unrestorable` — warn and leave the file in place. `applyPlannedWrite` no longer manufactures that record: an existing file whose before-read throws makes it append a `failed` entry and throw, refusing an unreversible overwrite. BREAKS: `applyPlannedWrite` writes `beforeContent: null` under `action: 'write'` whenever the before-read fails (EMFILE/EIO under c=8 — the iteration-6 class), and a content-nullness predicate reads that as "the apply created it" and `rmSync`s a file that pre-dated the transaction, then `removeEmptyParents` takes its ticket dir. Measured live against the shipped `services/transaction-ticket-ops.js`: `recoverCourseCorrectionFromLedger({mode:'reverse'})` deleted a pre-existing `rick_ticket_*.md` and its directory, reporting success. Assert the file SURVIVES; a "did it throw" or `recoveredSteps` oracle greens over the delete. ENFORCE: extension/tests/transaction-ticket-ops.test.js (`AP-EXT-ITER8-02` block — 3 cases, each mutation-verified RED; the apply-side case uses a write-only 0o200 fixture so the OS cannot stand in for the guard). PATTERN_SHAPE: `planReversal(` called at BOTH reversal sites and `restoreContent(` reached only from inside it; no `priorContent === undefined || priorContent === null` delete predicate in the file.

- `convergence-gate.ts` (AP-EXT-ITER38-02 an EMPTY changed set is DECLARED, never narrowed against — LANDED) — INVARIANT: `resolveGateTargetDirs` resolves the changed set ONCE, ahead of the arm split, and gates BOTH arms on the same `changedFiles.length === 0` skip (`gate_diff_scope_fallback` + `gate_skipped`, returned as an `earlyResult` that bypasses `finalizeGateResult`); `selectWorkspaceTargetDirs` RECEIVES the resolved set as a `string[] | null` parameter and no longer calls `getChangedSince` itself, so there is one enumeration and one disposition. `null` (scope is not `changed`, or no `since`) means the narrowing arm is unreachable, which is why the parameter is nullable rather than an empty array — an empty array is the SKIP, not "narrow to nothing". BREAKS: pre-fix the workspace arm computed its own changed set and narrowed on it — `affectsAllWorkspacePackages([])` is false, so `workspacePackages.filter(...)` excluded every candidate and returned `[]` with NO event, while the flat arm on identical input emitted both skip events and short-circuited. `getChangedSince` maps ANY git failure — an unreachable/rebased-away `since` SHA, a timeout — to `[]`, so on a workspace repo the gate ran ZERO checks and `finalizeGateResult` reported an executed `gate_run_complete` green, indistinguishable from a real pass. Same sink as AP-EXT-ITER34-01, reached by the enumeration-failure axis instead of the path-space axis. Measured live against the shipped `services/convergence-gate.js` with `since: '0'.repeat(40)` over a package whose test script exits 1: workspace -> `status green, 0 failures, events [gate_run_complete]`; the flat control, same input -> `[gate_diff_scope_fallback, gate_skipped]`. Assert the EVENTS, never the status: the pre-fix return value was ALREADY `green`, so a status-only oracle greens over its own bug. ENFORCE: extension/tests/convergence-gate-skip-not-pass.test.js (`AP-EXT-ITER38-02` block, 3 cases — mutation-verified in the compiled mirror: restoring the pre-fix arm ordering reddens ONLY the unresolvable-`since` case, and both controls (resolvable `since` still narrows and runs the changed package; `scope: 'full'` never consults the changed set) stay GREEN both ways, so a fix cannot pass by simply skipping everything or by running every package). PATTERN_SHAPE: `selectWorkspaceTargetDirs` declares a `changedFiles: string[] | null` parameter and its body contains ZERO `getChangedSince(` calls; `resolveGateTargetDirs`'s body contains exactly ONE. Scope both counts to the FUNCTION body, never the file: `getChangedSince(` legitimately appears 4x in `convergence-gate.ts` — its own definition, the R-ORSR-6 wrapper `getChangedFilesSince`, `buildNoDisownContext`'s no-disown axis (AP-EXT-ITER15-01, which has its own `size === 0` disposition and must NOT be routed through this skip), and the one call here — so a file-wide count of 2 reads as a phantom violation against its own fix. Do NOT restore the pre-fix wording ("a `getChangedSince(...)` result narrowed without a `changedFiles.length === 0` skip arm"): it names no symbol a grep can count.
- `convergence-gate.ts` (AP-EXT-ITER47-01 the sweep's arming input must be able to say "I could not measure") — INVARIANT: `getChangedExportedSymbols` returns `Set | null` — `null` whenever the enumeration did NOT COMPLETE — and its completion predicate is ONE check covering BOTH failure shapes: `(result.status ?? 1) !== 0 || result.error`. It declares `UNBOUNDED_READ_MAX_BUFFER`, the ONE 64 MB ceiling (AP-EXT-ITER8-01); the former local `32 * 1024 * 1024` was HALF it and exactly the per-file fork that trap door forbids — this spawn is a whole-branch PATCH, strictly larger than the `--name-only` list its `getChangedSince` sibling already carries the ceiling on. BREAKS: an empty `Set` is a POSITIVE finding ("this phase changed no exported symbol") that `runInterfaceChangeSweep` reads as `ran: false` and `applyInterfaceChangeSweepGuard` then drops, so ANY git failure silently disarmed R-ORSR-6 INV-NO-SELF-DISOWN — the guard whose whole job is that a phase cannot disown its own whole-repo interface break — and the run converged reporting success. The two shapes are NOT one: an unreachable `sinceCommit` exits 128, but `maxBuffer` overflow returns `status: 0` with `error.code === 'ENOBUFS'` and TRUNCATED stdout (both measured), so a status-only guard parsed a diff cut mid-stream as a COMPLETE symbol set and every declaration past the cut was invisible. `buildNoDisownContext` degrades `null` to an empty set on purpose: that context can only ever KEEP a failure the iteration caused, so a narrower set never invents one. Assert against a REAL repo and an unreachable SHA — all five pre-existing sweep cases inject `() => new Set()` and stay GREEN against the pre-fix runtime, which is the AP-EXT-ITER13-01 hand-authored-fixture failure again. ENFORCE: extension/tests/convergence-gate-no-disown-wiring.test.js (AP-EXT-ITER47-01 block) (3 mutations, each RED in isolation: restore `return new Set()`; re-collapse the sweep's `null` and `size === 0` arms into one door; delete the render block). PATTERN_SHAPE: `if ((result.status ?? 1) !== 0 || result.error) return null;` as the sole exit guard of `getChangedExportedSymbols`, with `maxBuffer: UNBOUNDED_READ_MAX_BUFFER` in the same spawn — a bare `status`-only test, a literal byte count, or a `new Set()` in that return is the regression. REPLAY (OPEN, next pass): the sibling `getChangedSince` at the same `return [];` shape still has no `result.error` arm; its `resolveGateTargetDirs` consumer DECLARES the empty set (AP-EXT-ITER38-02) but `buildNoDisownContext` and `runInterfaceChangeSweep`'s `getFiles` read it silently.
- `convergence-gate.ts`; `../bin/microverse-runner.ts` (AP-EXT-ITER48-01 the sweep's OTHER classifier axis must be able to say "I could not measure" — LANDED) — INVARIANT: `getChangedSince` returns `string[] | null` behind the SAME completion predicate its `getChangedExportedSymbols` sibling carries (`(result.status ?? 1) !== 0 || result.error`), the exported `getChangedFilesSince` wrapper PROPAGATES the `null`, and `runInterfaceChangeSweep` enumerates BOTH axes up front behind ONE unmeasurable disposition — `changedExportedSymbols === null || changedFilesList === null` → `ran: false` with a `skipped` naming the failed axis. BREAKS: AP-EXT-ITER47-01 closed the symbol axis and left this one mapping every git failure to `[]`. `isSelfIntroducedFailure` short-circuits on `ctx.changedFiles.size > 0`, so a fabricated empty list disarms the FILE axis of INV-NO-SELF-DISOWN while the sweep still returns `ran: true` — strictly worse than the symbol arm was, which went quiet: this one ASSERTS a verdict `applyInterfaceChangeSweepGuard` reads as evidence the phase did not break the repo. `[]` is not even a coherent reading at that call site — the sweep only reaches it once `changedExportedSymbols.size > 0`, and no exported declaration changes without its file changing, so an empty list there is PROVABLY an enumeration failure being read as data. Enumerating before the gate also means an unmeasurable sweep costs no whole-repo tsc. The two OTHER `getChangedSince` consumers keep `?? []` ON PURPOSE and must not be "unified" into the skip: `resolveGateTargetDirs` already lands a failed enumeration in its declared `length === 0` skip (AP-EXT-ITER38-02 — and `null` on that local keeps its other meaning, "the narrowing arm is unreachable"), and `buildNoDisownContext` can only ever KEEP a failure the iteration caused, so a narrower set never invents one. Assert against a REAL repo and an unreachable SHA: every pre-existing sweep case injects `getChangedFilesSinceFn`, so all of them stay GREEN against the pre-fix runtime. ENFORCE: extension/tests/convergence-gate-no-disown-wiring.test.js (`AP-EXT-ITER48-01` block, 4 cases — 2 mutations verified RED in isolation on the compiled mirror: restoring `return []` in the producer reddens ONLY the 2 real-producer cases; reverting just the sweep's null arm reddens the other 3; all 9 pre-existing cases GREEN under both). PATTERN_SHAPE: `getChangedSince(...): string[] | null` whose guard ORs `result.error`; `runInterfaceChangeSweep` body contains ZERO `getFiles(` calls after its `await opts.runGateFn(`. OPEN REPLAY (fence-blocked, NOT landed): `signature-caller-gap.ts:26 gitTrackedFiles` is a byte-copy of `../bin/check-readiness.ts:327` MINUS the `maxBuffer: UNBOUNDED_READ_MAX_BUFFER` ceiling, and its `return []` under-populates `trackedSourceFiles` → fewer caller candidates → FEWER findings, the fail-open direction (the `check-readiness.ts` twin fails CLOSED: an empty list reads as "not tracked" and over-reports).
