## Trap Doors

- `purge-update-cache.js` — INVARIANT: purge root MUST equal validated `getExtensionRoot()` resolution (`EXTENSION_DIR` only when install-root sentinel exists, else fallback to `~/.claude/pickle-rick`); `update-check.json` and `deploy-audit.log` are written by `extension/src/bin/check-update.ts` and `install.sh` to that path. BREAKS: purge no-ops in production when raw env paths or wrong defaults miss the live cache root; stale auto-updater cache poisons the next update check. ENFORCE: extension/tests/purge-update-cache.test.js. PATTERN_SHAPE: `process\.env\.EXTENSION_DIR\s*\|\||path\.join\(os\.homedir\(\), '\.claude', 'pickle-rick'\)`; INVARIANT: purge MUST delete both `pickle-update-*` download dirs and `pickle-extract-*` extraction dirs because `check-update.ts` uses both temp roots during release inspection/install. BREAKS: interrupted upgrades leave stale extracted payloads behind and operators get a false-clean purge. ENFORCE: extension/tests/purge-update-cache.test.js. PATTERN_SHAPE: `collect(Prefix|VarFolder)Matches\(.*'pickle-update-'` without adjacent `pickle-extract-`; INVARIANT: updater-prefix purge targets MUST be real directories because `check-update.ts` creates temp roots with `mkdtempSync`, never files. BREAKS: same-prefix files in `TMPDIR` or `/var/folders` are deleted as false positives. ENFORCE: extension/tests/purge-update-cache.test.js. PATTERN_SHAPE: `startsWith\('pickle-(update|extract)-'\)` without `isDirectory()`; INVARIANT: this script stays subprocess-only — it calls `fs.rmSync` at module top level with NO `path.basename(process.argv[1])` entry guard, unlike both sibling `bin/` scripts. BREAKS: any future `import` of it deletes the operator's deployed `update-check.json` and every `pickle-update-*`/`pickle-extract-*` temp root as an import side effect. Safe today ONLY because it exports nothing and has zero importers. ENFORCE: none today (gap) — extension/tests/purge-update-cache.test.js drives it via `spawnSync` only, so it stays GREEN if an importer appears. PATTERN_SHAPE: a top-level `fs.rmSync(` in `bin/purge-update-cache.js` reachable without an `if (process.argv[1] && path.basename(process.argv[1]) === 'purge-update-cache.js')` guard; INVARIANT: every `deploy-audit.log` writer MUST establish 0600 at creation — the `mode` option is umask-masked and applies on creation only, so pair it with a `chmodSync` taken when the file was absent. BREAKS: `appendAudit()` creates the file with a bare `fs.appendFileSync` => 0666 & ~umask = 0644 (reproduced: `stat -f %Lp` = 644), and the installer's post-install mode verification hard-exits 1 on any mode but 600 — so a purge on a box where the log does not yet exist bricks the next deploy; the log also records operator + invocation, so 0644 is a least-privilege regression on its own. The file's absence is the NORMAL state (the installer only ever creates it inside its two audit-append helpers, each `: > file; chmod 600`), which makes purge its first creator on a healthy box. Every sibling writer already uses create-then-chmod-600; `check-update.ts` `appendDowngradeAudit` is the canonical shape (`appendFileSync(..., {mode: 0o600})` AND `chmodSync(auditPath, 0o600)` when `!existed`). ENFORCE: none today (gap) — OPEN as AP-BIN-ITER14-01, verified conf=100 across three passes. The fix is 3 lines in `appendAudit()`: capture `const auditExisted = pathExists(auditPath)` before the write, pass `{ mode: 0o600 }` to `appendFileSync`, then `if (!auditExisted) fs.chmodSync(auditPath, 0o600)`. It is unlandable by anatomy-park itself: the branch scope fence excludes BOTH `bin/purge-update-cache.js` and `extension/tests/purge-update-cache.test.js`, so the loop can catalog this but never commit it — land it on a branch that touches those paths. PATTERN_SHAPE: `(append|write)FileSync` on a `deploy-audit.log` path lacking `mode: 0o600` and an adjacent `chmodSync`.
- `release-gate.sh` — INVARIANT: release tag semver MUST equal `extension/package.json` version before publish and after download verification. BREAKS: updater caches a phantom latest version and re-downloads the same tarball forever. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `--pre-tag|--post-tag` path without tag-name-vs-package-version parity check; INVARIANT: release gating MUST resolve `extension/package.json` and git tag contents from the active worktree root, not the caller's nested cwd. BREAKS: `bash ../bin/release-gate.sh` from `extension/` or another repo subdir false-fails before validating a real release tag. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `PKG_PATH=\"extension/package\.json\"|git show \"\\$tag:\\$PKG_PATH\"` without `git rev-parse --show-toplevel` root resolution; INVARIANT: post-tag verification MUST select the unique installable tarball by archive shape and require both `extension/package.json` and `install.sh`. BREAKS: sidecar assets or partial payloads false-green the release while updater install later fails on missing runtime files. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `tar -tzf .*extension/package\.json` without a same-archive `install\.sh` sentinel check; INVARIANT: post-tag verification MUST require exactly one shared payload root containing both `extension/package.json` and `install.sh` before reading version metadata. BREAKS: duplicate payload roots let the gate validate an arbitrary subtree and false-green a malformed release asset. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `for \\(root in pkg\\).*print root` without counting shared roots; INVARIANT: post-tag verification MUST require `extension/package.json` and `install.sh` under the same payload root before reading version metadata. BREAKS: split-root tarballs can false-green a malformed asset by mixing package metadata from one subtree with an installer from another. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `(^|/)extension/package\.json$` and `(^|/)install\.sh$` checked without shared-root intersection; INVARIANT: post-tag verification MUST reject any archive entry with an absolute or dot-segment path before validating payload roots. BREAKS: a tarball with one safe install payload and one traversal entry still false-greens and later extracts unsafe paths. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `tar -tzf` archive scan without `(^|/)\\.\\.?(\\/|$)|^/` rejection for every member; INVARIANT: post-tag verification MUST download release assets via `gh release download --pattern '*.tar.gz'`, never the source archive flag. BREAKS: the gate validates a GitHub source archive instead of the shipped installer payload and can false-green a broken release asset. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `gh release download .* (-A|--archive) '?tar\.gz'?`; INVARIANT: any `tar -tzf | awk` scan that detects an offending entry MUST drain all producer output (no `exit` on first match — guard with `!found` and decide in `END`). BREAKS: under `set -o pipefail` an early awk `exit` SIGPIPEs the still-writing tar on listings exceeding the 64KB pipe buffer; the pipeline returns 141 and the `if`-guard reads it as "no unsafe entry," so a traversal entry false-greens (small-listing tests hide it — tar finishes before the pipe fills). ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `tar -tzf .* \| awk` block containing a bare `exit` inside a per-line match rule (must be absent — drain to `END`); INVARIANT: the tag-name semver guard MUST accept a prerelease suffix — every tag this repo ships is `X.Y.Z-beta.N`. BREAKS: `die 12` fires before the version-parity comparison, so the load-bearing gate rejects 100% of real releases and every downstream payload check is unreachable. ENFORCE: extension/tests/release-gate.test.js. PATTERN_SHAPE: `\^\[0-9\]\+\\\.\[0-9\]\+\\\.\[0-9\]\+\$` as a tag-name guard without an optional `(-[0-9A-Za-z.-]+)?` group; INVARIANT: post-tag verification MUST reject every archive entry that is a symlink or hardlink (member TYPE, exposed only by `tar -tvzf`'s first mode char `l`/`h`) via `listing_has_link_entries` at the `select_installable_tarball` guard, alongside the `-tzf` name scan. BREAKS: the name-only `tar -tzf` scan is blind to a link whose NAME is safe but whose TARGET escapes the payload root (`../../../../tmp/X`); it false-greens and the extractor writes members landing after the link THROUGH it, escaping the install prefix. The new scan MUST drain to `END` (no bare `exit` — same pipefail SIGPIPE trap as the name scan). ENFORCE: extension/tests/release-gate.test.js (`safe payload plus a symlink member whose target escapes`, `safe payload plus a hardlink member`). PATTERN_SHAPE: `tar -tvzf` scan with `substr($1, 1, 1)` in `{l,h}` present before extraction; INVARIANT: every archive scan MUST consume a listing that `read_tarball_listing` already materialized AND status-checked (`|| die 21`) — never a live `tar ... | awk` pipe. The scan functions are pure functions of that text (`printf '%s\n' "$listing" | awk`), and the listing is read in `select_installable_tarball` / `post_tag`, NOT inside an `if`-guard. BREAKS: under `set -o pipefail` a pipeline yields TAR's status whenever tar fails, and that status MASKS awk's verdict — awk exits 0 on a detected link, tar's non-zero wins, and the `if`-guard reads non-zero as "no link found". A TRUNCATED tarball still LISTS its escaping symlink, so the gate sailed past the very link awk had just printed (it then exited 21 only by luck, when extraction later failed — so a status-only assertion cannot catch this; the test MUST pin the message). `die` cannot rescue it from inside a scan: an `if`-guard suppresses `set -e`, so a `die` raised in the guard's command substitution exits only its own subshell and the script marches on. An archive tar cannot fully list is unverifiable and MUST NOT ship. ENFORCE: extension/tests/release-gate.test.js (`truncated tarball hides an escaping symlink behind a tar read error`). PATTERN_SHAPE: `tar -t[v]?zf` piped directly into `awk` inside any scan function, or a `read_tarball_listing` call whose status is not checked; INVARIANT: EVERY arm of a two-arm archive-scan predicate needs its OWN fixture — one arm's test never exercises the other. BREAKS: deleting an arm keeps the suite GREEN, so the release gate silently stops rejecting that member class. Mutation-verified: dropping `type == "h"` was 26/26 green pre-fix; dropping `entry ~ /^\//` was 27/27 green pre-fix, and is now 27 pass / 1 fail (only the new test). The arms are DISJOINT — `/tmp/PWNED` carries no `.`/`..` segment, so the dot-segment arm never sees it and the absolute arm is its sole rejector. CLOSED (all arms of both scans now pinned): `h` arm — extension/tests/release-gate.test.js (`safe payload plus a hardlink member`); ABSOLUTE-path arm of `listing_has_unsafe_entries` — extension/tests/release-gate.test.js (`safe install payload plus an absolute-path archive entry`). NOT A GAP (investigated, disproven): `is_safe_root` (:81) carries the same absolute/dot-segment pair but its rejection arms are unreachable-by-construction — both callsites (`select_installable_tarball` :207, `post_tag` :249 on an already-vetted tarball) sit behind the :201 scan, which dies 21 before any root is derived. A fixture there would pin an unreachable branch. PATTERN_SHAPE: an awk `if (!found && (A || B))` in a `bin/release-gate.sh` scan function where `extension/tests/release-gate.test.js` has a fixture for only one of A, B; NOT A GAP (investigated, disproven): `extension/src/bin/check-update.ts` also scans `-tzf` names-only before `tar xzf`, but the shape is NOT identical — `release-gate.sh` only LISTS an archive, so it must scan member types itself, whereas `check-update.ts` EXTRACTS and therefore inherits tar's own extraction-time refusal of `..` and write-through-symlink members. Verified fail-closed on GNU tar 1.35 and bsdtar 3.5.3 (7 trials, zero escapes; `fs.rmSync` cleanup does not traverse symlinks either). Adding a name/link scan there would be a redundant guard over an already-closed path. The real (now enforced) invariant is that the containment is INHERITED, not implemented — see the `check-update.ts` trap door in `extension/src/bin/CLAUDE.md`.; INVARIANT: `pre_tag`'s two `die 10` arms (tag-NAME parity, tagged-COMMIT parity) need separate fixtures. BREAKS: every pre-tag drift fixture ALSO drifted the name, so the name arm rejected first and the B-RELTAG wrong-tree detector shipped unenforced — deleting it left the suite 40/40 GREEN while a correct-named tag on a stale tree printed `ok:` and exited 0. CLOSED (AP-BIN-ITER19-01) — ENFORCE: extension/tests/release-gate.test.js#exits 10 when the tagged commit package version drifts from HEAD even though the tag name matches. PATTERN_SHAPE: a `pre_tag` `die 10` arm whose own message literal is absent from extension/tests/release-gate.test.js; INVARIANT: post-tag fixtures MUST cover the BARE payload root — `.github/workflows/release.yml` builds the asset with no prefix directory, so production's root is `""`. BREAKS: all 24 post-tag fixtures use a `pickle-rick-claude/` prefix production never emits, so the only path a real release takes is unfixtured; dropping `pkg[""] = 1` keeps the suite 41/41 GREEN while every real asset dies 21 `missing install payload root` (measured against a listing built by replaying the workflow's own `tar -czf`). CLOSED (AP-BIN-ITER19-02) — ENFORCE: extension/tests/release-gate.test.js#passes when the release asset carries a bare payload root, the shape release.yml builds. Mutation-verified 4 ways at exactly 2 RED (both new cases) / 29 GREEN, zero collateral: dropping `pkg[""] = 1`, dropping `install[""] = 1`, and breaking `post_tag`'s empty-root member branch. The disjointness control — dropping the PREFIXED arm — reds ONLY the 5 pre-existing prefixed cases and never the bare ones, so the two arm families are provably enforced separately and neither pin can decay into a duplicate of the other. The fixture asserts its own archive is bare-rooted, so it cannot silently regrow a prefix. PATTERN_SHAPE: zero `extension/tests/release-gate.test.js` tarball fixtures built with `extension/package.json` and `install.sh` as top-level archive members; INVARIANT: `[ ${#installable[@]} -eq 1 ]` needs its OWN fixture, disjoint from `-gt 0`. BREAKS: the sidecar asset carries no `install.sh`, so only the `-gt 0` arm is exercised — `-ge 1` survives the suite and a release publishing TWO installable assets verifies `installable[0]` only, shipping the other unchecked. `makeSidecarTarball`'s `includeInstallScript` option was built for exactly this case and is never passed. CLOSED (AP-BIN-ITER19-03) — ENFORCE: extension/tests/release-gate.test.js#exits 21 when the release publishes two installable tar.gz assets. Mutation-verified 2 ways at exactly 1 RED (the new case) / 31 GREEN: `-ge 1`, and the arm replaced by `true`. The disjointness control — `-gt 0` → `-ge 0` — reds ONLY the 2 pre-existing missing-payload cases and never the new one, so the two count arms are provably enforced separately. PATTERN_SHAPE: `includeInstallScript: true` absent from extension/tests/release-gate.test.js; INVARIANT: the member name handed to `tar -xOzf` MUST be the STORED name read back out of the listing, never rebuilt from the `./`-stripped payload root. BREAKS: `normalized()` strips a leading `./` when DERIVING the root, and GNU tar matches members EXACTLY — so a `./`-prefixed asset dies 21 `could not read extension/package.json` while bsdtar normalizes the request and passes, making the divergence invisible on macOS (measured: GNU tar 1.34 rc=2 vs bsdtar 3.5.3 rc=0, both bare and prefixed roots). CLOSED (AP-BIN-ITER20-01) — ENFORCE: extension/tests/release-gate.test.js#reads the package member by its STORED name when the archive stores a ./ prefix. Mutation-verified at exactly 3 RED / 33 GREEN for the pre-fix reconstruction and for dropping `normalized()` from the comparison; the DISJOINTNESS control — emitting a `./` prefix UNCONDITIONALLY — reds ONLY the non-dot control and never the three dot cases. That control is load-bearing: every other extraction fixture uses the REAL tar, and bsdtar's leniency kept a universal-prefix implementation 35/35 GREEN on macOS. `[ -n "$pkg_member" ]` was dead (both old branches assigned a literal) and is now live — emitting nothing reds 10. PATTERN_SHAPE: a `tar -xOzf` member argument in `bin/release-gate.sh` built by string-concatenating `payload_root`, or any `extension/tests/release-gate.test.js` archive fixture list with zero `./`-prefixed member names.
- `verify-bundle.js` — INVARIANT: every `bundle/ac-dr-*.json` producer MUST emit the canonical bundle schema (`ac_id`, `checked_at`, `checker`, `checker_version`, `evidence`, `failure_reason`, `remediation_hint`). BREAKS: `verifyBundle()` rejects real session artifacts and the deploy-reversion bundle gate goes red before evaluating behavior. ENFORCE: extension/tests/verify-bundle.test.js, extension/tests/pipeline-runner.test.js. PATTERN_SHAPE: `write(StateFile|FileSync)\(.+bundle\/ac-dr-[^']+\.json`; INVARIANT: `EXPECTED_BUNDLE_AC_IDS` MUST exclude every AC the refined PRD marks `status: removed` (AC-DR-03/07/15, stripped by AC-STRIP-10; AC-DR-15's slot was the `AC-DR-PRE-FLIGHT` alias) and MUST RETAIN every live PRD AC except one carrying a recorded justification in the test's `UNDEMANDED_LIVE_AC_IDS` (today exactly `AC-DR-05`: 59120b5d deleted its only checker as structurally unfalsifiable and `bundle/section-c-still-needed.json` records Section C `still_needed: false`, so no artifact can ever exist — but its PRD row still lacks `status: removed`, so the absence cannot yet be derived). BOTH directions MUST be derived from the PRD table, never asserted against a hardcoded copy of the contract. BREAKS: demanding a removed AC's artifact pins the full-bundle run at INCONCLUSIVE forever (it can never exist); conversely, dropping a live AC whose evidence was simply never written false-greens the missing evidence — and a spec that pins the contract against a hardcoded twin CERTIFIES that drift instead of catching it: co-dropping a live AC from contract + twin (what 59120b5d did to AC-DR-05) left the pre-fix suite 17/17 GREEN. ENFORCE: extension/tests/verify-bundle.test.js (`removed-acs-are-not-demanded`, `live-acs-still-demanded`). PATTERN_SHAPE: an `EXPECTED_BUNDLE_AC_IDS` member whose refined-PRD row reads `status: removed`, or a live-AC assertion compared against a hardcoded id list instead of `liveAcIdsFromRefinedPrd()`; INVARIANT: single-AC verification MUST reject unknown requested AC ids before reading bundle files. BREAKS: typoed or stale `--ac` values false-pass against arbitrary `bundle/ac-dr-*.json` payloads outside the contract. ENFORCE: extension/tests/verify-bundle.test.js. PATTERN_SHAPE: `options\.ac` validation path without `EXPECTED_BUNDLE_AC_IDS` membership after canonicalization; INVARIANT: exported `verifyBundle()` and the CLI wrapper MUST default to the same repo root. BREAKS: nested-cwd callers read `extension/bundle` instead of repo `bundle` and false-report missing AC artifacts. ENFORCE: extension/tests/verify-bundle.test.js. PATTERN_SHAPE: `const repoRoot = options\.repoRoot \?\? process\.cwd\(\)`; INVARIANT: bundle recovery runs IF AND ONLY IF the artifact is absent — `readRecoverableJsonObject(filePath)` gated on `!fs.existsSync(filePath)`. BREAKS: skipping it false-reports an interrupted atomic write as MISSING; running it on a PRESENT base renameSyncs an orphan OVER the tracked receipt and reads that tmp’s `pass` as the AC verdict, so a `pass:true` orphan false-greens the release gate and the committed evidence is gone. Mutation-verified: dropping the guard reds only the new case (20/21) with `GARBAGE FROM ORPHAN TMP`. Replay (investigated, disproven): the `pickle_settings.json` reads (`jar-runner.ts:63,168`, `check-update.ts:140`, `spawn-refinement-team.ts:1147`) share the no-atomic-producer shape, but nothing emits `pickle_settings.json.tmp.<pid>` (install.sh uses `mktemp`) and `config-protection.ts:96` blocks planting one. ENFORCE: extension/tests/verify-bundle.test.js (`verify-bundle.recovers newer orphan tmp bundle artifacts before reporting missing files`, `verify-bundle.does not promote an orphan tmp over a present artifact`). PATTERN_SHAPE: `readRecoverableJsonObject\(filePath\)` in `bin/verify-bundle.js` whose result is reachable when `fs.existsSync(filePath)` is true; INVARIANT: the CLI entry guard MUST match on `path.basename(process.argv[1])`, per CLAUDE.md Required Patterns. BREAKS: Node realpaths `import.meta.url` but never `argv[1]`, so a realpath-exact guard stops firing under any symlinked invocation path (macOS `/tmp`, symlinked worktree/home) — the gate exits 0 printing nothing and a `pass:false` bundle reads green. ENFORCE: extension/tests/verify-bundle.test.js (`cli-entrypoint still fires when reached through a symlinked path`). PATTERN_SHAPE: an entry guard comparing `path\.resolve\(process\.argv\[1\]\)` against `fileURLToPath\(import\.meta\.url\)` or a constant derived from it; INVARIANT: every `validateBundleArtifact` rejection arm needs its OWN fixture — the suite's only assertion on it today is the happy-path `deepEqual(validateBundleArtifact(...), [])` at verify-recapture-fired.test.js:182, so every type-check arm is unfixtured. BREAKS: deleting the `pass must be a boolean` arm makes a `{pass:'false', failure_reason:'boom'}` artifact report exit 0 / `bundle PASS` — a silent false-green on the release-evidence gate, the same cardinal sin as a hardcoded `pass`. Mutation-verified both directions: intact source exits 1 (`pass must be a boolean`); with the arm gone the identical artifact exits 0 and the suite stays 48/48 GREEN. The arm set is BOTH predicates: `verifyBundle`'s `ac_id must equal` identity arm forges a verdict the same way — one AC's evidence copied into another's slot reads green without it. CLOSED (AP-BIN-ITER11-01): extension/tests/verify-bundle.test.js (`rejection-arms each reject their own malformed artifact`, `non-boolean pass cannot false-green the CLI gate`, `every rejection arm in the verifier source has a fixture here`). Mutation-verified three ways: deleting the `pass` arm makes `validateBundleArtifact` return `[]` for `{pass:'false'}` and reddens 3 tests; deleting `ac_id must equal` reddens 2; a NEW unfixtured arm reddens the source-derived pin. That pin DERIVES the arm set from the verifier's own source, so the hand-written fixture list cannot silently drift — a hand list is what let 8 arms ship unpinned. It greps message literals only; the behavioral cases are the real guard. PATTERN_SHAPE: an `errors.push(` arm in `bin/verify-bundle.js` whose message literal is absent from extension/tests/verify-bundle.test.js.
- `verify-recapture-fired.js` — INVARIANT: AC-DR-02 MUST match `baseline_recapture_attempted` only inside the latest anatomy window and MUST NOT pin a fixed iteration. BREAKS: stale runs or later-iteration recaptures false-pass or false-fail the bundle. ENFORCE: extension/tests/verify-recapture-fired.test.js. PATTERN_SHAPE: `iteration\s*===\s*1|findMatchingEvent\(activity,\s*anatomyWindows\(state\.history\)\)` without latest-window-only filtering; INVARIANT: session state reads for AC-DR-02 MUST promote recoverable `state.json.tmp.*` snapshots before any missing/unreadable verdict, then parse via `StateManager.read(statePath)`. BREAKS: missing or corrupt base state false-fails even when the orphan tmp contains the real anatomy-window recapture event. ENFORCE: `extension/tests/verify-recapture-fired.test.js`. PATTERN_SHAPE: `fs\.existsSync\(statePath\)` before `StateManager\.read\(statePath\)|JSON\.parse\(fs\.readFileSync\(statePath` in `verify-recapture-fired.js`; INVARIANT: orphan tmp recovery MUST compare tmp mtime to the live pid start time; PID liveness alone is not ownership. BREAKS: PID reuse hides the newest recoverable state and false-fails AC-DR-02. ENFORCE: `extension/tests/verify-recapture-fired.test.js`. PATTERN_SHAPE: `isProcessAlive\(tmpPid\)` on `state\.json\.tmp\.\d+` without a process-start-vs-mtime check; INVARIANT: runtime AC-DR-02 evidence MUST be written to `<session>/bundle/ac-dr-02.runtime.json` when `sessionRoot` is provided, and to `getDataRoot()/bundle/ac-dr-02.runtime.json` otherwise. BREAKS: one session's recapture proof overwrites another's evidence or a no-session probe dirties the repo tree and blocks later clean-tree gates. ENFORCE: `extension/tests/verify-recapture-fired.test.js`. PATTERN_SHAPE: `const (DEFAULT_)?RUNTIME_ARTIFACT_PATH = path\.join\(REPO_ROOT, 'bundle', 'ac-dr-02\.runtime\.json'\)|writeRuntimeArtifact\(`; INVARIANT: AC-DR-02 runtime artifacts MUST emit remediation hints that match the actual `failure_reason`, not a generic missing-recapture hint. BREAKS: missing or unreadable session state sends operators toward the wrong repair path and obscures the real verifier failure. ENFORCE: `extension/tests/verify-recapture-fired.test.js`. PATTERN_SHAPE: `remediation_hint:\s*pass \? null : 'Ensure anatomy-park records baseline_recapture_attempted`; INVARIANT: `StateError('MISSING')` from AC-DR-02 state reads must distinguish deleted paths from unreadable existing files before setting `failure_reason`. BREAKS: chmod/EACCES state files emit `state-missing`, exit 2, and the wrong remediation path. ENFORCE: `extension/tests/verify-recapture-fired.test.js`. PATTERN_SHAPE: `err\.code === 'MISSING'` without `fs\.existsSync\(statePath\)` disambiguation; INVARIANT: anatomy windows must close only on later phase-marker history entries, not timestamp-only audit rows. BREAKS: circuit-breaker or similar timestamped state.history records truncate the anatomy window and false-fail AC-DR-02. ENFORCE: `extension/tests/verify-recapture-fired.test.js`. PATTERN_SHAPE: `history\.slice\(index \+ 1\)\.find\([^)]*timestamp` without a `step|phase` guard; INVARIANT: pre-promotion of a MISSING `state.json` via the generic `readRecoverableJsonObject` MUST be gated on `!fs.existsSync(statePath)`; an existing base defers to `StateManager.read`'s schema-candidacy-guarded recovery. BREAKS: unguarded pre-promotion renameSyncs a future-schema / core-field-missing orphan `.tmp.<pid>` OVER a good readable base and destroys live session state (AC-DR-02 also false-fails `state-unreadable`). ENFORCE: `extension/tests/verify-recapture-fired.test.js`. PATTERN_SHAPE: `readRecoverableJsonObject\(statePath\)` in `verify-recapture-fired.js` NOT guarded by `if \(!fs\.existsSync\(statePath\)\)`; INVARIANT: no tracked `bundle/ac-dr-*.json` may hardcode `pass`. A tracked artifact must be content-stable, and a content-stable file cannot carry a run-dependent verdict — only a constant one. BREAKS: `verify-bundle` reads `pass` as the AC verdict, so the gate can never go red: the verifier exits 1 (`recapture-event-missing`) while `verify-bundle --ac AC-DR-02` exits 0. Absent is honest — missing reports INCONCLUSIVE. ENFORCE: extension/tests/verify-recapture-fired.test.js (`failing-run-cannot-false-green-the-AC-DR-02-bundle-gate`). PATTERN_SHAPE: a writer of `bundle/ac-dr-*.json` under the repo root whose `pass` is a literal, not a computed verdict; INVARIANT: AC-DR-02 reads `baseline_recapture_attempted` from the `getActivityDir()/<day>.jsonl` sink — where its only producer (`logActivity`, via `microverse-runner`) writes it — and MUST require `session === basename(sessionRoot)`, because that sink is shared host-wide. BREAKS: reading `state.json.activity` (the `writeActivityEntry`-only sink the event never reaches) makes the AC unpassable on a healthy run — a permanent false-RED, and the exact mirror of the false-GREEN above; an unscoped match lets a sibling session's recapture satisfy this AC. ENFORCE: extension/tests/verify-recapture-fired.test.js (`state-activity-only does NOT satisfy the AC`, `foreign-session event does not satisfy this session AC`). PATTERN_SHAPE: `state.activity` consumed in `verify-recapture-fired.js`, or `findMatchingEvent` lacking a `session` equality check; INVARIANT: the PRODUCER side of that join is equally load-bearing — `attemptStrictBaselineRecapture` (`extension/src/bin/microverse-runner.ts:582`) MUST stamp `session: path.basename(opts.sessionDir)` on `baseline_recapture_attempted`, because the consumer requires session equality. BREAKS: deleting the producer stamp makes AC-DR-02 a permanent false-RED while the whole suite stays GREEN — the two integration tests that drive the REAL producer (`tests/integration/anatomy-park-gate-baseline-recovery.test.js`, `anatomy-park-baseline-gate.test.js`) assert only event/iteration/ordering and contain ZERO `session:` assertions, while the consumer specs hand-emit their own fixtures. Neither side pins the join. CLOSED: the pin reads the recapture `logActivityFn({…})` call's OWN text in both the source and the compiled mirror that actually runs — a file-wide grep is NOT sufficient and would have stayed GREEN through the real deletion, because 8 sibling emissions in `microverse-runner.ts` carry an identical `session: path.basename(opts.sessionDir)` line (9 total). ENFORCE: extension/tests/verify-recapture-fired.test.js (`producer stamps the session the consumer joins on (AC-DR-02 join)`). PATTERN_SHAPE: the `logActivityFn({ … event: 'baseline_recapture_attempted' … })` call in `microverse-runner.ts` (source AND `extension/bin/microverse-runner.js`) with no `session:` property INSIDE that call's own braces; INVARIANT: every `failureReason` arm needs its OWN fixture — the reason and its paired hint are the sole observable, since all arms are `pass:false`/exit 1. BREAKS: `phase-window-missing` shipped unpinned from birth; deleting it left the suite 75/75 GREEN while the regressed-history-append class it exists to diagnose (named as the failure mode by BOTH the `history` trap door in extension/CLAUDE.md and the `pipeline-runner.ts` one in extension/src/bin/CLAUDE.md) misreported as `recapture-event-missing`, sending operators at the wrong repair. CLOSED: extension/tests/verify-recapture-fired.test.js (`phase-window-missing is distinguished from a missing recapture event`) — an A/B on ONE session, same name and same activity log, so the event is provably findable in both halves and cannot decay into a tautology. PATTERN_SHAPE: a `failureReason` ternary arm or `remediationHintForFailure` switch case in `bin/verify-recapture-fired.js` whose literal is absent from extension/tests/verify-recapture-fired.test.js.; INVARIANT: `readActivityEventsSince` runs ONLY with a resolved anatomy window — `latestWindow === null` settles `phase-window-missing` from `state.history` alone, so the scan cannot change the verdict. BREAKS: a `NaN` sinceMs DISABLES the day bound (`Number.isFinite`), so the FAILURE path read every retained activity file: measured 7 files / 381,795 events / 1358MB heap / 3.4s on a six-day host, unbounded in retention. The sibling scanners in `metrics-utils.ts` (:716, :828, :927) chose a 10MB per-file skip instead — that remedy silently drops evidence; the correct shape is not reading what the verdict does not need. Mutation-verified both arms: restoring the unbounded read reports `activity_count: 4` (1 red); swapping the arm precedence reds only the precedence pin. ENFORCE: extension/tests/verify-recapture-fired.test.js (`no anatomy window skips the activity scan instead of reading every retained day`, `absent activity dir still outranks a missing anatomy window`). PATTERN_SHAPE: a `readActivityEventsSince(` call reachable when `latestAnatomyWindow` returned null, or any `sinceMs` argument that can be non-finite; INVARIANT: an in-window activity day that cannot be READ yields the no-measurement verdict (`activity-missing`, `activity_count: null`), never `recapture-event-missing`. BREAKS: `catch { continue; }` scored an unreadable day as one holding no match, so `activity_count: 0` asserted a scan that never completed and the hint sent operators at the PRODUCER instead of the log's permissions — the same unreadable-vs-absent confusion `failureReasonForStateReadError` already fixed for `state.json` and `listActivityFiles` for the activity DIR, left unapplied to the day read. Collapsed onto the existing `activity-missing` arm rather than adding a sixth reason: same repair, one no-measurement state. Mutation-verified both placements — restoring the swallow reds the A/B; hoisting the read above the day bound reds the pre-window control. ENFORCE: extension/tests/verify-recapture-fired.test.js (`an unreadable in-window activity day reports no-measurement, not a missing recapture`, `an unreadable PRE-window day is bounded out and cannot force no-measurement`). PATTERN_SHAPE: a `catch` inside `readActivityEventsSince` that `continue`s instead of returning null, or a `failureReason` arm reaching `recapture-event-missing` when the in-window scan did not complete; INVARIANT: `isInWindow`'s two arms (`ts >= start`, `ts < end`) need separate fixtures. BREAKS: out-of-window fixtures all sat BEFORE the window, so deleting `&& ts < end` stayed 92/92 GREEN and a post-handoff recapture false-greens AC-DR-02. CLOSED (AP-BIN-ITER18-01) — ENFORCE: extension/tests/verify-recapture-fired.test.js#a-recapture-emitted-AFTER-the-anatomy-window-closes-does-not-satisfy-the-AC. PATTERN_SHAPE: a `bin/verify-recapture-fired.js` range predicate fixtured on one side only.; INVARIANT: every `findMatchingEvent` conjunct needs its OWN fixture; the event-name arm's is a same-session in-window event of a different name. BREAKS: any of a session's ~19 activity event names false-greens AC-DR-02. ENFORCE: extension/tests/verify-recapture-fired.test.js `a same-session in-window event of another name`. PATTERN_SHAPE: a `findMatchingEvent` conjunct whose deletion survives the suite.; INVARIANT: an anatomy-park history entry with an unparseable timestamp MUST drop the window, not open one at epoch 0. BREAKS: a null `start` makes `ts >= start` coerce to `ts >= 0`, admitting any earlier event, and turns the day bound non-finite, restoring the unbounded scan. CLOSED (AP-BIN-ITER21-02) — ENFORCE: extension/tests/verify-recapture-fired.test.js#an-unparseable-anatomy-timestamp-drops-the-window-instead-of-opening-it-at-the-epoch. PATTERN_SHAPE: `anatomyWindows` lacking a null-`start` early return.
- `section-c-still-needed.js` (DELETED — do not rebuild) — INVARIANT: no `bin/` gate may grep a watcher log for `◤ FEED TERMINATED ◢`. BREAKS: the banner is `process.stdout.write`-only into a tmux pane and `wrapWithStderrRedirect` captures stderr only (`2>>`), so the grep never matches — a permanent false-green. Making it file-visible instead yields a permanent false-RED: post-R-MWR-6 the banner marks NORMAL session end. ENFORCE: extension/tests/watcher-banner-is-pane-only.test.js. PATTERN_SHAPE: `FEED TERMINATED` present in any `bin/*.js`.
- `bin/` (subsystem enumeration floor) — INVARIANT: `bin/` MUST keep >= 3 files whose extension is in `{ts,js,py,go,rs,java,tsx,jsx}` — `.sh` does NOT count, so `release-gate.sh` is invisible to the count. BREAKS: `discoverSubsystems` silently drops the whole subsystem, leaving every release-critical script unreviewed by anatomy-park. Currently EXACTLY 3 (`purge-update-cache.js`, `verify-bundle.js`, `verify-recapture-fired.js`) — deleting one more drops `bin/` out of scope. The per-file `bin/` tests do NOT cover this: each script has its own spec, so deleting one ALONE reddens — but deleting a script TOGETHER with its test is the normal shape and stays green. `980656c7` did exactly that (`section-c-still-needed.js` + `section-c-gate.test.js` in one commit), taking `bin/` from 4 to 3; three earlier commits stripped `bin/*.js` the same way. CLOSED (AP-BIN-ITER13-01) — ENFORCE: extension/tests/pipeline-runner.test.js#the-real-bin-still-clears-the-subsystem-enumeration-floor — mirrors the REAL `bin/` listing into a small tmp root and runs the REAL `discoverSubsystems`, so the subject is the shipped file set, not a stand-in. Mutation-verified both directions: 2 source files -> RED, shipped 3 -> GREEN, 4.8ms. It MUST NOT walk the repo root directly — `discoverSubsystems(REPO_ROOT)` costs ~12s (1169 files under `extension/`), far past the fast tier. The prior `anatomy-park-resolveSubsystems-bin.test.js` anchor stays synthetic-only by design and never reads the real `bin/`. PATTERN_SHAPE: count of `bin/*.{ts,js,py,go,rs,java,tsx,jsx}` < 3.
- `bundle/` (tracked AC evidence receipts, read by `verify-bundle.js`) — INVARIANT: no code may write `bundle/ac-dr-*.json` under the repo root, and no tracked artifact may name a `*.test.js` as its `checker`. Runtime verdicts go to session/`getDataRoot()`-scoped paths. BREAKS: a tracked file must be content-stable, so a code writer can only emit a literal `pass`, and a failing test throws BEFORE rewriting it — `verify-bundle` reads the stale `pass:true` as the verdict (permanent false-green). AC-DR-06 shipped this shape even though iter 10 had already trap-doored it for AC-DR-02: the entry was prose-only, so a trap door with no ENFORCE guard does not hold. ENFORCE: extension/tests/bundle-artifact-verdict-independence.test.js. PATTERN_SHAPE: `write(FileSync|StateFile)` targeting `path.join(REPO_ROOT|DEFAULT_REPO_ROOT|repoRoot, 'bundle'` (direct or via a bound identifier), or a tracked `bundle/ac-dr-*.json` whose `checker` matches `\.test\.(js|ts)$`.
- `bin/CLAUDE.md` (this catalog's own ENFORCE sweep) — INVARIANT: `discoverCatalogs` in `extension/scripts/audit-trap-door-enforcement.sh` walks `<repoRoot>/*/CLAUDE.md` as well as `extension/src/*/`, so this file's 8 ENFORCE refs are gate-verified for missing file, bad `@tier`, and phantom `#anchor`. BREAKS: the src-only walk left every one unswept — `bin/` is the one subsystem `discoverSubsystems` enumerates outside `extension/src/`, so deleting a spec together with the source it covers (the `980656c7` shape: `section-c-still-needed.js` + `section-c-gate.test.js` in one commit) makes a phantom ENFORCE while the gate prints "N ENFORCE reference(s) verified" and exits 0. ENFORCE: extension/tests/trap-door-completion-evidence-conformance.test.js#AP-BIN-ITER15-01 (behavioral — drives the shipped audit and reads its own per-catalog census; a source grep would green over a walk that is present but unreachable). PATTERN_SHAPE: a `CLAUDE.md` carrying `ENFORCE:` that is absent from the audit's per-catalog census, or `readdirSync(subsystemCatalogRoot` as the ONLY catalog walk in `audit-trap-door-enforcement.sh`. OPEN GAP: citadel's `collectClaudeMdFiles` (`extension/src/services/citadel/trap-door-coverage-audit.ts`) repeats the identical src-only scoping, so this catalog is invisible to citadel's trap-door-coverage reader too — outside this loop's scope fence (AP-BIN-ITER15-02).
