# Back-port ledger — v2.1 branch → `main` (v2.0), 2026-07-13

`main` was 135 commits behind `experiment/fable-operating-manual`. This records the **reliability/security
subset** that was cherry-picked to the v2.0 ship line, and — critically — the branch→main SHA mapping, because
the `(cherry picked from ...)` provenance line did NOT survive the commit method used.

**Base**: `cad28cb2` · **Safety ref**: `backup/main-pre-backport-2026-07-13` · **Unpushed.**

## What was taken, and what was not

| Bucket | Count | Disposition |
|---|---|---|
| **GA-FIX** (reliability / security / data-loss / false-Done) | 30 | **TAKEN** |
| **Companions** (prerequisites: tests that make a fix falsifiable, test-seam collapses, catalog splits) | 11 | **TAKEN** — the GA fixes do not build or do not test without them |
| QUALITY (style, simplification, test-hardening) | ~48 | left on v2.1 |
| V21-ONLY (FOM manual, prompt infusion, codegraph track, version bumps) | 22 | left on v2.1 — the codegraph chain creates a file main does not have and cannot graft |
| LEDGER (`prds/`, `docs/`) | 29 | left on v2.1 |
| SUPERSEDED (already on main via the 4 earlier back-ports) | 5 | not re-taken (would duplicate/conflict) |

## Gate on `main` after the back-port

`npx tsc --noEmit` clean · `eslint --max-warnings=-1` clean · **9/9 audits** · `test:fast` **6731 / 0 fail**
· `test:integration` **1088 / 0 fail**. `test:expensive` (deploy soak) NOT run — required only at tag time,
and it self-skips unless `PICKLE_INSTALL_ROOT` points off `$HOME`.

## ⚠ Outstanding

- **`4fcc02fc` (config-protection, SECURITY) is on main with NO TESTS.** It closes a real hole — the STATE
  write-probe walked a narrower command class than the lint probe, so `sed -i` / `perl -i` / `vim` / `ed`
  in-place edits of live `state.json`, `~/.claude/pickle-rick/**`, and operator settings were APPROVED (6 of 7
  cases fail pre-fix). Its 7 regression tests were written on the branch, refused by the scope fence, and
  parked in a patch file that no longer exists. **Coverage must be rewritten before v2.0 GA — treat as a
  release blocker, not a nice-to-have.**
- Compiled mirrors (`extension/{bin,services,hooks,types}/*.js`) were NOT cherry-picked; main regenerates its
  own via `npx tsc`. Keep it that way — picking mirrors conflicts on every commit.

## Hand-resolved (not a clean pick — review these first)

| File | Resolution |
|---|---|
| `extension/src/bin/spawn-morty.ts` | The branch hunk bundled the GA sync-gate rule **with** a v2.1 prompt-honesty paragraph. Took main's prompt text + only the rule. |
| `extension/src/bin/CLAUDE.md` (export catalog) | Dropped `countCodegraphDegradedEvents` — codegraph, not exported on main; listing it fails `audit-subsystem-claude-md.sh`. |
| `extension/src/bin/setup.ts` | Dropped the `runGit` import — its only consumer lives in a commit not back-ported (eslint caught it). |
| `72280dac` | **Source only.** Its test hunks sit on 9 test-hardening commits not taken. |
| `2e69bf6b` | **Hand-applied 2 hunks only** (`e3b70fe0`): the advisory message named `scope.auto_extend_signature_callers` in `scope.json`, whose only reader `294edf47` deleted. Live flag is in `pickle_settings.json` via `resolveScopeSettings`. The rest of `2e69bf6b` is FOM/v2.1 and its other slices are already on main. |

## Branch → main SHA mapping

| v2.1 (`experiment/fable-operating-manual`) | main | Subject |
|---|---|---|
| `294edf47` | `0168920c` | refactor(readiness): demote signature_caller_gap to advisory — W5b, 24 |
| `593ba571` | `71158762` | fix(codegraph): compiled defaults OFF + mux enabled-gate — evidence-au |
| `45e6e83c` | `752bba27` | feat(8327c4d8): synchronous gate confirmation + honest commit-first ru |
| `024a09b5` | `bc161e2a` | feat(9da93c73): archive-on-ladder-flip + worker_produced_everything_bu |
| `1b9cc29f` | `8b8da31d` | audit(8f699e78): [CRITICAL] codegraph artifacts counted as recoverable |
| `5c3360fc` | `48dcc475` | anatomy-park: bin — HIGH fix release-gate prerelease tag rejection, tr |
| `33b5c88f` | `63acf7f2` | anatomy-park: bin — correct the prerelease-guard comment to match the  |
| `c29951a5` | `5733b70c` | anatomy-park: extension — HIGH subtract citadel PATTERN_SHAPE grep arm |
| `8c2cd90e` | `ee99413a` | anatomy-park: bin — CRITICAL fix unguarded state.json pre-promotion cl |
| `32371dcc` | `cf65fa0d` | anatomy-park: extension — HIGH collapse forked setup.js spawn helpers  |
| `cf95137a` | `f4fcf96f` | anatomy-park: bin — CRITICAL stop fabricating the AC-DR-02 bundle verd |
| `1976e112` | `c4768e5e` | anatomy-park: extension — CRITICAL release the session-map lock before |
| `4fd65dd2` | `86a2731f` | anatomy-park: extension — HIGH steal the session-map lock from a dead  |
| `b94425b9` | `5d02bd50` | anatomy-park: extension — split the double-triple setup.test.js trap d |
| `d374c2c0` | `6fa4e68c` | anatomy-park: extension — CRITICAL serialize the stale-lock steal so i |
| `7d6ebc13` | `e90c73ad` | anatomy-park: extension — CRITICAL stop unlinking git's index.lock on  |
| `bd989681` | `998d3799` | szechuan-sauce: Encapsulation — refuse to drive a tmux session that do |
| `27518578` | `61be64c4` | szechuan-sauce: Encapsulation — derive tmux ownership from the session |
| `dbed61cf` | `ce3f7e7d` | szechuan-sauce: Encapsulation — gate ensureMonitorWindow on tmux owner |
| `3010d66f` | `e695e0eb` | fix(0aff9d30): adopt pinned_sha at both start_commit heal seams (R-SCP |
| `f535cd40` | `7d237c52` | test(468a91b1): salvage-guard re-arm regression tests — orphan-reset d |
| `f470fe6a` | `ea75c097` | test(468a91b1): fix session tmp-dir leak in salvage-guard regression f |
| `5df5ac9e` | `a65d9fca` | harden(59a0948f): test isolation — sandbox PICKLE_DATA_ROOT for the in |
| `c07cf4ec` | `56a20899` | test(6309e498): make the R-SCPIN test surface discriminate the heal se |
| `a03c90de` | `ad909063` | anatomy-park: bin — HIGH fix AC-DR-02 reading the wrong activity sink, |
| `4fcc02fc` | `161667e0` | anatomy-park: extension — CRITICAL collapse the state gate's narrow wr |
| `cfe48767` | `5cbf545c` | anatomy-park: extension — CRITICAL orphan-tmp scan destroyed an UNREAD |
| `68156bb5` | `c66519a1` | anatomy-park: bin — HIGH collapse two realpath-exact CLI guards onto t |
| `09107aee` | `c8418cf4` | anatomy-park: extension — CRITICAL delete the hand-rolled scope.json o |
| `ae0e1a88` | `8248bf04` | anatomy-park: extension — HIGH gate lock had no dead-holder recovery;  |
| `b491f78c` | `ac181bec` | anatomy-park: bin — CRITICAL setup --resume resurrected a Failed ticke |
| `498efe04` | `f0ea1d6b` | anatomy-park: extension — HIGH restructure lock had no dead-holder rec |
| `4f748e00` | `8fd739d7` | anatomy-park: bin — CRITICAL release-gate tar scan was blind to link T |
| `da1a6470` | `a0cc456b` | anatomy-park: extension — HIGH acquireLockFile published the lock befo |
| `ea502a49` | `136a3301` | anatomy-park: bin — CRITICAL setup --resume flipped a RED-gated ticket |
| `42d801b2` | `7a19969c` | anatomy-park: extension — CRITICAL SQL clobber scan only ever inspecte |
| `1cc46bb0` | `9e9ff047` | szechuan-sauce: Fail-Fast — materialize tar listings so pipefail canno |
| `981a16b2` | `85a34cc1` | szechuan-sauce: Fail-Fast — verify a lock's inode before vacating its  |
| `9f19370b` | `aca5e0ac` | szechuan-sauce: Fail-Fast — gate the third ambient-#S consumer, so a p |
| `ce79c1bf` | `732222b1` | szechuan-sauce: Fail-Fast — match the SQL clobber RHS against constant |
| `72280dac` | `e3cd775c` | szechuan-sauce: Test Quality — drive the real breadcrumb emitter, so a |

Plus, authored directly on main:
| — | `ab937a6f` | fix(locks): acquire/steal scratch name unique per ATTEMPT ([[R-LSPC]]; ported back to v2.1 as `0ce62c6c`) |
| — | `e3b70fe0` | fix(readiness): advisory message named a flag whose reader 294edf47 deleted (the `2e69bf6b` hand-apply) |
| — | *(recompile)* | chore(backport): recompile mirrors on main |
